# docker compose --env-file .env.deploy -f compose.deploy.yml up -d --build
# 이 파일을 .env.deploy로 복사한 뒤 빈 값을 채운다. .env.deploy는 Git 추적 대상이 아니다.

DB_NAME=finalcall
DB_USERNAME=
DB_PASSWORD=
DB_ROOT_PASSWORD=

JWT_SECRET=
GATEWAY_INTERNAL_SECRET=

MAIL_USERNAME=
MAIL_PASSWORD=

OAUTH_NAVER_CLIENT_ID=
OAUTH_NAVER_CLIENT_SECRET=
OAUTH_KAKAO_CLIENT_ID=
OAUTH_KAKAO_CLIENT_SECRET=

MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=

# 수동 운영형 시드 CLI 전용(일반 애플리케이션 부팅에서는 읽지 않음)
SEED_SCENARIO=ops-20-v2
SEED_JDBC_URL=
SEED_DB_USERNAME=
SEED_DB_PASSWORD=
SEED_CONFIRM_TARGET_FINGERPRINT=
SEED_CONFIRM_WRITE=
SEED_ALLOW_PROD=false
SEED_PASSWORD_HASH=
# 게시판 시드 실행 시에는 SEED_SCENARIO=board-surf-20-v1 및
# fingerprint 끝값을 :board-surf-20-v1로 바꿉니다. 게시판 시드는 비밀번호 해시를 사용하지 않습니다.

APP_DESCRIPTION=게임 아이템 경매 플랫폼
