/**
 * domain 계층 — common, infra 에 의존한다(순수 비즈니스).
 *
 * <p>feature 단위로 자른다(package-by-layer 아님). api 를 참조하지 않는다.
 *
 * <p>하위 패키지: sample (Stage 1 검증용), 이후 도메인은 Stage D 이후 추가.
 */
package com.finalcall.domain;
