---
id: EPIC-FE-REDESIGN
type: epic
jira_key: null
title: 프론트 실코드 반영 — design-system v0.6.1 + 목업 집행
state: doing
children: [FC-047, FC-048, FC-049, FC-050, FC-051]
gate: null
---
## 목표
EPIC-DESIGN-TEMPLATE이 확정한 **정본(design-system v0.6.1)과 목업 6면을 실코드에 집행**한다.
목업은 시각 방향을 확정하는 도구였고, 이 에픽이 그 방향을 실제 화면으로 만든다.

## 왜 지금인가
- **실코드가 정본보다 3버전 뒤처져 있다.** `frontend/src/index.css`·`tailwind.config.js`에
  **v0.5 토큰조차 안 들어갔다**(현 정본 v0.6.1). 목업과 실화면이 다르다.
- 라우트 16개 중 **12개가 여전히 `PagePlaceholder`**, **홈(첫 화면)부터 placeholder**다.
- FC-036/037로 구현된 경매 목록·상세에 **목업 기준 부채 18건 이상**이 식별돼 있다.

## 선행 전제 (이미 확보됨)
| 자산 | 내용 |
|---|---|
| `design-system.md` **v0.6.1** | 역할 기반 타입 스케일 7단 · 표면 층 4단 · 골드포스 §5.12 · `text-disabled` 토큰 |
| 목업 6면 | 홈 2 · 경매 목록/상세 · 인증 2 (`docs/ux/mockups/`) |
| `references/auction-detail-references.md` | 레퍼런스 조사 823줄 |
| **FC-045 역할 매핑** | 14/15/16px **21개 역할 → 13(문장 축)·17(값 축)** 흡수처. **치환 작업지시서** |
| `api-contract.md` **v1.10** | §3.3.1 코드 사전(element 1~4 · kind의 subGroup 의존) |

## 하위 티켓 (순차 — 팬아웃 안 함)
| # | 티켓 | 내용 |
|---|---|---|
| 1 | **FC-047** | design-system v0.6.1 → `tailwind.config.js`·`index.css` 토큰 반영 |
| 2 | **FC-051** | 프론트 테스트 러너 도입(vitest + testing-library) |
| 3 | **FC-048** | 공통 셸(헤더·푸터·브랜드) + 홈 — FC-041 목업 집행 |
| 4 | **FC-049** | 경매 목록·상세 — FC-042 목업 집행 + 부채 18건 |
| 5 | **FC-050** | 인증 화면 — FC-043 목업 집행 |

**순차인 이유**: 공유 파일이 교차한다(`index.css`·`tailwind.config.js`·공통 UI 컴포넌트
`Button`·`StatusChip`·`Countdown`). §9 팬아웃 판정("같은 도메인이 아니라 같은 파일로 센다")상
병렬 조건 미충족. 아이템·경매 에픽도 전부 순차 단일패스였다.

**FC-051을 2번에 둔 이유**: FC-038 리뷰가 *"이 에픽 결함 2건은 렌더 테스트 1개면 잡혔을 유형이고
M-1은 리뷰까지 살아남았다"*며 **백로그가 아니라 실제 티켓으로 승격 권고**했다(m11). 먼저 깔아야
이후 티켓이 테스트를 쓰면서 진행한다. 나중에 두면 또 안 쓴다.

## 백엔드 동결 유지
이 에픽은 **프론트 전용**이다. `backend/**` 수정 금지. 백엔드 후속(FC-035 minor 5건 ·
EPIC-CLOSING · EPIC-SHOP)은 동결 상태를 유지한다.

## 미결 — 구현 중 결정 필요
FC-043이 남긴 **판단 대기 4건**은 인증 구현(FC-050) 시 사용자 결정이 필요하다:
약관 동의(문장 vs 체크박스 — **체크박스면 계약 §2에 동의 필드 추가 = 게이트2**) ·
네이버 버튼 대비(합성 후 1.94:1) · 가입 CTA 문구 · 폼 좌측 배치.

## DoD
- 목업 6면이 실화면으로 존재하고, 실코드가 **design-system v0.6.1을 참조**한다.
- 홈이 더 이상 placeholder가 아니다.
- 회귀 테스트가 존재한다(FC-051 이후 티켓은 테스트 동반).
- reviewer 통과 후 사용자 승인(게이트3)으로 done.
