---
id: EPIC-DESIGN-TEMPLATE
type: epic
jira_key: KAN-49
title: 디자인 템플릿 HTML — 페이지별 시각 디자인 확정
state: done
children: [FC-041, FC-042, FC-043]
gate: null
---
## 목표
- **정적 HTML 목업으로 시각 디자인을 먼저 확정한다.** 그다음 프론트 구현이 이를 집행한다.
- 방침(사용자 지시 2026-07-18): 백엔드 동결 → **디자인 템플릿 HTML 완성** → 이후 기능 에픽은 프론트+백엔드 동반.

## 왜 이 에픽이 생겼나 (반복 금지 기록)
사용자가 처음으로 브라우저에서 화면을 열고 **"디자인이 거의 안 되어 있다"**고 지적했고, 사실이었다.
- 라우트 16개 중 **12개가 `PagePlaceholder`**("스켈레톤 placeholder — feature 단계에서 구현됩니다"). **홈(첫 화면)부터 placeholder.**
- `PublicLayout`은 얇은 헤더 + 텍스트 링크 3개. 푸터·브랜드 표현 없음. 주석이 스스로 *"도메인 콘텐츠는 없다(스켈레톤)"*.
- 로그인은 흰 배경에 `h1` + 필드 2개 + 버튼. **접근성·토큰은 정확한데 시각 디자인이 없다.**

**근본 원인**: `design-system.md`는 **토큰 값 + 컴포넌트 계약**(무슨 prop·무슨 상태·무슨 a11y) 명세지 **"어떻게 보여야 하는가"가 아니다.** 그런데 아무도 시각 디자인을 맡지 않았고, 리뷰는 "규칙 위반 여부"만 물어서 통과했다. **문서 준수 ≠ 디자인 완성.**

## 이 에픽이 만드는 것
`docs/ux/mockups/` 아래 **자체완결 정적 HTML**. 선례: `redesign-commerce.html`.
- 브라우저로 바로 열어 보고 판단할 수 있어야 한다(빌드·서버 불요).
- React 구현이 아니라 **디자인 결정의 기록**이다. 확정 후 frontend-impl이 이를 집행한다.

## 분해 (순차 — 톤이 확정돼야 다음이 의미 있다)
- **FC-041**: 공통 셸(헤더·푸터·브랜드) + 홈/랜딩. **톤을 확정하는 티켓.**
- **FC-042**: 경매 목록 + 경매 상세. 확정된 톤을 **데이터 밀집 화면**에 적용해 실전에서 버티는지 검증.

## 계승할 원칙 (design-system.md)
- **§1.2 색 3계층**: 조작=블랙 CTA·퍼플은 액센트(링크·포커스·선택)만. **퍼플을 CTA 채움으로 쓰지 않는다.**
- **Game-Color Containment**: element 4색·검정 아트 슬롯은 **아이템 카드·속성 배지·아이템 필터 칩에만**. 버튼·탭·인풋·크롬·배경·내비에 쓰지 않는다 — 게임색이 조작/크롬으로 새면 커머스 신뢰감이 무너진다.
- 라이트 커머스(무신사·컬리 감각 참조, 값 미복제) · 등급 배지 폐기(D-073) · WCAG 2.1 AA.

## 자산
- `docs/game_ui/` **982파일 추적 중**(2026-07-18 커밋). `item_info/card_image/`(7 카테고리)·`ingame/`(인벤토리·프로필·카드정보 캡처)·`my_card/`.
- 게임 아트를 **어디까지 쓸지가 이 에픽의 핵심 판단** 중 하나다. Containment를 지키면서도 "게임 아이템 거래소"임이 읽혀야 한다.

## 에이전트
**늘리지 않는다**(사용자 지시). 기존 `frontend-impl` + **`impeccable` 스킬**로 진행한다.

## ★ 에픽 종료 (2026-07-19, 사용자 승인)
**FC-041·FC-042·FC-043 전건 done.** 나머지 화면 목업(placeholder 12면)은 **만들지 않고 종료**한다.

### 왜 12면을 안 만드는가
FC-042가 **세 번 반려**되는 동안 원칙이 계속 바뀌었고(타이포 위계 · 반응형 별도 설계 ·
레퍼런스 선행), **그때마다 앞서 만든 목업이 낡았다.** FC-043도 그래서 재점검이 필요했다.
12면을 지금 만들면 같은 일이 12배로 벌어진다. 반면 **실제 화면 몇 개를 React로 완성하면**
목업이 검증되고 남은 화면은 확정된 패턴을 따라가면 된다.

목업은 **시각 방향을 확정하는 도구**였고 그 목적은 달성됐다 — 라이트 커머스 톤, 표면 층 분리,
역할 기반 타이포, 디바이스별 IA 분리, 게임색 Containment 경계가 전부 정본(design-system v0.6.1)에
반영됐다. **정본이 정확하면 구현이 참조할 곳은 정확하다.**

### 이 에픽이 남긴 것
- 목업 6면(홈 2 · 경매 2 · 인증 2)
- `design-system` v0.3 → **v0.6.1**(표면 층 분리 · 골드포스 §5.12 · 역할 기반 타입 스케일 ·
  disabled 토큰화)
- `docs/ux/references/auction-detail-references.md`(823줄) — 레퍼런스 조사
- FC-045가 식별한 **21개 역할의 흡수처 매핑**(14/15/16 → 13 또는 17) — React 반영 시 작업지시서

### 교훈 (반복 금지)
1. **레퍼런스 없이 자체 논리로만 설계하지 마라.** FC-042 1·2차가 그래서 실패했다.
   "제약을 만족시키는 배치"와 "좋아 보이는 화면"은 다르다.
2. **"구리다"의 원인은 배치가 아니라 타이포일 수 있다.** 촘촘한 중간 크기가 모든 글자를
   비슷한 회색 덩어리로 만든다.
3. **원본을 정확히 재현하면 원본의 연식까지 재현된다.** 게임 카드정보 모달을 픽셀까지 맞췄으나
   2001년 UI처럼 보였다. **계승 대상은 아트이지 창(chrome)이 아니다.**
4. **총괄이 규칙의 예외를 만들 때 특히 의심하라.** §1.2 Containment 예외 판정("모달은 아이템
   표현의 연장")은 **규칙이 막으려던 바로 그것**을 정당화했고 철회됐다. 게이트 후로 개정을
   미룬 덕에 정본은 오염되지 않았다.

### 후속
→ **EPIC-FE-REDESIGN**(React 반영). 목업·정본을 실코드에 집행한다.
