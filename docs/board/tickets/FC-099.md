---
id: FC-099
type: task
epic: EPIC-MARKET-DATA
derived_from: FC-097
jira_key: KAN-111
title: 카드/목록/상세 스킬명 표시 + 마켓 대량 스크롤/성능 확인 (frontend)
state: done
owner: frontend-impl
depends_on: [FC-097]
blocks: [FC-100]
gate: null
review_status: passed
contract_ref: docs/spec/skill-exposure-spec.md v1.0
artifacts:
  - frontend/src/features/item/components/ItemSkillSummary.tsx
  - frontend/src/features/item/components/ItemCard.tsx
  - frontend/src/lib/api/auctions.ts
  - frontend/src/pages/ComparePage.tsx
---
## 완료 (frontend-impl · 2026-07-22 · 커밋 d9b380e, push 안 함)
- 공통 item 블록 `AuctionItemBlock`에 skill1Name/skill2Name(nullable) 추가(shop.item 재사용→고정가 자동 대칭). ItemSkillSummary 두 줄 형식(스킬1 줄 / 스킬2 줄 + skillPercent% 오렌지, 슬롯2에만). AuctionHeroCard·ShopHeroCard·ItemCard·ComparePage 배선. 폴백(name null→스킬 #{code})·마법 스킬1 부재 정상.
- 상세 히어로·비교표는 목업 fidelity 유지(발동확률 전용 행 존치, 퍼센트 병합 안 함).
- **검증**: typecheck·lint 그린·vitest **524 pass**·build 성공. backend/src 0 변경.
- **후속 플래그(미변경·no-regression)**: 마켓 목록 무가상화 + MarketPage useNow 매초 리렌더 → 5천 누적 시 잰더 가능. **FC-098 시드 후 실측 검증** 필요(필요 시 마켓 카드 per-second now 제거 or 가상화 별도 티켓).


## 목표
FC-097 계약대로 카드/목록/상세에 스킬명을 표시하고(현재 "스킬 #{code}" → 효과 서술), 마켓 5천 대량에서 무한스크롤/성능을 확인한다.

## DoD
- **스킬명 표시**: `features/item/skillSlots.ts` `skillLabelOf`가 노출된 스킬명을 렌더(방식 a=응답 필드 소비 / b=GET /skills 딕셔너리 훅). 표시 형식 **스킬1 줄 [효과] / 스킬2 줄 [효과] [퍼센트]%**(예 "트리플샷 33%"). 마법 스킬1 부재는 정상 표시(§6 #4). 미등록 코드는 중립표기 폴백 유지.
- 소비처 반영: ItemCard·ItemInstanceDetail·AuctionHeroCard·ShopHeroCard·ItemSkillSummary·비교표.
- **대량 성능**: 마켓 5천에서 커서 무한스크롤·중복요청 방지·카드 렌더 성능 확인(목업 §17). 회귀 없게.
- npm run build + eslint + vitest 그린.

## 근거인용
- skill-exposure-spec(FC-097)·frontend skillSlots/ItemSkillSummary·목업 §7(스킬 우선)·§17(페이징)·impeccable 스킬.

## 파생 경위: FC-097 계약 확정 후 프론트. FC-098(backend)와 파일 무교차 병렬.
