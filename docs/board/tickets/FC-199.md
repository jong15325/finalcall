---
id: FC-199
type: task
epic: EPIC-BOARD
derived_from: FC-196
jira_key: KAN-227
title: 댓글 — Comment CRUD·인가
state: done
owner: backend-impl
state_note: |
  구현 완료·검증(2026-08-06): Comment 엔티티(parent_comment_id 컬럼만 예약)·CommentErrorCode(COMMENT_001/002)·Repository·Service·Controller·DTO + PostRepository 원자 increment/decrementCommentCount(가법) + SecurityConfig 댓글 GET permitAll. allow_comments 게이팅(BOARD_003)·IDOR(COMMENT_002)·comment_count 동일TX 원자동기화·언더플로 가드. CommentApiIntegrationTest 12/12 그린, spotless·checkstyle 통과. 배치 reviewer 대기. 미커밋.
depends_on: [FC-198]
blocks: [FC-203]
gate: null
review_status: passed
contract_ref: docs/spec/api-contract.md v1.23 §6, board-domain-spec.md v1.0
artifacts: []
---
## 목표
댓글(Comment) 도메인 구현 — 게시판 allow_comments 옵션 존중. 작성·조회·수정·삭제. post.comment_count 비정규화 동기화.

## DoD
1. **Comment 엔티티** — public_id·post_id FK·author_id(nullable)·author_nickname·content(1000)·parent_comment_id(self-FK **예약만**·reply UI 다음 에픽)·soft delete.
2. **API**:
   - `GET /posts/{postPublicId}/comments`(offset 목록, 공개).
   - `POST /posts/{postPublicId}/comments`(인증 + board.allow_comments).
   - `PUT`·`DELETE /posts/{postPublicId}/comments/{commentPublicId}`(인증 + 작성자 OR ROLE_ADMIN).
3. **comment_count 동기화** — 작성/삭제 시 post.comment_count 증감(같은 TX).
4. **인가** — allow_comments 차단·작성자/관리자 소유검증(IDOR).
5. 빌드·spotless·checkstyle 통과.

## 근거인용
board-domain-spec v1.0(Comment·allow_comments), api-contract v1.23 §6.

## 파생 경위: FC-196 계약 확정 후 구현 팬아웃 T4.
