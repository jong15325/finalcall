---
id: FC-089
type: task
epic: EPIC-PURCHASE
derived_from: FC-088
jira_key: KAN-99
title: 즉시구매(PurchaseService)+거래내역 API+SettlementRecorder 추출 (backend)
state: review
owner: backend-impl
depends_on: [FC-088]
blocks: [FC-091]
gate: null
review_status: passed
contract_ref: docs/spec/purchase-spec.md v1.0
artifacts:
  - backend/src/main/java/com/finalcall/domain/settlement
  - backend/src/main/java/com/finalcall/api/purchase
---
## 구현 완료 (backend-impl · commit fda5240)
- SettlementRecorder 추출(공통 정산 꼬리·MANDATORY TX)·CloseService 리팩터(마감 SOLD/UNSOLD 무회귀 확인).
- PurchaseService(AuctionPurchaseContext FOR UPDATE·패자 RELEASE+OUTBID·구매자 직접차감·markSoldBuyNowIfLive CAS end_at>now)·PurchaseController(POST purchase·201·AUCTION_005/006/009·BID_005).
- orders API(QueryDSL cursor·buyer OR seller 스코프·IDOR ORDER_001/002·역할 DTO fee/settle SELLER만 @JsonInclude NON_NULL·마스킹).
- 검증: spotless/checkstyle · **test 279**(신규 24, Testcontainers): P-A~P-H 총량보존·동시 즉시구매 idempotency·즉시구매vs마감 시간축 배타·orders IDOR/역할노출. **마이그레이션 없음**.
- **관찰(reviewer 판정)**: 교차거래 데드락 표면(EPIC-CLOSING M1 동일, spec §3.4 재시도 수용) — strict A4(user_id 정렬) 적용 필요 여부.
- **concurrency-review 검수 대상(FC-090과 묶음).**
## A4 수정 (backend-impl · commit b44aea0) — 리뷰 MAJOR-1 반영
- 잔액 락 user_id 오름차순(`applyBalanceInUserIdOrder`): 구매자 차감·판매자 크레딧·패자 해제를 정렬 실행. SettlementRecorder에서 판매자 크레딧 분리(잔액 무접촉), CloseService가 크레딧 회수(마감 무회귀).
- PurchaseDeadlockRegressionIntegrationTest 신설: 교차 즉시구매·purchase×bid AB-BA → 락 실패 0. test 281. A4 델타 재검 대기.

## 목표
purchase-spec v1.0대로: SettlementRecorder 추출(CloseService 리팩터·마감 무회귀) + PurchaseService(즉시구매 동시성·직접차감·패자해제·BUYNOW live CAS) + PurchaseController(POST purchase) + 거래내역 API(GET /me/orders·/orders/{id}·역할 노출·IDOR). **스키마·마이그레이션 없음**.

## DoD
- 불변식 P-A~P-H(총량보존·이중정산 없음·에스크로 정합) 테스트. 동시성 통합(즉시구매 idempotency·마감 경합·패자해제·IDOR·역할노출).
- 기존 255 테스트 그린 유지(마감 SOLD 무회귀). spotless/checkstyle·:backend:test 통과.

## 근거인용
- purchase-spec v1.0·closing-domain-spec(정산 재사용)·fee-policy-spec·api-contract v1.13. concurrency-review·jpa-convention 스킬.

## 파생 경위: FC-088 게이트2 승인 후 backend 구현. FC-090(frontend)과 병렬(다른 코드베이스).
