---
id: FC-188
type: task
epic: EPIC-ITEM-DELIVERY
derived_from: FC-185
jira_key: KAN-214
title: 소유 이동 + 재판매 차단 + 실패 안전보관 (G5·G6)
state: review
owner: backend-impl
state_note: MAJOR-2 재작업 완료. 가드 상태집합을 LISTING_BLOCKING_STATUSES(PENDING/CLAIMED/DEFERRED/APPLIED=FAILED 아닌 전부)로 확장해 apply~IN_GAME lag 창 봉쇄(D-F). reconciler(APPLIED 리터럴)·sweeper(CLAIMED 리터럴)는 상수 미참조라 무영향. lag 차단 테스트 2건 추가→DeliveryLifecycleIntegrationTest 11/11 green, auction 20/20·shop 16/16 green. architect spec v1.1 정의와 수렴. FC-191 재검토 대기.
depends_on: [FC-186]
blocks: []
gate: null
review_status: passed
contract_ref: docs/spec/delivery-domain-spec.md
artifacts:
  - backend/src/main/java/com/finalcall/domain/item
  - backend/src/main/java/com/finalcall/domain/delivery
---
## 목표 (EPIC-ITEM-DELIVERY T4)
배송 상태에 따른 item_instance 소유 이동과 실패 회수 규약을 구현한다. 웹↔게임 이중 존재·재판매를 원천 차단.

## DoD
1. **G5 이동 모델** — 배송 APPLIED 시 item_instance를 "게임 이관됨" 상태로 전이(FC-185 확정 형상). 이관 아이템은 리스팅/재판매 불가(location XOR 불변식 연장).
2. **배송 대기 커스터디** — APPLIED 전까지 item_instance는 구매자 소유·finalcall 커스터디(TEMP/배송대기).
3. **G6 실패 회수** — 만실(확장 상한 96 도달)·타임아웃 시 우편함 DEFERRED 보관·멱등 재시도. 하드 실패(매핑 불가 등) FAILED 격리 + 관리자 개입 훅. **금전 미역전**(I-H 보존).
4. 상태 전이 불변식 테스트 + 재판매 차단 테스트.

## 근거인용
delivery-domain-spec(FC-185), proposal §6.1·§7.1. item-domain-spec §3.1 location XOR·§2.5 temp custody.

## 파생 경위: 게이트1 T4. FC-186 후. FC-187과 파일 겹쳐 직렬.
