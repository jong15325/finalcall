---
id: FC-012
type: task
epic: EPIC-FE-MEMBER
derived_from: null
jira_key: KAN-15
title: 프론트 내 계정 — 계약 정합 + 화면 spec + API 함수층 설계
state: done
owner: architect
depends_on: []
blocks: [FC-013, FC-014, FC-015]
gate: null
review_status: passed
contract_ref: docs/spec/api-contract.md §2·§2.5·§4.4
artifacts:
  - docs/spec/frontend-account-spec.md
---
## architect 산출 요약 (FC-012 done)
- 계약 사본 폐기 → 모노레포 단일정본 직접 참조(README 3건 정정 지시: 경로·v1.5·사본부재). errorCodes.ts에 COMMON_005 상수 추가 지시.
- **핵심 발견**: §2 login 응답에 user 요약 없음 → 로그인은 `GET /me` 하이드레이션 후 setSession(계약 변경 불요, §2.5 정본). `getMe`가 auth·member 공유.
- 잔액 표시=마이페이지 잔액 카드(§4.4 재사용, 탈퇴 동의 UI가 이미 요구). FC-015 디자인 게이트 불요.
- COMMON_005는 기존 client.ts 401 경로가 흡수(신규 분기 불요), "탈퇴 계정" 특정 카피 금지(SEC-007).
- **팬아웃 판정**: FC-013/014/015 병렬 부적합(getMe·ProfilePage·authStore.updateUser 공유 교차). → **단일 frontend-impl 1패스 실행**(보드는 3티켓 유지) 또는 순차. 총괄 채택: 1패스.
- 디자인 방향 초안: AuthFormLayout(로그인/가입)·ProtectedLayout 3섹션(프로필/잔액/탈퇴)·탈퇴 Modal(D-080 4요소). 목업 `docs/ux/mockups/member.html`·`wireframe-auth.html` 정합.
---
## 목표
- 프론트 내 계정 에픽(auth+마이페이지+잔액)의 계약/설계를 확정해 frontend-impl 팬아웃 근거를 만든다(contract-first).

## DoD
- **계약 정합(D-030)**: 프론트의 계약 참조를 모노레포 정본(`docs/spec/api-contract.md` v1.5)에 맞춘다. 프론트 README가 v1.4·미존재 사본을 참조 → 정본 경로·버전으로 정정(사본 유지 여부 판정, 모노레포라 단일정본 권장).
- **화면 spec**: 로그인·회원가입·마이페이지(프로필/수정/탈퇴)·잔액 표시 각 화면의 요소·플로우·상태 매트릭스(로딩/에러/빈/성공) + 에러코드→UI 매핑(AUTH_001/002/003, MEMBER_001/002, COMMON_005, 검증 400 errors[]).
- **API 함수층 설계**: `features/` 구조(auth·member), 소비 엔드포인트(§2 login/signup/logout·§2.5 me GET/PATCH/DELETE·§4.4 balance), TanStack Query 키/뮤테이션 규약, stub 세션 제거 계획.
- **디자인 게이트 입력**: 새 화면(로그인·가입·마이페이지) 디자인 방향 초안(레이아웃·핵심 UX)을 총괄이 디자인 게이트에서 사용자에 제시할 수 있게 정리.
- 산출물은 `docs/spec/`에 기록. 코드 수정 없음(architect = 읽기+spec 쓰기).

## 근거(인용)
> 계약 §2(auth)·§2.5(member)·§4.4(balance) 확정. 프론트 스켈레톤(lib/api/client 401 회전·authStore·레이아웃) 재사용. README D-030 계약 사본 규약.
