---
id: FC-001
type: task
epic: EPIC-MEMBER
derived_from: null
jira_key: KAN-3
title: 계약 2.5절 검증 + MEMBER_002 방식 정합 확인
state: done
owner: architect
depends_on: []
blocks: [FC-002, FC-003]
gate: null
review_status: pending
contract_ref: docs/spec/api-contract.md 2.5절
artifacts: []
---
## 목표
- api-contract 2.5절(GET/PATCH/DELETE /me)의 완결성·일관성을 검증한다. 신규 계약 작성은 불필요(v1.4 존재).
- MEMBER_002 탈퇴 차단을 (A) 확장 지점 방식(현재 gameMoneyHeld>0만, 경매·주문은 TODO)으로 갈 때 계약과 모순이 없는지 확인한다.

## DoD
- 2.5절 3개 엔드포인트의 요청/응답/에러가 구현 가능하게 완결됐음을 확인(누락·불명확 시 게이트2 상신).
- MEMBER_002 (A)안이 계약 문구와 정합함을 확인, 불일치 시 게이트2.

## 근거(인용)
> api-contract 2.5절(GET/PATCH/DELETE /me), MEMBER_001/002(5절). 게이트1 승인: MEMBER_002 = (A) 확장 지점.

## 검증 (완료)
- 계약 §2.5 3개 엔드포인트 + MEMBER_001/002가 erd·domain-spec과 3계층 정합. **게이트2 불요, 구현 착수 가능.**
- MEMBER_002 (A)안 정합: 계약을 더 좁게(관대하게) 이행할 뿐 모순 없음. auction/bid/order 부재로 false-allow 여지 없음.
- backend-impl 주의점(하위 티켓에 반영): ① UserRepository 조회는 `...AndIsDeletedFalse` 활성필터 동반(D-081) ② MEMBER_002 경매·주문 체크 TODO 명시 ③ AUTH_002(가입) vs MEMBER_001(수정) 같은 UK·다른 코드 ④ createdAt은 BaseTimeEntity 공통 컬럼 ⑤ 재탈퇴는 `_active` 생성 컬럼 UK 패턴(D-081).
