---
id: FC-134
type: task
epic: EPIC-EMAIL-TEMPLATE
derived_from: FC-129
jira_key: KAN-150
title: T2 — EmailTemplate 엔티티·EmailTemplateKey·MailContentType·Repository
state: todo
owner: backend-impl
depends_on: [FC-133]
blocks: [FC-135]
gate: null
review_status: pending
contract_ref: docs/spec/email-template-spec.md §3·§4
artifacts: []
---
## 목표
mail feature의 템플릿 엔티티·자연키 enum·리포지토리를 신설한다.

## DoD
- feature `com.finalcall.domain.mail`. `entity/`: `EmailTemplate`(BaseTimeEntity 상속·@NoArgsConstructor(PROTECTED)·@Builder·@Setter 금지, V18 스키마 매핑), `EmailTemplateKey`(enum, 자연키 = enum name, **필수 변수 이름 집합 코드 선언** — `EMAIL_VERIFICATION`→{code, expiryMinutes}), `MailContentType`(enum TEXT/HTML).
- `repository/`: `EmailTemplateRepository` + `findByKeyOrThrow(EmailTemplateKey, ErrorCode)` default 패턴(섹션 5). 비활성(`is_active=0`)은 조회 제외 또는 호출측 판정 — spec §5.4 MAIL_001(없음/비활성) 정합.
- ddl-auto=validate 통과(V18 적용 DB 필요). ArchUnit slice 규칙 통과(mail→infra 미참조·mail→common만).

## 근거인용
spec §3(엔티티)·§4(키·변수 계약), 섹션 5(엔티티·Repository 컨벤션).

## 파생 경위
EPIC-EMAIL-TEMPLATE 분해 T2. FC-133(V18) 선행.
