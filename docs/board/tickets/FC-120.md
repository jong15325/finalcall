---
id: FC-120
type: task
epic: EPIC-RESTRUCTURE
derived_from: null
jira_key: KAN-131
title: Phase 0/1 — ArchUnit 신규 규칙 병존 + 커널 위치 확정
state: review
owner: backend-impl
depends_on: [FC-119]
blocks: [FC-121]
gate: null
review_status: pending
contract_ref: docs/common/proposals/layer-restructure-proposal-v0.1.md §5·§3
artifacts: []
---
## 목표
feature-first용 신규 ArchUnit 규칙을 **구 규칙과 병존**으로 추가하고, 공용 커널 위치를 확정한다. 이동 중 강제 공백을 없앤다.

## DoD
- 신규 ArchUnit 규칙 추가(FC-119 스펙 기준): (1) 슬라이스 내부 계층방향(naming rule) (2) 슬라이스 비순환 (3) 커널(`common`·`infra`) 무의존. **구 `LayerDependencyTest` 최상위-레이어 규칙은 남겨둠**(Phase 3에서 제거).
- 커널 확정: `common`(17)·`infra`(19)·`domain/common` BaseEntity 3종 위치 결정(대개 제자리). 이동 필요 시 최소화.
- `./gradlew :backend:test` green(신·구 규칙 동시 통과 — 아직 코드는 구 배치라 구 규칙 통과, 신 규칙은 커널·명명 기준 통과).
- `:backend:spotlessApply` 통과.

## 근거인용
proposal §3 옵션 B/C ArchUnit 재설계, §5 Phase 0·1, §8 병존 전략.

## 파생 경위
EPIC-RESTRUCTURE Phase 0(code)+Phase 1. FC-119 규약 선행.
