---
id: FC-132
type: task
epic: EPIC-EMAIL-VERIFY
derived_from: FC-114
jira_key: KAN-147
title: B7 — 이메일 엔드포인트 3종 + service + /me 노출
state: review
owner: backend-impl
depends_on: [FC-118, FC-128, FC-129, FC-130, FC-135]
blocks: []
gate: null
review_status: pending
contract_ref: docs/spec/email-verify-spec.md §4.2~4.6·§2.3
artifacts:
  - backend/src/main/java/com/finalcall/domain/member/config/EmailVerificationStoreConfig.java
  - backend/src/main/java/com/finalcall/domain/member/service/EmailVerificationService.java
  - backend/src/main/java/com/finalcall/domain/member/controller/MemberEmailController.java
  - backend/src/main/java/com/finalcall/domain/member/dto/EmailSetRequest.java
  - backend/src/main/java/com/finalcall/domain/member/dto/EmailResponse.java
  - backend/src/main/java/com/finalcall/domain/member/dto/EmailVerifyRequest.java
  - backend/src/main/java/com/finalcall/domain/member/dto/EmailVerifiedResponse.java
  - backend/src/main/java/com/finalcall/domain/member/dto/MemberProfileResponse.java
  - backend/src/test/java/com/finalcall/domain/member/service/EmailVerificationServiceTest.java
  - backend/src/test/java/com/finalcall/integration/MeEmailApiIntegrationTest.java
---
## 목표
이메일 설정·인증 코어 엔드포인트 3종 + 서비스 + 프로필 노출을 구현한다.

## DoD
- `PUT /api/v1/me/email`: `assignEmail`(정규화)·`email_verified=false` 재초기화·pending 코드/쿨다운 삭제(TOCTOU 주방어)·동일 이메일 no-op. 응답 200 `{email, emailVerified:false}`.
- `POST /api/v1/me/email/verification-request`: 미설정→`EMAIL_006`·이미인증→`EMAIL_005`·쿨다운→`EMAIL_004`·통과 시 코드 생성·저장·SMTP 발송·쿨다운. 응답 **202**(본문 없음).
- `POST /api/v1/me/email/verify`: `{code}`(`@Pattern("\\d{6}")`) → §2.3 검증 시퀀스 → `markEmailVerified`. 응답 200 `{emailVerified:true}`. 에러 EMAIL_001/002/003/005.
- `EmailVerificationService` + `MemberService` 이메일 설정. 배치 = `com.finalcall.domain.member.{controller,service,dto}`.
- `MemberProfileResponse`에 `emailVerified`(bool)·`emailMasked`(nullable) 추가(GET /me·PATCH /me 공유). 원문 미노출.
- `/api/v1/me/**`는 이미 authenticated → SecurityConfig 무변경. 주체=SecurityContext(임의 이메일 파라미터 없음).

## 근거인용
spec §4.2~4.6·§2.3(TOCTOU·Lua 검증). 동시성·인가 concurrency-review 스킬.

## 파생 경위
EPIC-EMAIL-VERIFY 분해 B7. FC-118·128·129·130 선행.

## 소비 배선 갱신 (2026-07-27 · EPIC-EMAIL-TEMPLATE 연동)
verification-request 경로에서 `EmailSender`를 직접 호출하지 않고 **템플릿 렌더 후 발송**으로 조립한다(spec email-template §2.3):
1. `code = codeStore.issue(userId, email)` (FC-128, 평문 코드·민감값)
2. `rendered = emailTemplateService.render(EmailTemplateKey.EMAIL_VERIFICATION, {code, expiryMinutes})` (FC-135)
3. `emailSender.send(email, rendered.subject(), rendered.body(), rendered.html())` (FC-129 reworked)
- `expiryMinutes` = `EmailVerifyProperties.ttlSeconds/60` 파생. depends_on에 FC-135 추가. **엔드포인트 계약·응답 형상은 불변**.
- FC-128 verify enum = `VerifyOutcome`{EXPIRED, ATTEMPTS_EXCEEDED, MISMATCH, SUCCESS} → EMAIL_001/002/003 매핑, issue Optional.empty()=쿨다운=EMAIL_004(FC-128 구현 노트).
