---
id: FC-093
type: task
epic: EPIC-SHOP
derived_from: FC-092
jira_key: KAN-104
title: shop 엔티티/Repo/Flyway + 등록·구매·취소 서비스 + 만료 워커 + ShopController (backend)
state: review
owner: backend-impl
depends_on: [FC-092]
blocks: [FC-095]
gate: null
review_status: pending
contract_ref: docs/spec/shop-spec.md v0.2
artifacts:
  - backend/src/main/java/com/finalcall/domain/shop/
  - backend/src/main/java/com/finalcall/api/shop/
  - backend/src/main/resources/db/migration/V15__shop.sql
---
## 완료 (backend-impl · 2026-07-22 · 커밋 5855626, push 안 함)
- 신규: domain/shop(Shop·ShopStatus·ShopErrorCode SHOP_001~006·ShopListingProperties(기본 7일)·ShopExpiryWorkerProperties(60초·200)·Repo(QueryDSL·FOR UPDATE 프로젝션)·ShopService(등록·목록·상세·취소)·ShopPurchaseService·ShopExpiryService/Worker) + api/shop(Controller·DTO) + V15__shop.sql.
- 변경: InventoryService(`recoverExpiredToTemp` LISTED→TEMP 무조건·소유자 불변)·SecurityConfig(GET /shops 공개)·application.yml.
- 재사용: SettlementRecorder(SHOP,shop.id)·FeeCalculator·Inventory markListed/releaseFromListing·UserBalance inc/dec·orders 자동 유입.
- **검증**: shop 신규 28건 + 전체 **309건 0실패/0에러**, `:backend:build` SUCCESSFUL(spotless·checkstyle maxWarnings 0). **계약 이탈 0**(잔액 user_id 오름차순·종료성 CAS 시간축 배타·sale_order UK 이중판매·만료 TEMP 직행·SHOP_001 403·cancel POST·등록 body 서버 end_at 자동·총량보존 S-H).
- **후속 하드닝 백로그**(게이트2 승인분 아님·범위 밖): (1) 목록 endAt keyset은 유한 기한만 가정 — 향후 무기한 캐시아이템(end_at NULL) 목록 노출 시 nullable keyset 필요. (2) shop 목록 필터 축소집합(category·level·price, skill/goldforce 제외) — 프론트 요구 시 파리티 확장.

## 목표
FC-092 계약(shop-spec v1.0·게이트2 승인분)대로 고정가 마켓 서버를 구현한다. 정산 공통 꼬리(SettlementRecorder)·수수료·소유이전·에스크로는 재사용, shop 애그리거트 머리만 신규.

## DoD
- shop 엔티티 + ShopStatus(ACTIVE/SOLD/EXPIRED/CANCELLED) + ShopRepository(구매 락 프로젝션 FOR UPDATE·종료성 CAS) + 신규 Flyway shop 테이블.
- ShopService(등록: INVENTORY→LISTED CAS 재사용 / 취소: releaseFromListing 재사용) + ShopPurchaseService(구매: 락 스냅샷→재검증→수수료 1회→잔액 buyer/seller user_id 오름차순→SettlementRecorder(SHOP,shop.id) 위임→SOLD CAS) + 만료 워커(기한 스캔·EXPIRED 전이·TEMP 회수).
- ShopController(POST/GET /shops·POST /shops/{id}/purchase·PATCH .../cancel) + SHOP ErrorCode enum(SHOP_001~006).
- 기한 설정 옵션 @ConfigurationProperties. 총량보존·이중판매 UK 준수. 테스트(구매 동시성·만료·취소·불변식).
- spotlessApply + checkstyle 통과. 계약 이탈 금지(게이트2 승인분 정확히 준수 — 하드닝 후속으로 남기지 말 것, FC-089 A4 교훈).

## 근거인용
- shop-spec v1.0·closing/purchase-spec(정산 재사용)·concurrency-review·jpa-convention 스킬.

## 파생 경위: FC-092 계약 확정 후 백엔드 구현. FC-094(frontend)와 파일 무교차 병렬.
