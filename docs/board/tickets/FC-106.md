---
id: FC-106
type: task
epic: EPIC-SEARCH
derived_from: null
jira_key: KAN-120
title: 검색 ②단계 구현 spec 확정(엔진·동기·인덱스·한글·로컬) + q/relevance 계약 → 게이트2
state: done
owner: architect
depends_on: []
blocks: [FC-107, FC-108]
gate: gate2
review_status: passed
contract_ref: docs/spec/search-spec.md v0.1
artifacts:
  - docs/spec/search-spec.md
  - docs/spec/api-contract.md
---
> review_status=passed 근거: spec 산출물 + 게이트2 승인(2026-07-22). architect 직접 확정.
## 완료 (architect · 게이트2 승인 2026-07-22)
- `search-spec.md` v0.3(§12 구현 결정) + api-contract §3 PROPOSAL(C1~C3).
- **게이트2 승인분(사용자 결정)**: A2 엔진=**Elasticsearch**(+nori 플러그인)·A3 동기=**Debezium CDC**(MySQL binlog→Kafka KRaft→ES sink, 멱등 upsert·snapshot 백필)·A1 인프라(신규 컨테이너 elasticsearch·kafka·kafka-connect)·A5 alias 재색인+주기 화해·C1 q(마켓+경매)·C2 relevance(무q relevance=400)·C3 규약(2~64자·match만·인젝션 불요). sellerGrade 부스트 제외(GRADE 의존).
- **로컬 부담 큰 구성**(포트폴리오 사실성 선택). dual-write 금지·MySQL 정본·정확성은 DB.
- 인계: FC-107 backend(docker 인프라·CDC 커넥터·색인·검색API·화해) · FC-108 frontend(검색 UI).

## 목표
search-spec ②단계(전용 검색엔진)를 구현 가능한 수준으로 확정한다. 큰 아키텍처 결정(엔진·동기·인프라)은 게이트2로 상신. 아키텍처·계약·스키마(인덱스) 파급이라 게이트2.

## DoD
- **spec 확정(search-spec v0.2 승격)**: 목표 아키텍처를 구현 결정으로 — (1) **엔진 추천**(OpenSearch vs Elasticsearch, §8 근거·로컬 기동성·라이선스), (2) **동기 방식 추천**(Outbox vs Debezium CDC — **로컬 Kafka 필요 여부**·멱등 싱크·정합), (3) **인덱스 매핑**(§5 SearchDoc, sellerGrade 제외), (4) **재색인/화해**(alias 스위치·count 화해), (5) **한글**(nori+ngram 멀티필드·userdict 운영), (6) **랭킹**(BM25 + relevance 정렬, 등급 부스트 제외=GRADE 의존).
- **★ 로컬 기동성**: 이 PC(Windows/docker)에서 데모가 돌아야 함 — 컨테이너 구성(엔진 1개 + 동기)·Kafka 회피 여부·docker-compose 반영안. 무거운 운영 구성은 범위 밖.
- **계약(C1~C3)**: `q` 파라미터(마켓+경매 목록)·`relevance` 정렬·q 규약(최소길이·이스케이프·무q 시 relevance 처리). api-contract 델타(PROPOSAL).
- 반환에 **게이트2 상신 항목**(A1~A3·A5·C1~C3) 추천 + 근거 + 로컬 기동성.

## 근거인용
- search-spec v0.1 전절·api-contract §3/§4.1·domain-spec §8(ES 정본 아님·정확성은 DB)·erd(nameSnapshot)·기존 Shop/Auction 목록 실측.

## 파생 경위: EPIC-SEARCH 게이트1 승인(방식 B·마켓+경매) 후 contract-first. ②단계 구현 확정.
