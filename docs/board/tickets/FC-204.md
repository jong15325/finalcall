---
id: FC-204
type: task
epic: EPIC-BOARD
derived_from: FC-196
jira_key: KAN-231
title: 이미지 첨부 UI + 기존 공지 페이지 새 API 전환
state: done
owner: frontend-impl
review_status_note: CHANGES-REQUESTED 해소(2026-08-06) — MINOR-2(순차 업로드로 순서 보존)·MINOR-3(이동버튼 focus-within) 수정. build/type/lint 통과. E2E 실검증 대기.
state_note: |
  구현 완료·검증(2026-08-06): boardImages API·ImageUploader(선택·업로드·미리보기·순번·좌우이동·삭제·≤10·5MB 가드)·client FormData 멀티파트·PostForm imagePublicIds 제출·수정 최종집합 프리필·NoticeSection GET /boards/notice/posts 실연동. 죽은 /notices 참조 0(grep). presigned 미저장·본문 URL 미저장. typecheck·lint·vite build 통과, 696 pass(3 fail=선재 oauth). E2E는 백엔드 기동 후. 미커밋.
depends_on: [FC-200, FC-201, FC-202]
blocks: []
gate: null
review_status: passed
contract_ref: docs/spec/api-contract.md v1.23 §6.4
artifacts: []
---
## 목표
글 작성 화면 이미지 첨부 UI(presigned 업로드) + 기존 홈/공지 페이지를 새 게시판 API로 전환.

## DoD
1. **이미지 첨부 UI** — 업로드(`POST /board-images`)→imagePublicId 바인딩→글 상세 응답 images[].url(presigned) 렌더. 여러 장·정렬. 본문 URL 미저장(첨부 갤러리 모델).
2. **공지 페이지 전환** — 기존 홈 "공지" 위젯·`/community`(ComingSoon 등) 대상을 새 board API(`GET /boards/공지/posts`)로 전환. 흡수된 공지 데이터 노출 검증.
3. 이미지 미리보기·삭제, 업로드 실패 처리.
4. 디자인은 FC-202 승인 방향 계승.

## 근거인용
api-contract v1.23 §6.4(이미지)·§6(게시판). 게이트2(b) 공지 흡수 프론트 전환.

## 파생 경위: FC-196 계약 확정 후 구현 팬아웃 T9.
