---
id: FC-116
type: task
epic: null
derived_from: FC-110
jira_key: null
title: 운영 온디맨드 재색인 API + 무중단 alias 스위치 구현 — 관리자 페이지 에픽 대기 (백로그)
state: todo
owner: backend-impl
depends_on: []
blocks: []
gate: null
review_status: pending
contract_ref: search-spec v0.4 §12.5.1~12.5.5 · api-contract v1.14 §4.5
artifacts: []
---
## 목표
FC-110 DoD#3(운영 초기색인 전략)의 **구현**. 계약·절차는 architect가 확정(search-spec v0.4 §12.5·api-contract v1.14 §4.5)했고, **구현만 이월**된 상태다.

## 이월 경위 (2026-07-24 사용자 결정)
게이트2로 "관리자 온디맨드 재색인 API + 무중단 blue-green"을 지금 구현하기로 했으나, 착수 직전 확인에서 **관리자 계정 체계가 없음**(is_admin 기본 false·승격 경로·관리자 UI 부재)이 드러났다. 사용자가 **"관리자는 추후 관리자 페이지를 분리 신설하며 함께"** 하기로 결정 → 재색인 API 구현도 그 관리자 에픽으로 이월. FC-110은 #1·#2·#4·#5로 종결.

## DoD (착수 시 — 계약은 이미 확정)
- `AdminSearchController`: `POST /api/v1/admin/search/reindex`(비동기 202+jobId)·`GET .../reindex/{jobId}`(상태).
- `ReindexJobService`: 인메모리 single-flight·단일 스레드 executor·ULID jobId·상태 홀더.
- 인덱스 스위치 유틸: 신 `listings_v{n+1}` 생성·count 검증·`_aliases` 원자 스위치·구 인덱스 드롭(ES indices API).
- `ListingIndexer`: 물리 인덱스 타깃 지정 오버로드(현재 alias 고정) + `reindexAll(targetIndex)` 백필.
- `SearchErrorCode`: `SEARCH_002`(409)·`SEARCH_003`(404) 추가.
- `SecurityConfig`: `/api/v1/admin/**` `hasRole("ADMIN")` 배선.
- 관리자 계정 프로비저닝(관리자 에픽에서 함께 — 시드 or 승격 경로).

## 선행
- **관리자 페이지 에픽**(회원 롤·관리자 UI·계정 프로비저닝)이 선행 또는 동반. 이 에픽 착수 시 게이트1.

## 파생 경위: FC-110 #3의 구현분. 계약 확정(architect) 후 사용자가 관리자 페이지 에픽으로 이월(2026-07-24).
