---
id: FC-198
type: task
epic: EPIC-BOARD
derived_from: FC-196
jira_key: KAN-226
title: 게시글 — Post CRUD·커서목록·인가
state: done
owner: backend-impl
state_note: |
  구현 완료·검증(2026-08-06): Post 엔티티·PostErrorCode(POST_001/002)·Repository(+Custom/Impl 커서 keyset 2축)·Service·Controller·DTO(PostCreate/Update/Detail/Summary) + SecurityConfig 게시글 GET permitAll. 인가=write_policy(BOARD_002)·IDOR(POST_002)·조회수 원자 UPDATE. PostApiIntegrationTest 13/13·BoardApiIntegrationTest 3/3 그린, spotless·checkstyle·compile 통과. 이미지(imagePublicIds 수용·바인딩 no-op)·comment_count(항상0) 슬롯 개방 — FC-200/199 가법 배선. 배치 reviewer 대기. 미커밋.
depends_on: [FC-197]
blocks: [FC-199, FC-201, FC-202]
gate: null
review_status: passed
contract_ref: docs/spec/api-contract.md v1.23 §6, board-domain-spec.md v1.0
artifacts: []
---
## 목표
게시글(Post) 도메인 구현 — 작성·조회·수정·삭제·커서 목록. 쓰기정책·작성자/관리자 인가.

## DoD
1. **Post 엔티티** — public_id(ULID)·board_id FK·author_id(nullable)·author_nickname 스냅샷·title·content·view_count·comment_count(비정규화)·is_pinned·soft delete.
2. **API**:
   - `GET /boards/{slug}/posts`(커서 목록, 공개) — CursorResponse<T>.
   - `POST /boards/{slug}/posts`(인증 + write_policy: ADMIN_ONLY→ROLE_ADMIN / AUTHENTICATED).
   - `GET /boards/{slug}/posts/{postPublicId}`(공개·조회수 증가).
   - `PUT`·`DELETE`(인증 + 작성자 OR ROLE_ADMIN — IDOR 차단).
3. **인가** — 주체=SecurityContext(B-009), 쓰기정책 게이팅·수정삭제 소유검증.
4. 빌드·spotless·checkstyle 통과.

## 근거인용
board-domain-spec v1.0(Post·인가 불변식), api-contract v1.23 §6.2·§6.3.

## 파생 경위: FC-196 계약 확정 후 구현 팬아웃 T3.
