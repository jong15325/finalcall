---
id: FC-097
type: task
epic: EPIC-MARKET-DATA
derived_from: null
jira_key: KAN-109
title: 스킬 마스터 435 시드 설계 + 스킬명 노출 방식 + 5천 시드 설계 → 게이트2 상신
state: done
owner: architect
depends_on: []
blocks: [FC-098, FC-099]
gate: gate2
review_status: passed
contract_ref: docs/spec/skill-exposure-spec.md v1.0
artifacts:
  - docs/spec/skill-exposure-spec.md
  - docs/spec/api-contract.md
---
> review_status=passed 근거: spec 산출물 + 게이트2 승인(2026-07-22). architect 직접 확정.
## 완료 (architect · 게이트2 승인 2026-07-22)
- `skill-exposure-spec.md` v1.0 + api-contract §3.3 델타(PROPOSAL).
- **게이트2 승인분**: (제품) 스킬명=§5 효과 서술 그대로·5천 **다양한 가격 분포**. (기술 추천 채택) G1 노출=뷰에 skill1Name/skill2Name 필드 추가(쿼리가 skill_definition 이미 fetch join → **N+1 없음**)·G2 미사용 코드(198·199·210~299) 시드 제외·G3 5천=LocalDemoSeeder 확장(로컬 전용, 마이그레이션 아님 → 운영 오염 없음)·G4 스킬 마스터=Flyway V16(UPDATE 5 + INSERT 239, DELETE 금지 FK 안전).
- **정정 사실**: 실제 스킬 244개(스킬1 100~197=98·스킬2 200~209·300~435=146). "435"는 코드 상한. name VARCHAR(50) 충분 → 스키마 무변경. skillPercent 이미 뷰·계약에 존재(표시 % 프론트 조립).
- **파급**: skill1Name/skill2Name는 공통 item 블록이라 AuctionItemView에도 대칭 추가(additive nullable) — EPIC-AUCTION 불변식·CAS·쿼리 무접촉, 티켓 되돌림 없음.
- 인계: FC-098 backend(V16 시드·뷰 노출·5천 시더) · FC-099 frontend(skillLabelOf 배선·대량 성능).

## 목표
마켓 실데이터화 계약을 확정한다. (1) `skill_definition` 435 시드 설계, (2) 카드/목록/상세 **스킬명(+skillPercent) 노출 방식** 결정, (3) 마켓 5천 대량 시드 설계. 계약·스키마·시드라 게이트2 상신.

## DoD
- **스킬 마스터 시드 설계**: §5(스킬1 100~199·스킬2 200~435) → `skill_definition(skill_code, name, description)` 매핑 규칙. **name = §5 효과 서술 그대로**(사용자 확정 "기존 이름 그대로"), description = 적용대상·전체효과 보강. 미사용 코드(198·199·210~299) 처리(제외 or 표기). Flyway 방식(V9 5건 대체/확장) 지정.
- **스킬명 노출 방식 결정(게이트2 핵심)**: 카드/목록 뷰(ShopItemView·AuctionItemView)에 스킬명 노출을 (a) 응답에 skill1Name/skill2Name(+skillPercent) 필드 추가(조인) vs (b) `GET /skills` 딕셔너리 1회 로드 후 프론트 매핑 — **추천안 + 근거**. 표시 형식은 스킬1 줄/스킬2 줄·퍼센트(예 "트리플샷 33%").
- **5천 시드 설계**: LISTED-direct 인스턴스 발행 + shop 직접 INSERT(정식 register 경로 회피 — 슬롯 96칸 정원·CAS 마찰). item_template 40종×skill_definition 다양성. **프로파일 격리**(로컬 데모 전용 — 운영 오염 회피, LocalDemoSeeder 확장 vs 전용 마이그레이션 택1 추천). 멱등·end_at 동적.
- 반환에 **게이트2 상신 항목**(노출 방식·시드 규칙·프로파일 격리·계약 델타).

## 근거인용
- game-item-skill-format.md §5·item-domain-spec §2.2/§2.3·erd §4.3·api-contract(skill1/skill2 코드)·design-brief E.4(스킬명 노출 백로그)·rebuild-contract-map(스킬 중립표기)·LocalDemoSeeder/V12/V13 실측.

## 파생 경위: EPIC-MARKET-DATA 게이트1 승인 후 contract-first 선행. 게이트2 상신 준비.
