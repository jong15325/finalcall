---
id: FC-007
type: task
epic: EPIC-CURRENCY
derived_from: null
jira_key: KAN-10
title: 계약 4.4/erd 검증 + 교환비율 저장 기전 확정
state: done
owner: architect
depends_on: []
blocks: [FC-008, FC-009]
gate: gate2
review_status: pending
contract_ref: docs/spec/api-contract.md 4.4절
artifacts: []
---
## 목표
- 계약 4.4절(잔액/교환)과 erd `user_balance`·`money_exchange` 스키마의 완결성·정합 검증. FC-009 마이그레이션(V5) 대상 필드 확정.
- **교환비율 저장 기전 확정**: 값 = 1 캐시 = 1,000,000 게임머니(게이트1 승인). 추후 변경 가능 요구를 (a) DB 추적표 vs (b) @ConfigurationProperties(옵션) 중 어느 것으로 충족할지 판정. **새 테이블 추가가 필요하면 스키마 영향이므로 게이트2로 상신.**

## DoD
- 4.4절 잔액/교환 요청·응답·에러(EXC_001/002)가 구현 가능하게 완결됐음을 확인.
- money_exchange 필드(cash_amount·game_money_amount·applied_rate·idempotency) 확정.
- 교환비율 기전 판정(+새 테이블 시 게이트2 상신).

## 근거(인용)
> api-contract 4.4절, erd `money_exchange`(applied_rate 스냅샷·교환비율 파라미터 ON-HOLD), domain-spec 화폐 2단 모델. 게이트1: 비율 1,000,000·변경가능.

## 검증 (완료)
- 교환비율 기전 = **@ConfigurationProperties(+@Validated, env override, 기본 1,000,000)**. rate 테이블 불요 — applied_rate 스냅샷이 추적 충족(입찰 증분 config 선례 domain-spec 4절). 게이트2 불요.
- 잔액 원자화(FC-008) = user_balance 기존(V3), 조건부 @Modifying UPDATE(WHERE cash_balance>=:amt, 영향행 0→EXC_001) 쿼리만. 스키마 영향 없음, 게이트2 불요.
- **게이트2 1건(FC-009)**: money_exchange에 `idempotency_key` + `(user_id, idempotency_key)` 복합 UK 신설 필요(SEC-004 멱등 DB 강제, charge.pg_tx_id 선례). erd 스키마 변경 → 승인 경유.
- V5 = money_exchange 단독. applied_rate DECIMAL precision/scale 명시 필요(예 DECIMAL(20,6)). charge·money_hold는 범위 밖.
