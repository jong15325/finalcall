---
id: FC-052
type: task
epic: EPIC-FE-REDESIGN
derived_from: FC-048
jira_key: KAN-60
title: 아이템 시드 재작성·확장 (V12) — 코드 사전 정합 + 아트 매핑 커버리지
state: done
owner: backend-impl
depends_on: [FC-044]
blocks: [FC-049]
gate: null
review_status: passed
contract_ref: docs/spec/api-contract.md v1.10 §3.3.1, docs/spec/proposals/item-code-dictionary.md §3.3
artifacts: []
---
## 목표
**아이템 아트가 화면에 뜨게 만든다.** 현재 `ItemArtSlot`이 전 화면에서 플레이스홀더("Lv.N" 텍스트)로
나가는데, 원인은 프론트가 아니라 **백엔드 시드의 `type_code`가 아트 파일과 짝이 안 맞는 것**이다.

## ★ 백엔드 한시 동결 해제 (사용자 승인 2026-07-19)
백엔드는 2026-07-18 방침 전환 이후 동결 상태다. **이 티켓만 예외**로 승인됐다.
**범위는 시드 마이그레이션 1건**이며, 도메인 로직·엔티티·API·스키마를 건드리지 않는다.
작업 후 **동결 상태로 복귀**한다. EPIC-CLOSING·EPIC-SHOP·FC-035 minor는 여전히 동결이다.

## ★ 같이 해야 하는 이유 — FC-044가 시드 재작성을 대기시켜 뒀다
FC-044(게이트2 승인 2026-07-19) **D2 = 자리 의미 교정**이 확정됐으나 **집행은 동결 해제 후로
이월**됐다. 지금 커버리지만 늘리면 **낡은 코드 규약으로 시드를 쓴 뒤 또 고쳐야 한다.**

원본 `itm_type` = `[상품군][mainCategory][element][kind]`인데 우리 V9 시드는
`[main][subGroup][element][kind]`로 **두 선두 자리 의미가 뒤바뀌어** 있다.
- 현재 V9: `main_category` 1/2 · `sub_group` 고정 1 → `1111`(물의 검) · `2111`(물의 방패)
- 교정 후: **`main_category` 1 고정**(상품군=아이템 카드) · `sub_group` 1/2/3(무기/방어구/마법)
  → `1111`(무기·물·kind1) · `1211`(방어구·물·kind1)
- **동시 해소**: `2111`이 원본 SILVER 상품 대역을 침범하던 충돌이 사라진다.

**작업지시서**: `docs/spec/proposals/item-code-dictionary.md §3.3` 대조표
(**코드 변경 4건 + 표시명 정정 6건**). 원본 기준 `kind1=AXE`라 현행 "물의 검"은 실제로 `1131`이다.

## 확정된 코드 사전 (계약 v1.10 §3.3.1)
```
type_code = main*1000 + sub*100 + element*10 + kind     (산식 무변경)
main_category : 1 = 아이템 카드 (현 스코프에서 상수)
sub_group     : 1 무기 · 2 방어구 · 3 마법
element       : 1 물 · 2 불 · 3 흙 · 4 바람
kind (무기)   : 1 도끼 · 2 완드 · 3 검 · 4 활
kind (방어구) : 1 방패 · 2 펜던트 · 3 갑옷 · 4 부츠
kind (마법)   : 1 일반 · 2 특수          ← 2값뿐
```

## 아트 파일 규칙 (매핑 대상)
```
docs/game_ui/item_info/card_image/gold_black/level{1~9}/{l|s}/{water|fire|earth|wind}/
  {axe|wand|sword|bow|shield|pendant|armor|boots|magic}.png
```
- `l` 50×93(상세·카드) · `s` 26×28(행 썸네일)
- **마법은 `magic.png` 1장뿐** — kind 1(일반)·2(특수)가 아트를 공유한다(2:1로 접힘)
- **레벨이 아트에 구워져 있다**(level1~9 디렉터리가 실제로 다른 스프라이트)

## DoD
- **Flyway `V12__item_seed_recode.sql` 신규 생성.** ★ **V9를 편집하지 마라** — append-only 규율
  (FC-035 m9. V11 편집으로 체크섬이 깨진 전례가 있다).
- FC-044 §3.3 대조표대로 **기존 8건의 `type_code`·`sub_group`·`main_category`·`display_name` 교정.**
- **아트가 실제로 있는 조합으로 커버리지 확장.** 홈(피처드 1 + 행 4 + 격자 8)과 경매 목록
  (20장 규모)이 **반복 없이 채워질 만큼**은 되어야 한다. 무기·방어구·마법 3분류와 속성 4종이
  고루 섞이고, **레벨도 흩어져야** 한다(아트가 레벨별로 다르므로 시각 다양성이 여기서 나온다).
- `item_instance`도 함께 늘린다(경매에 올릴 실물이 있어야 화면이 찬다).
  **인벤토리 슬롯 UK·`slot_key`(V8) 제약을 지킨다.**
- 기존 시드 데이터와 **FK·UK 충돌이 없어야 한다.** `public_id` 패턴(`SEEDITEM` + 18자리 0패딩)
  유지, 인덱스 이어서 부여.
- **로컬 Flyway 검증** — 기존 DB에서 `V12`가 적용되고 `validate`를 통과한다.
- 기존 테스트 전건 통과(**235 테스트 / 실패 0**이 현재 기준선).

## 제약
- **시드 마이그레이션만.** 엔티티·리포지토리·서비스·컨트롤러·API·스키마 **수정 금지.**
  컬럼 추가·타입 변경·인덱스 변경 전부 금지(D2는 산식·컬럼·UK **무변경**이 전제다).
- `frontend/**`·`docs/**` 수정 금지(발견은 보고만). 아트 매핑 프론트 구현은 **FC-049 소관**이다.
- **아트가 없는 조합을 시드에 넣지 마라** — 넣으면 그 카드가 플레이스홀더로 나가 이 티켓의 목적이
  반쯤 무산된다. `docs/game_ui/item_info/card_image/gold_black/**`를 **실제로 확인**하고 짝을 맞춰라.
- 코드 스타일 CLAUDE.md §7(`:backend:spotlessApply` → checkstyle).

## 비고
- **게이트2 불요**: D2는 이미 승인됐고(FC-044) 이 티켓은 그 **집행**이다. 커버리지 확장은 데이터지
  계약이 아니다. 다만 작업 중 **스키마 변경이 필요해지면 즉시 멈추고 상신**하라 — 그건 게이트2다.
- 이 티켓이 끝나면 FC-049가 `ItemArtSlot`에 실아트를 매핑한다. **두 티켓이 짝**이다.

## 결과 (2026-07-19)
커밋 `891cf33`. **V12 신규**(V9 무편집). **테스트 235/0 기준선 유지** · Flyway 재검증 통과
(`Successfully validated 12 migrations`) · `ddl-auto: validate` 통과 · 스키마 무변경.

- 교정: 코드 4건(`2111→1211` 등 방어구를 sub_group=2로 이동) + 표시명 6건
  (물의 검→**물의 도끼** 등 — 원본 `kind1=AXE` 정합). `UPDATE`라 FK 유지, 인스턴스 재작성 불요.
- 확장: `item_template` +32 → **40건**(무기16·방어구16·마법8, 상품군 1 전수) ·
  `item_instance` +32 → **42건** · 소유이력 +32(`NOT EXISTS`로 중복 발행 차단).
- 커버리지: element 1~4 **각 10건 균등** · 레벨 1~9 산포(4~5건씩) · **40 템플릿 전부 실물 보유**.
- 아트 짝맞춤: `gold_black/` 72개 디렉터리를 전수 순회해 각 9개 PNG 확인, 총 **648 = 9×2×4×9**
  교차 검증. **아트 없는 조합 0건.**

### 시드 결합 테스트 3건 — 최소 수정
`ItemApiIntegrationTest` 2건(8→40 · 10→42)과 `AuctionRepositorySliceTest.persistTemplate`.
후자는 `persistItem(owner, 1221)`이 템플릿을 무조건 `em.persist`해 **`type_code` UK 중복**으로
깨졌다 → 형제 통합 테스트가 이미 쓰는 **find-or-create** 패턴으로 통일.
`카탈로그_대분류_필터`는 단정이 **공허하게 참**이 되어(시드에 main=2가 사라짐) `!=1 doesNotExist`로 교체.

### ★ 외부 원본 자료로 독립 확인됨 (2026-07-19, 같은 날 후속 조사)
`D:\private_server\...\BackUp\기타\item_type.txt`(원저자 작성, 2019-05-04)가 **FC-044 D2 판정을
독립 확인**했다. 헤더가 `1(무기)1(무기타입)2(속성)2(무기종류)`이고 실제 코드가
`1 [sub_group] [element] [kind]` 구조다:
```
물: 도1111 지1112 검1113 활1114 / 방1211 펜1212 갑1213 신1214 / 필1311 ss필1312
불: 112x·122x·132x   흙: 113x·123x·133x   바람: 114x·124x·134x
```
즉 **FC-052가 집행한 교정이 원본과 정확히 일치**한다. 추론이 아니라 원저자 문서로 확증됐다.

### 이월 (범위 밖)
- **경매 시드 없음** — 인스턴스가 전부 `INVENTORY`라 경매 목록 화면을 채우려면 등록 API를 태워야
  한다. FC-049에서 판단 필요.
- `SeedTestSupport` javadoc이 `V9__item_seed.sql`만 언급(시드 정본이 V9+V12로 늘어난 사실 미반영).
- `AuctionRepositorySliceTest`의 `2221`이 이제 원본 SILVER 대역을 가리킨다(기능 무해, 관례 불일치).
- 계약 §3.3.1이 방어구 kind4를 **"신발"**로 확정 → 티켓 본문의 "부츠" 대신 **계약을 정본**으로
  삼아 시드했다(에이전트 판단, 옳음).
