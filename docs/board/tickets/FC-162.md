---
id: FC-162
type: task
epic: EPIC-NICKNAME-UX
derived_from: FC-160
jira_key: KAN-182
title: 회원가입 닉네임 라이브 중복확인 버튼 활성화
state: done
owner: frontend-impl
depends_on: [FC-160]
blocks: [FC-163]
gate: null
review_status: passed
contract_ref: FC-160 확정 계약 (가용성 조회 엔드포인트)
artifacts: []
---
## 목표
회원가입 화면의 비활성 "중복확인" placeholder를 실동작으로 활성화한다. (버튼 자리 기존재 → 단순 수정, 디자인 게이트 불요)

## DoD
- 닉네임 입력 + "중복확인" 버튼 → FC-160 가용성 조회 API 호출 → **사용 가능/중복** 피드백 표시(성공·에러 상태·로딩·접근성).
- 닉네임 변경 시 확인 상태 초기화(재확인 유도), 미확인·중복 시 제출 처리 정책은 기존 폼 UX와 정합.
- 제출 시 서버 검증(중복=409)은 그대로 최종 방어선으로 유지.
- `npm run build`·lint·관련 vitest 그린. 커밋 금지.

## 근거인용
FC-160 계약. EPIC-NICKNAME-UX. 현 SignupForm의 비활성 중복확인 자리(placeholder).

## 구현 결과 (2026-07-30)
- 수정: `frontend/src/lib/api/auth.ts`(가용성 조회 함수·타입), `SignupForm.tsx`(닉네임 라이브 버튼·상태·피드백), `SignupForm.test.tsx`(테스트 5건 추가). 16/16 그린, typecheck·lint·build 그린.
- UX: 입력→"중복 확인"→`GET /auth/nickname/availability`(auth:false)→성공/중복/오류 피드백(`role=status aria-live`), 값 변경 시 초기화. advisory라 미확인/중복도 제출 비차단(최종=서버 409 AUTH_002).

## 관찰(총괄 판단 대기 — 스코프 밖, 비차단)
1. **아이디(loginId) "중복확인"은 여전히 "준비 중"**: 기존 비활성 placeholder는 닉네임이 아니라 **loginId** 필드에 있었음(백엔드 `/auth/ids/availability` 미존재). 이 티켓은 닉네임 라이브 버튼을 신규 추가했고 loginId placeholder는 유지. → loginId 가용성 조회도 추가할지(신규 백엔드 엔드포인트=별도 티켓) / placeholder 제거할지 총괄·사용자 결정 필요.
2. **사전존재 실패 테스트 3건**(`frontend .../oauth.test.ts`): FC-162 변경과 무관(stash 클린 상태에서도 실패). 마스터 회귀 → 별도 확인 필요.

## 파생 경위: FC-160 계약 확정 후 프론트 배선.
