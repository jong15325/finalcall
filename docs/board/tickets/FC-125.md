---
id: FC-125
type: task
epic: EPIC-CONVENTION-V2
derived_from: null
jira_key: KAN-140
title: 축3 — Properties 소비범위 분리
state: done
owner: backend-impl
depends_on: [FC-123]
blocks: [FC-126]
gate: null
review_status: passed
contract_ref: docs/common/proposals/layer-restructure-proposal-v0.1.md §9 (FC-123 개정본)
artifacts: []
---
## 목표
Properties를 **소비범위로 분리**한다 — 공통(cross-cutting)은 `common`, feature 전용은 `domain/<feature>/config/`.

## DoD
- feature 루트 Properties(bid·currency·search×2·settlement×2·shop×2) → 각 `domain/<feature>/config/`로 이동.
- 공통 Properties(현 `infra/config`의 AppProperties·JwtProperties·GatewayInternalProperties) common vs infra 경계는 FC-123 규약 따름.
- `@ConfigurationProperties` 스캔 경로(`@EnableConfigurationProperties`/`@ConfigurationPropertiesScan`) 갱신 — 부팅 바인딩 무회귀 확인.
- 로직 0줄 변경. `:backend:test` green + `:backend:spotlessApply`. **원자 커밋**(사용자 승인).

## 근거인용
proposal §9 개정본(축3), EPIC-CONVENTION-V2.

## 파생 경위
EPIC-CONVENTION-V2 축3. FC-123 선행. FC-124와 파일 무교차 → 병렬 가능.
