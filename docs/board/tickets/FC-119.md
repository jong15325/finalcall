---
id: FC-119
type: task
epic: EPIC-RESTRUCTURE
derived_from: null
jira_key: KAN-130
title: Phase 0 — feature-first 규약 확정·CLAUDE.md 개정
state: review
owner: consultant
depends_on: []
blocks: [FC-120]
gate: null
review_status: pending
contract_ref: docs/common/proposals/layer-restructure-proposal-v0.1.md
artifacts: []
---
## 목표
옵션 C(feature-first, 도메인 내부 계층 하위패키지) 규약을 확정하고 CLAUDE.md·proposal에 반영한다. **코드 이동 없음**(docs·규약만).

## DoD
- **proposal v0.2**: 확정 3결정 반영 + 도메인별 목표 레이아웃 정밀화(feature 목록·내부 하위패키지 규칙·`common`/`infra` 경계) + **신규 ArchUnit 규칙 스펙**(슬라이스 내부 `..controller→..service→..repository/entity` 방향 naming rule + 슬라이스 비순환 + 커널 방향) + 이전 순서.
- **CLAUDE.md 섹션 1·3·4·5·7 개정**: 최상위 `api>domain>infra>common` 서술 → feature-first(`com.finalcall.<feature>.<layer>`, `domain.` 접두 없음)로. 이전 진행 중임을 알리는 **전환 주석**(신규 코드는 feature-first 준수) 포함.
- `docs/backend/references/spring-skeleton-prompts.md` 상단에 "구조는 재구성으로 feature-first 대체(이 문서는 스켈레톤 당시 기록)" 주석 1줄.
- `jpa-convention`·`coding-discipline` 스킬이 계층 경로를 참조하면 점검·갱신 목록 산출.

## 근거인용
proposal §3(옵션 C)·§4·§6, 에픽 확정 결정.

## 파생 경위
EPIC-RESTRUCTURE 게이트1 분해 Phase 0(docs).
