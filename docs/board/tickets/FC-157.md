---
id: FC-157
type: task
epic: EPIC-OAUTH
derived_from: null
jira_key: KAN-176
title: OAuth 보안+QA 리뷰 + /security-review
state: done
owner: reviewer
depends_on: [FC-154, FC-156]
blocks: []
gate: null
review_status: passed
contract_ref: EPIC-OAUTH, FC-152 계약
artifacts: []
---
## 목표
OAuth 구현(FC-153~156)을 Done 전에 통합 리뷰한다. reviewer = 확인소(도메인 인가 최종 판정).

## DoD (리뷰 관점)
- **state/CSRF**: authorize 요청·콜백의 state 생성·검증이 위조를 막는가.
- **열거방지(SEC-007)**: 소셜 자동가입·find-or-create가 계정 존재 여부를 누설하지 않는가.
- **토큰 발급 정합**: 소셜 경로가 기존 `TokenProvider`·`RefreshTokenStore` 계약(회전·재사용 탐지·revokeAll)과 일치하는가.
- **콜백·code 검증**: code 재사용·잘못된 redirect_uri·provider 오류 처리. 시크릿 노출(로그·응답) 없음.
- **커넥션 위생**: OAuthService HTTP 클라이언트가 풀링·타임아웃(FC-151 교훈).
- **에픽 완료 직전 `/security-review` 1회**(온디맨드). 심각도 랭킹(critical/major/minor).
- 결과 = `docs/board/reviews/FC-157-review.md`. critical/major면 해당 구현 티켓 재작업.

## 근거인용
EPIC-OAUTH 보안 제약. concurrency-review 스킬(JWT). SEC-006/007·D-068.
