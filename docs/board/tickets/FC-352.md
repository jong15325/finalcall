---
id: FC-352
type: task
epic: EPIC-MEMBER-CHARACTER
derived_from: null
jira_key: KAN-399
title: 기본 캐릭터 자산 매핑과 계약 확정
state: review
depends_on: []
blocks: [FC-353, FC-354, FC-355, FC-356, FC-357]
gate: null
review_status: passed
contract_ref: docs/spec/member-domain-spec.md, docs/spec/api-contract.md, docs/spec/erd.md
artifacts:
  - docs/spec/member-domain-spec.md
  - docs/spec/api-contract.md
  - docs/spec/erd.md
---
## 목표 / DoD / 검증
- 기본 캐릭터 1~12와 Avatar 25~28을 uc 16개 프로필에 매핑한다.
- 프리미엄 13~24와 모든 `btn_2` 계열은 선택·동기화에서 제외한다.
- 기본값, 허용 ID, 스키마, PATCH 의미와 외부 프로필 노출 범위를 확정한다.
- 변경 계약 확정: member-domain-spec v1.1, api-contract v1.32, erd v2.3. 2026-08-22 사용자 승인 완료.

## Gate 2 승인 결정 (2026-08-22 사용자 승인)
- 승인: `user.primary_character_id TINYINT UNSIGNED NOT NULL DEFAULT 1`, CHECK `1..12 OR 25..28`, V28 backfill.
- 권고: `PATCH /me`를 `{ nickname?, primaryCharacterId? }` 부분 수정으로 확장하고 두 필드 동시 요청은 원자 처리.
- 승인: 서버는 `primaryCharacterId`만 노출하고 URL/key를 저장하지 않으며, 16개 허용 선택지→uc 16개 매핑은 member spec 정본으로 파생.
- 권고: 프로필은 현재값 표시로 정의해 게시글/댓글/쪽지 snapshot 컬럼을 추가하지 않고 batch join한다.
- 확정 자산: 기본 1~12 normal/click과 Avatar 25~28 normal/click만 선택 UI에 사용. premium 13~24와 모든 `ch_*_btn_2_*` 전면 제외.
- 영향 티켓: FC-353~358. 승인된 계약에 따라 구현 진입 허용.
