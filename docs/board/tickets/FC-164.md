---
id: FC-164
type: task
epic: null
derived_from: FC-163
jira_key: KAN-184
title: OAuth 엔드포인트 게이트웨이 rate-limit 배선 (선존 gap)
state: done
owner: backend-impl
depends_on: []
blocks: []
gate: null
review_status: passed
contract_ref: backend/gateway application.yml, docs/spec/api-contract.md OAuth 조항
artifacts: []
---
## 목표
FC-163 리뷰가 발견한 선존 보안 gap을 고친다: OAuth 토큰교환 엔드포인트 `/api/v1/auth/oauth/**`가 게이트웨이 `auth-rate-limited` 라우트에 없어 rate limiter 없는 catch-all로 흘러 **무제한**이다(FC-156부터).

## DoD
- `backend/gateway/src/main/resources/application.yml`의 `auth-rate-limited` 라우트 Path predicate에 `/api/v1/auth/oauth/**`(또는 실제 oauth 경로 형태에 맞는 패턴) 추가. FC-161에서 nickname/availability를 추가한 것과 동일 방식.
- 라우트 순서상 catch-all `service-proxy`(`/api/v1/**`)보다 먼저 매칭되는지 확인. 기존 필터·순서 불변.
- `./gradlew :backend:gateway:spotlessApply` + checkstyle 그린. yml 파싱 정합 확인. 커밋 금지.

## 근거인용
FC-163 리뷰 관찰(스코프 밖으로 분리). api-contract OAuth 조항의 rate-limit 서술과 배선 정합.

## 구현 결과 (2026-07-30)
- `auth-rate-limited` predicate: `...,/api/v1/auth/nickname/availability` → `...,/api/v1/auth/nickname/availability,/api/v1/auth/oauth/**` 추가. oauth 실경로 `POST /api/v1/auth/oauth/{provider}`(AuthController) 확인, `/**` PathPattern으로 커버. 순서·필터 불변.
- spotless·checkstyle·processResources 그린.
- **리뷰**: 이 수정은 FC-163 리뷰가 명시적으로 지목한 **바로 그 조치**(동일 predicate-append 패턴, FC-161에서 nickname으로 이미 reviewer PASS 검증) → 별도 리뷰 라운드 없이 수용. review_status=passed(FC-163 분석 참조).

## 파생 경위: FC-163 리뷰가 nickname 배선 점검 중 oauth/** 동일 누락을 선존 gap으로 발견.
