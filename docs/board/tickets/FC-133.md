---
id: FC-133
type: task
epic: EPIC-EMAIL-TEMPLATE
derived_from: FC-129
jira_key: KAN-149
title: T1 — Flyway V18 email_template 테이블 + EMAIL_VERIFICATION 시드
state: review
owner: backend-impl
depends_on: []
blocks: [FC-134]
gate: null
review_status: pending
contract_ref: docs/spec/email-template-spec.md §3·§3.1
artifacts:
  - backend/src/main/resources/db/migration/V18__add_email_template.sql
---
## 목표
재사용 메일 템플릿 저장소 테이블을 신설하고 인증 코드 템플릿 1건을 시드한다.

## DoD
- Flyway **V18** `email_template` 테이블(spec §3 DDL): `id`·`template_key`(VARCHAR(50) UK `uk_email_template_key`)·`subject`(VARCHAR(255))·`body`(TEXT)·`content_type`(VARCHAR(10) DEFAULT 'TEXT')·`description`(VARCHAR(500) NULL)·`is_active`(BIT DEFAULT 1)·`created_at`·`updated_at`. **soft delete 없음·생성컬럼 UK 불요**(V6 item_template 마스터 테이블 선례). 자연키 `template_key` 직접 UK.
- 초기 시드(동일 마이그레이션, spec §3.1): `EMAIL_VERIFICATION` 1건 — 제목 `[장터] 이메일 인증 코드`, 본문에 `{{code}}`·`{{expiryMinutes}}` placeholder, `content_type='TEXT'`, `is_active=1`. **실제 코드 절대 미영속(placeholder만)**.
- 실 DB 부팅 시 V18 자동 적용·기존 검증 무오염 확인(비파괴 검증 권장 — 스크래치 스키마).

## 근거인용
spec §3·§3.1(테이블·시드), V6 item_template(마스터 테이블 선례), D-081(마스터라 생성컬럼 UK 불요).

## 파생 경위
EPIC-EMAIL-TEMPLATE 분해 T1. FC-129 rework와 파일 무교차 → 병렬 가능.
