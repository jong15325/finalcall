---
id: FC-179
type: task
epic: EPIC-CARD-SYSTEM
derived_from: FC-178
jira_key: KAN-201
title: T1 — 인벤토리 스킬명 API (ItemSummaryResponse += skill1/2Name)
state: done
owner: main
depends_on: []
blocks: [FC-184]
gate: null
review_status: passed
contract_ref: docs/spec/api-contract.md §4.2·v1.21(가법 델타)
state_note: reviewer PASS(critical/major 0). 순수 가법·N+1 없음(fetch join 기존)·null 폴백 대칭·계약 v1.21 정합·통합 테스트 값 단언. minor 2(임시보관 직접 테스트·Javadoc stale) 비차단. 커밋 승인 대기.
artifacts:
  - backend/src/main/java/com/finalcall/domain/item/dto/ItemSummaryResponse.java
  - docs/spec/api-contract.md
---
## 목표 (EPIC-CARD-SYSTEM T1)
인벤토리/임시보관 요약이 스킬 **이름**을 내려주게 한다(현재 코드만 → 프론트 "스킬 #코드" 폴백). 스킬명 단일 원천 = 백엔드 `SkillDefinition`. **게이트2 A 승인(2026-08-04)**. **구조 독립 → 선행 가능**(FE 배선은 T6).

## DoD
1. `ItemSummaryResponse`에 `skill1Name`/`skill2Name`(String, nullable) 추가 — `AuctionItemResponse.from`(:50-51) 패턴 이식: `instance.getSkill1()==null?null:instance.getSkill1().getName()`. 추가 조회 없음(코드용으로 이미 참조·N+1 아님).
2. `api-contract.md` §3.3(요약/인벤토리 item 블록)에 **가법 델타** 명시(기존 필드·형상 불변).
3. DB 마이그레이션 **없음**(SkillDefinition 기존). 스킬명 관련 슬라이스/유닛 테스트 갱신. spotlessApply·checkstyle 통과.

## 근거인용
게이트2 A. `AuctionItemResponse.java`(스킬명 이식원)·`ItemSummaryResponse.java`·`SkillDefinition.java`. 계획 §4.

## 파생 경위: FC-178 라이브 후 "스킬명 표시" 피드백 → 게이트2 A → EPIC-CARD-SYSTEM T1(백엔드, 선행).
