---
id: FC-038
type: task
epic: EPIC-FE-AUCTION
derived_from: null
jira_key: KAN-46
title: EPIC-FE-AUCTION 통합 리뷰 (접근성·디자인 시스템 준수·계약 정합)
state: review
owner: reviewer
depends_on: [FC-036, FC-037]
blocks: []
gate: null
review_status: passed
contract_ref: docs/spec/api-contract.md v1.9 §3.1·§3.3, docs/ux/design-system.md, docs/ux/accessibility.md
artifacts:
  - docs/board/reviews/FC-038-review.md
---
## 목표
- EPIC-FE-AUCTION(FC-036 목록 · FC-037 상세) 구현 전체를 Done 전이 전에 통합 리뷰한다.
- **총괄 누락 보정**: 에픽 분해 시 reviewer 티켓을 빠뜨렸다(EPIC-BID엔 FC-035를 뒀으면서). 섹션 9 파이프라인상 reviewer 통과는 Done 전 필수 선행이다.

## DoD
- **접근성**(`docs/ux/accessibility.md` 정본): `Countdown`의 `aria-live=off` + 전환 시점 폴라이트 안내가 실제로 그렇게 동작하는가. 필터 칩 제거(x) 타깃 44px. 키보드 내비·포커스 링·포커스 관리(상세 진입). 무한스크롤 센티넬의 **"더 보기" 폴백이 키보드·스크린리더로 실제 도달 가능**한가. 색 단독 정보 전달 금지 위반 여부.
- **디자인 시스템 준수**(`design-system.md`): §1.2 색 3계층(조작=블랙 CTA·퍼플은 액센트만 — **퍼플이 CTA 채움으로 쓰인 곳 없는지**), **Game-Color Containment**(element 색이 아이템 카드·속성 배지·필터 칩 밖으로 샜는지), ElementBadge 라이트 패턴(흰 배경 위 텍스트 직접 사용 금지), 등급 배지 미사용(D-073).
- **계약 정합**(v1.9): 요청 파라미터가 계약 화이트리스트를 벗어나지 않는가(정렬 키). `AuctionSummary`·`AuctionDetail` 필드 사용이 계약과 일치하는가. **`schema.ts` 타입이 계약 v1.9 §3.3 타입 표와 일치**하는가(정수 7종·nullable 3종).
- **★ 정직한 빈 값 표현**: `highestBidAmount`=null일 때 **"입찰 없음 · 시작가 N"**으로 표시하고 `startPrice`를 현재가인 양 보여주지 않는가. 상세의 최고가·입찰수 영역이 **값 없어도 자리를 유지**하는가(EPIC-BID 완료 시 레이아웃이 흔들리지 않도록).
- **중복 구현**: FC-037이 FC-036 컴포넌트를 실제로 재사용했는가, 포크·중복 정의가 없는가.
- **산출물**: `docs/board/reviews/FC-038-review.md` — critical/major/minor 심각도 랭킹. critical·major 0이어야 통과.
- 읽기 전용 — 코드를 수정하지 않는다.

## 총괄이 이미 수용한 판단 (재지적 불요)
- **판매유형 칩 미구현** — 경매 전용 목록에서 상수 반복은 노이즈. 수용됨.
- **게임 아트 플레이스홀더** — 시드 typeCode가 `SEEDITEM` 하나뿐이라 매핑 짝 없음. 디자인 게이트 승인 사항.
- **필터 핵심 4종만** — 디자인 게이트 승인 사항.
- **`StatusChip` neutral 톤 추가** — `SCHEDULED` 대응 의미색 부재. 새 색이 아니라 기존 토큰 조합.
- **`Countdown` size 프롭 확장** — §5.9가 크기를 규정하지 않음. 포크 대신 프롭.
- **`specSnapshot` 미표시** — 서버가 만든 기계 문자열이라 구조화 스탯과 중복·노이즈.
- **`typeCode`·`mainCategory`·`subGroup`·`kind` 미표시** — 코드→표시명 매핑 부재(코드 사전은 EPIC-ITEM 소관).

위 항목의 **판단 자체를 다시 다투지 말고**, 그 결과가 접근성·디자인 시스템·계약을 실제로 위반하는지만 보라.

## 알려진 제약 (감점 사유 아님)
- **실데이터 미검증**: 백엔드 :8080 미기동으로 cursor 연속 로드·정렬 전환 시 커서 초기화·빈 상태를 확인하지 못했다. 코드 상 정합만 보라.
- **백엔드 `minNextBidAmount` 미구현**(계약 v1.8 F3) — FC-033으로 회부됨. 프론트가 `—`로 자리만 유지하는 것은 올바른 대응이다.

## 근거(인용)
> CLAUDE.md 섹션 8 "reviewer … 정합성·QA + 도메인 인가 최종 판정" / 섹션 9 파이프라인 "reviewer(보안+QA+접근성) → Done(reviewer 통과가 필수 선행)".
> design-system.md §1.2 "Game-Color Containment: element 4색·검정 슬롯은 아이템 카드·속성 배지·아이템 필터 칩에만 쓴다 … 게임색이 조작/크롬으로 새면 이원 구조가 무너지고 커머스 신뢰감이 흐려진다."
