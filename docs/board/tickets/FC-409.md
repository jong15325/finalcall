---
id: FC-409
type: task
epic: EPIC-HOME-MARKET-RECOMMEND
derived_from: null
jira_key: KAN-464
title: 홈 추천 마켓 후보 조회와 조립 API 구현
state: done
owner: backend-impl
depends_on: [FC-408, FC-414]
blocks: [FC-413]
gate: null
review_status: passed
contract_ref: docs/spec/shop-spec.md 홈 추천 절 및 docs/spec/api-contract.md
artifacts:
  - backend/src/main/java/com/finalcall/domain/shop/controller/HomeShopRecommendationController.java
  - backend/src/main/java/com/finalcall/domain/shop/service/ShopRecommendationService.java
  - backend/src/main/java/com/finalcall/domain/shop/dto/ShopRecommendationsResponse.java
  - backend/src/integrationTest/java/com/finalcall/integration/HomeShopRecommendationApiIntegrationTest.java
---
## 목표
- 확정 계약에 따라 공개 추천 목록을 일관된 기준시각으로 계산해 반환한다.

## DoD
- ACTIVE·미만료 자격, 쿼터, 중복 제거, 다양성, 부족분 보충을 구현한다.
- 추천 이유와 기존 ShopSummary를 계약 형상 그대로 반환한다.
- 행별 판매실적 조회 없이 제한된 쿼리 수로 동작하고 실행계획·회귀 테스트를 남긴다.

## 근거(인용)
> 추천 판정과 표시 순서는 서버가 권위를 가지며 프론트에서 여러 목록을 합성하지 않는다. (게이트1 분석)

## 검증
- 실제 MySQL Testcontainers 통합 테스트 3건과 기존 Shop API 통합 테스트 통과.
- `SliceArchitectureTest`, spotless, checkstyleMain/Test, bootJar, `git diff --check` 통과.
- 전체 `ConventionArchitectureTest`는 기존 `OAuthMetrics$Result` 위반 1건으로 실패(FC-409 변경과 무관).
- 운영 규모 `EXPLAIN ANALYZE`는 신규·검증 판매자 정렬의 후속 성능 확인 대상으로 유지.

## reviewer 재작업 요청 (FC-413)
- 후보 30건 선절단이 제한 밖 다양 후보를 누락해 판매자 제한을 조기 완화할 수 있음.
- 31건 이상 경계 회귀와 운영 규모 상당 `EXPLAIN ANALYZE` 증거가 필요.

## 실행계획 게이트2 대기 (2026-08-31)
- 신규 후보: ACTIVE 2,000행 전체 scan + created_at/id 정렬, 약 1.63ms.
- 마감 임박: 기존 `(status,end_at)` covering index, 30행 검사, 약 0.022ms.
- 검증 판매자: ACTIVE 2,000행 scan + 판매완료 집계·정렬, 약 3.91ms.
- 현재 절대 시간은 작지만 데이터 증가에 선형 비례해 인덱스·read model·캐시 대안 결정 필요.

## 추가 성능 게이트2 승인 (2026-08-31, 사용자)
- 최신 후보용 `(status, created_at, id)` 인덱스 1개 추가.
- 검증 판매자 쿼리·무캐시 정책 유지, FC-414 완료 후 FC-409 재검증.

## 재작업 완료
- 고정 30건 선절단을 30건 단위 순차 탐색으로 교체하고 실제 후보 소진 후에만 제한을 완화.
- 상위 30건 동일 판매자·31번째 이후 B~F 판매자 경계 회귀 통과.
- FC-414 복합 인덱스 적용 후 ACTIVE 10,000건 최신 조회가 전체 scan·정렬 3.71ms에서 인덱스 30행·무정렬 0.109ms로 개선.

## reviewer 2차 재작업 요청 (FC-413)
- 30건 OFFSET 순차 탐색이 동질 후보 10,000건에서 수백 DB round trip을 만들 수 있음.
- 전체 데이터량이 아니라 최대 6개 결과에 비례한 고정 쿼리 상한 필요.
- 실행계획 테스트를 실제 fetch join 쿼리와 동형으로 보강 필요.

## 2차 재작업 완료
- OFFSET 제거, 선택 shop·seller·한도 도달 template을 DB 제외 조건으로 전달.
- 후보 ID 선택 후 PK fetch join hydrate로 분리해 LIMIT 이전 대량 조인·정렬 제거.
- 후보 18 + hydrate 6 + 완료판매 배치 1 = 서비스 SQL 최대 25회 상한.
- 동질 ACTIVE 10,000건 실제 서비스 호출 SQL 18회, 결과 6건 확인.
- 후보 ID 인덱스 조회 0.107ms, PK hydrate 1행·무정렬 확인.
- 관련 통합·회귀·구조·스타일·빌드 대상 테스트 37건 통과.
