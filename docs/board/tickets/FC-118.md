---
id: FC-118
type: task
epic: EPIC-EMAIL-VERIFY
derived_from: FC-114
jira_key: KAN-136
title: User 엔티티 email 필드·도메인 메서드
state: done
owner: backend-impl
depends_on: [FC-117, FC-121]
blocks: [FC-131, FC-132]
gate: null
review_status: passed
contract_ref: docs/spec/email-verify-spec.md §2.1
artifacts:
  - backend/src/main/java/com/finalcall/domain/member/entity/User.java
---
## 목표
`User` 엔티티에 `email`·`emailVerified` 필드와 도메인 메서드를 추가한다.

## DoD
- `email`(String, nullable)·`emailVerified`(boolean) 필드 추가. `@Setter` 금지(섹션 5).
- 도메인 메서드: `assignEmail(String normalizedEmail)` — 이메일 설정/변경, `emailVerified`를 false로 (재)초기화.
- 도메인 메서드: `markEmailVerified()` — 인증 성공 시 `emailVerified=true`.
- 정규화(lowercase+trim)는 서비스 계층 책임(엔티티는 정규화된 값 수신 전제).

## 근거인용
spec §2.1(엔티티 필드·도메인 메서드, @Setter 금지), CLAUDE.md 섹션 5.

## 파생 경위
EPIC-EMAIL-VERIFY 게이트1 분해안 B2. B1(V17) 선행.

## 보류 (2026-07-25)
EPIC-RESTRUCTURE hold 대기. FC-121 완료(도메인 이전됨) → 재개 시 신 경로 `com.finalcall.domain.member.entity.User`에 이메일 필드 추가(기능 불변). 이메일 엔드포인트·서비스도 `com.finalcall.domain.member.<layer>` 규약 준수.
