---
id: FC-023
type: task
epic: EPIC-ITEM
derived_from: FC-019
jira_key: KAN-28
title: 최소 시드 (seed user+balance·template·skill·instance·소유이력) V9
state: done
owner: backend-impl
depends_on: [FC-020, FC-021, FC-022]
blocks: [FC-024]
gate: null
review_status: passed
contract_ref: docs/spec/item-domain-spec.md §8.1, docs/spec/erd.md §6
artifacts: []
---
## 목표
- 아이템 도메인이 구동 앱에서 실제로 보이도록 최소 시드를 넣는다(진입경로=시드-only, 게이트2 (a)). 카탈로그·인벤토리·(후속)경매 공급을 성립시킨다.

## DoD
- **Flyway V9**(`V9__item_seed.sql`) 단일 마이그레이션, 생성 순서: seed user 1~2(+user_balance) → item_template 8(대분류2×종류2×속성2) → skill_definition 5 → item_instance 10(location=INVENTORY, transfer_type=SEED, seed user 소유) → item_ownership_history 첫 행(from_owner NULL).
- **member 시드 부재 해소**: 기존에 user 시드가 없으므로(signup으로만 생성) 시드가 seed user+balance부터 생성. 비밀번호는 BCrypt 해시(로컬 더미).
- **검증**: 앱 부팅 후 `GET /item-templates`에 8건, seed user의 `GET /me/inventory`에 10건 노출. slot_key UK(V8) 이후 실행이라 slot 배정 안전.
- **범위 밖**: 원게임 실데이터 대량·정밀 시드(D-067 이연). spotless·checkstyle 통과.

## 근거(인용)
> 게이트2 (a) 진입경로=최소 시드만, (b) template8+skill5+instance10 승인. item-domain-spec §8.1 시드 배치. architect: member 시드 전무 확인 → seed user부터 생성.

## 파생 경위: FC-019 게이트2 (a)=시드-only + member 시드 부재 확인으로 전용 시드 티켓 신설(V9).
