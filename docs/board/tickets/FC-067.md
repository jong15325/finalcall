---
id: FC-067
type: task
epic: EPIC-FE-REBUILD
derived_from: FC-066
jira_key: KAN-75
title: 앱 셸·라우팅·장터 브랜드 토큰 + 템플릿 폐기(318)
state: review
owner: frontend-impl
depends_on: [FC-066]
blocks: [FC-068, FC-070, FC-071, FC-072, FC-073, FC-074, FC-075, FC-076, FC-077]
gate: null
review_status: passed
contract_ref: docs/ux/rebuild-contract-map.md
artifacts:
  - frontend/src/app/router.tsx
  - docs/board/reviews/FC-067-068-review.md
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/components/common/CodeAmount.tsx
---
## 구현 완료 (frontend-impl · commit bdc5779)
- 폐기 513파일 / 보존 49파일(의존 클로저 스크립트 계산) 무손상. **보존 lib 테스트 209건 통과**로 폐기 검증.
- `src/app`(paths·router 3계층) · `src/components/layout`(AppShell·Sidebar·TopNavbar·MobileBottomNav·AuthLayout) ·
  `common`(CodeAmount·Dropdown·PlaceholderView) · `pages`(실연동 빈셸 12 + 준비중 3 + NotFound) · 브랜드 토큰(퍼플 제거).
- 검증: typecheck/lint 0 · **test 215**(승계 209 + CodeAmount 6) · build ✓.
- **토대 레이어 리뷰는 FC-068 완료 후 묶어 진행**(reviewer 미실행).

## 목표
재구축 토대: Ecme 템플릿(318) 폐기 + 장터 앱 셸(Sidebar·TopNavbar·MobileBottomNav·RouteOutlet)·
라우팅·브랜드 토큰·CodeAmount 확립. 보존 45 lib(계약 인코딩)은 그대로 승계.

## DoD
- 폐기 318 실행, 보존 45 lib + 테스트 유지(**lib 테스트 통과가 폐기 검증**).
- AppShell + 라우팅(실연동 라우트 빈 셸 + 자리보류 "준비 중").
- 장터 팔레트/타이포 토큰(퍼플 전면 제거) + `<CodeAmount>` (compact/full, code.png, 정수값).
- `typecheck`·`lint`·`test`·`build` 통과. 모바일 우선(min-w-0·grid-cols-1).

## 근거인용
- 계약 성경 `docs/ux/rebuild-contract-map.md`(라우트 지도·통화·주의 5개).
- 디자인 정본 목업 index.html·market.css·HANDOVER_FULLSTACK §5·§20·§24(레포 밖·읽기).
- 폐기/보존 목록 `EPIC-FE-REBUILD.md`.

## 검증
- 보존 lib 테스트 그린 유지. 셸 라우트 클릭 시 404 없음(자리보류 비활성).

## 파생 경위: FC-066 계약 매핑 확정 후, 화면 구현의 선행 토대로 분해된 첫 구현 티켓.
