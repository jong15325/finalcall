---
id: FC-195
type: task
epic: null
derived_from: null
jira_key: KAN-221
title: fix — 인벤 카드 그리드 폭 정렬(ItemCardTile fullHeight w-full 복구)
state: done
owner: frontend-impl
depends_on: []
blocks: []
gate: null
review_status: passed
contract_ref: null
artifacts:
  - frontend/src/features/item/components/ItemCardTile.tsx
---
## 목표 (버그 수정 · 라이브 발견)
`/me/inventory` 인벤 카드들의 가로 폭이 제각각(130~169px)이라 6열 그리드 정렬이 깨지던 문제 수정. 카드 디자인은 정상(사용자 확인) — 폭/채움만.

## 원인
인벤 그리드 셀 `<li className="flex">` 안에서 카드 루트 `ItemCardTile`(`.shop-card`)이 `fullHeight`일 때 `h-full`만 있고 **`w-full` 부재** → flex row 자식이 stretch 안 되고 콘텐츠(스킬/마켓 정보 텍스트) 폭으로 shrink → 아이템별 텍스트 길이로 폭 제각각. **FC-178부터 잠복**(인벤이 마켓 카드로 전환될 때 유실, FC-190 회귀 아님).

## 수정
`frontend/src/features/item/components/ItemCardTile.tsx` — 루트 className `fullHeight` 분기 `'h-full '` → `'h-full w-full '`(1곳). 마켓 `ShopCard`는 `fullHeight` 미전달 → 무영향. 디자인·아트(72px)·색·반응형 브레이크포인트 불변.

## 검증
- `npm run typecheck` green, 인벤 테스트(InventoryItemCard 7·InventorySlotGrid 5) 12건 green.
- **라이브 브라우저 재측정**: 6열 카드 전부 셀 폭 225px로 균일(수정 전 130/147/169px 제각각 → 후 전부 225px). 시각 정렬 확인.

## 파생 경위: 서버 기동 후 사용자 인벤 화면 점검 중 발견. 라이브 진단(DOM 폭 측정)으로 근본 원인 확정 후 surgical 수정.
