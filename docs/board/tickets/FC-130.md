---
id: FC-130
type: task
epic: EPIC-EMAIL-VERIFY
derived_from: FC-114
jira_key: KAN-145
title: B5 — EmailErrorCode·EmailVerifyProperties·yml
state: todo
owner: backend-impl
depends_on: [FC-128, FC-129]
blocks: [FC-131, FC-132]
gate: null
review_status: pending
contract_ref: docs/spec/email-verify-spec.md §2.4·§3·§5·§6.2
artifacts: []
---
## 목표
에러코드·정책 바인딩·메일 설정을 확정한다.

## DoD
- `EmailErrorCode` enum(`EMAIL_001`~`EMAIL_007`) — **배치 = `com.finalcall.common.exception`(V2 규약)**. api-contract §5 등재(프론트 errorCodes 동기화는 F3 소관).
- `EmailVerifyProperties`(@ConfigurationProperties + @Validated, member `config/`): code-length·ttl-seconds·resend-cooldown-seconds·max-attempts. 0/음수/빈값 부팅 차단.
- `application.yml` `spring.mail.*`(host·port·SSL, username/password는 공통 yml에 기본값 금지) + `email.verify.*` + 프로파일별 `sender-enabled`(local false).
- 상태코드 매핑(422/429/409) spec §5 정합.

## 근거인용
spec §2.4·§3·§5·§6.2. V2 규약(ErrorCode common/exception·Properties feature config).

## 파생 경위
EPIC-EMAIL-VERIFY 분해 B5. FC-128·129 값 참조.
