---
id: FC-184
type: task
epic: EPIC-CARD-SYSTEM
derived_from: null
jira_key: KAN-207
title: T6 — ItemCard variant 정비 + 스킬명 FE 배선
state: done
owner: main
depends_on: [FC-179, FC-182, FC-183]
blocks: []
gate: null
review_status: passed
contract_ref: docs/common/proposals/card-system-consolidation-proposal-v0.1.md §2.3·§4
state_note: reviewer PASS(critical/major 0, minor 1). variant 모델(hidePrice→nullable price·skillFlip→variant browse/market)·스킬명 정본 배선(skillSlots) 정합, 형상 보존 diff 통과, 카드 48건 green. minor 'compact' dead 예약값=커밋 전 제거(union 축소, 재검증 green). 커밋·Done 사용자 승인 대기.
artifacts:
  - frontend/src/features/item/components/ItemCard.tsx
  - frontend/src/features/item/components/skillSlots.ts
  - docs/board/reviews/FC-184-review.md
---
## 목표 (EPIC-CARD-SYSTEM T6 · 위험↑ 마켓 시각)
`ItemCard`를 **variant 모델**로 정비(boolean 폭발 제거)하고, FC-179 스킬명을 **정본 경로 한 곳**에 배선해 인벤·마켓·경매가 같은 배선으로 이름을 얻게 한다.

## DoD (계획 §2.3·§3 단계4)
1. `hidePrice` boolean → **nullable `price`**(부재=줄 없음). `skillFlip` boolean → **`variant: 'browse'|'market'|'compact'`**. slot(footer/overlay/detailLink) 유지, boolean 추가 중단.
2. **스킬명 FE 배선**: 인벤토리 `ItemSummary`(api) 타입 += `skill1Name?`/`skill2Name?`(FC-179 백엔드 델타 소비), `ItemCard`·`CardInfoDialog` 스킬 섹션이 이름 표시. `skillLabelOf`가 실제 이름 사용(인벤 "스킬 #코드" 해소).
3. 소비자(ShopCard·InventoryItemCard·MyShopCard) variant/price로 정리.
4. **시각 픽셀 보존**(마켓 카드 회귀 없음) — ItemCard 11·ShopCard 6 스위트 green + 시각 diff 게이트.

## 근거인용
계획 §1.4(6 prop 폭발)·§2.3·§4. FC-179 스킬명 소비.

## 파생 경위: 게이트1 T6. T1(스킬명 API)·T4·T5 후 직렬.
