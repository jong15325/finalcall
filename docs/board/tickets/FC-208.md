---
id: FC-208
type: task
epic: EPIC-COMMENT-V2
derived_from: FC-206
jira_key: KAN-237
title: 공감/비공감 백엔드 — comment_reaction·토글·카운트
state: done
owner: backend-impl
review_status: passed
review_status_note: CHANGES-REQUESTED 해소(2026-08-06) — M-1(same-user 동시 반응 UK 500) = 접근B(comment FOR UPDATE + 반응 잠금 read로 스냅샷 우회→수렴). same-user 8회 동시 LIKE 수렴 테스트 + 멀티유저 40 회귀 유지. m-1~3 accept. 전 테스트 그린.
state_note: |
  구현 완료·검증(2026-08-06): CommentReaction 엔티티·ReactionType·Repository + 반응 토글 API(PUT reaction·등록/전환/취소)·CommentRepository 원자 like/dislike 증감(언더플로 가드)·myReaction 배치 채움(N+1 회피·optional-auth)·COMMENT_003 자기반응 금지. ★동시성 교착(반응INSERT S→카운트UPDATE X 승격) 발견·수정(카운트 UPDATE 선행 직렬화, 40스레드 테스트 그린). CommentReactionApi·Concurrency(40유저=40) + 회귀·ArchUnit 그린. spotless·checkstyle 통과. BEST myReaction 헬퍼 재사용 슬롯 개방(FC-209). 미커밋.
depends_on: [FC-206, FC-207]
blocks: [FC-209, FC-211]
gate: null
review_status: pending
contract_ref: api-contract.md v1.24 §6.3, board-domain-spec.md v1.1 §13
artifacts: []
---
## 목표
댓글별 공감/비공감 반응. 유저당 댓글당 1행 토글·카운트 비정규화.

## DoD
1. **comment_reaction 테이블**(Flyway — FC-207 V24에 포함 or 후속 버전): id·comment_id FK·user_id FK·reaction_type(LIKE|DISLIKE)·created/updated_at, **UK(comment_id,user_id)**. comment.like_count/dislike_count 비정규화.
2. **반응 토글 API** `PUT /posts/{id}/comments/{cid}/reaction {type:LIKE|DISLIKE}` — 등록·전환(LIKE↔DISLIKE)·취소(같은 타입 재요청). 유저당 1행, like/dislike_count 동일 TX 원자 UPDATE(손실·언더플로 가드).
3. **자기 댓글 반응 금지** COMMENT_003(422).
4. 응답 `{likeCount,dislikeCount,myReaction}`. 목록·상세에 myReaction(뷰어 종속·optional-auth) 포함.
5. 주체 SecurityContext. spotless·checkstyle·통합테스트(등록·전환·취소·중복차단·self 금지·동시성).

## 근거인용
board-domain-spec v1.1 §13·§14(B1), api-contract v1.24 §6.3. money_exchange 멱등·N-1 원자 카운트 선례.

## 파생 경위: 게이트1 T3(반응 백엔드).
