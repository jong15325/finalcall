---
id: FC-025
type: task
epic: EPIC-AUCTION
derived_from: null
jira_key: KAN-31
title: 경매 — 계약 §3.1 검증 + auction-domain-spec 확정 + 티켓 슬라이싱
state: done
owner: architect
depends_on: []
blocks: [FC-026, FC-027, FC-028]
gate: null
review_status: passed
contract_ref: docs/spec/api-contract.md §3.1, docs/spec/domain-spec.md §3·§5, docs/spec/erd.md (auction)
artifacts:
  - docs/spec/auction-domain-spec.md
  - docs/spec/api-contract.md
---
## architect 산출 요약 (FC-025 done, 게이트2 승인 반영)
- 정본: auction-domain-spec v0.2(신규)·api-contract v1.7. erd 무변경(auction 테이블 v0.9 완비).
- **게이트2 6결정 확정**: (a) lazy 활성화 파생 (b) 상세 최고가 null/0 (c) 소프트클로즈 config 저장만(기본30/30) (d) AuctionStatus 5값 정의·전이는 →ACTIVE·→CANCELLED만 (e) cancel `highest_bidder_id IS NULL` 앵커 (f) AUCTION_001 403 단일화 (G6) 취소 SCHEDULED|ACTIVE.
- **계약 정밀화 v1.7**: AUCTION_001 403 단일, 취소 대상 "SCHEDULED|ACTIVE & 입찰0".
- **⚠️ G4 markListed CAS 교정 요구**(FC-026): ItemInstance.markListed()가 dirty-checking → 조건부 @Modifying UPDATE(WHERE location='INVENTORY')+영향행 판정 필요.
- **팬아웃: FC-026→027→028 순차 단일패스**(AuctionController·Service·Repository 공동편집 + Flyway V10 단일채번 + item 파일 교차). 영향 티켓 없음(BID/CLOSING 미착수).
## 목표
- 경매 에픽의 계약/설계를 확정해 backend-impl 팬아웃 근거를 만든다(contract-first). 계약·erd 완비 → 검증 + 슬라이싱 + 갭·경계 식별이 핵심.

## DoD
- **계약 검증**: 계약 §3.1(등록·목록·상세·취소 — 입찰/구매 제외)이 erd auction 테이블·domain-spec §5 FSM과 정합한지 검증. 불일치·갭 목록화.
- **auction-domain-spec 확정**: 산출물 `docs/spec/auction-domain-spec.md`(신규) — auction 엔티티 필드(startPrice·buyNowPrice?·startAt?·endAt·softClose config·maxEndAt·status·resultType), status enum·FSM 전이(EPIC-AUCTION은 SCHEDULED/ACTIVE/CANCELLED만), item LISTED 에스크로 전이(등록 CAS·취소 복귀 만실 temp), 등록 검증(buyNowPrice>startPrice·시간 파라미터 SEC-009), 목록/상세 응답 필드·마스킹·페이지네이션, AuctionErrorCode 초안(AUCTION_001~008).
- **티켓 슬라이싱 + 팬아웃 판정**: FC-026/027/028의 쓰기 파일 집합 명시, 병렬 팬아웃 가능 여부(auction 엔티티·Flyway 공유 충돌 판정). 교차 시 순차 권고.
- **게이트2 상신 정리**: (a) SCHEDULED→ACTIVE 활성화 방식 — 마감 스케줄러는 EPIC-CLOSING이므로 lazy 활성화(조회/입찰 시 startAt 판정) 권고 여부. (b) 상세 API 현재최고가·최고입찰자 — 입찰 미구현(EPIC-BID)이라 startPrice/null 처리. (c) 소프트클로즈 config 컬럼(window/extend/maxEndAt) 저장만·로직은 BID/CLOSING. (d) status enum 전체 정의 vs AUCTION 전이분만.

## 근거(인용)
> 계약 §3.1 "POST /auctions … 아이템을 인벤토리→출품 에스크로(location LISTED)로 CAS 이동", "cancel … 입찰 0건 & ACTIVE일 때만 CANCELLED. 아이템 에스크로 해제(인벤토리 복귀, 만실 시 임시보관)". domain-spec §5 경매 FSM. erd 결정플래그 B(LISTED). 보안 층 첫 실적용(경매 에픽부터).
