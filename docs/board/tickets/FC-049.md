---
id: FC-049
type: task
epic: EPIC-FE-REDESIGN
derived_from: FC-042
jira_key: KAN-61
title: 경매 목록·상세 React 반영 — FC-042 목업 집행 + 실아트 매핑 + 부채 18건
state: review
owner: frontend-impl
depends_on: [FC-047, FC-051, FC-048, FC-052]
blocks: []
gate: null
review_status: pending
contract_ref: docs/ux/design-system.md v0.6.1, docs/spec/api-contract.md v1.10 §3, docs/spec/references/game-item-skill-format.md
artifacts: []
---
## 목표
FC-042 목업(3차 확정안)을 실코드로 집행하고, **`ItemArtSlot`에 실아트를 매핑**한다.
경매 목록·상세는 이미 구현돼 있으나(FC-036·037) 목업 기준 **부채 18건 이상**이 식별돼 있다.

## ★ 이 티켓의 최대 가치 — 실아트
지금 `ItemArtSlot`이 전 화면에서 플레이스홀더("Lv.N" 텍스트)다. **FC-052가 시드를 아트와
짝맞춰 재작성**했으므로(템플릿 40종 전부 아트 보유) 이제 매핑이 가능하다.
`ItemArtSlot`은 **공유 컴포넌트**라 고치면 홈·목록·상세가 동시에 살아난다.

### 아트 경로 규칙
```
docs/game_ui/item_info/card_image/gold_black/level{표시레벨}/{l|s}/{water|fire|earth|wind}/
  {axe|wand|sword|bow|shield|pendant|armor|boots|magic}.png
```
- `l` 50×93(카드·상세) · `s` 26×28(행 썸네일) — **둘 다 세로형**
- 마법은 `magic.png` 1장 — `kind` 1·2가 **공유**한다(2:1)
- **레벨이 아트에 구워져 있다** → 웹이 `l`에 레벨을 중복으로 그리지 않는다(FC-042 판정)

### ★★ 레벨 축 주의 — 근거 `docs/spec/references/game-item-skill-format.md §2`
원게임 `itm_level`은 **0-based**(0 = 실제 1레벨)이나, **우리 `item_instance.level`은 표시
레벨(1~9)을 저장**한다. **아트 디렉터리도 표시 레벨**이다.
→ **우리 데이터에는 보정을 하지 마라.** `level{level}` 그대로다.
보정이 필요한 건 원게임 데이터를 이식할 때뿐이고 이 티켓 범위가 아니다.
**틀리면 전 아이템이 한 단계씩 어긋난 그림으로 나가고, 눈으로는 잘 안 보인다.**

### 자산 경로
목업은 `docs/ux/mockups/`에서 `../../game_ui/...`로 상대참조했다. **React는 `frontend/`
밖 파일을 번들할 수 없다** — 자산을 어떻게 서빙할지(public 복사 · vite 별칭 · 심볼릭 등)
**판단하고 근거를 반환하라.** 복사한다면 **무엇을 얼마나** 복사했는지 명시하라(전체 648장은 과하다).

## 집행할 목업 (3차 확정안)
`docs/ux/mockups/template-auction-list.html` · `template-auction-detail.html`

**확정된 구조** — 근거는 `docs/ux/references/auction-detail-references.md`:
- **데스크톱 = 안2**: 상단 가로 2열(아트 | 스펙) + 마감·입찰 전폭 + 입찰 이력 전폭 + 스티키 입찰 바
- **카드 클릭 = 아트 확대 라이트박스** (스펙 모달 아님 — Baymard "스펙 중심 상품에 퀵뷰 금지")
- **모바일 = 고정 하단 바**(현재가·잔여시간·CTA) + 입찰 시 **바텀시트**
- 타이포는 **역할 기반**(라벨 11 / 값 17 / figure 28 / figure-xl 44). **중간 크기 금지**
- 필터에서 **`kind`를 단독 축으로 그리지 마라** — `subGroup` 종속(계약 v1.10 §4.1 경고)

## ★ 반응형 별도 설계 (지속 원칙)
> 반응형은 배치 변경이 아니다. 웹은 웹, 모바일은 모바일에 맞게 짜여야 한다.

데스크톱·모바일 구조를 **각각 도식으로 제시**한다. 목업의 IA 대조표를 따른다 —
입찰 이력(표 ↔ 카드 리스트) · 입찰 입력(인라인 ↔ 바텀시트) · 마감(절대시각 ↔ 상대시간).

## 부채 18건 (FC-042 산출에서 식별)
| # | 대상 | 내용 |
|---|---|---|
| 1 | `element.ts` | ~~1·2만 등재~~ → **FC-048에서 해소됨** |
| 2 | `ItemArtSlot` | 플레이스홀더 → **실아트** |
| 3 | 〃 | `bg-surface-slot`(#000) → **속성별 딥 글로우**(§2.7) |
| 4 | 〃 | `Lv.{level}` 오버레이 → `l`은 아트에 구워져 있어 **삭제**, `s`에서만 표기 |
| 5 | 〃 | `aspect-[4/3]` 가로 → **세로 슬롯**(아트가 50×93) |
| 6 | 〃 | 골드포스 표시 없음 → §5.12 아웃라인 + 배지 + 본문줄 **3경로** |
| 7 | `AuctionFilterBar` | `subGroup`·`kind`·레벨·골드포스 필터 **부재** |
| 8 | 〃 | 상단 가로 배치 → 레일/시트 |
| 9 | `AuctionCard` | 가격 구분이 **라벨 문구 하나뿐** → 문구+색·굵기+실선/파선 3중 |
| 10 | `Countdown` | 임박 3구간 → **칩 종류를 바꾼다**(색만으론 부족) |
| 11 | `AuctionCard` | 종료 오버레이 "마감" → **사유 병기**("마감·낙찰"/"마감·유찰") |
| 12 | `AuctionDetailPage` | 아트 좌측 3/5 전폭 → 안2 구조 |
| 13 | `AuctionTradePanel` | 카운트다운 본문 크기 → 최대 활자 + 임박 시 배너 승격 |
| 14 | 〃 | `buyNowPrice` null이면 **줄이 사라짐** → "설정 없음"으로 자리 유지 |
| 15 | 〃 | 소프트클로즈가 `extensionCount>0`일 때만 → **규칙 상시 안내** |
| 16 | 없음 | **입찰 이력 화면 부재** → 표 + offset 페이저 |
| 17 | 없음 | 무한스크롤 "더 보기" 폴백 → 센티넬만으론 **키보드 접근 불가** |
| 18 | `Button` | disabled 전경 → **FC-047에서 해소됨**(`text-disabled`) |

**FC-038 이월 minor 5건도 함께**: SR "0분 남음" · **소프트클로즈 연장 안내**(입찰이 붙는 순간
accessibility §6 실효 요건) · 마지막 페이지 포커스 소실 · IntersectionObserver 재구독 ·
목록 카드 시간 반응성.

**계약 v1.8 주가 못 박은 것**: `BID_003`(자기 경매)·`BID_007`(미개시) **화면 분기 부재**.
*"안내 문구·재시도 가능성이 정반대"*라 뭉뚱그리면 안 된다. `errorCodes.ts`에 `BID_007` 미등재.
단 상세가 **판매자 본인 여부를 알 수 없다**(`sellerNickname`만 내려옴) — 계약 한계면 **보고**하라.

## ★ 알려진 제약 — 경매 데이터가 없다
FC-052가 시드를 확장했으나 **`item_instance`가 전부 `INVENTORY`이고 경매 시드는 없다.**
→ **경매 목록·홈이 실행 시 빈 상태로 나온다.** 이 티켓에서 백엔드를 건드릴 수 없으므로
**빈·로딩·에러 상태를 제대로 만드는 것**으로 대응하고(FC-048이 세운 기준을 따른다),
**시각 검증이 불가능하다는 사실을 반환에 명시**하라.

## DoD
- 목업 2면이 실화면으로 존재한다. `ItemArtSlot`에 **실아트가 뜬다**.
- 부채 목록 처리(해소·이월을 각각 명시).
- **테스트 동반** — 최소: 목록·상세 렌더 · 빈/로딩/에러 · 아트 경로 생성 로직(**레벨·속성·종류
  조합** 단위 테스트. 눈으로 못 잡는 오류라 테스트가 유일한 방어다).
- `npm run test`·`typecheck`·`lint`·`build` 전건 통과.

## 제약
- **`backend/**` 수정 금지**(동결 복귀 상태). `docs/**` 수정 금지(발견은 보고만).
- **§1.2 Game-Color Containment** — 게임색은 아이템 카드·속성 배지·필터 칩·아트 슬롯에만.
- 등급 배지 금지(D-073). WCAG AA **수치 검증**. 다크모드 금지.
- 계약에 없는 데이터 창작 금지. 필요하면 **보고**(게이트2 대상).
- 목업의 타이포는 v0.6 이전 상태다 — **정본(v0.6.1)이 우선**, 충돌 시 보고.

## 결과 (2026-07-19)
커밋 `b31fd2e`, **프론트 38파일만**. **테스트 101건 통과**. **부채 18건 전건 해소, 이월 0건.**
FC-038 이월 minor 5건도 전건 해소.

### 자산 서빙 — 빌드 시 복사 + gitignore (판단 좋음)
`frontend/scripts/sync-game-art.mjs`(`predev`/`prebuild` 훅) → `frontend/public/art`.
**복사본은 `.gitignore` 대상**이라 바이너리가 레포에 이중 등재되지 않는다(총괄 `git check-ignore` 확인).
정본은 `docs/game_ui` 한 곳뿐이고 **드리프트가 구조적으로 불가능**하다.
vite 별칭은 dev만 해결하고 build를 해결하지 못해 기각.

**전량 648장 복사** — "과하다"는 지시에 대한 반론이 타당하다: **도달 가능한 조합이 곧 전량이다.**
시드 40종이 element×kind 전수를 덮고 `level`이 인스턴스마다 1~9로 흩어지므로 "시드가 쓰는 조합"으로
좁혀도 결과가 같다. 줄이려면 레벨을 고정해야 하는데 **그건 데이터 왜곡**이다.

### 레벨 축 — 보정 없음, 테스트로 고정
`itemArt.test.ts` **24건**이 레벨 1~9 전수 경로를 고정한다 — **0-based 보정을 넣으면 즉시 실패**한다.
360 조합 전수 검증(고유 파일 324장). 눈으로 못 잡는 오류의 유일한 방어가 실제로 세워졌다.

### earth·wind 글로우는 창작이 아니다
§2.7이 남긴 **조건부 지시**("3·4가 확정되면 같은 공식으로 파생")의 집행이고, 계약 v1.10이 조건을
충족시켰다. 독립 재계산이 FC-042 값과 **정확히 일치**(5.58/5.72/8.01/8.23).

### 대비 23쌍 전건 AA, 최저 4.59.

## ★ 게이트2 상신 — `BID_003` 화면 분기 미구현
상세 응답에 **판매자 식별자가 없다**(`sellerNickname`만). 닉네임 문자열 비교로 본인을 판정하는 것은
**인가 판단을 표시명에 태우는 것**이라 하지 않았다 — 옳은 판단이다.

**필요한 계약 변경**: `AuctionDetail`에 **`isSeller: boolean`(서버 파생)** 또는 `sellerPublicId`.
→ **전자를 권고**한다. 후자는 사용자 식별자를 공개 엔드포인트에 노출해 SEC-007과 마찰한다.

`BID_007`은 `errorCodes.ts`에 등재하고 `SCHEDULED` 화면 분기를 구현했다(계약 v1.8 F4 누락분 해소).

## 정본 우선 2건 (목업과 충돌 → 정본 채택)
1. **임박 신호 퍼플 승격 미채택** — 목업은 warn/crit에 `--primary`를 썼으나 §1.2가 퍼플을
   액센트(링크·포커스·선택)로 가두고, references §8-5가 이를 **디자인 게이트 미결**로 명시했다.
   §5.9의 warning→danger를 쓰고 긴박은 §3.1 원칙 5대로 **크기·단위가 운반**한다.
   → **게이트에서 승격이 승인되면 `CHIP_CLASS` 두 줄만 바꾸면 된다.**
2. **임박 임계값 5분/30초 유지** — references는 임계값을 "데이터·정책 사안"으로 열어 뒀으나
   **정본 §5.9는 값을 갖고 있다.** 창작하지 않고 정본을 따랐다.

## 의도적으로 만들지 않은 것
- **입찰 바텀시트** — 입찰 뮤테이션이 없어 만들면 **제출 없는 죽은 UI**가 된다. 하단 고정
  바(실정보 운반)만 구현하고 시트는 입찰 티켓으로 이월.
- **`mainCategory`·`skill1/2` 필터** — 전자는 값이 1뿐이라 축이 안 되고, 후자는 코드→이름 매핑
  API가 계약에 없어 선택지를 만들 수 없다. **자리조차 두지 않았다**(누르면 빈 결과가 나오는 축을
  그리지 않는다).

## ★ 시각 검증 불가 — 경매 시드 부재
`item_instance`가 전부 `INVENTORY`이고 경매 시드가 없어 **목록·홈·상세가 전부 빈 상태**로 나온다.
아래는 **눈으로 보지 못했다**:
- **실아트가 실제로 뜨는 모습** — 경로는 테스트로 고정, 파일 존재도 확인(`dist/art` 648장)했으나
  글로우 위 색감·좌우 띠 폭·정수배 확대 선명도는 미확인
- **골드포스 금 링 두께·셰인** — `mask-composite`를 jsdom이 재현 못 한다
- **카드 20장이 섞였을 때의 스캔 가능성**(부채 9의 실제 판정 대상)
- **★ 스티키 입찰 바가 헤더와 겹치지 않는지** — `top-4`는 **추정값**이다(목업은 `--hdr-h:114px`
  실측을 썼으나 AppShell 헤더 높이를 실측 못 함). **재검증 1순위**
- 모바일 하단 바 등장 타이밍·`pb-28` 여유 · 라이트박스 시트 높이(88vh에 4x 아트)

## 발견 (백엔드 동결로 미처리)
`GET /auctions/{id}/bids`는 백엔드에 구현돼 있어 입찰 이력을 **실배선**했으나, 입찰 데이터가 없어
항상 빈 상태다.
