---
id: FC-107
type: task
epic: EPIC-SEARCH
derived_from: FC-106
jira_key: KAN-121
title: 검색엔진 인프라 + 동기 + 색인/재색인 + q 검색 API (backend)
state: doing
owner: backend-impl
depends_on: [FC-106]
blocks: [FC-109]
gate: null
review_status: changes-requested
contract_ref: docs/spec/search-spec.md v0.3 (FC-106 확정)
artifacts:
  - backend/docker-compose.local.yml
  - backend/docker/search/
  - backend/src/main/java/com/finalcall/domain/search/
---
## 완료 (backend-impl · 2026-07-22 · 커밋 cb91c64, push 안 함)
- **인프라**: docker-compose에 elasticsearch(nori 커스텀 이미지·userdict)·kafka(KRaft)·kafka-connect(Debezium+ES sink) + MySQL binlog(log-bin·ROW·row-image=full·server-id·gtid, 볼륨 보존). connector JSON(source snapshot.mode=initial·sink upsert key=public_id·null=delete)·listings-index.json(nori_kr+ngram_kr·코드축 keyword·numeric·alias listings_search)·런북(README).
- **검색 API**: ListingSearchService(multi_match nameSnapshot^3 nori + .ngram^1·코드축 filter AND·relevance _score/publicId search_after 커서·**match만→DSL 인젝션 차단**). AuctionController/ShopController q(무q relevance→400)·**ES=매칭/랭킹/커서 + MySQL=표시 하이드레이션**. SearchReconciliationWorker(@Scheduled count 드리프트)·ListingIndexer(코드축 enrichment bulk upsert).
- **검증**: spotless·checkstyle(maxWarnings 0)·build·테스트 그린(SearchCursorTest·ListingSearchIntegrationTest[ES Testcontainer: 색인·매칭·코드축 AND·종류격리·relevance 커서·q 규약]·LayerDependency·회귀). 계약 이탈 0.
- **★ 스펙 갭(이탈 아님)**: auction/shop 행에 코드축·level·skill·gfExpireAt 부재(item 소재) → 단일테이블 CDC로 텍스트/가격/상태/시각만 채워지고 **코드축은 ListingIndexer(정본 조인 읽어 bulk upsert=재색인/화해 보정, dual-write 아님)로 채움**. reviewer 검증 대상.
- **후속/분할 제안**: 실질 2티켓 규모(인프라/CDC vs 검색API). 후속 = 코드축 CDC 완전화·등급 부스트(GRADE)·패싯·q+비relevance 정렬·전체 CDC 자동 통합테스트(현재 런북 수동).
## 목표
FC-106 게이트2 승인분대로 검색엔진(②단계)을 구현한다. MySQL=SoT·엔진=파생 read-model. 마켓+경매 q 검색.

## DoD (FC-106 확정 후 세부화)
- **인프라**: 엔진 컨테이너(docker-compose·로컬 기동)·인덱스 매핑(nori+ngram 멀티필드·코드축 keyword·level/price numeric).
- **동기**: FC-106 확정 방식(Outbox 릴레이 or CDC) — dual-write 금지·멱등 upsert(문서 id=리스팅 public_id)·초기 백필.
- **재색인/화해**: alias 스위치 재색인 + count/histogram 주기 화해(드리프트 탐지).
- **검색 API**: `q`(자유문·한글)·필터·`relevance` 정렬(BM25). 마켓(/shops)+경매(/auctions). 정확성 필요 경로(구매·정산)는 ES 미신뢰(DB 읽기) 준수.
- 테스트(색인·검색·동기 멱등·화해·q 규약). spotless+checkstyle. 기존 회귀 없게.

## 근거인용
- search-spec v0.2(FC-106)·domain-spec §8·concurrency-review·jpa-convention.

## 파생 경위: FC-106 확정 후 백엔드. FC-108(frontend)와 파일 무교차 병렬.
