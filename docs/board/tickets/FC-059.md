---
id: FC-059
type: task
epic: EPIC-FE-ECME
derived_from: FC-058
jira_key: null
title: 경매 목록 — 필터·그리드·커서 무한스크롤
state: doing
owner: frontend-impl
depends_on: [FC-055, FC-056, FC-057, FC-058]
blocks: [FC-064]
gate: design
review_status: pending
contract_ref: docs/spec/api-contract.md v1.10 §3.1·§3.3·§4.1
artifacts: []
---
## 목표
경매 목록 화면을 만든다. 지금 `PagePlaceholder`다.

## ★ 범위를 쪼갰다 — 상세는 FC-064
에픽 원안은 "경매 목록·상세"였으나 **한 티켓에 화면 둘은 위험**하다. 근거: **홈 한 화면이
3번 재작업**됐다(피드백 12건 → 아웃라인 정정 → 모바일 파손). 목록·상세를 묶으면 한쪽 반려가
다른 쪽까지 되돌린다.
→ **FC-059 = 목록**, **FC-064 = 상세 + 입찰**(신규).

## 재사용 — 이미 있는 것
FC-058이 만든 것을 **그대로 쓴다**(공유 위치라 재사용이 전제다):
- `features/auction/components/` — `AuctionGridCard`·`AuctionFeatureCard`·`AuctionRowItem`·
  `CountdownText`·`UrgencyBadge`·`SnapCarousel`·`interactionClass`
- `features/item/components/ItemArtSlot` + `itemOutline.css`(§5.12 2겹 링·크로마키 처리)
- `features/item/lib/` — `itemArt`·`itemCode`·`element`·`goldforce`
- `lib/api/auctions.ts`·`lib/queries/auctions.ts`

**카드를 새로 만들지 마라.** 이 에픽의 목적이 *"컴포넌트가 한 몸이라 통일성이 나온다"*이고,
지난번 지적("영역별로 디자인이 틀어진다")의 원인이 화면마다 카드를 따로 만든 것이었다.
**부족하면 공유 컴포넌트를 확장**하고, 그 변경이 **홈에도 반영된다는 것을 의식**하라.

## 화면 구조 (레이아웃 B — 사용자 확정)
- **데스크톱**: 좌측 **필터 레일** + 우측 카드 그리드
- **모바일**: 필터를 **바텀시트로 교체**(레일을 접는 게 아니다) + 적용된 필터 칩 줄
- ★ **모바일 우선으로 짜라** — FC-058이 데스크톱 기준으로 짜서 320·360·390·640·768·1024가
  전부 깨졌다. 참고: `demo/src/views/dashboards/EcommerceDashboard/`
  (`max-w-full overflow-x-hidden` 루트 가드 · `grid-cols-1`에서 시작 · 고정 폭은 큰 BP에서만)

## 필터 — 계약 §3·§4.1
| 축 | 주의 |
|---|---|
| `subGroup` · `kind` | **★ `kind`는 `subGroup` 종속** — `kind=1`이 무기면 도끼, 방어구면 방패. **단독 축으로 그리지 마라.** 계약 §4.1이 경고를 명문화했고 서버는 400으로 막지 않는다(다의성 해소는 **클라 책임**) |
| `element` | 1~4 확정(v1.10 §3.3.1). **미등록 코드 중립 폴백**은 유지 |
| `minLevel`/`maxLevel` · `goldforceActive` · `minPrice`/`maxPrice` · `status` | |
| **`mainCategory`** | **값이 1뿐이라 축이 되지 않는다** — 만들지 마라 |
| **`skill1`/`skill2`** | 코드→이름 매핑 API가 **계약에 없다** — 선택지를 만들 수 없다. 만들지 마라 |
| **자유문 검색** | **계약에 `q`/keyword가 없다** — 만들면 **동작하지 않는 컨트롤**이다 |

**정렬**: 화이트리스트만(`price`·`endAt`·`createdAt`·`highestBidAmount`). `bidCount` **없다**.

## 목록 동작
- **커서 페이징** — `useInfiniteQuery`. 캐시 키를 홈 프리뷰(`auctionKeys.preview`)와 **분리**하라
  (홈이 목록 필터 변경에 딸려 무효화되면 안 된다 — FC-055가 이 이유로 react-query를 유지했다)
- **"더 보기" 폴백 필수** — 센티넬만으로는 **키보드 접근 불가**
- **커서 페이지엔 총건수가 없다** — "N개"가 아니라 **"N개 이상"**(구 프론트 테스트가 고정했던 규칙)
- **정렬 전환 시 커서 초기화**

## 상태 설계 (FC-058 기준)
로딩은 데이터 무관 부분 **즉시 렌더** · **빈 결과**는 필터를 되돌릴 수단을 준다 · 에러 격리.
**★ 기본 목록에 종료 경매가 안 섞인다** — `statusScope()`가 status 미지정 시 `SCHEDULED·ACTIVE`만
노출한다(계약대로). "마감·낙찰/유찰"은 **`?status=SOLD` 명시 필터로만** 보인다.

## DoD
- 목록이 `PagePlaceholder`가 아니다. 필터·정렬·무한스크롤 동작.
- **모바일 우선** — 320~1440 폭에서 파손 0. **가로 스크롤 없음.**
- **테스트 동반** — 필터 URL 동기화 · `kind` 종속 · 빈/로딩/에러 · "N개 이상" · 키보드 폴백.
- `npm run test`·`typecheck`·`lint`·`build` 통과 + **번들 실측**.

## 제약
- **`backend/**`·`docs/**` 수정 금지**(발견은 보고만).
- **화면은 목록만.** 상세·입찰은 FC-064.
- **템플릿 컴포넌트 우선** — 자체 제작 시 사유 보고. 템플릿 `Alert`는 `framer-motion`을 끌고 오니
  쓰지 마라(FC-058 확인).
- 우리 시각 언어 부활 금지(색·액센트). **단 §5.12 아웃라인은 사용자 승인분이라 유지.**
- 등급 배지 금지(D-073). WCAG AA **수치 검증**.
- **스테이징 범위를 좁혀라** — prettier가 템플릿 원본 26개를 건드린 사고가 있었다.
