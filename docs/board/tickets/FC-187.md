---
id: FC-187
type: task
epic: EPIC-ITEM-DELIVERY
derived_from: FC-185
jira_key: KAN-213
title: 정산 TX 내 enqueue — SettlementRecorder 꼬리에 배송 생성
state: doing
owner: backend-impl
depends_on: [FC-186]
blocks: [FC-189]
gate: null
review_status: pending
contract_ref: docs/spec/delivery-domain-spec.md
artifacts:
  - backend/src/main/java/com/finalcall/domain/settlement
---
## 목표 (EPIC-ITEM-DELIVERY T3 · 최고위험 정산 임계경로)
소유이전과 배송 생성을 **exactly-once로 결합**한다(트랜잭셔널 아웃박스). 정산 TX가 커밋되면 배송 우편함 1행(PENDING)이 반드시 함께 존재.

## DoD
1. **SettlementRecorder 공통 꼬리에 item_delivery INSERT(PENDING)** — 낙찰(closing)·즉시구매(purchase) **양 경로** 커버(purchase-spec §6 공통 꼬리 재사용).
2. **같은 TX 원자성** — 정산 커밋과 배송 생성이 한 트랜잭션. 별도 단계 enqueue 금지("팔렸는데 배송 없음" 차단).
3. **자족 스냅샷 채움** — type_code·level(1-based)·skill 3필드·gf·item_uuid를 배송 시점에 확정해 행에 실음(게임 번역 대비).
4. **sale_order_id 1:1 UK로 이중 배송 DB 차단** 검증 + 동시성/멱등 테스트.

## 근거인용
delivery-domain-spec(FC-185), proposal §3.3·§5·§6.2. closing/purchase SettlementRecorder. concurrency-review 스킬.

## 파생 경위: 게이트1 T3. FC-186 스키마 후. FC-188과 정산영역 파일 겹쳐 직렬.
