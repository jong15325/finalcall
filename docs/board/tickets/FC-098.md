---
id: FC-098
type: task
epic: EPIC-MARKET-DATA
derived_from: FC-097
jira_key: KAN-110
title: skill_definition 435 시드 + item 뷰 스킬명 노출 + 마켓 5천 로컬 데모 시드 (backend)
state: review
owner: backend-impl
depends_on: [FC-097]
blocks: [FC-100]
gate: null
review_status: pending
contract_ref: docs/spec/skill-exposure-spec.md v1.0
artifacts:
  - backend/src/main/resources/db/migration/V16__skill_definition_seed.sql
  - backend/src/main/java/com/finalcall/api/shop/ShopItemView.java
  - backend/src/main/java/com/finalcall/api/auction/AuctionItemView.java
  - backend/src/main/java/com/finalcall/api/support/LocalDemoDataService.java
  - backend/src/main/java/com/finalcall/api/support/LocalDemoSeeder.java
---
## 완료 (backend-impl · 2026-07-22 · 커밋 5db6184, push 안 함)
- **skill_definition 244행**: V16(UPDATE 5 + INSERT 239). 100~197(98)·200~209(10)·300~435(136), 미사용(198·199·210~299) 제외. name=§5 효과 서술 verbatim.
- **뷰 스킬명 노출**: ShopItemView·AuctionItemView에 skill1Name/skill2Name(nullable). 기존 fetch join `.getName()`만 추가 → **N+1 없음**. 마법 스킬1 null 폴백.
- **마켓 5천 시드**: LocalDemoSeeder.seedMarket(기본 5000·demo.seed.market-count)·LocalDemoDataService.bulkCreateListedShopItems(LISTED-direct + shop + 이력, JdbcTemplate batch). end_at 동적·@Profile local·demo1 마커 멱등. 다양가.
- **검증**: spotless·checkstyle(maxWarnings 0)·신규 테스트 그린(244 정합·§5 대조·미사용 제외·조인·마법 폴백·대량 발행/페이징)·회귀 그린(Testcontainers). 계약 이탈 0.
- **후속 플래그(reviewer 판단)**: **code 140** — spec §1.4 요약(140=크리50)이 §5.1 위치매핑(140=45·141=50)과 off-by-one 모순. **§5(해독 정본) 채택 = 140 크리45**. 제품 의도가 50이면 spec 정정 + V17 UPDATE 1건.
- **★ 시드 멱등 주의(총괄)**: 마켓 시드가 demo1 마커에 포섭 → **기존 demo1 있으면 재부팅해도 5천 미시드**. 실측하려면 DB 리셋으로 신선 시드 필요.


## 목표
FC-097 계약대로 스킬 마스터를 채우고, 카드/목록/상세에 스킬명(+skillPercent)을 노출하며, 마켓 5천 대량 데모 시드를 넣는다.

## DoD
- **skill_definition 435 시드**: §5 → Flyway(V9 5건 대체/확장). name=효과 서술·description 보강. 미사용 코드 규칙 준수.
- **item 뷰 스킬명 노출**: FC-097 결정 방식대로 — (a) ShopItemView·AuctionItemView에 skill1Name/skill2Name(+skillPercent) 조인 필드 or (b) GET /skills 딕셔너리 엔드포인트. 계약 정확 준수(이탈 금지).
- **마켓 5천 로컬 데모 시드**: LISTED-direct 인스턴스 + shop 직접 INSERT(정식 register 회피). item_template×skill 다양성·다수 판매자 분산·price 분포·end_at 동적. **로컬 프로파일 격리·멱등**.
- 테스트(스킬명 조인·시드 정합·대량 목록 페이징). spotlessApply+checkstyle. 기존 회귀 없게.

## 근거인용
- skill-exposure-spec(FC-097)·game-item-skill-format §5·jpa-convention·coding-discipline 스킬.

## 파생 경위: FC-097 계약 확정 후 백엔드. FC-099(frontend)와 파일 무교차 병렬.
