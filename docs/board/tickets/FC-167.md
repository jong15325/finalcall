---
id: FC-167
type: task
epic: EPIC-LOGINID-CHECK
derived_from: FC-165
jira_key: KAN-188
title: 회원가입 아이디 준비중 placeholder → 라이브 중복확인 교체
state: done
owner: frontend-impl
depends_on: [FC-165]
blocks: [FC-168]
gate: null
review_status: passed
contract_ref: FC-165 확정 계약 (login-id/availability)
artifacts: []
---
## 목표
회원가입 아이디(loginId) 필드의 "준비 중" 비활성 placeholder(`PendingAction`)를 **라이브 중복확인으로 교체**한다. 닉네임(FC-162)과 동일 UX. 디자인 게이트 불요(교체·활성화).

## DoD
- 아이디 입력 + "중복 확인" 버튼 → FC-165 `login-id/availability` 조회 → 사용 가능/중복 피드백(닉네임 FC-162와 동일: `role=status`·`aria-live`·로딩·에러·원문 미노출).
- 기존 아이디 "준비 중" `PendingAction` placeholder 제거(교체). 값 변경 시 확인 상태 초기화.
- 제출 시 서버 검증(중복 409)은 최종 방어선 유지.
- `npm run build`·lint·관련 vitest 그린. 커밋 금지.

## 근거인용
FC-165 계약. FC-162 닉네임 라이브 확인 선례(패턴·컴포넌트 재사용). 사용자 승인(준비중 제거+중복확인 추가).

## 구현 결과 (2026-07-30)
- 수정: `lib/api/auth.ts`(`checkLoginIdAvailability`), `SignupForm.tsx`(아이디 `PendingAction` 제거→라이브 확인), `SignupForm.test.tsx`(테스트 미러).
- **재사용 개선(리팩터)**: `NicknameCheck`→공용 `AvailabilityCheck`, `NicknameCheckButton`→공용 `AvailabilityCheckButton`으로 일반화(아이디·닉네임 공유). 버튼 구분은 `ariaLabel`("아이디/닉네임 중복 확인").
- UX: 닉네임 1:1 미러(입력→버튼→피드백·초기화·`role=status`·advisory·409 AUTH_001 재사용). 20/20 그린·lint·build 그린.

## 파생 경위: FC-165 계약 확정 후 프론트 배선(닉네임 미러).
