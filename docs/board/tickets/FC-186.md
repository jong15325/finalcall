---
id: FC-186
type: task
epic: EPIC-ITEM-DELIVERY
derived_from: FC-185
jira_key: KAN-212
title: 우편함 스키마 — item_delivery 테이블 + Flyway V21 + 엔티티/리포지토리
state: review
owner: backend-impl
state_note: V21 마이그레이션 + ItemDelivery/DeliveryStatus 엔티티 + Repository + DeliveryErrorCode(common 중앙화) 구현. item_instance.location IN_GAME 추가(VARCHAR라 DDL 무변경). 테스트 13건 green(단위 8·Testcontainers 슬라이스 5), spotless/checkstyle/build SUCCESS. SettlementRecorder 공통 꼬리 실존 확인(settlement/service/SettlementRecorder.java, record() 말미가 FC-187 삽입점). FC-191 통합리뷰 대기.
depends_on: [FC-185]
blocks: [FC-187, FC-188, FC-189]
gate: null
review_status: passed
contract_ref: docs/spec/delivery-domain-spec.md
artifacts:
  - backend/src/main/resources/db/migration/V21__item_delivery.sql
  - backend/src/main/java/com/finalcall/domain/delivery/entity/ItemDelivery.java
  - backend/src/main/java/com/finalcall/domain/delivery/repository/ItemDeliveryRepository.java
---
## 목표 (EPIC-ITEM-DELIVERY T2)
확정 계약(FC-185)의 `item_delivery` 형상을 실물 스키마·엔티티·리포지토리로 구현한다.

## DoD
1. **Flyway V21**(`V21__item_delivery.sql`) — 최신 V20 다음 채번. proposal §7.2 컬럼·UK(public_id·sale_order_id 1:1·item_uuid)·인덱스((status,created_at)·(recipient_user_id,status)).
2. **엔티티** `ItemDelivery` — BaseCreatedEntity(append 원장), 상태 enum, 자족 스냅샷 필드, 도메인 메서드(claim/apply/defer/fail 상태 전이), `@Setter` 금지.
3. **리포지토리** `ItemDeliveryRepository` — findByIdOrThrow 패턴, poller/접속claim 조회 default 메서드.
4. **DDL validate 통과**(전 프로파일 validate, 스키마=Flyway) + 슬라이스 테스트.

## 근거인용
delivery-domain-spec(FC-185), proposal §7.2. jpa-convention 스킬.

## 파생 경위: 게이트1 T2. FC-185 계약 확정 후.
