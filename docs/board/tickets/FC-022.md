---
id: FC-022
type: task
epic: EPIC-ITEM
derived_from: FC-019
jira_key: KAN-27
title: 인벤토리 3 API (inventory·temp-storage·relocate) + slot 유일성 UK·cursor 인덱스
state: done
owner: backend-impl
depends_on: [FC-021]
blocks: [FC-023]
gate: null
review_status: passed
contract_ref: docs/spec/api-contract.md §4.2, docs/spec/item-domain-spec.md §3.2·§5.4, docs/spec/erd.md §4.3·§5
artifacts: []
---
## 목표
- 정규 인벤토리(96칸)·임시보관(오버플로우)·임시보관→정규 이동 API를 구현하고, location XOR 불변식을 앱+DB로 강제한다.

## DoD
- **Flyway V8**(`V8__temp_storage_and_indexes.sql`): temp_storage 테이블 + **G2 slot 유일성** 생성컬럼 `slot_key` UK(`uk_item_instance_slot`, D-081 선례) + **G3** temp cursor 인덱스 `(owner_id, stored_at, instance_id)`. erd v0.9 §4.3·§5 정합.
- **엔티티/도메인**: `TempStorage`, `InventoryErrorCode`. `ItemInstance.java`에 relocate 도메인 메서드 추가(편집).
- **API 3종**: `GET /me/inventory`(capacity:96·used·items[slotNo]), `GET /me/temp-storage`(cursor 페이지), `POST /me/temp-storage/{id}/relocate`(정규 슬롯 여유 필요, TEMP→INVENTORY, temp_storage 행 제거·응답 {slotNo}).
- **불변식**: location INVENTORY→slot_no 0~95 / TEMP→temp_storage 행 / LISTED→활성 리스팅. relocate 동시성 이중배정을 slot_key UK로 DB 차단(주체=SecurityContext).
- **테스트**: 인벤토리·relocate 동시성/경계(만실·중복) 슬라이스/단위. spotless·checkstyle 통과.

## 근거(인용)
> 계약 §4.2 "GET /me/inventory — 내 정규 인벤토리(96칸) … capacity:96", "POST /me/temp-storage/{id}/relocate … 정규 슬롯 여유 필요". erd 결정플래그 B XOR 불변식 앱+DB 강제. 게이트2 승인 G2·G3.

## 파생 경위: FC-019 슬라이싱에서 인벤토리 계층+스키마 갭 G2/G3 반영으로 분리(V8).
