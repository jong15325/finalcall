---
id: FC-405
type: task
epic: EPIC-CHAT
derived_from: FC-404
jira_key: KAN-459
title: 운영 시드 채팅방 ULID 정합과 실시간 전달 복구
state: doing
owner: backend-impl
depends_on: [FC-404, FC-378, FC-379]
blocks: []
gate: null
review_status: pending
contract_ref: docs/spec/chat-domain-spec.md §5·§8
artifacts:
  - backend/src/main/java/com/finalcall/support/seed/OperationsSeedFixture.java
  - backend/src/test/java/com/finalcall/support/seed/OperationsSeedFixtureIntegrationTest.java
  - docs/board/tickets/FC-405.md
---

## 목표
- `ops-20-v2` 채팅방 공개 ID를 ULID 계약에 맞추고, 운영의 기존 메시지·읽음 상태를 보존한 채 실시간 fan-out을 복구한다.

## DoD
- fixture가 생성하는 채팅방 ID 24개가 listener와 동일한 ULID 형식을 만족한다.
- 기존 verify·cleanup·재실행 멱등성이 새 namespace에서도 유지된다.
- 운영 DB의 기존 방 ID와 outbox aggregate/payload를 단일 트랜잭션으로 매핑하고 메시지·읽음 상태를 보존한다.
- 운영에서 WebSocket `101 → CONNECTED`와 `MESSAGE_CREATED` 실시간 수신을 검증한다.

## 근거인용
> chat_room.public_id는 외부 room ID이며 CHAR(26) NOT NULL UK다. — chat-domain-spec §5.1

## 게이트
- 2026-08-26 게이트2 승인: listener 검증은 유지하고 fixture와 운영 데이터를 ULID 계약에 맞추며 기존 채팅 데이터는 보존한다.

## 검증
- `OperationsSeedFixtureIntegrationTest`, Spotless, main/test Checkstyle 통과.
- 운영 변경 전 채팅 테이블 백업: `backups/seed/chat-ulid-fc405-20260826-150047/fc405-chat-before-v3.sql`(Git 제외).
- 운영 단일 트랜잭션 교정 결과: 기존 `OP2ROM%` 방 0, 신규 `0P2R0M%` 방 24, ULID 형식 통과 24, outbox aggregate/payload 불일치 0.
- 메시지 436개와 room member state 48개가 동일 내부 room PK로 보존됐다.
- backend 이미지 빌드·재배포 성공, health 정상, 공개 `/ws/chat` Upgrade 101 확인.
- 실제 두 사용자 `MESSAGE_CREATED` 수신은 사용자 재확인 대기.
