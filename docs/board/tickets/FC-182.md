---
id: FC-182
type: task
epic: EPIC-CARD-SYSTEM
derived_from: null
jira_key: KAN-205
title: T4 — CardInfoDialog 정본 셸 + Shop/Inventory 리팩터
state: done
owner: main
review_status: passed
state_note: reviewer PASS(critical/major 0). "killed" 오보 후 에이전트 완료·되돌리기 잔재 0. CardInfoDialog 정본 셸+Shop/Inventory 리팩터(구매 뮤테이션 footer 슬롯)+CSS/channelLimit shop→item 승격. DOM 바이트·a11y 보존, 291 테스트 green. minor 1(prettier=적용). 커밋됨.
depends_on: []
blocks: [FC-183]
gate: null
review_status: pending
contract_ref: docs/common/proposals/card-system-consolidation-proposal-v0.1.md §2.2·§3
artifacts:
  - frontend/src/features/item/components/CardInfoDialog.tsx
---
## 목표 (EPIC-CARD-SYSTEM T4 · 위험↑ a11y)
카드정보 모달 **셸을 정본 `CardInfoDialog`**로 신설(헤더+썸네일+속성표+스킬 섹션 + `footer`/`extraRows` 슬롯). 모달 배선(초점트랩·스크롤락·Esc·backdrop) **한 곳**. `ShopCardInfoDialog`·`InventoryCardInfoDialog` 포크(~50줄 중복)를 셸+슬롯 소비로 리팩터. **구매 뮤테이션은 footer 슬롯으로 소비자 주입**(결합 차단 — FC-178 포크 사유 해소). 카드정보 CSS·`channelLimit`를 shop→item 승격.

## DoD (계획 §3 단계2)
1. `features/item/components/CardInfoDialog.tsx` 셸 + a11y 배선 정본화.
2. Shop(구매 CTA·확인 서브뷰 footer 주입)·Inventory(판매 등록 CTA) 소비로 리팩터. 포크 제거.
3. `ShopCardInfoDialog.css`·`shop/lib/channelLimit` → `item` 승격(member→shop·shop 자체 경계 정리).
4. **시각 픽셀 보존·a11y 회귀 없음**(ShopCardInfoDialog 8·초점트랩/Esc 스위트 green). 시각 diff 게이트 권고.

## 근거인용
계획 §1.4·§2.2·§2.4(뮤테이션 슬롯)·§3 단계2. reviewer FC-178 M1(CSS·channelLimit 승격).

## 파생 경위: 게이트1 T4. T5/T6과 파일 공유라 직렬.
