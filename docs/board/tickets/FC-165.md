---
id: FC-165
type: task
epic: EPIC-LOGINID-CHECK
derived_from: FC-160
jira_key: KAN-186
title: 아이디(loginId) 가용성 조회 계약 확정
state: done
owner: architect
depends_on: []
blocks: [FC-166, FC-167]
gate: null
review_status: pending
contract_ref: docs/spec/api-contract.md §2 (nickname/availability 미러)
artifacts: []
---
## 목표
FC-166·FC-167 구현 전 loginId 가용성 조회 계약을 확정한다(닉네임 패턴 미러).

## DoD
1. **엔드포인트 계약**: `GET /api/v1/auth/login-id/availability?loginId={아이디}` → `ApiResponse<{available:boolean}>`(DTO `LoginIdAvailabilityResponse`). permitAll. 판정=기존 loginId 존재 검사 재사용. 입력 검증(형식·길이=기존 loginId 규칙 재사용). advisory(최종 권위=제출 시 중복 409). api-contract §2에 추가(닉네임 조항과 대칭).
2. **게이트웨이 배선 명시(필수)**: 계약 DoD에 "이 경로를 게이트웨이 `auth-rate-limited` 라우트 predicate에 등재"를 **명문화**(FC-161 MAJOR-1 재발 방지). FC-166가 backend+게이트웨이 동시 수행.
3. **게이트2 판단**: additive 공개 GET·스키마 무변경이면 게이트2 불요(닉네임 선례). 되돌리기 큰 결정 있으면 상신 플래그.
4. FC-166·FC-167 구현 지시 한 줄씩.

## 근거인용
EPIC-LOGINID-CHECK. 닉네임 선례(FC-160 계약·api-contract v1.17). 사용자 승인(2026-07-30).

## 확정 결과 (2026-07-30)
- **엔드포인트**: `GET /api/v1/auth/login-id/availability?loginId={아이디}` → `ApiResponse<{available}>`(DTO `LoginIdAvailability{Request,Response}`). permitAll. 판정=`existsByLoginIdAndIsDeletedFalse` 재사용(signup 동일 경로). 검증=`@NotBlank @Size(max=50)`(erd VARCHAR(50)). advisory — 최종 권위 = 제출 시 `AUTH_001`(409). 신규 에러코드 없음.
- **게이트웨이 배선(DoD 명문화)**: FC-166이 backend 코드 + `auth-rate-limited` predicate에 경로 추가 + SecurityConfig permitAll을 **한 티켓에서** 수행(MAJOR-1 재발 방지).
- **게이트2 불요**: additive 공개 GET·스키마 무변경. loginId 열거 민감도는 signup AUTH_001이 이미 노출(순증분 0) + rate limit 완화 → FC-168 강화 점검 포인트로 이관.
- **spec**: `api-contract.md` v1.17→v1.18(§2 loginId 가용성 조항).

## 파생 경위: 닉네임 라이브 중복확인(FC-160~162) 패턴을 loginId로 확장.
