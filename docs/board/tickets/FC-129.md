---
id: FC-129
type: task
epic: EPIC-EMAIL-VERIFY
derived_from: FC-114
jira_key: KAN-144
title: B4 — 메일 인프라 (starter-mail·EmailSender·local skip)
state: done
owner: backend-impl
depends_on: []
blocks: [FC-130, FC-132]
gate: null
review_status: passed
contract_ref: docs/spec/email-verify-spec.md §6
artifacts:
  - backend/build.gradle
  - backend/src/main/java/com/finalcall/infra/mail/EmailSender.java
  - backend/src/main/java/com/finalcall/infra/mail/LoggingEmailSender.java
  - backend/src/main/java/com/finalcall/infra/mail/SmtpEmailSender.java
---
## 목표
SMTP 발송 인프라를 신설한다(local skip/log, dev/prod 실발송·fail-fast).

## DoD
- `backend/build.gradle`에 `spring-boot-starter-mail` 추가.
- `MailConfig` + `EmailSender` 추상화 + `email.verify.sender-enabled` 플래그: local=false(SMTP 미호출·코드 로그), dev/prod=true(실발송, `${MAIL_USERNAME}`/`${MAIL_PASSWORD}` 미주입 시 fail-fast).
- 인증 코드 메일 템플릿(제목·본문).
- 발송 실패(SMTP 예외)는 `verification-request`만 실패시키고 가입·로그인 무영향.
- **시크릿 커밋 금지**: `MAIL_PASSWORD`는 env only.

## 근거인용
spec §6(SMTP·네이버·sender-enabled), §4.1(자동발송 미채택 근거).

## 파생 경위
EPIC-EMAIL-VERIFY 분해 B4. 신규 파일 → FC-117·118·128과 병렬 가능.

## rework (2026-07-27 · 게이트2 — EPIC-EMAIL-TEMPLATE 연동)
초안(review 도달분)은 EmailSender에 인증 문구 상수를 포함했다. 메일 문구 DB 이관 결정으로 **템플릿 책임을 제거**하고 범용 발송기로 축소한다(review→doing 회귀). 변경:
- `EmailSender.sendVerificationCode(toEmail, code)` → **`send(toEmail, subject, body, boolean html)`**(인증·제목·본문 미인지 순수 발송기).
- `SmtpEmailSender`의 `SUBJECT`/`BODY_TEMPLATE` 상수 제거. HTML 대비 `SimpleMailMessage` → `MimeMessage`/`MimeMessageHelper`(html 분기) 전환.
- `LoggingEmailSender`도 새 시그니처로(로그는 subject/body 출력, local 한정).
- 조건부 빈(sender-enabled) 전략·starter-mail 의존은 유지. spec email-verify §6.3 델타·email-template §2.2 근거.
- FC-133(V18)과 파일 무교차 → 병렬. FC-132가 소비(render→send).
