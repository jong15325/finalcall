---
id: FC-117
type: task
epic: EPIC-EMAIL-VERIFY
derived_from: FC-114
jira_key: KAN-135
title: Flyway V17 — user 테이블 email 컬럼 3종 추가
state: review
owner: backend-impl
depends_on: [FC-121]
blocks: [FC-118]
gate: null
review_status: pending
contract_ref: docs/spec/email-verify-spec.md §2.1·2.2
artifacts: []
---
## 목표
`user` 테이블에 이메일 인증용 컬럼 3종을 추가하는 Flyway V17 마이그레이션.

## DoD
- `V17__add_user_email.sql` 신규(V16 최신 → V17).
- `email VARCHAR(255) NULL`, `email_verified BIT NOT NULL DEFAULT 0`, `email_active VARCHAR(255) GENERATED ALWAYS AS (IF(is_deleted, NULL, email)) STORED` 추가.
- `uk_user_email_active` 유니크 키(NULL 제외 — 활성·이메일 설정 회원 간만 유일성).
- 기존 행은 `email=NULL`·`email_verified=0`으로 backfill(nullable이라 충돌 없음).
- 로컬 부팅 시 Flyway validate 통과(JPA ddl-auto=validate).

## 근거인용
spec §2.1·2.2(생성컬럼·V4 `login_id_active` 패턴 동형·NULL 유니크 제외), erd §4.1.

## 파생 경위
EPIC-EMAIL-VERIFY 게이트1 분해안 B1(FC-114 실기능화).

## 보류 (2026-07-25)
EPIC-RESTRUCTURE(feature-first 전환) hold 대기. FC-121(도메인 이전) 완료 후 착수 — 마이그레이션 파일 경로만 새 레이아웃 기준으로 갱신(기능·계약 불변).
