---
id: FC-103
type: task
epic: EPIC-SHOP-MANAGE
derived_from: FC-096
jira_key: KAN-116
title: 내 판매목록 조회 계약 확정 → 게이트2
state: doing
owner: architect
depends_on: []
blocks: [FC-104, FC-096]
gate: gate2
review_status: pending
contract_ref: docs/spec/api-contract.md §3.2
artifacts:
  - docs/spec/api-contract.md
  - docs/spec/shop-spec.md
---
## 목표
판매자의 진행 중 고정가 리스팅 조회 계약을 확정한다. 취소는 기존 `POST /shops/{id}/cancel` 재사용, 조회 엔드포인트만 신규. read 엔드포인트라 게이트2 소량 상신.

## DoD
- **조회 엔드포인트 계약**: `GET /me/shops`(또는 `/shops?mine=true`) — **판매자=SecurityContext 주체**·상태 필터(ACTIVE 기본, 옵션 전체)·커서 페이징(기존 ShopCursor 재사용). 응답 = ShopSummary(+ 본인 리스팅이라 fee/settle 노출 가능 여부 결정).
- api-contract §3.2 델타(PROPOSAL). 스키마 무변경(인덱스 `(seller_id,status)` 이미 존재). 
- 반환에 게이트2 상신 항목(엔드포인트 형태·역할별 노출·상태 필터).

## 근거인용
- shop-spec v1.0·api-contract §3.2·erd 인덱스 `(seller_id,status)`·ShopRepository/ShopService 실측.

## 파생 경위: EPIC-SHOP-MANAGE 게이트1 승인 후 contract-first. FC-096 재평가로 조회 API 부재 확인.
