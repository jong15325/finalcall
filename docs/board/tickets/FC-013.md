---
id: FC-013
type: task
epic: EPIC-FE-MEMBER
derived_from: null
jira_key: KAN-16
title: 프론트 auth 실구현 — 로그인·회원가입·로그아웃
state: review
owner: frontend-impl
depends_on: [FC-012]
blocks: [FC-016]
gate: null
review_status: passed
contract_ref: docs/spec/api-contract.md §2
artifacts:
  - frontend/tailwind.config.js                       # 토큰 U-020→U-021 교체(에픽 선행)
  - frontend/src/index.css                            # :root 라이트값 교체
  - frontend/index.html                               # data-theme="dark" 제거
  - frontend/src/pages/LoginPage.tsx                  # stub 제거→실 로그인 폼
  - frontend/src/pages/SignupPage.tsx                 # placeholder→실 가입 폼
  - frontend/src/features/auth/types.ts
  - frontend/src/features/auth/api/authApi.ts
  - frontend/src/features/auth/api/useAuth.ts
  - frontend/src/features/auth/hooks/useRateLimit.ts
  - frontend/src/features/member/api/memberApi.ts     # getMe(하이드레이션 공유, FC-014와 공유)
  - frontend/src/lib/api/fieldErrors.ts
  - frontend/src/components/ui/Button.tsx
  - frontend/src/components/ui/Field.tsx
  - frontend/src/components/ui/Alert.tsx
  - frontend/src/components/feedback/LoadingState.tsx # surface-raised→sunken(토큰 교체 후속)
  - frontend/src/components/feedback/ErrorState.tsx   # surface-raised→sunken
  - frontend/src/components/ThemeToggle.tsx           # surface-raised→sunken
  - frontend/README.md                                # 계약 절 정정(v1.5·사본 폐기)
---
## 목표
- login/signup placeholder(stub 세션)를 실 API 구현으로 대체하고 로그아웃을 연결한다.

## DoD
- 로그인 폼: POST /auth/login → setSession(토큰3종+user). AUTH_003(401) 에러 매핑. returnUrl 복귀 유지.
- 회원가입 폼: POST /auth/signup → 성공 후 로그인 유도/자동. AUTH_001(409 loginId)·AUTH_002(409 nickname)·검증 400 errors[] 매핑.
- 로그아웃: POST /auth/logout(refresh 무효화) → clearSession. **stub 세션 버튼 제거.**
- 폼 검증(클라)·로딩/에러 상태·접근성(레이블·키보드). 디자인 게이트 승인 방향 준수.
- typecheck/lint 그린. reviewer 확인(FC-016).

## 근거(인용)
> 계약 §2 signup/login/logout/refresh. LoginPage/SignupPage stub 대체(코드 주석 명시). authStore.setSession/clearSession, lib/api/client 401 회전 재사용.
