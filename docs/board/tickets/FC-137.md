---
id: FC-137
type: task
epic: EPIC-EMAIL-VERIFY-FE
derived_from: FC-131
jira_key: KAN-154
title: F1 — 가입 폼 email 선택 입력·@Email 검증
state: done
owner: frontend-impl
depends_on: [FC-136]
blocks: []
gate: null
review_status: passed
contract_ref: docs/spec/api-contract.md §2(signup email? 선택), email-verify-spec.md §4.1
artifacts:
  - frontend/src/features/auth/components/SignupForm.tsx
  - frontend/src/lib/api/auth.ts
---
## 목표
회원가입 폼에 **선택** 이메일 입력을 추가한다. 미입력 시 이메일 없는 계정 생성(계약 §2 무변경 흐름), 입력 시 `@Email`·≤255 형식 검증.

## DoD
- `SignupForm.tsx`: email 입력 필드 추가, **"선택" 표기 명시**(필수 아님). 빈 값 허용(생략 전송). 값 있으면 이메일 형식 검증 + 255자 상한.
- `lib/api/auth.ts` signup 페이로드: email 제공 시만 포함(`{ loginId, password, nickname, email? }`), 미제공 시 필드 생략(null 전송 아님).
- 중복 이메일(`EMAIL_007`) 서버 에러를 필드 에러로 표면화(FC-136 메시지 사용).
- 응답 201은 무변경(email·인증상태 미노출) — 가입 후 인증은 별도 화면(F2).
- 기존 `AuthTextField`·폼 검증 패턴 재사용. `SignupForm.test.tsx` 갱신·통과.

## 근거인용
api-contract §2 signup(email? 선택·미제공 시 생략·응답 201 무변경). email-verify-spec §4.1.

## 파생 경위
EPIC-EMAIL-VERIFY-FE 분해 F1. 백엔드 FC-131(signup email) 계약 프론트 소비. FC-136(errorCodes) 선행.
