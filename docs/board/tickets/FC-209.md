---
id: FC-209
type: task
epic: EPIC-COMMENT-V2
derived_from: FC-206
jira_key: KAN-238
title: 정렬·BEST 백엔드 — 순공감/최신/과거 + BEST 댓글
state: done
owner: backend-impl
state_note: |
  구현 완료·검증(2026-08-06): BEST 엔드포인트(GET .../comments/best·순공감 (like-dislike) DESC·min-likes 임계·max-count·tombstone 제외·myReaction 재사용)·BoardCommentBestProperties env화(기본3·3)·LIKES 정렬 확정(FC-207 골격 활용)·SecurityConfig best GET permitAll. CommentBestApiIntegrationTest 5 + LIKES 정렬 + 회귀 그린. spotless·checkstyle 통과. 미커밋.
depends_on: [FC-208]
blocks: [FC-212]
gate: null
review_status: passed
contract_ref: api-contract.md v1.24 §6.3, board-domain-spec.md v1.1 §13
artifacts: []
---
## 목표
댓글 정렬(최신순 기본·과거순·순공감순) + BEST 댓글 선정.

## DoD
1. **정렬** `GET /posts/{id}/comments?sort=LATEST|OLDEST|LIKES` — 기본 LATEST(id DESC)·OLDEST(id ASC)·LIKES(공감순). 인덱스 커버((post_id,parent_comment_id,id) 정·역, (post_id,parent_comment_id,like_count)).
2. **BEST 댓글** `GET /posts/{id}/comments/best` — 공감 상위((like-dislike) DESC·임계 board.comment.best.*). 목록 기본 정렬과 분리.
3. env화 임계값(@ConfigurationProperties). spotless·checkstyle·통합테스트(정렬 3종·BEST 선정·임계).

## 근거인용
board-domain-spec v1.1 §13.3, api-contract v1.24 §6.3. 게이트2 기본정렬=LATEST 확정.

## 파생 경위: 게이트1 T4(정렬·BEST 백엔드).
