---
id: FC-332
type: task
epic: EPIC-CHAT
derived_from: FC-329
jira_key: KAN-376
title: outbox retention 인덱스 계약 보정
state: done
owner: architect
depends_on: [FC-326]
blocks: [FC-333]
gate: null
review_status: passed
contract_ref: docs/spec/chat-domain-spec.md §10.2
artifacts:
  - docs/spec/chat-domain-spec.md
  - docs/spec/erd.md
---
## 목표 / DoD / 검증
- `(created_at, id)` retention 인덱스와 `(occurred_at, id)` pipeline 관측 인덱스의 역할을 구분한다.
- 보존 기간·CDC safe checkpoint·binlog guard와 외부 API 계약은 변경하지 않는다.
- V27 가법 migration과 운영 DDL·롤백·검증 기준을 확정한다.

## 파생 경위
- FC-329 150/s 재검증 중 비인덱스 outbox purge와 지연 급증이 같은 시각에 관측되어 게이트2 승인 후 파생했다.
