---
id: FC-200
type: task
epic: EPIC-BOARD
derived_from: FC-196
jira_key: KAN-228
title: 이미지 업로드 — MinIO/StoragePort·presigned 서빙
state: done
owner: backend-impl
state_note: |
  구현 완료·검증(2026-08-06): StoragePort(infra.storage)+S3StoragePort 단일구현(MinIO/S3)·BoardImageStorageProperties/Config(버킷 idempotent)·PostImage 엔티티/repo·PostImageService(upload 매직바이트 sniff·bind 최종집합·render presigned)·BoardImageController(POST /board-images)·PostImageErrorCode(IMAGE_001/002) + build.gradle AWS SDK v2·docker-compose minio·application(-local).yml env화 + PostService 이미지 바인딩·썸네일 가법 배선. 실 MinIO Testcontainer 왕복·MySQL 통합 9케이스·단위·회귀 전부 그린(스킵 없음). spotless·checkstyle 0위반. 미커밋.
  ★유의: (1)ErrorCode 클래스명 PostImageErrorCode vs spec§10 BoardImageErrorCode 상충(코드값 IMAGE_001/002는 계약 일치)—reviewer 확인. (2)바인딩 검증실패=COMMON_001(400, 전용코드 미발명). (3)운영 BOARD_IMAGE_ENDPOINT env 필수. (4)고아 sweeper·post 삭제 물리정리는 범위 밖(delete 구현·미배선).
depends_on: [FC-197]
blocks: [FC-204]
gate: null
review_status: passed
contract_ref: docs/spec/board-domain-spec.md v1.0 §7, api-contract.md v1.23 §6.4·§6.5
artifacts: []
---
## 목표
게시글 이미지 첨부 — 오브젝트 스토리지(MinIO 로컬 / S3 운영) 업로드 + 비공개 버킷 + presigned GET 서빙. 2단계 업로드(고아→post 바인딩).

## DoD
1. **로컬 인프라** — docker-compose.local.yml에 minio 서비스(포트 9000/9001·env·healthcheck·볼륨). 버킷 `finalcall-board-images`(비공개) idempotent 생성.
2. **StoragePort** — AWS SDK v2 S3Client 단일 구현(endpoint 설정으로 MinIO/S3 커버, 로컬 디스크 구현 없음). object key `board/{yyyy}/{MM}/{imagePublicId}.{ext}`.
3. **설정 env화**(config-env-ification-preference 준수) — `board.image.storage.{endpoint,region,bucket,access-key,secret-key,presign-ttl,path-style}`. @ConfigurationProperties+@Validated. 로컬 `${ENV:기본값}`·운영 fail-fast.
4. **API**:
   - `POST /api/v1/board-images`(multipart, 인증) — 업로드 → imagePublicId + presigned GET url(TTL) 반환. MIME/size 검증.
   - PostImage 엔티티(public_id·post_id nullable·uploader_id·storage_key·MIME/size·sort_order).
   - **`/raw` 프록시 엔드포인트 없음**(presigned 직접 서빙).
5. 조회 시 presigned GET 재생성(응답마다 갱신·클라 저장 금지·본문 URL 미저장).
6. 빌드·spotless·checkstyle 통과.

## 근거인용
board-domain-spec v1.0 §7.6·§7.7, api-contract v1.23 §6.4·§6.5. 게이트2(a) 승인=전용 오브젝트 스토리지.

## 파생 경위: FC-196 계약 확정 후 구현 팬아웃 T5.
