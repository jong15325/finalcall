---
id: FC-055
type: task
epic: EPIC-FE-ECME
derived_from: null
jira_key: KAN-66
title: Ecme 템플릿 기반 이식 — 빌드·dev 통과 + 프록시 토큰 복구 + 레이아웃 후보 제시
state: done
owner: frontend-impl
depends_on: []
blocks: [FC-056, FC-057, FC-058, FC-059, FC-060, FC-061, FC-062]
gate: null
review_status: passed
contract_ref: docs/spec/api-contract.md v1.10, docs/spec/references/game-item-skill-format.md
artifacts: []
---
## 목표
Ecme `starter`를 기반으로 `frontend/`를 **재구성**하고, **`npm run dev`·`build`가 통과**하며
**백엔드에 붙는** 상태까지 만든다. 화면 이식은 후속 티켓이다.

템플릿 경로: `D:\web_template\Ecme - React Tailwind Admin Template\TypeScript\starter`
(`demo/`는 참조용 — 컴포넌트 사용례·도메인 화면이 거기 있다)

## ★★ 1순위 — `X-Gateway-Token` 프록시를 반드시 복구하라
기존 `frontend/vite.config.ts`에 dev 프록시(`/api` → `:8080`) + **`X-Gateway-Token` 헤더 주입**이
있다. **이게 없으면 화면 전체가 `GATEWAY_403` 에러 상태로만 보인다** — 이 프로젝트에서 **실제로
한 번 그렇게 나갔고** 한참 뒤에야 발각됐다(HANDOVER 기록).
→ **기존 `vite.config.ts`를 먼저 읽고 그대로 옮겨라.** 옮겼음을 반환에 명시하라.

## ★ 이관 대장을 먼저 만들어라 (삭제 전에)
기존 `frontend/src/**`를 지우기 전에 **무엇을 살려야 하는지 목록화**하라. 코드가 아니라
**규칙**이 자산이다. 최소한 아래는 어디에 무엇이 있었는지 반환에 적어라:
- **SEC-007** — 로그인 실패가 **필드를 강조하면 회원 열거 힌트**가 된다(`LoginPage.test.tsx`가 고정)
- **미등록 코드 중립 폴백** + **코드 집합 크기 하드코딩 금지**(계약 v1.10 §3.3.1 클라 의무)
- **`kind`의 `subGroup` 종속** — 필터에서 단독 축 금지
- **아트 경로 규칙**(`features/item/lib/itemArt.ts`) + **레벨 축**
  (`docs/spec/references/game-item-skill-format.md §2` — **0-based 함정**)
- **아트 동기화 스크립트**(`scripts/sync-game-art.mjs`) + `public/art` gitignore
- 테스트 126건이 고정한 규칙들

기존 코드는 **git에 커밋·push돼 있어 복구 가능**하다(`origin/master` `05b5e1b`). 그래도
**대장 없이 지우지 마라.**

## 스택 판단
| | 기존 | 템플릿 | 판단 |
|---|---|---|---|
| React | 18.3 | 19.0 | 템플릿 |
| Tailwind | 3.4 | **4.0** | 템플릿(설정 방식이 CSS-first로 다르다) |
| Vite | 5.4 | 6.3 | 템플릿 |
| 라우터 | `react-router-dom` 6 | `react-router` 7 | 템플릿 |
| **서버 상태** | `@tanstack/react-query` | zustand + axios | **★ 네가 판단하라** |
| 테스트 | vitest + testing-library | (없음) | **유지 권고** |

**서버 상태 판단 근거를 대라.** 우리 API 계층은 react-query 기반이고 커서 페이징·캐시 키 분리 등이
그 위에 있다. 템플릿 관례를 따를지 react-query를 얹을지는 **후속 티켓 전체에 영향**을 준다.
**vitest는 유지**하라 — 126건이 고정한 규칙을 다시 세울 때 필요하다.

## ★ 레이아웃 — 후보와 근거를 제시하라 (확정하지 마라)
템플릿은 **관리자 템플릿**이라 레이아웃이 전부 사이드바 기반이다
(`collapsibleSide`·`stackedSide`·`topBarClassic`·`framelessSide`·`contentOverlay`·`blank`).
**우리는 공개 커머스**라 IA가 다르다.

→ **후보 2~3개를 근거와 함께 제시**하라. 예: `topBarClassic` 채택 / `blank` + 자체 셸 / 혼합.
**사용자가 판정한다.** 이 티켓에서 셸을 완성하지 마라 — FC-057 소관이다.
데스크톱뿐 아니라 **모바일 IA**도 함께 판단하라(지속 원칙: 반응형은 배치 변경이 아니다).

## DoD
- `frontend/`가 템플릿 기반으로 재구성되고 **`npm run dev`·`npm run build`·`typecheck`·`lint` 통과**.
- **`X-Gateway-Token` 프록시 복구 확인.**
- 라우팅 골격이 우리 경로(`ROUTES`)와 맞물린다(화면 내용은 비어 있어도 된다).
- **vitest 동작**(테스트 0건이어도 러너는 서야 한다).
- **아트 동기화 스크립트·`public/art` gitignore 유지.**
- 이관 대장 + 레이아웃 후보 제시.

## 제약
- **`backend/**` 수정 금지**(동결). **`docs/**` 수정 금지**(발견은 보고만).
- **템플릿 원본 디렉터리를 수정하지 마라** — 사용자 구매 자산이다. 복사해서 쓴다.
- **화면을 만들지 마라.** 이 티켓은 기반이다. 화면은 FC-057~062.
- 기존 코드를 지우기 전 **이관 대장 필수**.
- 코드 스타일: 템플릿 관례를 따른다(기존 CLAUDE.md §7은 백엔드 checkstyle 기준이라 프론트엔
  prettier·eslint가 적용된다 — 템플릿 설정을 확인하고 보고하라).

## 비고
`design-system.md` v0.6.1은 **템플릿 도입으로 지위가 바뀐다**(에픽 문서 참조). 이 티켓에서
문서를 고치지 말고, **무효가 되는 항목을 보고**하라 — 정본 개정은 총괄이 게이트2로 처리한다.

## 결과 (2026-07-19)
커밋 `ce1f9ef` — **903파일 / +150,511**. 프론트 통째 교체. **백엔드·문서 무접촉**(총괄 확인).
build 4.88s · typecheck · lint 0 · vitest 5/5 · dev 프록시 실측 통과.

### ★ X-Gateway-Token — 설정이 아니라 "헤더 도달"을 검증했다
에코 upstream을 :8080에 띄우고 vite dev(:5199) 경유로 실측:
```
upstream saw path: /api/v1/auctions
X-Gateway-Token: finalcall-local-gateway-shared-secret-change-me
```
> **템플릿 원본은 `:3000` + 헤더 없음이었다. 그대로 덮어썼다면 그 사고를 정확히 재현했다.**

### 서버 상태 = **react-query 유지** (근거 4)
1. **커서 페이징에 대응물이 없다** — `useInfiniteQuery`의 `getNextPageParam`이 계약 커서 응답과 1:1.
2. **캐시 키 분리가 무효화 전략이다** — `auctionKeys.preview`를 `list`와 나눈 건 홈 프리뷰가 목록
   필터 변경에 딸려 무효화되지 않게 하려는 것. 키 계층이 있어야 성립한다.
3. **입찰 후 무효화가 이 도메인의 핵심** — 낙관적 업데이트 + 롤백 필요.
4. **비용 0에 가깝다** — 템플릿 37종은 전부 props 기반이라 데이터 계층과 무관.

**단 HTTP 클라이언트는 하나여야 한다** — 우리 `apiClient`(fetch, envelope 언랩 + refresh
single-flight)를 단일 전송로로 쓰고 **템플릿 axios 서비스를 FC-056에서 걷어낸다**(현재 공존).

### 템플릿 구조적 불일치 2건 — 1건 수정
- `Layouts.tsx`가 `authenticated`로 셸을 갈라 **비로그인은 셸 없는 화면**을 본다. 커머스에선
  손님이 첫 화면부터 헤더·내비가 필요하다. → **셸은 FC-057 소관이라 미수정**.
- 라우트 public/protected 이분법("비로그인은 볼 게 없다" 전제) → **`sharedRoutes` 3버킷으로 수정**.

### 스타일 규약 변경
프론트는 CLAUDE.md §7(백엔드 checkstyle)이 아니라 **템플릿 prettier**를 따른다 —
`singleQuote` · **`semi: false`** · **`tabWidth: 4`** + eslint flat config.
기존 프론트(세미콜론 有, 2스페이스)와 다르다. **템플릿 관례 채택.**

## ★ 이관 대장 (원본 `origin/master` `05b5e1b`)
| 규칙 | 원위치 | 복원 예정 |
|---|---|---|
| X-Gateway-Token 프록시 | `vite.config.ts` | ✅ **완료** |
| **SEC-007** — 로그인 실패는 폼 배너만, 필드 강조 금지 | `LoginPage.test.tsx` 단언 | FC-060 |
| 대비 규칙 — **가입은 반대**(`AUTH_001`·`AUTH_002`는 필드 단위) | `SignupPage.test.tsx` | FC-060 |
| 미등록 코드 중립 폴백 + 집합 크기 하드코딩 금지 | `element.ts`·`itemCode.ts`·`codeFallback.test.tsx` | FC-062 |
| **`kind`는 `subGroup` 종속** — 단독 표 export 금지, `subGroup` 동반 함수만 | `itemCode.ts` | FC-059/062 |
| `typeCode` 분해(인벤토리 summary엔 typeCode만 온다) | `decodeTypeCode` | FC-061 |
| **아트 경로 + 0-based 함정** — 우리 `level`은 표시 레벨, **보정 금지** | `itemArt.ts`+test | FC-062 |
| 아트 둘 다 세로형(l 50×93·s 26×28), **정수배 확대만** | `ART_BASE_SIZE` | FC-062 |
| 마법 `kind` 2종, `magic.png` 2:1 공유 | `itemCode.ts` | FC-062 |
| **429 재시도 정책** — 4xx 무재시도, `GATEWAY_429`만 `Retry-After` 존중 | `queryClient.ts` | FC-056 |
| **refresh single-flight** — 동시 401 다발을 1회로 합류 | `api/client.ts` | FC-056 |
| 테스트 setup 스토어 초기화 — 없으면 **"혼자 통과, 전체 실패"** 순서 의존 | `test/setup.ts` | FC-056 |
| 아트 동기화 + `public/art` 미추적 | `sync-game-art.mjs` | ✅ 유지 |
| `ROUTES`/`buildPath` | `routes/paths.ts` | ✅ 이관 |

**테스트 126건 전체 이름을 캡처**해뒀다 — 카운트다운 임계값, "0분 남음" SR 문장,
**시작가↔현재가 위장 방지**, 비활성 CTA 사유 필수, **커서 페이지엔 총건수 없음("N개 이상")** 등.

## `design-system.md` v0.6.1 — 무효/유지 (게이트2 대상)
**무효(템플릿에 양보)**: §2 색 팔레트 전반 · **타이포/`theme.fontSize`**(FC-047의 통째 교체는
이관되지 않는다 — Tailwind 4는 설정 방식 자체가 다르다) · 간격·라운드·섀도 ·
§5 컴포넌트 스펙 대부분 · **§5.12 골드포스 아웃라인**(지적 3·5의 원인 → FC-062 재검토, **태그 삭제 확정**)

**유지(우리 고유, 템플릿이 모름)**: §1.2 **Game-Color Containment** · §2.7 **아이템 아트 규격** ·
**등급 배지 금지**(D-073) · §5.9 **카운트다운 임계값**(도메인 규칙이지 컴포넌트가 아니다) ·
**색 단독 전달 금지**(속성은 도트+라벨 병기)

**판정 필요**: `.slot-glow-*`·`element.*` 토큰이 Tailwind 3 config 기반이라 **Tailwind 4 `@theme`로
재작성 필요** — 값은 유효, 표현만 이관(FC-062 소관).

## 발견 — 미처리
1. **404가 홈으로 조용히 리다이렉트**된다. 템플릿은 보호구역 안에 catch-all을 둬 비로그인 오타
   URL이 **로그인 화면**으로 갔고 그건 고쳤으나, 지금도 주소가 갈려 오타를 알 수 없다.
   **전용 404 화면이 옳다**(화면 제작이라 범위 밖).
2. **★ `accessTokenPersistStrategy`** — 템플릿 기본 쿠키, 일단 `localStorage`. refresh 토큰
   회전(계약 §2)과 얽힌 **보안 결정** → FC-056 재판정(**게이트2 후보**).
3. **템플릿 의존성이 무겁다** — firebase·fullcalendar·gantt·tiptap·react-simple-maps 등 미사용
   다수. 초기 청크 411KB. 임포트 연쇄 위험으로 미정리.
4. **`UserProfileDropdown` 청크 245KB** — firebase가 딸려 들어온 것으로 보임. FC-057 확인 권장.
5. **`enableMock`을 껐다** — 켜져 있으면 **실백엔드 연결 여부가 화면상 구분되지 않는다.**
   1번 GATEWAY_403 사고와 같은 종류의 함정.

## ★ 레이아웃 확정 — **B (blank + 자체 셸)** (사용자 판정 2026-07-19)
시각 비교본으로 3안을 제시하고 사용자가 B를 선택했다.

> **레이아웃은 우리가 짜고, 컴포넌트 37종만 정본으로 쓴다.**

- **데스크톱**: 상단 브랜드+검색+계정 / 2행 내비 / 목록은 **좌측 필터 레일** + 카드 격자 /
  상세는 아트–정보 2단 + **하단 고정 입찰 바**
- **모바일**: 상단 브랜드+검색만 / **하단 탭바**(홈·경매·판매·인벤·MY) /
  필터는 **바텀시트**(레일을 접는 게 아니라 **다른 컴포넌트로 교체**) / 입찰은 하단 고정 바

**A 기각**: 사이드바 IA를 상단으로 접은 형태라 카테고리 축이 깊어지면 드롭다운이 비대해지고,
모바일이 햄버거→드로어(**관리자 패턴, 커머스엔 약함**).
**C 보류**: `/me`·`/admin` 화면이 아직 거의 없어 이르다. **B로 가고 그쪽이 두꺼워지면 C로 확장.**

**판단의 실제 내용**: 세 안 모두 컴포넌트 37종을 쓰므로 **통일성은 어느 쪽이든 동일하게 얻는다.**
차이는 **IA가 우리 제품에 맞느냐**에서만 났다.

→ FC-057 셸 작업이 이 방향으로 착수한다. `Layouts.tsx`의 `authenticated` 셸 분기
(비로그인이 셸 없는 화면을 보는 문제)도 FC-057에서 해소한다.
