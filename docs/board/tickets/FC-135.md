---
id: FC-135
type: task
epic: EPIC-EMAIL-TEMPLATE
derived_from: FC-129
jira_key: KAN-151
title: T3 — EmailTemplateService(렌더링)·RenderedEmail·MailErrorCode
state: todo
owner: backend-impl
depends_on: [FC-134]
blocks: [FC-132]
gate: null
review_status: pending
contract_ref: docs/spec/email-template-spec.md §5·§5.4·§6
artifacts: []
---
## 목표
템플릿 조회+변수 치환 렌더링 서비스와 렌더 결과 VO·에러코드를 신설한다.

## DoD
- `service/EmailTemplateService`: `render(EmailTemplateKey key, Map<String,Object> vars)` → `RenderedEmail`. 조회(비활성/없음→`MAIL_001`) + **단순 `{{name}}` 치환**(템플릿 엔진 미도입, spec §5.1) + 변수 검증(§5.2): 필수 변수 누락·치환 후 잔여 `{{...}}` 남으면 `MAIL_002` fail-fast, 여분 변수는 무시.
- `service/RenderedEmail`(record VO — 영속·직렬화 아님, §9.10 service 잔류): `(String subject, String body, boolean html)`. `html`은 template `content_type`에서 파생.
- `MailErrorCode`(`com.finalcall.common.exception`, `ErrorCode` 구현): `MAIL_001`(500 템플릿 없음/비활성)·`MAIL_002`(500 변수 누락/잔여 placeholder). **api-contract §5 미등재(게이트2 결정)** — 내부 500, GlobalExceptionHandler 일반 처리.
- **보안 경계(spec §6)**: 실제 코드는 render 인자로만 주입, RenderedEmail은 transient(저장·로그 금지, LoggingEmailSender local 로그는 예외).
- 단위 테스트: 정상 렌더·필수변수 누락(MAIL_002)·잔여 placeholder(MAIL_002)·템플릿 없음/비활성(MAIL_001)·여분변수 무시.

## 근거인용
spec §5(렌더링·검증)·§5.4(MailErrorCode)·§6(코드 미영속 경계).

## 파생 경위
EPIC-EMAIL-TEMPLATE 분해 T3. FC-134 선행. blocks EPIC-EMAIL-VERIFY FC-132(소비).
