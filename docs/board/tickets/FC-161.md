---
id: FC-161
type: task
epic: EPIC-NICKNAME-UX
derived_from: FC-160
jira_key: KAN-181
title: 닉네임 가용성 조회 API + 소셜 항상-꼬리표 구현
state: done
owner: backend-impl
depends_on: [FC-160]
blocks: [FC-163]
gate: null
review_status: passed
contract_ref: FC-160 확정 계약
artifacts: []
---
## 목표
FC-160 계약대로 백엔드를 구현한다.

## DoD
1. **가용성 조회 엔드포인트**: FC-160 확정 계약대로 컨트롤러·서비스 구현. 기존 `existsByNicknameAndIsDeletedFalse` 재사용, 비로그인 접근 허용(SecurityConfig permitAll 경로 추가). 형식/길이 검증은 기존 닉네임 규칙 재사용.
2. **소셜 항상-꼬리표**: `SocialAccountRegistrar.resolveUniqueNickname`을 **provider명 + 항상 랜덤 꼬리표** 방식으로 변경(현: 충돌 시에만 접미사). 베이스명 정규화·30자 제한·충돌 재시도 유지. 신원 유일성은 `(provider, provider_user_id)` UK 담당(무영향).
3. **테스트**: 가용성 조회(가능/중복/형식오류) + 소셜 닉네임 부여(항상 꼬리표·충돌 재시도) 슬라이스/단위 테스트. 기존 nickname UK 관련 테스트 회귀 없음.
4. 스타일 규약(섹션 7): `./gradlew :backend:spotlessApply` + checkstyle 그린. 커밋 금지.

## 근거인용
FC-160 계약. EPIC-NICKNAME-UX. 유니크 제약·중복검사 로직 무변경(nickname_active UK 유지).

## 구현 결과 (2026-07-30)
- **추가**: `auth/dto/NicknameAvailabilityResponse.java`(`{available}`), `auth/dto/NicknameAvailabilityRequest.java`(쿼리 바인딩 record, `@NotBlank @Size(max=30)`), `integration/NicknameAvailabilityIntegrationTest.java`.
- **변경**: `AuthController`(`GET /nickname/availability`), `AuthService.isNicknameAvailable`, `SecurityConfig`(permitAll 정확경로 추가), `SocialAccountRegistrar`(항상-꼬리표), 테스트 3건 개정.
- **검증 방식**: 쿼리 → `@Valid` record 바인딩 → 검증실패 시 `MethodArgumentNotValidException` → 기존 `GlobalExceptionHandler` `COMMON_004` 400+errors[](signup 동일). `@RequestParam` 메서드검증(500 위험) 회피.
- **소셜**: `normalizeStem` trim·fallback(`user`)·스템 25자 절단, 루프에서 **항상** `_XXXX`(4자) 부착, 충돌 시 재시도(10회). 총 ≤30자. 유니크 제약·검사 무변경.
- **검증**: spotless·checkstyle(위반0)·단위·통합(Testcontainers) 전부 그린.

## 재작업 (FC-163 리뷰 CHANGES-REQUESTED, MAJOR-1)
- **MAJOR-1**: 신설 `/api/v1/auth/nickname/availability`가 게이트웨이 `auth-rate-limited` 라우트 predicate(정확경로 login·signup·refresh)에 누락 → rate limiter 없는 catch-all로 흘러 무제한. 계약 §2 v1.17의 "게이트웨이 rate limit 열거완화"와 배선 불일치.
- **조치(옵션 a)**: 게이트웨이 `application.yml`의 `auth-rate-limited` Path predicate에 해당 경로 추가(계약 이행 = 새 결정 아님, 게이트2 불요).
- **관찰(스코프 밖)**: `/api/v1/auth/oauth/**`(FC-156)도 같은 predicate에서 누락된 선존 gap — 별도 후속(총괄이 사용자 상신).

## 파생 경위: FC-160 계약 확정 후 백엔드 구현.
