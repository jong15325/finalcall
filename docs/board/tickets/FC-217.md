---
id: FC-217
type: task
epic: null
derived_from: null
jira_key: KAN-245
title: BEST 댓글 기능 제거 (프론트 UI + 백엔드 엔드포인트·spec)
state: done
owner: backend-impl
review_status: passed
state_note: |
  완료·검증(2026-08-07): 백엔드 BEST 제거(controller best·getBestComments·findBestRootComments·BoardCommentBestProperties·BestCommentsResponse·CommentBestApiIntegrationTest·SecurityConfig best·application.yml best) + spec(api-contract v1.25·board-spec v1.2 BEST 절 삭제, 정렬 LIKES 유지). 프론트 BEST(getBestComments·useBestComments·CommentBestSection·isBest·patchReactionInCaches 분기) 제거. grep 잔존 0. 댓글/Reaction/Threading/Board/Post 테스트 그린. 미커밋.
depends_on: []
blocks: []
gate: null
review_status: pending
contract_ref: api-contract.md §6.3, board-domain-spec.md §13
artifacts: []
---
## 목표
BEST 댓글 기능을 전면 제거한다(사용자 요청). 정렬 순공감순(sort=LIKES)은 **별개 기능이라 유지** — BEST 엔드포인트·섹션만 제거.

## DoD
### 백엔드 (backend-impl)
1. 제거: `CommentController` best 엔드포인트·`CommentService.getBestComments`·`CommentRepository.findBestRootComments`·`config/BoardCommentBestProperties`·`dto/BestCommentsResponse`·`CommentBestApiIntegrationTest`·`SecurityConfig` best permitAll 라인·`application.yml` `board.comment.best.*`.
2. **spec 갱신**: `api-contract.md` §6.3에서 BEST 엔드포인트 절 제거, `board-domain-spec.md` §13에서 BEST 규칙 제거(정렬 LIKES는 유지). 버전로그에 "BEST 제거" 기록.
3. LIKES 정렬·myReaction·tombstone 등 나머지 불변. 죽은 참조·import 잔존 금지. 컴파일·기존 테스트 그린.

### 프론트 (frontend-impl — 별도 위임)
- CommentSection BEST 섹션·`getBestComments`·`useBestComments`·`commentKeys.best`·`patchReactionInCaches` BEST 처리·CommentItem isBest 배지 제거. 정렬 UI 유지.

## 근거
사용자 신고(2026-08-07): "댓글 베스트 댓글 기능 제거". 단순 제거(디자인 게이트 불요).

## 파생 경위: 사용자 직접 신고.
