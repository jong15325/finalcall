---
id: FC-028
type: task
epic: EPIC-AUCTION
derived_from: FC-025
jira_key: KAN-34
title: 판매자 취소 API (POST /auctions/{id}/cancel) + 에스크로 해제
state: done
owner: backend-impl
depends_on: [FC-027]
blocks: [FC-029]
gate: null
review_status: passed
contract_ref: docs/spec/api-contract.md §3.1(취소), docs/spec/auction-domain-spec.md §4.2
artifacts: []
---
## 목표
- 판매자 본인의 경매 취소를 구현하고, 출품 아이템의 에스크로(LISTED)를 인벤토리로 복귀시킨다.

## DoD
- **취소 API** `POST /api/v1/auctions/{auctionPublicId}/cancel`(인증 필요, 판매자 본인=SecurityContext): **SCHEDULED|ACTIVE & 입찰0(`highest_bidder_id IS NULL`)**일 때만 CANCELLED로 CAS 전이(게이트2 e·G6). 응답 200 `{status}`.
- **에스크로 해제**(신규 경로): item LISTED→INVENTORY 복귀, 만실(96칸) 시 TEMP. `domain/item/`에 재사용 public 메서드 추가(InventoryService 슬롯탐색·capacity·flush매핑 재사용). location XOR 불변식 유지.
- **에러**: AUCTION_007(입찰 존재로 취소 불가, 409)·AUCTION_006(이미 종료, 409)·판매자 아님(403, 주체=SecurityContext, 타인 취소 차단·IDOR).
- **테스트**: 취소 happy(SCHEDULED·ACTIVE)·입찰존재 거부·종료된경매 거부·타인 취소 403·에스크로 복귀(정규/만실 temp). spotless·checkstyle 통과.

## 근거(인용)
> 계약 §3.1(v1.7) "cancel … SCHEDULED|ACTIVE & 입찰0일 때만 CANCELLED. 아이템 에스크로 해제(인벤토리 복귀, 만실 시 임시보관)". auction-domain-spec 게이트2 (e)(G6). 도메인 인가 주체=SecurityContext.

## 파생 경위: FC-025 슬라이싱에서 취소+에스크로 해제 계층으로 분리.
