---
id: FC-121
type: task
epic: EPIC-RESTRUCTURE
derived_from: null
jira_key: KAN-132
title: Phase 2 — feature 단위 순차 이전 (10개 도메인)
state: done
owner: backend-impl
depends_on: [FC-120]
blocks: [FC-122, FC-117, FC-118]
gate: null
review_status: passed
contract_ref: docs/common/proposals/layer-restructure-proposal-v0.1.md §5
artifacts: []
---
## 목표
`api/<f>` + `domain/<f>`를 `com.finalcall.<f>/{controller,service,repository,entity,dto,...}`로 도메인 단위 이전한다. **로직 0줄 변경**(패키지 선언·import만).

## DoD
- 이전 순서(저위험 leaf부터): sample → notice → member → currency → item → auction → bid → search → **settlement(구 order+purchase 통합)** → shop.
- 각 도메인 = ①api/domain 파일을 `<f>/` 트리로 이동 + 내부 계층 하위패키지 배치 ②package 선언·import 일괄 치환 ③해당 테스트 이동 ④`:backend:test`(ArchUnit·슬라이스 포함) green ⑤`:backend:spotlessApply` → **도메인당 원자 커밋 1개**(사용자 승인).
- settlement은 `api/order`·`api/purchase`·`domain/settlement` 3트리를 `settlement/` 한 트리로 통합.
- 모든 이전 후 전체 `:backend:test` green.

## 근거인용
proposal §5 Phase 2, §6 파급(≈217+75), §8 완화책(원자 커밋·회귀 그물).

## 파생 경위
EPIC-RESTRUCTURE Phase 2. 완료 시 member 이전으로 FC-117·118(이메일) 경로 확정 가능 → unblock.
