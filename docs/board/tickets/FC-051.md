---
id: FC-051
type: task
epic: EPIC-FE-REDESIGN
derived_from: FC-038
jira_key: null
title: 프론트 테스트 러너 도입 — vitest + testing-library
state: todo
owner: frontend-impl
depends_on: [FC-047]
blocks: [FC-048, FC-049, FC-050]
gate: null
review_status: pending
contract_ref: docs/board/reviews/FC-038-review.md (m11)
artifacts: []
---
## 목표
프론트에 회귀 테스트 기반을 만든다. **현재 프론트 테스트가 0건이다.**

## 왜 지금인가 (reviewer 승격 권고)
`reviews/FC-038-review.md` m11이 **백로그가 아니라 실제 티켓으로 승격**을 권고했다:
> vitest·testing-library 미도입으로 회귀 테스트 0건. 이 에픽 결함 2건(M-1 "코드 null" ·
> m6 행 점프)은 **렌더 테스트 1개면 잡혔을 유형**이고 **M-1은 리뷰까지 살아남았다.**

**에픽 앞쪽에 두는 이유**: 먼저 깔아야 이후 티켓(FC-048~050)이 테스트를 쓰면서 진행한다.
뒤에 두면 또 안 쓰게 된다.

## DoD
- vitest + @testing-library/react + jsdom 도입, `npm run test` 동작.
- **스모크 테스트 최소 세트** — 렌더 깨짐을 잡는 수준으로 충분하다:
  - 기존 실구현 화면(로그인·회원가입·프로필·경매목록·경매상세)이 **크래시 없이 렌더**된다
  - **M-1 재현 방어**: 코드값이 `null`·미등록일 때 중립 폴백이 뜨고 크래시하지 않는다
    (계약 v1.10 §3.3.1의 클라이언트 의무 — "미등록 코드는 중립 표기 폴백 + 코드 집합 크기를
    가정한 하드코딩 금지")
- CI 연동 여부는 이 티켓에서 판단해 보고(현재 PR 워크플로우 미도입 상태).
- `.run/`에 IntelliJ 실행 구성 추가 검토(기존 4종 dev·build·typecheck·lint와 정합).

## 제약
- **테스트 기반 구축까지만.** 커버리지를 채우려 들지 마라 — 화면 테스트는 각 구현 티켓이 동반한다.
- `backend/**`·`docs/**` 수정 금지.
