---
id: FC-108
type: task
epic: EPIC-SEARCH
derived_from: FC-106
jira_key: KAN-122
title: 검색바·검색 결과 UI (마켓+경매) (frontend)
state: review
owner: frontend-impl
depends_on: [FC-106]
blocks: [FC-109]
gate: null
review_status: passed
contract_ref: docs/spec/search-spec.md v0.3 (FC-106 확정)
artifacts:
  - frontend/src/components/common/ListSearchBar.tsx
  - frontend/src/features/auction/lib/auctionFilters.ts
  - frontend/src/features/shop/lib/shopFilters.ts
  - frontend/src/pages/AuctionListPage.tsx
  - frontend/src/pages/MarketPage.tsx
---
## 완료 (frontend-impl · 2026-07-22 · 커밋 fcf7e03, push 안 함)
- ListSearchBar(디바운스 300ms·URL 정본·C3 클라힌트 2~64자·접근성) 신규. 마켓·경매 필터 카드 상단 배선(ShopFilters "준비 중" 자리 실기능화). q 있을 때만 '관련도순' 노출.
- auctionFilters/shopFilters에 q 축·normalizeQ·normalizeSort(relevance q종속)·URL 왕복. api q 파라미터. 결과수 aria-live·빈결과 "검색 지우기" 동선.
- **C2 방어**: 무q relevance 조합 UI 미생성 + normalizeSort 강등(URL 직접입력·뒤로가기 방어). 서버 400/무시 어느 규약이든 안전(클라가 안 보냄).
- **검증**: typecheck·eslint 0·prettier·vitest **552 passed**·build 성공. backend/src·docker 0 변경.
- 후속: q 실호출 통합검증은 백엔드 스택 기동 후(메인세션).
## 목표
FC-106 계약대로 마켓+경매 목록에 자유문(`q`) 검색 UI를 붙인다. 기존 필터·카드 그리드 재사용.

## DoD
- **검색바**: 마켓·경매 목록 상단에 자유문 검색 입력(q). URL searchParams 정본(기존 필터와 병존)·디바운스·빈결과/로딩 상태.
- **relevance 정렬**: q 있을 때 관련도 정렬 옵션(계약 규약대로). 기존 정렬(가격·마감 등)과 토글.
- lib/api·queries에 q 파라미터 추가(getShops·getAuctions 확장). 기존 필터·무한스크롤·카드 재사용.
- 반응형·접근성. npm build+eslint+vitest 그린.
- (참고: 상단 전역 검색바는 기존에 "안 씀" 규약 — 페이지 내 목록 검색으로. mockup §5.2 확인.)

## 근거인용
- search-spec v0.2(FC-106)·api-contract(q)·기존 AuctionListPage/MarketPage 필터·impeccable.

## 파생 경위: FC-106 확정 후 프론트. FC-107(backend)와 파일 무교차 병렬.
