---
id: FC-064
type: task
epic: EPIC-FE-ECME
derived_from: FC-059
jira_key: KAN-72
title: 경매 상세 + 입찰
state: review
owner: frontend-impl
depends_on: [FC-059]
blocks: []
gate: null
review_status: passed
contract_ref: docs/spec/api-contract.md v1.10 §3.1·§3.3·§4.1·§5
artifacts:
  - frontend/src/views/auctions/AuctionDetail.tsx (+ components 5종)
  - frontend/src/features/auction/lib/{auctionPhase,bidErrors}.ts
  - frontend/src/components/shared/{Sheet.tsx,sheet.css,SectionNotice.tsx}
---
## 목표
경매 상세 화면과 입찰 흐름을 만든다. 지금 `PagePlaceholder`다.

## 파생 경위: FC-059(경매 목록·상세)에서 상세를 분리했다 — 홈이 3번 재작업된 뒤라 한 티켓에 화면 둘을 묶으면 한쪽 반려가 다른 쪽까지 되돌린다.

---

## 계약 분석 결과 (architect · 2026-07-19 · 총괄 대조 완료)

### ✅ 계약이 이미 충족 — 그냥 쓰면 되는 것
| 항목 | 확인 |
|---|---|
| `GET /auctions/{id}` | 공개. 계약 §3.3 ↔ `AuctionDetailResponse` **17필드 1:1 일치** |
| `minNextBidAmount` | **서버 파생값이 내려온다.** `AuctionService`와 `BidService`가 **같은 `BidIncrementProperties`**를 쓰므로 "안내 금액으로 입찰했는데 거부"가 구조적으로 불가. **증분표를 클라에 복제하지 마라** |
| `POST /auctions/{id}/bids` | 요청 `{amount}`, 201 `{bidPublicId, amount, currentHighestAmount, endAt}`. **`endAt`은 소프트클로즈 연장 반영 후** 값이라 스티키 바 카운트다운을 응답으로 즉시 재동기화할 수 있다 |
| `GET /auctions/{id}/bids` | **존재하고 공개다.** offset 페이징(§1.3), `amount desc` 고정 |
| `BID_001~007` · `AUCTION_004` | 계약 §5 ↔ `BidErrorCode` **7값 전수 일치**. 에러 envelope에 `code`가 실려 화면 분기 가능 |

### ★ 벽 3건 — 전부 우회로가 있어 착수를 막지 않는다

**1. `isSeller` 부재 → 닉네임 비교로 파생한다 (계약 변경 불필요)**
`sellerNickname`이 **마스킹 없이 원문**으로 내려오고, 닉네임은 활성 회원 간 **유일**하다
(`uk_user_nickname_active`). `GET /me`의 `nickname`과 비교하면 된다.
탈퇴 닉네임 재사용이 유일한 오탐 경로인데 **진행 중 경매를 가진 판매자는 탈퇴가 차단**된다(`MEMBER_002`).
→ **★ 표시 제어일 뿐 인가가 아니다.** 입찰 버튼을 숨기되 서버 `BID_003`(403) 처리는 **반드시 구현**한다.

**2. 스킬 이름 → 지금은 코드 중립 표기로 폴백**
HANDOVER는 "사전 부재"로 적었으나 **정정**: `GET /items/{itemInstancePublicId}`가
`ItemSkillResponse{skillCode, name}`으로 **이름을 이미 내린다.** 진짜 벽은
**`AuctionItemView`에 `itemInstancePublicId`가 없어 링크가 끊긴 것**이다.
→ 계약 §3.3이 *"사전에 없는 코드는 중립 표기로 폴백"*을 이미 의무화했으므로
**`스킬 #{code}` + `skillPercent`(정수, 항상 존재)** 표기가 계약 준수다. **게이트2 대기 중 이대로 간다.**

**3. 즉시구매 → 화면에서 제외한다**
계약 §3.1에 `POST /auctions/{id}/purchase`가 **완전 명세돼 있으나 백엔드 미구현**이다
(`AuctionController`에 매핑 없음 · `AUCTION_005/009`가 enum에 부재 — EPIC-CLOSING 소유).
→ **버튼을 만들면 404다.** `buyNowPrice`는 **정보 표기로만** 쓴다.
→ ★ **미구현 경로 목록에 `/auctions/{id}/purchase`를 추가하라** — 계약에 있어서 구현된 줄 알기 쉽다
(FC-048이 `/shops`로 똑같이 당했다).

### ★ 4. 마감 판정은 클라가 시각으로 한다
`resultType`은 **항상 null**이고(EPIC-CLOSING 소유), `Auction.displayStatus()`는
**SCHEDULED→ACTIVE 승격만 하고 마감 강등을 안 한다**(마감 워커 없음).
→ **`endAt`이 지난 경매도 `status: ACTIVE`로 내려온다.** 서버 status를 그대로 믿으면
**종료된 경매에 활성 입찰 바가 뜬다.**
→ **`now >= endAt`이면 자체적으로 "마감"으로 렌더**하고 입찰 바를 비활성화한다.
이는 서버와 정합한다 — `BidService.isBiddable()`도 status가 아니라 **시각**으로 판정하고
이 경우 `BID_006`으로 거부한다.
→ 낙찰자는 `highestBidderMasked`로 표기하되 **"낙찰"이라 단정하지 마라**("최고 입찰"까지만).

### 계약↔구현 불일치 (로그아웃 바디 누락과 동종)
- **판매자 취소 403이 계약에 미기재** — `AuctionService.cancel()`이 주체≠판매자에 `AUCTION_001`(403)을
  던지는데 계약 §3.1 cancel은 `AUCTION_007`·`AUCTION_006`만 열거한다.
  → **상세에 판매자 취소 버튼을 넣는다면 `AUCTION_001` 처리를 명시 구현하라.**
- **입찰 이력 `size` 상한 100** — 서버가 조용히 정규화(`normalizeSize`)한다. 계약엔 없다.
  → **무한스크롤 페이지 크기를 100 이하로.**

---

## 화면 구조 (안2 — 사용자 확정 · FC-042 3차 재설계 확정안)
**디자인 게이트는 이 기존 승인으로 충족된다**(총괄 판정 2026-07-20). 안2는 `docs/ux/references/
auction-detail-references.md`(823줄) 조사 후 사용자가 확정한 **배치 구조** 결정이고, 현재 디자인
방침(*"레이아웃 등 배치 구조만 우리가 정한다"*)이 정확히 보존하는 범주라 EPIC-FE-REDESIGN
무효화에도 살아남는다. **무효인 것은 그때의 색·컴포넌트 생김새이지 배치가 아니다.**

- **데스크톱 = 안2**: 상단 가로 2열(아트 | 스펙) + 마감·입찰 **전폭** + 입찰 이력 **전폭**
  - 근거: **Steam 커뮤니티 마켓이 정확히 이 구조**이고 "작은 아트 + 조밀한 스펙 + 거래 데이터"라는
    우리와 동일 조건에서 그것을 택했다.
  - **안1(좌우 분할) 기각** — 50×93 아트로 우측 높이를 채우려면 과확대나 억지 여백뿐이고,
    높이를 맞추면 sticky 여유가 0이 되어 상시 노출 수단을 스스로 파괴한다.
- **카드 클릭 = 아트 확대 라이트박스.** 게임 카드정보 모달은 **폐기**됐다. 아트 확대 + 정체성
  한 줄만. **스펙·탭·거래 액션을 넣지 마라**(스펙은 페이지가 이미 전부 보여준다).
  근거: Baymard가 **스펙 중심 상품에 퀵뷰를 만들지 말라**고 명시.
- **모바일 = 고정 하단 바**(현재가·잔여시간·입찰 CTA 상시) + 입찰 시 **바텀시트**.
  근거: NN/g가 이커머스 상품 상세를 바텀시트로 만들지 말라 경고 → **"상세는 페이지 / 입찰은 시트"**.

### ★★ 반응형 철학 (사용자 지시 — 지속 원칙)
> **반응형은 배치 변경이 아니다. 웹은 웹, 모바일은 모바일에 맞게 짜여야 한다.**

데스크톱을 만들고 좁은 폭에서 열을 접는 방식은 **불충분**하다. **"좁아지면 1열로 접는다"류는
설계로 인정하지 않는다.**
**구체 요구**: **모바일에서 입찰 정보와 카드 정보가 함께 보여야 한다.** 데스크톱 좌우 분할을
모바일에서 위아래로 쌓기만 하면 카드를 보는 동안 입찰 정보가 화면 밖으로 밀려 요구가 깨진다.
경매는 *"지금 얼마·언제 끝나는가"*와 *"무엇을 사는가"*를 **동시에** 봐야 판단이 되는 화면이다.

★ **모바일 우선으로 짜라.** FC-058이 데스크톱 기준으로 짜서 320~1024가 전부 깨졌다.
참고: `demo/src/views/dashboards/EcommerceDashboard/`(루트 `overflow-x-hidden` · `grid-cols-1` 시작 ·
고정 폭은 큰 BP에서만). FC-059는 결과 컬럼 **`min-w-0`**으로 flex 자식 가로 넘침을 막았다.

## 재사용 — 카드를 새로 만들지 마라
`features/auction/components/`(`CountdownText`·`UrgencyBadge`·`interactionClass`) ·
`features/item/components/ItemArtSlot` + `itemOutline.css`(§5.12) · `features/item/lib/` ·
`lib/api/auctions.ts`·`lib/queries/auctions.ts`.
**부족하면 공유 컴포넌트를 확장**하고 그 변경이 **홈·목록에도 도달한다는 것을 의식**하라.
★ FC-059가 남긴 자리: **`specSnapshot`이 아직 미사용 — 상세가 그 자리다.**
★ `SectionNotice`가 `views/home/`에 있는데 목록도 쓴다 — **상세가 세 번째 소비처면 `components/shared`로 옮겨라.**

## 입찰 흐름
- 뮤테이션 성공 시 **응답의 `endAt`으로 카운트다운 재동기화**(소프트클로즈 연장)
- 상세·이력·목록 캐시 무효화 — **홈 프리뷰(`auctionKeys.preview`)까지 번지지 않게** 키를 확인하라
  (FC-059가 `preview`/`browse`로 분리해 둔 이유다)
- `BID_001~007`·`AUCTION_004` **코드별 문구 분기**. 특히 `BID_003`(자기 경매)·`BID_006`(마감)·
  **`BID_001`(최소 증분 미달 — `minNextBidAmount` 재안내)**
  > ★ **정정(2026-07-20)**: 이 줄이 처음에 *"`BID_002`(하한 미달)"*로 잘못 적혀 있었다.
  > 계약 §5·§3.1과 `BidErrorCode` 모두 **`BID_001` = 최소 증분 미달, `BID_002` = buyNowPrice 이상**이다.
  > frontend-impl이 **티켓이 아니라 계약을 정본으로 삼아** 올바르게 구현하고 오기를 보고했다.
  > 티켓은 계약의 사본이 아니라 **요약**이므로, 충돌하면 언제나 계약이 이긴다.

## DoD
- 상세가 `PagePlaceholder`가 아니다. 입찰·이력 동작.
- **모바일 우선** — 320~1440 폭 파손 0. **가로 스크롤 없음.**
- **마감된 경매에 입찰 바가 뜨지 않는다**(클라 시각 판정 — 위 4항).
- **즉시구매 버튼이 없다**(미구현 경로 미호출).
- 테스트 동반 — 입찰 성공/실패 코드 분기 · 마감 판정 · isSeller 분기 · 카운트다운 재동기화.
- `npm run test`·`typecheck`·`lint`·`build` 통과 + **번들 실측**.

## 제약
- **`backend/**`·`docs/**` 수정 금지**(발견은 보고만).
- **미구현 경로 호출 금지**: `/shops` · `/market-prices` · `/me/orders` · `/charges` · `/admin/*`
  · **`/auctions/{id}/purchase`**(신규 등재).
- 우리 시각 언어 부활 금지(색·액센트). **단 §5.12 아웃라인은 사용자 승인분이라 유지.**
- 등급 배지 금지(D-073). WCAG AA **수치 검증**.
- **템플릿 컴포넌트 우선** — 템플릿 `Alert`는 `framer-motion`을 끌고 오니 쓰지 마라.
  `ui/Checkbox`는 **배럴 대신 직접 임포트**(FC-065 미해결 — 배럴은 약 33 kB를 끈다).
- **스테이징 범위를 좁혀라** — prettier가 템플릿 원본 26개를 건드린 사고가 있었다.

---

## 결과 (2026-07-20 · 커밋 `6ccbf1d` · 21파일 · backend·docs 무접촉)

### DoD 충족
| 항목 | 결과 |
|---|---|
| 자리표시자 탈피 · 입찰/이력 동작 | 충족 |
| **마감 경매에 입찰 바 없음** | 충족 — `status:"ACTIVE"` + 과거 `endAt` 테스트로 고정. 본문 폼·하단 바 CTA 둘 다 사라짐 |
| **즉시구매 버튼 없음** | 충족 — `/purchase` 호출 0회를 테스트가 검증 |
| 테스트 | 신규 **59건** / 전체 **347 통과** |
| **카드 신규 제작** | **0장** — `ItemArtSlot`·`CountdownText`·`GoldforceChip`·`auctionPrice` 전부 재사용 |
| 폭 320~1440 | **10지점 전수 계산 파손 0**. 최악 여유 **+60**(1024↑ 고정열). 병목은 아트 프레임 |
| 대비 | 신규 조합 최저 **4.86:1**, AA 미달 0 |
| 번들 | 엔트리 480.98 → **482.63 kB**(gzip +0.44) · 상세 청크 **16.03 kB** 신규. framer-motion 0 · `cloneDeep` 0 |

`test` · `typecheck` · `lint` · `build` · `prettier --check` 전부 통과.

### 설계 판단 — 기록해둘 것
- **`FilterSheet` → `shared/Sheet(placement)` 일반화.** 이번에 모달이 둘(입찰 시트·라이트박스) 더
  필요했고, 셋 다 하는 일이 포털·`role=dialog`·초점 이동/가둠/복귀·Escape·스크롤 잠금으로 **동일**하고
  기하만 다르다. 복제했으면 **접근성 6항목이 세 곳에서 표류**한다.
- **★ 하단 바가 셸 `MobileTabBar`(`fixed bottom-0 z-30`)와 실제로 충돌했다.** `bottom-0 z-40`이면
  **내비게이션을 통째로 덮는다.** `bottom-[calc(4rem+env(safe-area-inset-bottom))]`로 띄웠고,
  safe-area 패딩은 탭바가 이미 가져 **중복 적용하지 않았다.**
- **★ `<form noValidate>`가 필요했다.** `min={minNextBidAmount}` 때문에 브라우저 기본 검증이
  제출을 가로채 자체 말풍선을 띄웠다(**테스트가 이걸 잡았다** — 제출 이벤트가 아예 안 났다).
  같은 실패(`BID_001`)가 두 모습으로 보이게 된다. **검증을 끈 게 아니라 말하는 주체를 하나로 모았고**
  `submit`이 동일 조건을 검사한다. `min`은 AT용 의미로 남겼다.
- **입찰 성공 시 시트를 닫지 않는다** — 닫으면 소프트클로즈 연장 안내를 아무도 못 보고
  카운트다운이 늘어난 이유를 설명할 자리가 없어진다.
- **모바일에서 "진행 중"일 때만 본문 입찰 카드를 숨긴다** — 마감·시작 전에는 하단 바에 상태 글자만
  남아 "왜 못 하는가"를 말할 자리가 본문뿐이다.
- **무효화 반경**: `detail(id)`·`browses()`·`balance.me()`. **`previews()`는 건드리지 않는다**
  (홈 격리 유지 — 홈 51건 전건 통과로 확인).
- **테스트에 가짜 시계를 켰다** — 판정이 `now >= endAt`이라 실제 시계로 돌리면 **러너가 도는 날짜에
  따라 입찰 폼이 사라진다.** 코드가 아니라 달력 때문에 붉어지는 테스트를 만들지 않았다.

### 브라우저 미확인 (누적 — FC-063 승계)
하단 바 + 탭바 **2단 스택 실기 감각**(`env(safe-area-inset-bottom)`이 jsdom에서 0이라 iOS 홈
인디케이터 위 실제 간격 미확인. `pb-28` 여유 176 vs 필요 148은 계산치) · 라이트박스 **320px 여유
+22px**(계산이지 렌더 측정 아님) · `fc-sheet-center`의 Tailwind v4 `translate` 분리 전제 ·
`scaleNarrow` 479px 경계 아트 전환 · 다크모드 인상 · number input OS별 스피너 · `inputMode="numeric"`
키패드. **FC-059 이월분**(바텀시트 `85dvh` iOS 주소창 거동 · 센티넬 자동 로드)도 그대로.

### 발견 — 미처리
1. **★ 이 티켓의 `BID_002` 오기** — 총괄이 잘못 적었고 구현이 계약대로 바로잡았다. **위 정정 참조.**
2. **`AuctionItemView`에 `itemInstancePublicId` 없음** — 스킬 이름을 내려주는 `GET /items/{id}`로
   갈 링크가 끊겨 있다. 아이템 인스턴스 이동 동선도 못 만든다. **게이트2 후보.**
3. **입찰 이력 `page` 기준이 계약에 없다**(0-기준/1-기준 미명시) — `page`/`totalPages` 산술 대신
   **`받은건수 < totalElements`**로 판정해 기준과 무관하게 만들었다. 계약 §1.3 명시하면 우회 불필요.
4. **`resultType` 항상 null · 마감 강등 워커 없음** — 클라가 시각으로 대신 판정 중.
   **EPIC-CLOSING 완료 시 `auctionPhase.ts`의 폴백 주석을 재검토**해야 한다.
5. **템플릿 `ui/Input`이 `lodash/isNil`을 끈다** — 이번엔 `Input` 청크가 3.83 kB로 분리돼 무해했다.
   **FC-065 조사 시 참고**(Checkbox 배럴과 양상이 다르다).
6. **판매자 취소 403(`AUCTION_001`)이 계약 §3.1 cancel 에러 목록에 없다** — 이번 화면에 취소 버튼을
   넣지 않아 미해당이나 계약 공백은 그대로.

---

## ★★ SUPERSEDED (2026-07-20) — 게이트3 미도달, UI 산출물 폐기

reviewer 2차가 **`passed`**(critical 0 · major 0)를 냈고 게이트3(사용자 Done 승인 + push)만
남은 상태에서, 사용자가 **디자인 전면 변경**을 결정했다. → `EPIC-FE-REBUILD`

**`gate: gate3` → `null`.** 이 게이트는 답변되지 않는다. `state: review` · `review_status: passed`는
**도달했던 사실 그대로 남긴다**(닫지 않고 이력으로 보존 — 사용자 결정).

### 폐기되는 것
`views/auctions/**`(상세 페이지 + 컴포넌트 5종) · `components/shared/{Sheet,SectionNotice}` ·
화면 테스트 2파일.

### ★ 승계되는 것 — 이 티켓의 진짜 산출물
| 자산 | 왜 살아남나 |
|---|---|
| `features/auction/lib/auctionPhase.ts` | 마감 판정이 **서버 status가 아니라 시각**이어야 하는 이유(마감 강등 워커 부재)를 인코딩 |
| `features/auction/lib/bidErrors.ts` | 계약 §5 코드별 문구. **총괄 오기를 따르지 않고 계약을 정본으로 삼은 결과물** |
| `features/auction/lib/bidAmount.ts` | 리뷰 major(M-1)의 해답인 **금전 3갈래 규칙** + 테스트 22건 |
| 리뷰 2회분 | `reviews/FC-064-review.md` — **critical/major/minor 8건과 그 근거.** 새 UI에서 같은 함정을 판다 |

### 새 프론트가 반드시 승계할 함정 2개
1. **`Sheet` 초점 강탈(C-1)** — effect 의존에 인라인 콜백이 있고 부모가 매초 리렌더하면
   **초점이 매초 강탈**된다. 모바일에서 소프트 키보드가 닫혀 입력이 불가능해진다.
   해법: 콜백을 ref에 담고 의존을 `[open]`으로. **모달을 새로 만들 때 반드시 다시 만난다.**
2. **입찰 금액 덮어쓰기(M-1)** — 최소가 변경 시 사용자 입력을 무조건 치환하면
   **스나이핑 금액이 하향 치환**된다. `refetchOnWindowFocus`라 탭 전환만으로 발동하고
   확인 단계가 없어 되돌릴 수 없다.
