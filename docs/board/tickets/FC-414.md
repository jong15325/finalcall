---
id: FC-414
type: task
epic: EPIC-HOME-MARKET-RECOMMEND
derived_from: FC-409
jira_key: KAN-469
title: 홈 최신 추천 복합 인덱스와 실행계획 검증
state: done
owner: backend-impl
depends_on: [FC-408]
blocks: [FC-409, FC-413]
gate: null
review_status: passed
contract_ref: docs/spec/shop-spec.md §12.5
artifacts:
  - backend/src/main/resources/db/migration/V29__add_shop_recommendation_created_index.sql
  - backend/src/test/java/com/finalcall/integration/ShopRecommendationQueryPlanIntegrationTest.java
---
## 목표
- 홈 신규·GENERAL 후보의 ACTIVE 전체 scan과 정렬을 복합 인덱스로 제거한다.

## DoD
- append-only Flyway로 `ix_shop_status_created_at_id(status, created_at, id)`를 추가한다.
- ERD 인덱스 목록을 실제 마이그레이션과 일치시킨다.
- 실제 MySQL 8.0, ACTIVE 2,000건 이상에서 `EXPLAIN ANALYZE`로 신규 후보의 인덱스 순서·limit 사용을 증명한다.
- 등록·구매·취소·만료와 추천 회귀 테스트를 통과한다.
- 검증 판매자 쿼리와 무캐시 정책은 변경하지 않는다.

## 근거(인용)
> 추가 게이트2 승인: 최신 추천의 선형 전체 스캔을 제거하기 위해 `(status, created_at, id)` 인덱스 하나를 추가하고 검증 판매자 쿼리와 무캐시 정책은 유지한다. (2026-08-31 사용자)

## 검증
- MySQL 8.0 ACTIVE 10,000건에서 전체 scan+정렬 3.71ms → 역방향 index lookup 30행·무정렬 0.109ms.
- 마감 인덱스 회귀 유지, 추천·등록·구매·취소·만료·정산 포함 대상 테스트 35건 통과.
- spotless, checkstyleMain/Test, bootJar, `git diff --check` 통과.

## 보안 재작업 요청
- V29를 runbook과 일치하는 명시적 MySQL 8 online DDL 형식으로 정렬.

## 보안 재작업 완료
- V29를 `ALGORITHM=INPLACE, LOCK=NONE` 명시 online DDL로 변경하고 MySQL 8 Flyway·SHOW INDEX·실행계획 회귀 통과.

## 파생 경위
- FC-409 실행계획 리뷰에서 최신 후보가 ACTIVE 전체 scan+정렬하는 major가 확인되어 파생했다.
