---
id: FC-193
type: task
epic: EPIC-ITEM-DELIVERY
derived_from: FC-190
jira_key: KAN-219
title: 주문 목록 배송 배지 활성 — OrderSummary += itemInstancePublicId
state: todo
owner: backend-impl
depends_on: [FC-190, FC-192, FC-191]
blocks: []
gate: null
review_status: pending
contract_ref: docs/spec/api-contract.md §4.3
artifacts:
  - docs/spec/api-contract.md
  - backend/src/main/java/com/finalcall/domain/order
  - frontend/src/types (필요 시)
---
## 목표 (EPIC-ITEM-DELIVERY · FC-190 미결 해소)
구매내역(주문 목록) 화면의 배송 배지를 활성화한다. 현재 `OrderSummary`(목록)에 `itemInstancePublicId`가 없어 프론트가 배지를 graceful하게 숨긴 상태(FC-190). 사용자 결정=**옵션1(주문 목록에 조인 키 추가·additive)**.

## DoD
1. **계약(api-contract §4.3)**: `OrderSummary`에 `itemInstancePublicId` **additive** 추가(기존 필드·형상 불변, `OrderDetail`이 이미 보유한 필드와 동일 의미). architect 계약 갱신 선행.
2. **백엔드**: 주문 목록 응답/쿼리가 `itemInstancePublicId`를 실어 반환. 기존 응답 형상은 필드 추가만(제거·개명 없음).
3. **프론트**: 별도 작업 최소 — OrdersPage가 이미 optional로 읽어 배지를 조건부 표시하므로, 필드 도착 시 자동 활성. 타입에 필드 반영·graceful-degrade 가드 정리만.
4. 검증: 주문 목록 응답 필드 추가 + 거래내역 배지 표시(다계정/상태 전이) + 기존 주문 응답 회귀 없음.

## 근거인용
FC-190 반환(주문 목록 조인 키 부재), api-contract §4.3·§4.6. 사용자 게이트 결정(옵션1, 2026-08-05).

## 파생 경위: FC-190 구현 중 주문 목록 조인 키 부재 발견 → 사용자 옵션1 선택으로 보완 신설. FC-191 리뷰 후 착수(워킹트리 충돌 회피).
