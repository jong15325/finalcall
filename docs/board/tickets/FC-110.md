---
id: FC-110
type: task
epic: null
derived_from: FC-107
jira_key: KAN-124
title: 검색 정합성 하드닝 — CDC 라이브 동기·화해 histogram·운영 초기색인 (후속)
state: done
owner: backend-impl
depends_on: [FC-107]
blocks: []
gate: null
review_status: passed
contract_ref: docs/spec/search-spec.md v0.4 §12.5·§12.8
artifacts:
  - backend/src/main/java/com/finalcall/domain/search/SearchReconciliationWorker.java
  - docs/board/reviews/FC-110-review.md
---
## 목표
EPIC-SEARCH의 **정합성(MySQL SoT ↔ ES 파생 read-model) 하드닝**을 마무리한다. 사용자 결정(2026-07-23): 검색 기능은 완성·라이브 검증됐고 **정합성은 추후 과제로 분리**.

## 배경 (EPIC-SEARCH에서 분리)
검색 기능(q·nori·relevance)은 라이브 실동작 확인됨(부팅 재색인이 주 populator). 다만 **근실시간 CDC 동기·화해·운영 색인 전략**은 데모 수준이라 하드닝 필요.

## Done (2026-07-24, 게이트3 사용자 승인)
FC-110 범위(#1·#2·#4·#5) 완결. #1·#4 검증 그린, #2·#5 구현 reviewer pass(커밋 db3f000). #3은 FC-116 이월. reviewer minor 2건(M1 화해 스냅샷 비원자성·M2 ES 쿼리 병합)은 비차단 후속 관찰(리뷰 파일 기록).

## 진행 (2026-07-24)
선행조건(binlog mysql 재생성 + CDC 커넥터 등록)이 이번 세션 도커 정리로 충족됨. backend-impl 검증 결과:
- **DoD#1 CDC 라이브 동기 = ✅ 그린** — 런타임 binlog 변경이 CDC 파이프라인만으로 **~0.7초** 내 ES 반영(price·status). 필드 정합(§12.4 SMT)·kafka 토픽·sink task RUNNING 확인.
- **DoD#4 ES sink 버전 정합 = ✅ 그린** — Aiven 7.17 ↔ ES 8.18.8 typeless bulk 정상, 거부 0. 버전 상향 불요.
- **#2 화해 histogram·#5 관측 = 구현 완료**(커밋 db3f000, 테스트 7/7 그린, reviewer 검수 중).
- **#3 운영 재색인 API = 이월** → [[FC-116]]. 게이트2로 "지금 구현" 승인됐으나 착수 직전 **관리자 계정 체계 부재** 확인 → 사용자가 관리자 페이지 에픽으로 이월 결정(2026-07-24). 계약은 확정(search-spec v0.4·api-contract v1.14, 구현 미착수·미구현 마킹).
- ★ **FC-110 최종 범위 = #1·#2·#4·#5** (검증 2 + 구현 2). #3은 FC-116으로 분리.

## DoD (착수 시 확정)
- **CDC 라이브 동기 end-to-end 검증**: finalcall-mysql을 binlog 옵션으로 재생성(현재 compose 밖 컨테이너라 미적용) → Debezium source·Aiven ES sink 등록 → MySQL 변경(등록·구매·상태전이)이 ES에 근실시간 반영되는지 실측. 현재는 부팅 재색인만 검증됨.
- **화해 완전화**: SearchReconciliationWorker의 count → **count + price/status histogram** 대조(reviewer FC-109 minor). 상태전이 드리프트 탐지.
- **운영 초기색인 전략**: local만 reindex-on-startup·correct-on-drift on. **운영은 트리거 없음** → 대량 재색인 배치/온디맨드 재색인 API/alias 스위치 절차 확정(FC-107 backend-impl 후속 명시).
- **ES sink 버전 정합**: Aiven 7.17 클라 vs ES 8.18.8 서버 typeless bulk 동작 확인(런타임 거부 시 sink 커넥터 버전 상향).
- **관측**: CDC lag·화해 드리프트 메트릭/로그.

## 파생 경위: EPIC-SEARCH 라이브 실측 후 사용자가 정합성을 후속 분리 결정(2026-07-23). 검색 기능은 done, 정합성 하드닝만 이월.
