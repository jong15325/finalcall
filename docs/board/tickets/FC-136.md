---
id: FC-136
type: task
epic: EPIC-EMAIL-VERIFY-FE
derived_from: FC-132
jira_key: KAN-153
title: F3 — errorCodes.ts EMAIL_001~007 동기화·메시지 분리
state: done
owner: frontend-impl
depends_on: []
blocks: [FC-137, FC-138, FC-139]
gate: null
review_status: passed
contract_ref: docs/spec/api-contract.md §5(EMAIL_001~007), email-verify-spec.md §5
artifacts:
  - frontend/src/types/errorCodes.ts
  - frontend/src/types/errorCodes.test.ts
---
## 목표
프론트 `errorCodes.ts`에 `EMAIL_001`~`EMAIL_007`을 동기화한다. **빌드 선행 필수** — `errorCodes.test.ts`가 api-contract §5 표를 파싱해 누락 시 프론트 테스트 실패(spec §5). 현재 EMAIL 코드 0개.

## DoD
- `src/types/errorCodes.ts`에 `EMAIL_001`~`EMAIL_007` 7개 등재. 계약 §5 상태코드·의미와 1:1.
- **메시지 분리**(재시도성·안내가 각각 다름, spec §8 F3): `EMAIL_001` 코드 불일치(다시 입력)·`EMAIL_002` 만료/미발송(코드 재발급)·`EMAIL_003` 시도초과·폐기(코드 재발급)·`EMAIL_004` 재전송 쿨다운(잠시 후)·`EMAIL_005` 이미 인증(재시도 불필요)·`EMAIL_006` 이메일 미설정(먼저 등록)·`EMAIL_007` 이미 사용 중(다른 이메일).
- 기존 `errorCodes.ts` 등재 컨벤션(키·메시지 구조) 그대로 따름 — 새 구조 발명 금지.
- `errorCodes.test.ts` 통과(계약 §5 파싱 정합). 프론트 빌드·테스트 그린.

## 근거인용
api-contract §5(EMAIL_001~007 등재됨, 572~578행). email-verify-spec §5·§8 F3.

## 파생 경위
EPIC-EMAIL-VERIFY-FE 분해 F3. 백엔드 FC-132(엔드포인트) 계약의 프론트 소비 선행. 나머지 프론트 티켓 빌드 차단 해소용.
