---
id: FC-004
type: task
epic: EPIC-MEMBER
derived_from: null
jira_key: KAN-6
title: 프로필 3종 GET/PATCH/DELETE /me 구현
state: todo
owner: backend-impl
depends_on: [FC-002, FC-003]
blocks: [FC-005]
gate: null
review_status: pending
contract_ref: docs/spec/api-contract.md 2.5절
artifacts: []
---
## 목표
- GET /me(프로필 조회) · PATCH /me(nickname 수정) · DELETE /me(탈퇴) 구현 + DTO + 테스트. MemberController/MemberService 확장(공유 파일이라 직렬).

## DoD
- GET /me → 200 `{userPublicId, nickname, isAdmin, createdAt}`.
- PATCH /me → nickname 수정, 중복 시 MEMBER_001(409), 검증 400.
- DELETE /me → `balanceForfeitAcknowledged` 필수(누락 400) · soft delete + 세션 폐기(FC-003) · MEMBER_002 = **gameMoneyHeld>0만 차단**(경매·주문 체크는 TODO 확장 지점 명시) · 재가입 UK 정합.
- 주체 = SecurityContext(타인 /me 불가). 계약 준수 + jpa-convention·coding-discipline·concurrency-review 참조 + 테스트 그린 + 빌드 통과.

## 근거(인용)
> api-contract 2.5절 전문(GET/PATCH/DELETE /me). 게이트1: MEMBER_002 = (A) 확장 지점.
