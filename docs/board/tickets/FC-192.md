---
id: FC-192
type: task
epic: EPIC-ITEM-DELIVERY
derived_from: FC-185
jira_key: KAN-218
title: 배송 조회 API — GET /me/deliveries (+/{id})
state: review
owner: backend-impl
state_note: MAJOR-1 재작업 완료. 근본원인=공유 ItemTemplate 재사용(CloseWorkerConcurrencyIntegrationTest가 9301~9304 "마감템플릿" 커밋)→하드코딩 displayName 단언 파손. 수정: 표시문자열 단언을 persist 캡처 동적 기대치로 전환 + content[0]→findByDeliveryPublicId 조회. 결정값(typeCode/level/skill/status)은 정밀 단언 유지. 전체 suite 맥락 DeliveryQueryApiIntegrationTest 7/7 green. recipient 스코프는 원래 airtight(IDOR 아님). FC-191 재검토 대기.
depends_on: [FC-186]
blocks: [FC-191]
gate: null
review_status: passed
contract_ref: docs/spec/api-contract.md §4.6
artifacts:
  - backend/src/main/java/com/finalcall/domain/delivery/controller
  - backend/src/main/java/com/finalcall/domain/delivery/service
  - backend/src/main/java/com/finalcall/domain/delivery/dto
---
## 목표 (EPIC-ITEM-DELIVERY · 분해 갭 보완)
FC-190 프론트가 소비할 **구매자 배송 상태 조회 API**를 구현한다. 분해안(FC-186~189)에 읽기 API 티켓이 누락돼 보완 신설(2026-08-05).

## DoD (api-contract §4.6)
1. **`GET /me/deliveries`** — 주체=SecurityContext(본인 배송만, IDOR 금지). `CursorResponse<DeliverySummary>` 반환(커서 목록). status·itemInstancePublicId 포함.
2. **`GET /me/deliveries/{id}`** — 단건 상세(본인 소유 검증).
3. **읽기 전용 쿼리 서비스** — `@Transactional(readOnly=true)`. FC-188(쓰기·상태전이)과 파일 분리(별도 QueryService/Controller). FC-186 리포지토리 재사용(findByRecipientUserIdAndStatusIn...).
4. Controller 반환 `ApiResponse<T>`, `@Valid`, 전역 핸들러. DTO=record + static from.
5. 슬라이스/단위 테스트(본인 격리·커서 페이지네이션).

## 근거인용
api-contract §4.6, delivery-domain-spec §10·§11. jpa-convention 스킬. 도메인 인가(주체=SecurityContext·/me IDOR)는 reviewer 확인소 대상.

## 파생 경위: FC-190 착수 중 읽기 API 백엔드 티켓 부재 발견 → 보완 신설. FC-186 스키마 후 착수.
