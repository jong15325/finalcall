---
id: FC-072
type: task
epic: EPIC-FE-REBUILD
derived_from: FC-066
jira_key: KAN-80
title: 경매 상세 + 입찰 화면 /auctions/:id — FC-064 함정 집중
state: done
owner: frontend-impl
depends_on: [FC-067, FC-068, FC-071]
blocks: [FC-073]
gate: null
review_status: passed
contract_ref: docs/ux/design-brief.md B-3·B-4
artifacts:
  - frontend/src/pages/AuctionDetailPage.tsx
  - frontend/src/features/auction
---
## 구현 완료 (frontend-impl · commit ed426d7)
- 목업 .auction-detail-grid(hero 245px + bid-panel sticky)·.bid-dialog 1:1, 색=브랜드. AuctionHeroCard·BidPanel·BidDialog·BidHistory.
- **FC-064 함정 6건 방어(테스트 포함)**: ①모달 초점 [open]+콜백ref(리렌더 초점 재탈취 방지 테스트) ②금액 덮어쓰기 보존 reconcileAmountWithMin(미만일때만 상향, 2테스트) ③비활성 DOM ④스킬 슬롯 필터 ⑤minNextBid 서버파생 ⑥endAt 재동기.
- 마감 클라판정(now>=endAt)·즉시구매 버튼 없음(참고가만)·판매자취소(입찰0&미마감)·비로그인 returnUrl·마스킹 표기.
- 마스킹 미결: isSeller=닉네임 비교로 격리(장차 서버신호 교체 1지점).
- 검증: typecheck/lint 0 · **test 295**(신규 16) · build ✓. 목업 접근 성공, 이탈 없음.
- **경매 축(070~073) 묶음 reviewer 검수 대상.**

## 목표
경매 상세(AuctionDetail 17) + 입찰 이력(BidSummary offset) + 입찰 다이얼로그(POST bids). 최고 위험 화면 —
금전·FC-064 함정 6건 전부 여기. 목업 상세/다이얼로그(market-brief.js·market.css) 정본, 보존 lib 승계.

## DoD
- 목업 .auction-detail-grid(hero+bid-panel sticky) 충실. AuctionDetail 17필드·bid-meta·현재가.
- 입찰 다이얼로그: 프리필=minNextBidAmount·bidErrors 매핑·noValidate·전송중 disabled.
- 상태: 마감 클라판정(now>=endAt)·비로그인/판매자본인 분기·즉시구매 버튼 없음(404).
- FC-064 함정 6: 모달 초점(ref+[open])·금액 덮어쓰기(bidAmount 미만일때만 상향)·비활성 DOM·스킬 슬롯·minNextBid 서버파생·endAt 재동기.
- 스킬 `스킬 #{code}` 중립. 색=브랜드(§2.9). 단위 테스트 + 4게이트.

## 근거인용
- design-brief B-3·B-4·C-5 · rebuild-contract-map(입찰 정합·주의5) · 목업 상세/bid-dialog(레포 밖·읽기).
- 보존 bidErrors·bidAmount·auctionPhase·countdown·useNow.

## 파생 경위: 화면 축 3. 경매 축 핵심. FC-064 승계 함정의 실제 재구현처.
