---
id: FC-031
type: task
epic: EPIC-BID
derived_from: FC-030
jira_key: KAN-38
title: bid·money_hold 스키마(Flyway V11) + 엔티티 + 홀드 도메인 로직
state: review
owner: backend-impl
depends_on: [FC-030]
blocks: [FC-032]
gate: null
review_status: passed
contract_ref: docs/spec/erd.md(bid·money_hold), docs/spec/bid-domain-spec.md
artifacts:
  - backend/src/main/resources/db/migration/V11__bid_and_money_hold.sql
  - backend/src/main/java/com/finalcall/domain/bid/
  - backend/src/main/java/com/finalcall/domain/currency/MoneyHoldService.java
---
## 목표
- 입찰 애그리거트의 저장 계층을 세운다 — `bid`·`money_hold` 테이블·엔티티·홀드 생성/해제 도메인 로직.

## DoD
- **Flyway `V11__bid_and_money_hold.sql`**(단일 채번 — V10까지 auction 소비). erd §4.2 정의대로 `bid`(auction_id·bidder_id·amount·생성시각) + `money_hold`(user_id·bid_id UK·amount·status). 인덱스: `bid(auction_id, amount DESC)`·`bid(bidder_id)`·`money_hold(user_id, status)`.
- **엔티티 2종**: `Bid`·`MoneyHold` — CLAUDE.md 섹션 5 컨벤션(BaseTimeEntity 상속, `@NoArgsConstructor(PROTECTED)`, `@Builder`, `@Setter` 금지, 도메인 메서드).
- **홀드 도메인 로직**: 생성(잔액 차감 연동)·해제(잔액 복원). 상태 전이는 조건부 CAS로 — 이중 해제·미해제가 불가능해야 한다.
- **BidErrorCode**: `BID_001~006`(계약 §5와 1:1). 네이밍 `BID_{3자리}`.
- **리포지토리**: `BidRepository`·`MoneyHoldRepository`(+ 필요 시 Custom/Impl QueryDSL). `findByIdOrThrow` 패턴.
- **테스트**: 슬라이스 테스트(스키마·엔티티 매핑·홀드 생성/해제 불변식). 부팅 시 Flyway V1~V11 검증 통과.
- 스타일: `./gradlew :backend:spotlessApply` 후 checkstyle 통과.

## 범위 밖
- 입찰 API·직렬화·소프트클로즈 연장(FC-032). 조회 API(FC-033).

## 근거(인용)
> erd §4.2 `money_hold` — "입찰 시 게임머니 홀드(에스크로, D-052). 상위 입찰 시 즉시 해제(P-008), 낙찰 시 차감." / `bid` — "경매 입찰(D-004). 자기·연속 입찰 금지는 앱 검증. 홀드는 money_hold가 참조."
> erd §5 인덱스 — `bid (auction_id, amount DESC)`·`bid (bidder_id)`·`money_hold (user_id, status)`.
