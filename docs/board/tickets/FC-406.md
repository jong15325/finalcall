---
id: FC-406
type: task
epic: EPIC-CHAT
derived_from: FC-404
jira_key: KAN-460
title: 채팅 읽음 상태 실시간 갱신 누락 수정
state: review
owner: reviewer
depends_on: [FC-404, FC-405]
blocks: []
gate: null
review_status: passed
contract_ref: docs/spec/chat-domain-spec.md §8
artifacts:
  - frontend/src/features/chat/lib/useChatController.ts
  - frontend/src/features/chat/components/ChatWorkspace.test.tsx
---

## 목표
- 열린 채팅방에서 상대 메시지를 수신한 즉시 읽음 API가 호출되고 발신자에게 `READ_UPDATED`가 전달되도록 한다.

## DoD
- 신규 상대 메시지 수신 시 로컬 읽음 상태의 선반영 때문에 `markRead`가 조기 종료하지 않는다.
- 기존 메시지 병합·unread 계산·`READ_UPDATED` 수신 동작을 보존한다.
- 실시간 수신 후 다음 sequence의 읽음 API 호출을 회귀 테스트로 검증한다.

## 파생 경위
- FC-404 운영 진단에서 프론트의 `lastReadSequence` 선전진과 `markRead` 조기 반환이 직접 원인으로 확정되어 파생했다.

## 검증
- `ChatWorkspace.test.tsx` 11건 통과.
- TypeScript, ESLint, Prettier, production build 통과.
- reviewer 재검토 결과 critical/major/minor 없음, 통과.
