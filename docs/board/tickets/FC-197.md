---
id: FC-197
type: task
epic: EPIC-BOARD
derived_from: FC-196
jira_key: KAN-224
title: 게시판 레지스트리 + 스키마(V22 4테이블) + 시드
state: done
owner: backend-impl
state_note: |
  구현 완료·검증(2026-08-06): V22(board·post·comment·post_image 4테이블+인덱스5종) + Board 엔티티/enum(WritePolicy·BoardType)/BoardErrorCode(BOARD_001~003 선정의)/Repository/Service/Controller + 시드 3건(notice·community·event) + SecurityConfig boards GET permitAll. spotless·checkstyle·compileJava 통과, BoardApiIntegrationTest(Testcontainers 실MySQL+Flyway) 그린. 배치 reviewer 대기(백엔드 4티켓 모인 뒤 1회). 미커밋.
depends_on: [FC-196]
blocks: [FC-198, FC-199, FC-200, FC-201, FC-202]
gate: null
review_status: passed
contract_ref: docs/spec/board-domain-spec.md v1.0, erd.md v1.8 §4.5
artifacts: []
---
## 목표
게시판 시스템의 기반 — Board 레지스트리 도메인 + Flyway V22(board·post·comment·post_image 4테이블 일괄 생성 + 인덱스 5종) + board 시드 3건(커뮤니티·공지·이벤트).

## DoD
1. **Flyway V22** — board·post·comment·post_image 4테이블 + 인덱스 5종(erd §5) 생성. FK 순서 일관.
2. **Board 엔티티·레지스트리** — slug(UK 자연키)·name·description·sort_order·is_active·write_policy(ADMIN_ONLY|AUTHENTICATED)·allow_comments·board_type(GENERAL|NOTICE|EVENT). BaseEntity 상속·도메인 메서드·@Setter 금지.
3. **board 시드**(Flyway) — 커뮤니티(AUTHENTICATED·댓글on·GENERAL)·공지(ADMIN_ONLY·댓글off·NOTICE)·이벤트(ADMIN_ONLY·댓글on·EVENT). erd 시드 형상 준수.
4. **게시판 조회 API** — `GET /api/v1/boards`·`GET /api/v1/boards/{slug}`(공개). ApiResponse 래핑·컨벤션.
5. 빌드·spotlessApply·checkstyle 통과.

## 근거인용
board-domain-spec v1.0 §4~§6, erd v1.8 §4.5·§5·§6(group7), api-contract v1.23 §6. jpa-convention 스킬·CLAUDE.md 섹션 5.

## 파생 경위: FC-196 계약 확정 후 구현 팬아웃 T2(기반).
