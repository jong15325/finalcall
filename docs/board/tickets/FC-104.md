---
id: FC-104
type: task
epic: EPIC-SHOP-MANAGE
derived_from: FC-103
jira_key: KAN-117
title: 내 판매목록 조회 쿼리·컨트롤러 (backend)
state: done
owner: backend-impl
depends_on: [FC-103]
blocks: [FC-105]
gate: null
review_status: passed
contract_ref: docs/spec/api-contract.md §3.2 (FC-103 확정)
artifacts:
  - backend/src/main/java/com/finalcall/api/shop/MeShopController.java
  - backend/src/main/java/com/finalcall/api/shop/MyShopSummaryResponse.java
  - backend/src/main/java/com/finalcall/domain/shop/ShopService.java
  - backend/src/main/java/com/finalcall/domain/shop/ShopRepositoryImpl.java
---
## 완료 (backend-impl · 2026-07-22 · 커밋 3e3eac3, push 안 함)
- `GET /api/v1/me/shops`(MeShopController) — 주체=SecurityContext(seller 파라미터 없음·IDOR 차단)·status 화이트리스트(생략 ACTIVE·ALL 센티널·미허용 400)·sort 화이트리스트. 응답 MyShopSummaryResponse(ShopSummary + estimatedFee/estimatedSettle, 공개 ShopSummary와 격리).
- ShopService.getMyShops(FeeCalculator 주입·예상 정산 파생)·ShopRepository findBySellerCursor(QueryDSL seller keyset·인덱스 ix_shop_seller_status 재사용). **스키마 무변경**. 취소는 기존 재사용.
- **검증**: spotless·checkstyle(maxWarnings 0)·테스트 그린(본인만·타인 미노출·ACTIVE/ALL/SOLD·커서·예상정산 fee 51,000/settle 949,000·공개 /shops 미유입·401)·회귀 없음. 계약 이탈 0.
- **후속(doc)**: api-contract/shop-spec가 PROPOSAL 상태 — 게이트2 승인분 확정 문구 반영은 별도(비차단).
## 목표
FC-103 계약대로 판매자 본인의 리스팅 조회를 구현한다. 취소는 기존 재사용, 조회만 신규.

## DoD
- 조회 쿼리: `ShopRepository`에 seller_id + status 필터 커서 조회(인덱스 `(seller_id,status)` 재사용). 주체=SecurityContext.
- 컨트롤러 엔드포인트(FC-103 확정 형태) + 응답 DTO(역할별 노출 계약 준수). 커서 페이징 기존 재사용.
- 테스트(본인 것만 조회·상태 필터·페이징·타인 리스팅 미노출). spotlessApply+checkstyle. 회귀 없게.

## 근거인용
- FC-103 계약·shop-spec·jpa-convention·concurrency-review(취소 동시성은 기존).

## 파생 경위: FC-103 계약 확정 후 백엔드. FC-096(frontend)와 파일 무교차 병렬.
