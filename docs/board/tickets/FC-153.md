---
id: FC-153
type: task
epic: EPIC-OAUTH
derived_from: FC-152
jira_key: KAN-172
title: OAuth 계정 모델·스키마 (Flyway V19·SocialAccount·find-or-create)
state: done
owner: backend-impl
depends_on: [FC-152]
blocks: [FC-154]
gate: null
review_status: passed
contract_ref: FC-152 확정 계약·erd
artifacts: []
---
## 목표
FC-152 확정 스키마대로 소셜 계정 모델·find-or-create 로직을 구현한다.

## DoD
- **Flyway V19**: `user_social_account`(UK provider+provider_user_id) 생성 + `user.password_hash` nullable화. JPA `validate` 통과.
- **엔티티/리포**: `SocialAccount` 엔티티(BaseTimeEntity·`@Builder`·no setter, feature 배치 규약), `SocialAccountRepository`(findByProviderAndProviderUserId).
- **find-or-create**: provider+id로 조회 → 있으면 연결된 User, 없으면 신규 User(비번 없음)+`UserBalance(0,0,0)` 생성 + SocialAccount 연결. **닉네임 유니크 접미사**(충돌 시 자동). 이메일 자동연결 안 함(결정 2).
- 단위/슬라이스 테스트(신규 소셜·기존 소셜 재로그인·닉네임 충돌·이메일 미연결). `spotlessApply :checkstyleMain :test` 그린. 커밋 금지(승인 대기).

## 근거인용
FC-152 계약·erd. User/UserBalance 생성 흐름(AuthService.signup 선례). 닉네임 UK(V4 nickname_active).

## 파생 경위: FC-152 계약 확정 후 백엔드 모델 구현.
