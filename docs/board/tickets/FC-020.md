---
id: FC-020
type: task
epic: EPIC-ITEM
derived_from: FC-019
jira_key: KAN-25
title: item_template·skill_definition 엔티티 + 카탈로그 API (GET /item-templates)
state: done
owner: backend-impl
depends_on: [FC-019]
blocks: [FC-021]
gate: null
review_status: passed
contract_ref: docs/spec/api-contract.md §4.1, docs/spec/item-domain-spec.md, docs/spec/erd.md §4.3·§5
artifacts: []
---
## 목표
- 아이템 정의 마스터(item_template)와 스킬 정의(skill_definition) 엔티티 + 카탈로그 검색 API를 구현한다. 도메인의 최상위 마스터 계층.

## DoD
- **Flyway V6**(`V6__item_template_and_skill.sql`): item_template(대분류·중분류·속성·종류·표시명·typeCode UK, 등급 없음 D-073) + skill_definition(skill_code UK). erd §4.3·§5 인덱스 정합.
- **엔티티**: `ItemTemplate`, `SkillDefinition` — CLAUDE.md 섹션 5 컨벤션(BaseTimeEntity·@Builder·@NoArgsConstructor(PROTECTED)·@Setter 금지).
- **카탈로그 API** `GET /api/v1/item-templates`: 공통 필터(mainCategory·subGroup·element·kind — 4축) offset 페이지, 응답 = 템플릿(축·표시명·typeCode). item-domain-spec 응답 필드 정합.
  - (정정 2026-07-18: 초안의 "+ skill" 필터는 오기 — 계약 §4.1·spec §5.1은 4축만이며 template엔 스킬 데이터 없음(스킬=인스턴스 슬롯). 구현이 정본과 일치.)
- **Repository**: `findByIdOrThrow` 패턴, QueryDSL 커스텀(RepositoryCustom+Impl)로 필터.
- **테스트**: 엔티티·카탈로그 검색 슬라이스/단위 테스트. `./gradlew :backend:spotlessApply` 통과 + checkstyle 통과.

## 근거(인용)
> 계약 §4.1 "GET /item-templates — 아이템 정의 카탈로그(검색 메타) … 템플릿 = 대분류·중분류·속성·종류·표시명·typeCode". item-domain-spec(v0.2) 엔티티·응답 확정. jpa-convention 스킬.

## 파생 경위: FC-019 슬라이싱에서 마스터 계층(template+skill+카탈로그)으로 분리(V6).
