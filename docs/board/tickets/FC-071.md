---
id: FC-071
type: task
epic: EPIC-FE-REBUILD
derived_from: FC-066
jira_key: KAN-78
title: 경매 목록 화면 /auctions — 필터·정렬·커서 무한스크롤 실연동
state: done
owner: frontend-impl
depends_on: [FC-067, FC-068]
blocks: [FC-070, FC-072]
gate: null
review_status: passed
contract_ref: docs/ux/design-brief.md B-2
artifacts:
  - frontend/src/pages/AuctionListPage.tsx
  - frontend/src/features/auction
---
## 구현 완료 (frontend-impl · commit f22dfb9 초기 + b3a2c30 목업 정렬)
- `GET /auctions`(cursor) + `GET /item-templates` 실연동. AuctionCard **가로형**(목업 `.auction-card` 1:1:
  112px art[공용 ItemFrame]+copy/stats/foot, 3/2/1 그리드 @1200/576). 필터 = 목업 `.auction-command`(pills+속성/상태/정렬).
- 커서 무한스크롤·상태 분기(로딩 스켈레톤/빈결과/에러/부분실패)·URL 동기·카운트다운(`now>=endAt`)·FC-064 배선.
- **초기 이탈 2건 목업 정렬 재작업**: 세로 ItemCard→가로 AuctionCard, 추가 필터·FilterSheet 제거(코디네이터 market.css 실측 지시).
- 색: 브랜드 navy 적용(목업 Vuexy 블루 아님, §2.9 원칙).
- 검증: typecheck/lint 0 · **test 267** · build ✓.
- **경미 미결(리뷰 대상, 비차단)**: 정렬/상태 select가 목업 3옵션의 상위집합(보존 lib 5옵션, 계약 화이트리스트 내).
  CompareToggle는 FC-077(비교 상태 소유)로 보류. → 축 리뷰 시 판정.

## 목표
첫 실연동 화면. `GET /auctions`(cursor) + `GET /item-templates`(필터) 연동, ItemCard 그리드,
필터(kind는 subGroup 종속)·정렬 화이트리스트·커서 무한스크롤·카운트다운(마감 `now>=endAt`).

## DoD
- 목업 `#auction` 레이아웃 충실. AuctionSummary 10필드 카드. URL 필터 동기화.
- 상태: 로딩 스켈레톤(목록만)·빈결과·에러·성립불가 조합=빈결과.
- FilterSheet 초점 강탈 방지(FC-064 C-1). 비활성 DOM 속성. 인기순·minNextBidAmount 목록 부재.
- 보존 lib(queries/auctions preview·browse 분리·auctionFilters·auctionPhase·useNow) 재사용.
- 단위 테스트 + typecheck·lint·test·build 통과.

## 근거인용
- design-brief B-2·C-3·C-5·C-7 · rebuild-contract-map(라우트·매핑·주의5).
- 목업 #auction 뷰(market.js·market.css, 레포 밖·읽기).

## 검증
- 필터 정규화·상태 분기·마감 판정 테스트 그린. 앱 스크린샷으로 목업 대조(총괄 체크포인트).

## 파생 경위: FC-066 계약 매핑 후 화면 축 1번(첫 실연동). 홈·상세의 패턴 검증 겸.
