---
id: FC-019
type: task
epic: EPIC-ITEM
derived_from: null
jira_key: KAN-24
title: 아이템·인벤토리 — 계약 §4.1/4.2 검증 + 도메인 spec 확정 + 티켓 슬라이싱
state: done
owner: architect
depends_on: []
blocks: [FC-020, FC-021, FC-022, FC-023]
gate: null
review_status: passed
contract_ref: docs/spec/api-contract.md §4.1·§4.2, docs/spec/erd.md §4·§7, docs/spec/domain-spec.md §7
artifacts:
  - docs/spec/item-domain-spec.md
  - docs/spec/erd.md
---
## architect 산출 요약 (FC-019 done, 게이트2 승인 반영)
- 정본 개정: erd v0.9(§4.3 slot_key 생성컬럼 UK[G2]·§5 temp cursor 인덱스[G3]·시세 이연 주석·§6 시드 EPIC-ITEM 편입), item-domain-spec v0.2(§9 게이트2 4결정·미해결 갭 0).
- **게이트2 결정**: (a) 진입경로=시드-only(관리자 API 미도입), (b) 시드 template8+skill5+instance10, (c) market-prices 제외 확정, (d) G2·G3 스키마 승인.
- **member 시드 전무 확인** → 시드가 seed user+balance부터 생성(FC-023 V9).
- **팬아웃 판정: FC-020→021→022→023 전부 순차 단일 체인(병렬 불가)** — FK 선형 의존 + Flyway 단일 채번 공유 충돌 + ItemInstance.java 쓰기 교차. 단일 backend-impl 단일 패스 권고.
- Flyway 채번: V6(FC-020)·V7(FC-021)·V8(FC-022)·V9(FC-023 시드). 착수 전 타 티켓 영향 없음(경매/입찰 미착수).
## 목표
- 아이템·인벤토리 에픽의 계약/설계를 확정해 backend-impl 팬아웃 근거를 만든다(contract-first). 계약은 이미 완비 → **검증 + 구현 슬라이싱 + 갭 식별**이 핵심.

## DoD
- **계약 검증**: 계약 §4.1(item-templates·items·market-prices)·§4.2(인벤토리 3종)이 erd §4/§7 테이블(item_template·item_instance·item_ownership_history·temp_storage)과 정합한지 검증. 불일치·갭(nullable·UK·인덱스·FK)을 목록화.
- **item 도메인 spec 확정**: 엔티티 필드·불변식(특히 `item_instance.location` INVENTORY/TEMP/LISTED XOR — slot_no 0~95 / temp_storage 행 / 활성 리스팅), 소유이력 기록 트리거, 카탈로그/상세/인벤토리 API 응답 필드·마스킹(소유자)·페이지네이션(offset/cursor) 확정. 산출물 `docs/spec/item-domain-spec.md`(또는 기존 spec 보강).
- **티켓 슬라이싱**: FC-020/021/022의 쓰기 파일 집합을 명시해 **병렬 팬아웃 가능 여부**(무교차) 판정. 교차 시 순차/단일패스 권고.
- **게이트2 상신 항목 정리**: (1) 아이템 최초 진입 경로 — MVP에서 item_instance가 owner+INVENTORY로 어떻게 생기나(게임연동 없음 전제: 관리자 지급 API? 최소 시드? 테스트 픽스처?). (2) 최소 스텁 시드 범위(게이트1: 대량 실데이터 시드는 이연). (3) `market-prices` 이연 확인(거래 데이터 선행). → 총괄이 게이트2로 사용자 상신.
- 산출물은 `docs/spec/`에 기록. 코드 수정 없음(architect = 읽기+spec 쓰기).

## 근거(인용)
> 계약 §4.1 "GET /item-templates … 템플릿=대분류·중분류·속성·종류·표시명·typeCode", §4.2 "GET /me/inventory … capacity:96", erd §4 결정플래그 B "item_instance.location(enum INVENTORY/TEMP/LISTED) 단일 디스크리미네이터 … XOR 불변식은 앱+DB 제약으로 강제", domain-spec §7 아이템(등급 없음 D-073).
