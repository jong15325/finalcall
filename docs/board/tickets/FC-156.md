---
id: FC-156
type: task
epic: EPIC-OAUTH
derived_from: FC-152
jira_key: KAN-175
title: OAuth 콜백 처리 (/oauth/callback·토큰 교환·복귀)
state: done
owner: frontend-impl
depends_on: [FC-152, FC-155]
blocks: [FC-157]
gate: null
review_status: passed
contract_ref: FC-152 확정 계약 (POST /api/v1/auth/oauth/{provider})
artifacts: []
---
## 목표
provider 리다이렉트 복귀를 받아 백엔드와 토큰을 교환하고 로그인 상태로 진입한다.

## DoD
- **`/oauth/callback` 라우트·페이지**: URL 쿼리에서 `code`·`state` 추출, state 검증(CSRF), provider 식별.
- **`lib/api/auth.ts`에 `oauthLogin(provider, code, state)`** 추가 → `POST /api/v1/auth/oauth/{provider}` 호출.
- 응답 토큰 저장을 **기존 로그인 경로와 동일**하게(토큰 스토어·axios 배선 재사용) → `returnUrl` 복귀(없으면 홈).
- 로딩·에러 상태 표현(교환 실패·취소). 반응형. `npm run build`·lint 그린. 커밋 금지.

## 근거인용
FC-152 계약. 기존 로그인 토큰 저장·라우팅(lib/api/auth.ts·auth 상태). state/CSRF(SEC).

## 파생 경위: FC-155 버튼 → provider → 콜백 복귀 처리.
