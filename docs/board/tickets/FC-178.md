---
id: FC-178
type: task
epic: null
derived_from: FC-177
jira_key: KAN-200
title: 인벤토리를 아이템 마켓과 동일하게(카드 목록·카드정보 모달, '바로구매'→'판매 등록')
state: done
owner: main
depends_on: [FC-177]
blocks: []
gate: null
review_status: passed
state_note: reviewer PASS(critical/major 0). 마켓 회귀 없음(hidePrice 가법·ShopCard/ShopCardInfoDialog 미변경·마켓 스위트 green). 카드정보 모달 충실·접근성 계승. minor 2(크로스 feature 임포트 승격 권고·스켈레톤 높이 점프) 비차단. 라이브 검증 후 done.
contract_ref: 없음(프론트 UX·계약/백엔드 무변경)
artifacts:
  - frontend/src/pages/InventoryPage.tsx
  - frontend/src/features/member/components/InventorySlotGrid.tsx
  - frontend/src/features/member/components/InventoryItemDialog.tsx
---
## 목표
인벤토리 아이템 영역을 **아이템 마켓과 완전히 동일하게** 만든다. FC-177의 커스텀 타일·다이얼로그를 **마켓 컴포넌트 재사용**으로 대체 — 목록 카드 = 마켓 카드, 클릭 시 **카드정보 모달**(마켓과 동일 구조), 마켓의 '판매자+판매가+바로구매' 자리에 **'판매 등록'**. 인벤토리 레이아웃(용량·빈슬롯)은 유지. 디자인 게이트 승인(목업 2026-08-04).

## 목업(정본 참조)
`scratchpad/sell-flow-mockup.html`(인터랙티브). 카드정보 모달 = 헤더(카드정보)·속성표(타입/명칭/채널제한/속성/골드포스)·특수스킬·하단 CTA '판매 등록'. 판매자·판매가·구매확인 없음.

## DoD
1. **인벤토리 타일 = 마켓 카드**: 마켓의 `ShopCard`/`ItemCard`(`skillFlip`)와 동일한 카드로 렌더. **가격·판매자 미표시**(price 미전달, sellerNickname 없음). 카드 전체가 클릭 영역(마켓 ShopCard의 `absolute inset-0` 오버레이 버튼 패턴 이식) → 카드정보 모달 열기. 인벤토리 데이터(`InventoryItem`)를 `ItemCardData`로 매핑.
2. **카드정보 모달(마켓 동일)**: `ShopCardInfoDialog`의 구조·CSS(`ShopCardInfoDialog.css`)를 재사용/파생해 인벤토리용 카드정보 모달 구현 — 헤더(카드정보 CARD INFO)·썸네일+속성표(타입/명칭/채널제한/속성/남은 골드포스)·특수 스킬. **판매자 행·판매가·구매확인 서브뷰 제거**, 하단 CTA = **'판매 등록'** → `/sell?item=<itemInstancePublicId>`. 모달 배선(focus trap·Esc·scroll lock·backdrop·role=dialog)은 ShopCardInfoDialog에서 계승. 공용화(공유 컴포넌트 추출) 또는 인벤토리 전용(`InventoryCardInfoDialog`) 중 침습 적은 쪽을 impl 판단(중복 최소·마켓 회귀 없게).
3. **FC-177 산출 대체/정리**: FC-177의 `InventoryItemDialog`(커스텀)를 위 카드정보 모달로 대체. 인벤토리 타일도 마켓 카드로 교체. InventorySlotGrid/InventoryPage 갱신. 빈 슬롯·용량 배지·전체폭 마켓 셸(FC-177)은 유지.
4. **SellPage 선점 모드는 FC-177 그대로**(무변경) — `/sell?item=`.
5. 타입체크·lint·테스트 통과(대체된 다이얼로그 테스트 갱신, 마켓 카드 재사용·카드정보 모달·판매 등록 네비 테스트). 계약/백엔드 **무변경**. **마켓(ShopCard/ShopCardInfoDialog) 회귀 없게**(공유 시 특히). coding-discipline·impeccable 준수.

## 근거인용
사용자 요청(2026-08-04): "아이템 마켓도 클릭 시 카드정보 모달·바로구매 버튼 → 인벤토리도 동일하게, 목록 디자인 그대로(레이아웃 유지), 카드정보 뜨고 판매 등록 버튼으로". 마켓 `ShopCard.tsx`·`ItemCard.tsx`·`ShopCardInfoDialog.tsx`(+`.css`) 재사용 대상. 백엔드 무변경.

## 파생 경위: FC-177(인벤토리 개편·선점) 라이브 후 사용자가 "마켓과 완전 동일(카드정보 모달)" 방향으로 정정 → 마켓 컴포넌트 재사용으로 재구현.
