---
id: FC-092
type: task
epic: EPIC-SHOP
derived_from: null
jira_key: KAN-103
title: shop 도메인 spec 확정(등록·구매·취소·기한/만료) + 테이블 델타 + api-contract 정밀화 → 게이트2 상신
state: done
owner: architect
depends_on: []
blocks: [FC-093, FC-094]
gate: gate2
review_status: passed
contract_ref: docs/spec/shop-spec.md (신규)
artifacts:
  - docs/spec/shop-spec.md
  - docs/spec/erd.md
  - docs/spec/api-contract.md
---
> review_status=passed 근거: spec 산출물 + 게이트2 승인(2026-07-22 사용자). architect 직접 확정.
## 완료 (architect · 게이트2 승인 2026-07-22)
- `shop-spec.md` v0.2·erd/api-contract 델타(PROPOSAL). 상태머신 ACTIVE→SOLD|CANCELLED|EXPIRED.
- **게이트2 승인분**: (제품) 판매 기한=**관리자 단일 설정값 자동적용**(`shop.listing.default-duration-days` 기본 7일, 판매자 미선택·최대값 개념 없음), `end_at` nullable 유지(향후 무기한 캐시아이템)·취소 허용. (기술 추천 채택) C1 구매 shop 행 FOR UPDATE+종료성 CAS·C2 잔액 user_id 오름차순(buyer/seller 2행)·C3 sale_order UK 이중판매 차단(신규 0)·C4 만료 워커 60초·TEMP 직행·C6 cancel POST.
- **스키마 영향 = 신규 테이블 1개(shop, V15), 컬럼 변경 0**. 정산·수수료·거래내역·수익원장·인벤토리 CAS 재사용(코드 변경 0). 파급 없음.
- 인계: FC-093 backend(shop 애그리거트·구매·취소·만료 워커) · FC-094 frontend(마켓 화면·판매등록·디자인 게이트).

## 목표
고정가 마켓(shop) 계약을 확정한다. 판매자 등록 → 구매자 즉시 구매 → 취소/만료 회수의 flow·상태머신·동시성·금전모델을 spec화하고, shop 테이블 신규 Flyway 델타 + api-contract 정밀화를 PROPOSAL한다. 금전·스키마라 게이트2 상신.

## DoD
- `shop-spec.md` v1.0: shop 애그리거트(상태머신 ACTIVE→SOLD|CANCELLED|EXPIRED)·등록(INVENTORY→LISTED CAS)·구매(shop 행 FOR UPDATE·종료성 CAS·수수료 1회·잔액 buyer/seller 2행 user_id 오름차순·SettlementRecorder(SHOP,shop.id) 위임)·취소(releaseFromListing)·만료 워커(기한 스캔·회수 목적지 TEMP)·불변식(총량보존 I-H·이중판매 UK(source_type,source_id)).
- **기한 정책**: 기한 설정 옵션(@ConfigurationProperties, 관리자 조정 가능·하드코딩 금지) + 무기한(end_at null) 지원. 만료 시 자동 회수 → 임시 보관함(TEMP).
- erd 델타: shop 테이블(public_id·seller_id·item_instance_id·price·status·end_at nullable·item 스냅샷·인덱스 (status,end_at)/(seller_id,status)) PROPOSAL.
- api-contract 정밀화: shop 엔드포인트(POST/GET /shops·POST /shops/{id}/purchase·PATCH .../cancel)·역할별 노출·SHOP_001~006 매핑.
- 반환에 **게이트2 상신 항목**(제품 결정은 게이트1에서 확정됨 — 기술 결정 추천안 정리).

## 근거인용
- closing-domain-spec v1.0(SOLD 정산·I-H)·purchase-spec v1.0(SettlementRecorder/PurchaseService 분할·A4 잔액 락 순서)·fee-policy-spec v1.0(판매자 부담·유형 무분기)·erd v1.4 §4.2(shop 예비)·domain-spec §2·§5(FixedSale)·api-contract v1.13 §3.2·§5·백엔드 settlement/auction/item/inventory 실측.

## 파생 경위: EPIC-SHOP 게이트1 승인 후 contract-first 선행. 게이트2 상신 준비.
