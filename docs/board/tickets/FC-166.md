---
id: FC-166
type: task
epic: EPIC-LOGINID-CHECK
derived_from: FC-165
jira_key: KAN-187
title: 아이디 가용성 조회 API 구현 (+게이트웨이 배선)
state: done
owner: backend-impl
depends_on: [FC-165]
blocks: [FC-168]
gate: null
review_status: passed
contract_ref: FC-165 확정 계약
artifacts: []
---
## 목표
FC-165 계약대로 loginId 가용성 조회 API를 구현한다(닉네임 FC-161 미러).

## DoD
1. **엔드포인트**: `GET /api/v1/auth/login-id/availability` 컨트롤러·서비스 구현. 기존 loginId 존재 검사 재사용, 쿼리 record `@Valid` 바인딩(닉네임 FC-161 방식 미러 — `@RequestParam` 메서드검증 회피). `LoginIdAvailabilityResponse{available}`. SecurityConfig permitAll 정확경로 추가.
2. **게이트웨이 배선(필수)**: `backend/gateway/.../application.yml`의 `auth-rate-limited` predicate에 이 경로 추가(MAJOR-1 재발 방지).
3. **테스트**: 가능/중복/형식오류. 기존 loginId 검사 회귀 없음.
4. 스타일: `./gradlew :backend:spotlessApply` + `:backend:gateway:spotlessApply`, checkstyle 그린. 커밋 금지.

## 근거인용
FC-165 계약. FC-161 구현 선례(패턴 재사용). EPIC-LOGINID-CHECK MAJOR-1 교훈.

## 구현 결과 (2026-07-30)
- **추가**: `auth/dto/LoginIdAvailability{Request,Response}.java`, `integration/LoginIdAvailabilityIntegrationTest.java`(4건).
- **변경**: `AuthController`(`GET /login-id/availability`), `AuthService.isLoginIdAvailable`(`existsByLoginIdAndIsDeletedFalse` 재사용), `SecurityConfig`(permitAll 정확경로), `gateway application.yml`(**게이트웨이 predicate 처음부터 등재** — MAJOR-1 재발 방지), `AuthServiceUnitTest`(+2건).
- 게이트웨이 predicate 최종: `...,/api/v1/auth/oauth/**,/api/v1/auth/login-id/availability`.
- **검증**: spotless·checkstyle·단위·통합(Testcontainers) 전부 그린. loginId 정규화 없음(signup 동일), advisory·최종 AUTH_001.

## 파생 경위: FC-165 계약 확정 후 백엔드 구현.
