---
id: FC-021
type: task
epic: EPIC-ITEM
derived_from: FC-019
jira_key: KAN-26
title: item_instance·소유이력 엔티티 + 상세 API (GET /items/{id})
state: done
owner: backend-impl
depends_on: [FC-020]
blocks: [FC-022]
gate: null
review_status: passed
contract_ref: docs/spec/api-contract.md §4.1, docs/spec/item-domain-spec.md, docs/spec/erd.md §4.3·§7
artifacts: []
---
## 목표
- 개별 아이템(item_instance)과 소유이력(item_ownership_history) 엔티티 + 인스턴스 상세 API를 구현한다.

## DoD
- **Flyway V7**(`V7__item_instance_and_ownership.sql`): item_instance(template FK·레벨·스킬 2슬롯·발동확률·골드포스·소유자 FK·location enum) + item_ownership_history(from/to owner·transfer_type). erd §4.3·§7 정합.
- **엔티티**: `ItemInstance`(+ `ItemLocation` enum INVENTORY/TEMP/LISTED), `ItemOwnershipHistory`(+ `TransferType` enum, EPIC-ITEM은 SEED만 사용 — TRADE는 경매/주문 에픽), `ItemErrorCode`. 섹션 5 컨벤션.
- **상세 API** `GET /api/v1/items/{itemInstancePublicId}`: 템플릿·레벨·스킬1/2(코드+표시명)·발동확률·골드포스 만료·현재 위치(공개범위: location 노출·slot_no 비공개)·소유자(마스킹). item-domain-spec §5 정합.
- **소유이력 트리거**: 최초 생성 시 ownership_history 첫 행(from_owner NULL) 기록 규약.
- **테스트**: 엔티티·상세 API 슬라이스/단위. spotless·checkstyle 통과.

## 근거(인용)
> 계약 §4.1 "GET /items/{itemInstancePublicId} … 템플릿·레벨·스킬1/2·발동확률·골드포스 만료·현재 위치(공개 가능 범위) + 소유자(마스킹)". erd 결정플래그 B(location 단일 디스크리미네이터). item-domain-spec §5.

## 파생 경위: FC-019 슬라이싱에서 인스턴스+소유이력 계층으로 분리(V7).
