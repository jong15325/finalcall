# FC-227 리뷰

- 판정: PASS
- Critical: 0
- Major: 0
- Minor: 1

## 확인 내용

- 경매 카드에서 중복 기계 문자열 `specSnapshot`을 숨기고 `Lv` 정보는 유지한다.
- 공용 `ItemSkillSummary`와 원본 슬롯 번호 규칙을 유지한다.
- 카드 범위에서만 스킬 행 배경, 텍스트 대비, 슬롯 라벨 간격과 장문 줄바꿈을 보강한다.
- 슬롯 라벨 뒤 실제 공백을 제공해 시각 표현과 접근성 텍스트가 모두 분리된다.
- 카드와 경매 로딩 스켈레톤의 최소 높이는 256px로 일치한다.
- 상세 링크와 비교 버튼의 형제 구조 및 포커스 동작에 회귀가 없다.

## 검증

- 전체 프론트 테스트 88개 파일, 724건 통과.
- 관련 재작업 테스트 36건 통과.
- TypeScript 검사, 린트, 프로덕션 빌드 통과.
- `git diff --check` 통과.

## 잔여 Minor

- jsdom은 실제 레이아웃을 계산하지 않아 320px·375px의 `scrollWidth`를 자동 검증하지 못했다. `minmax(0, 1fr)`, `min-width: 0`, 장문 줄바꿈과 overflow 해제 구조를 정적으로 확인했다.
