# FC-229 리뷰

- 판정: PASS
- Critical: 0
- Major: 0
- Minor: 0

## 확인 내용

- 경매 상세의 아이템 정보는 카드 타입, 명칭, 속성, 종류, 레벨, 골드포스, 특수 스킬 순으로 구성된다.
- 카드 타입과 골드포스 잔여일은 공용 활성 판정을 사용하며 비활성·만료 상태도 `없음`으로 명시한다.
- 속성은 마켓 카드 정보 모달과 동일한 `elementLabelOf` 어휘를 사용한다.
- 종류는 대분류 종속 매핑을 사용하고 미등록 코드 폴백을 유지한다.
- 스킬 원본 슬롯, 스킬 2 확률, 이름 폴백과 무스킬 빈 상태가 정상이다.
- 기계 문자열 `specSnapshot`은 제거됐고 입찰 패널·이력·모바일 순서는 변경되지 않았다.
- 모달 chrome과 포커스 트랩을 상세 본문에 재사용하지 않아 접근성 부작용이 없다.

## 검증

- 신규 AuctionHeroCard 테스트 7건 통과.
- 전체 프론트 테스트 89개 파일, 735건 통과.
- TypeScript 검사, 린트, Prettier, 프로덕션 빌드 통과.
- 기존 번들 크기 및 NoticeSection key 경고만 유지.
