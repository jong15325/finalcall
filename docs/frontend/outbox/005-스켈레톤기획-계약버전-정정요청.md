상태: ANSWERED → design/outbox/028-프론트-skeleton계약버전-정정회신.md ((a) v1.4 통일 채택·정정 완료. skeleton-plan v0.2·screen-spec v0.3)
회신대상: design/outbox/026-스켈레톤기획-완료보고.md
# [프론트 → 기획] 결정 요청: skeleton-plan 계약 기준 버전 불일치 정정 (v1.3 / v1.4)

배경: skeleton-plan v0.1을 F 구성의 선행 기준선으로 인계받았다. 문서 내부에서 계약 기준 버전이 갈린다 — 헤더 근거·변경이력·라우트표는 **v1.4**인데 본문 일부는 **v1.3**이다. skeleton-plan [7] "이 문서가 계약·토큰 정본과 어긋난다고 판단되면 정합시키지 말고 기획에 회신하라"에 따라 F가 임의 정합하지 않고 회신한다(D-083 이의 의무).

근거(인용): api-contract 상태줄 — "상태: v1.4 — G3 확정(2026-07-14) + 6절 계약 변경 4건(D-070, D-073, 엣지 오류 명세/057, 회원 리소스 공백 보완/069)". 실재 확인: [2.5] 회원 리소스(GET/PATCH/DELETE `/me`), [5] `MEMBER_001`(409)·`MEMBER_002`(409).

## 불일치 지점 (skeleton-plan v0.1)

v1.4 표기: 헤더 근거("api-contract **v1.4**(계약 정본)") · 변경이력 v0.1("기준 계약 v1.3→**v1.4**") · [2] 제외("계약 v1.4 [2.5] 확정으로 근거 확보") · [3] 라우트표(`/me/profile` 포함) · [6] 공백4 해소("계약 v1.4 [2.5]").

v1.3 잔존: [2] 포함표 공용 타입 근거("계약 v1.3") · [2] 제외 repo 줄("계약 v1.3 복사본") · [5] 제목("계약 정합 (api-contract v1.3)") · [5] 본문("기준은 **v1.3**이다") · [5] 계약 복사본("v1.3 복사본") · [7]("003의 v1.2 참조는 **v1.3**으로 읽는다").

## F 구성에 미치는 실질 영향 (표기 문제 아님)

1. **에러코드 상수 집합**: [2]는 "에러코드([5])"를 스켈레톤 포함으로 둔다. v1.4면 `MEMBER_001`·`MEMBER_002`가 상수에 들어가고, v1.3이면 빠진다.
2. **계약 복사본 버전**: [2]·[5]가 "v1.3 복사본"이라 repo 생성 시 뜨는 버전이 갈린다(D-030 헤더 표기).
3. **outbox/003 재발신 기준**: [7]이 "v1.3으로 읽는다"라 Claude Code 프롬프트의 참조 버전이 갈린다.

## 안건 1. 스켈레톤 계약 기준 버전 확정

- 선택지: (a) **v1.4로 통일** — [2]·[5]·[7]의 v1.3 잔존을 v1.4로 정정 (b) v1.3 유지 + 회원 리소스분만 예외
- 추천: **(a)** — 기준 위계상 확정 스펙(v1.4)이 최상위이고(collaboration-guide 3절), skeleton-plan 자신의 헤더·변경이력·라우트표가 이미 v1.4다. [3]에 `/me/profile`을 넣은 판단과 [5] "v1.3"이 서로 모순이라 (b)는 성립하기 어렵다. v1.3→v1.4 델타([2.5] 신설)는 스켈레톤 IA·구조 무영향이고 **에러코드 상수 2건 추가·복사본 버전 표기**로 국한된다.
- 요청: (a) 채택 시 skeleton-plan 해당 줄 정정(문서 소유=P). F는 정정본 기준으로 구성 확정 + outbox/003 재발신.

## 뿌리 점검 (D-085)

같은 원인이 다른 층에도 공백을 만들었다. 원인은 "버전 상향 시 상태·변경이력·영향 절은 갱신했으나 **근거 줄·버전 태그가 붙은 절 제목**은 미전파"다.
- **screen-spec v0.2**: 상태줄은 "계약 v1.4 기준"인데 근거 줄은 "api-contract **v1.3**(최상위 — 라우트·엔드포인트·응답 스키마)". 동일 패턴, 동일 원인.
- 제안: 버전 상향 시 해당 문서 내 **전 버전 문자열 grep**을 마무리 절차로 두면 재발이 막힌다(design/022가 grade 제거 때 "grep 잔여 없음"으로 검증한 선례와 동일한 방식).

## 진행 상태

- F 구성(F-002)은 **차단되지 않는다** — IA 셸·토큰 방침·범위는 버전과 무관해 병렬 진행 중.
- `frontend/outbox/003`(Claude Code 생성)은 **재발신 보류 유지**. 버전이 확정돼야 에러코드 상수·복사본 버전이 고정되므로, 확정 후 재발신한다(코드 생성 후 재작업 회피).

회신: 필요 — 안건 1 채택/기각 (1줄이면 충분)
신규 발번 ID: 없음 (계약 정본 인용·문서 정정 요청. F 신규 결정 아님)
