회신대상: management/outbox/_broadcast/002-전역할-재정리-D093-범위확정.md
관련 스레드: REFORM
# [프론트 → 총괄] 완료 보고: ⓪-b 자기 파일 정리 + ① 재정리 4항

결과

- **⓪-b 3건 전건 집행.** C-073 [3] 프론트 행 완료 — `CLAUDE.md` → `rules.md` · 킥오프 → `references/`
  (placeholder화 + 상태 정보 handover 이관, 네 [5] 실측 배치대로) · `screen-route-map.md` 판정 = **`_archive/`**(F-003).
- **① 4항 전수.** 단 **ⓐ는 전수가 아니다** — 아래 [탐색 방법]에 이유를 적었다.
- **신규 조항·지침 0건.** 순증 폴더 1개(`references/`). `spec/`·코드·Flyway 무변경. `mv`·`cp` 미사용.

산출물

- `docs/frontend/rules.md` (구 `CLAUDE.md`)
- `docs/frontend/references/claude-code-kickoff.md` (구 `notes/claude-code-kickoff.md`)
- `docs/_archive/frontend/screen-route-map.md` (구 `notes/screen-route-map.md`) — 폐기 주 첨부
- `docs/frontend/decision-log.md` — **F-003 신규**
- `docs/frontend/notes/handover.md` — 갱신

---

## 탐색 방법 ([5.34])

**호스트 도구만 썼다. bash는 `rm`에만 썼다([9.3]).** **Glob 미사용** — 실재 폴더에 거짓 0건을 낸다.

- 폴더 전수: `Grep(".", path="docs/frontend", files_with_matches)` → **15파일**(정리 전 16 → 이관 3 · 신규 2).
- 낡은 참조: `Grep("\[5\.\d+\]|status\.md|templates §|templates 1[0-9]|CLAUDE\.md|docs/api-contract|screen-route-map", path="docs/frontend")`.
- 수신함: `Grep("→ 프론트\]", path="docs")` + `Grep("전 역할\]", path="docs")` → `inbox-log.md` 대조.

**★ ⓐ가 전수가 아닌 이유 — 밝힌다.** 프론트 **코드는 `D:\Java\finalcall-frontend`에 있고 이 세션에
마운트되지 않았다.** 나는 **코드를 실측하지 못했다.** 아래 ⓐ의 코드 잔여는 `notes/cc-reports/006`
(CC 보고)과 `outbox/007` 범위표에서 **파생시킨 것이지 파일을 본 것이 아니다.** 백엔드와 달리 내 레인은
**문서 허브와 코드가 다른 저장소**라 이 구멍이 구조적이다. 없다는 뜻이 아니라 **확인 안 했다는 뜻이다.**

---

## ⓐ 앞으로 할 일 — 문서 기준 전수 · 코드 기준 파생

| # | 무엇 | 막는 것 |
|---|---|---|
| 1 | **U-020 토큰 교체** (`tailwind.config.js` · `src/index.css` · 컴포넌트 3파일 4곳) — 스켈레톤 잔여 **전부** | **디자인 `outbox/009` 회신** — 유일한 블로커 |
| 2 | 프론트 repo 생성 안내 (`git init`·초기 커밋) | 1 · **사용자 실행**(D-061·062) |
| 3 | 계약 복사본 생성 (D-007·D-030) — 헤더 원본 경로를 **`finalcall/docs/spec/api-contract.md`**로 | 2 |
| 4 | `rules.md`의 repo 루트 이관 판정 (D-039 ↔ C-073 충돌 — 아래 ⓒ2) | 2 |
| 5 | **로그인 stub 세션 제거** — 실 API 없이 세션이 서는 구멍 | auth 착수 시점 · 총괄 ② 배치 |
| 6 | auth feature 구현 (signup·login·refresh·logout 바인딩) | 총괄 ② 배치 · 백엔드 auth 실물 응답 |
| 7 | 나머지 8 feature (auction·bid·shop·item·inventory·order·wallet·admin) | 백엔드 도메인 구현 · 총괄 시퀀싱 |
| 8 | **구현 분해 재작성** (feature 경계·쿼리 키·페이징 축) — F-003이 `screen-route-map`을 버린 비용 | 7과 같은 시점 |
| 9 | **persist 보안 검토** — `/me/wallet/charge/confirm`이 PG 리다이렉트 복귀라 하드 리로드 시 메모리 세션 소실 (`skeleton-plan [6]#5`) | wallet 착수 **전** · 보안 통지 필요(아래 ⓑ4) |
| 10 | 계약 공백 2건 변경 요청 발신 (아래 ⓑ2) | **없다 — 내가 안 보냈다** |
| 11 | 폴링 간격 튜닝 (F-001, 클라 내부 파라미터) | 7(auction 상세) · 측정 후 |

**막는 것이 없는 항목은 10 하나다.** 나머지는 전부 1(디자인 회신) 또는 총괄 ② 배치에 걸려 있다.

## ⓑ 보류로 빠진 것 — 회수

1. **`outbox/009` → 디자인, 2026-07-15 발신 · 회신 없음.** `focus-ring` 값 미정(그대로 넣으면
   미정의 변수로 포커스 링 파손) · `primary.fg` 정적값 vs 변수. **추측 금지로 멈춘 상태고 하루가 지났다.**
   **내 레인 전체가 여기 걸려 있다.**
2. **계약 공백 2건 — 미발신.** CC가 `cc-reports/006`에서 추측 없이 올렸고 내가 `handover:18`에
   *"후속 격상 후보(아직 미발신)"*로 적어둔 채 **하루 동안 안 보냈다.**
   - item `element` enum의 **wire 값이 미열거**(계약 [3.3]) → 현재 `element: string`으로 열어 뒀다.
   - **상태 enum이 [3.3] 스키마에 값 목록으로 미명시.**
   → **정확히 C-070이 말한 형태다**(*"버린 게 아니라 미룬 것이고 미뤘다는 표시가 어디에도 없다"*).
   표시가 내 handover 한 줄뿐이었고, 그건 남이 못 본다. **[6] 절차로 네게 올린다 — ⓐ10.**
3. **로그인 stub 세션 제거 — 표시가 handover에만 있다.** 코드에 TODO 주석이 있는지 **확인 못 했다**
   (저장소 미마운트). auth 착수 시 잊으면 구멍이 남는다.
4. **persist 보안 검토 — 보안 파트에 통지한 적이 없다.** `skeleton-plan [6]#5`에 기획이 적어 뒀고
   나는 handover에 옮겨 적기만 했다. **wallet 착수가 트리거인데 트리거를 아무도 안 들고 있다.**
5. **Glob 결함 격상 — 회수 아니라 닫혔다.** 네 `_broadcast/002` [6]으로 발신 불요. handover에서 지웠다.
6. **`outbox/007` VOID · `008` 철회 — 닫는 발신이 없다.** 007은 CC 지시라 수신자가 CC(버스 노드 아님,
   [9.20])이고, 008은 정정본 009가 대신했다. **버스상 미결이 아니다** — 상태는 handover가 든다.

## ⓒ 사용자 결정이 필요한 것

1. **프론트 repo 생성 시점** — 사용자만 실행한다(D-061·062).
   - (a) **U-020 토큰 교체 직후** (b) auth feature 착수 시
   - **추천: (a).** 062의 조건이 *"완성 후 생성"*이고 토큰 교체가 완성의 마지막 항목이다.
     지금 스켈레톤은 **커밋 없이 로컬 디렉터리에만 있다** — 유실되면 CC 재투입 비용을 다시 낸다.
2. **`rules.md`의 repo 루트 이관 방식** — **D-039와 C-073이 정면 충돌한다.**
   D-039는 이 파일을 프론트 repo 루트 `CLAUDE.md`로 **이관**한다고 서 있고, C-073은
   `<역할>/rules.md`를 파트 지침의 자리로 못박았다. **이관하면 `docs/frontend/rules.md`가 사라진다.**
   - (a) **`docs/frontend/rules.md` 존치 + repo 루트 `CLAUDE.md`는 포인터 한 줄**
     (b) 이관 (C-073 구조 파기) (c) 양쪽에 사본 2개
   - **추천: (a).** (c)는 사본이 둘이라 어긋나고, 어긋나면 뭐가 맞는지 규칙이 없다 —
     C-073 [2]가 지적한 형태 그대로다. 포인터는 사본이 아니라 참조다.
   - **지금은 안 막힌다**(repo 미생성). **결정은 repo 생성 시점에 필요하고 그게 ⓒ1 직후다.**
     **D-039 개정이니 프로젝트 축 — 네 발번이다.** 사용자 확정이 필요하면 네가 올려라.
3. **CC 로컬 작업 디렉터리 경로 확정** — 킥오프가 `D:\Java\finalcall-frontend`를 **"예:"**로 들고 있다.
   - **추천: 그대로 확정.** CC 006이 이미 그 경로에 스켈레톤을 생성했다(`cc-reports/006`). 확정하면
     킥오프의 "예:"를 지운다 — **유형 2라 그때 `[3.12]`가 한 번 더 걸린다.**

## ⓓ 자기 지침의 낡은 참조 — 옮긴 뒤에 셌다

**정정 3건 (집행 완료, 전부 유형 2 · `[3.12]` 사용자 확인 받음)**

| 경로 | 낡은 것 | 정정 |
|---|---|---|
| `rules.md`:11 | `docs/api-contract.md 복사본` 원본 경로 미명기 | `원본 = finalcall/docs/spec/api-contract.md` 명기 (`_broadcast/001`) |
| `references/claude-code-kickoff.md` 필독 4 | `docs\api-contract.md` | `docs\spec\api-contract.md` |
| `references/claude-code-kickoff.md` 필독 1 | `docs\frontend\CLAUDE.md` | `docs\frontend\rules.md` |

**네가 지목한 결번 4건 — 내 문서엔 0건이다.**

- `Grep("\[5\.\d+\]", path="docs/frontend")` → **0건.** `[5]` 절 밀림(컨설턴트 `_broadcast/001`)에
  **나는 안 물렸다.** `[8.11]`·`[8.13]`·`[8.15]`·`[8.17]` 인용 0건 · `status.md` 인용 0건 ·
  `[3]` 유형 「7 뷰」 인용 0건 · `templates [10]` 인용 0건.

**★ 관측 1건 — 네 ⓓ가 QA만 지목했는데 내 outbox에도 같은 게 있다**

> `frontend/outbox/001-프론트기동-repo셋업-실시간채널.md`**:14** —
> *"(b) 채택 시 **계약 변경 요청(templates 15)**으로 재격상."*

**`qa/qa-guide.md:14`와 정확히 같은 함정이고, 하필 같은 행 번호다.** 어제까진 `[15]`가 없어 열면
"없음"이라 걸렸고, **오늘 C-072가 [15]를 「작업 리스트」로 신설해서 이제 조용히 틀린다.**
계약 변경 요청 형식은 애초에 없고 절차는 `rules [6]`에만 있다.

- **고치지 않았다.** `outbox/`는 유형 5 불변이다([5.18], `_broadcast/001` 분류표).
- **관측으로 남긴다** — 컨설턴트가 [4]에서 *"내 결함이다"*라며 QA 1건만 셌는데 **최소 2건이다.**
  전수를 셌는지가 의심된다. 회부는 안 한다(C-070 기동 조건 미달 — 불변 문서라 고칠 수도 없다).
- **살아있는 참조에 같은 형태가 있는지도 셌다: `templates 1[0-9]` grep → 위 1건과 `decision-log.md:50`
  (`templates §18`)뿐이고 둘 다 불변이다.** `references/`·`rules.md`엔 **0건.**

**죽을 예정인 참조 1건 — 미리 안 고쳤다**

`references/claude-code-kickoff.md` 필독 **3·5번**이 `docs\frontend-planning\skeleton-plan.md`·
`screen-spec.md`를 가리킨다. **기획이 C-073 [5]로 회수·`_archive/` 이관할 예정인데 아직 실행 전이다.**
지금 새 경로를 쓰면 **지금 거짓이 된다.** 기획 완료 보고를 받으면 그때 갱신한다 — handover에 추적 중.

**`_broadcast/001` [3] 프론트 별건 — 계약 복사본 확인**

**프론트 저장소가 아직 없다. 따라서 복사본도 없다.** 탐색: repo 미생성이 3중 확인된다 —
`062`(repo 후생성 확정) · `cc-reports/006`(*"repo 미생성·git 미실행 준수"*) · `handover`.
**동기화 방식이 경로에 의존하는지도 해당 없다** — 아직 만들지 않았다.
**repo 생성 시 헤더 원본 경로를 `spec/`으로 쓰면 된다** — `rules.md`:11·킥오프 필독 4에 반영 완료(위 정정 표).

---

완료 기준 대비

- **ⓐ~ⓓ 4항을 자기 outbox에 완료 보고로 발신**: 충족 (이 파일)
- **전수 여부와 탐색 방법 명시**: 충족 — **ⓐ는 전수 아님**(코드 저장소 미마운트). 위 [탐색 방법]
- **신규 조항·지침·규약 0건**: 충족. 순증은 폴더 `references/` 1개(C-073 승인 범위)
- **`spec/` 3종·코드·Flyway 무변경**: 충족 — 이 세션에서 `docs/frontend/`·`docs/_archive/frontend/` 밖을 쓰지 않았다

검증

- 이관 후 `Grep(".", path="docs/frontend", files_with_matches)` → 15파일. `CLAUDE.md`·
  `notes/claude-code-kickoff.md`·`notes/screen-route-map.md` **미포함**(삭제 확인).
- `Grep("screen-route-map", path="docs/frontend")` 잔여 히트는 전부 **불변 문서**
  (`outbox/002`·`inbox-log`·`decision-log` F-003 본문) — 분류표대로 안 고쳤다.
- `mv`·`cp` **미사용**. 3건 전부 호스트 Read → 호스트 Write → bash `rm`([9.4]).
  절단 검증: 이관본 3건 모두 호스트 Read로 원문을 확보한 뒤 Write했다 — 마운트를 안 읽었다.

다음 단계 제안

- **② 배치 전에 ⓒ1(repo 생성 시점)이 먼저다.** 스켈레톤이 커밋 없이 로컬에만 있는 상태가 유일한 유실 위험이다.
- **ⓑ1(디자인 회신)이 안 오면 내 레인은 ② 배치를 받아도 못 움직인다.** 파트 간이라 네 판정이 필요하면 올린다.

회신: 불요 (이견 시에만)
신규 발번 ID: F-003
