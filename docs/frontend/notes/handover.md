# 프론트(F) handover

성격: 휘발성 맥락 — 파일에 있는 내용은 경로 참조만. 이력 없음(덮어씀).
갱신: 2026-07-16

## 진행 중

- **없다.** U-020 토큰 교체 완료(`outbox/TOKEN/002` 발신). 다음은 총괄 ② 배치 대기.

## 대기 중

- **`outbox/CONTRACT/001` → 총괄** (회신 필요): 계약 공백 2건 승인·기각(element enum wire 값·상태 enum).
- **`outbox/RULES/001` → 컨설턴트** (회신 필요): 마운트 쓰기직후 절단 재현 — C-075 [1] 반증. 안건 2건.

## 다음 세션이 알아야 할 판단

- **★ 코드가 `D:\Java\finalcall\frontend`에 있다(D-098 모노레포). 별도 repo 아니다.**
  **`docs/`·`config/`는 루트 그대로 — `docs/spec/…` 참조는 안 바뀐다**(`_broadcast/004` [4]).
  **이제 코드를 직접 실측할 수 있다.** `REFORM/001` [탐색 방법]의 "ⓐ 전수 아님(코드 미마운트)" 구멍은
  **닫혔다** — 앞으로 서술 대신 실측을 붙인다.
- **★ 죽은 것 — 근거로 쓰지 마라**: **D-096**(모노라 repo 루트 포인터가 무의미. **`docs/frontend/rules.md`
  존치는 유효**) · **D-007·D-030**(계약 복사본. **만들지 마라** — `docs/spec/api-contract.md`를 직접 본다) ·
  **`rules [9.2]`·`[9.4]`·`[9.25]`**(C-075로 결번. **`mv` 써도 된다.** `[9.1]` 호스트 도구 판단은 존속).
- **★ 참조 표기가 바뀌었다(C-082, `rules [3.11]` 개정)**: **`<문서> [N.M]`**으로 쓴다 —
  `templates [15]` · `rules [6]` · `CLAUDE.md [4]`. 자기 문서 안이면 `[3.4]`. **`§`·`N절` 금지.**
  내 살아있는 지침 잔여 **0건**(`rules.md` 2건 · 킥오프 1건 정정 완료).
- **`rules [5.22]` 수신함 스캔이 3형태로 바뀌었다**: `[X → 나]` · **`[X → A, 나, B]`(나열형)** ·
  `[X → 전 역할]`. **발신자를 패턴에 박지 마라** — 그래서 QA가 5세션 놓쳤다.
- **★★ 코드는 내가 안 쓴다. CC가 쓴다** — `frontend/rules.md [8]` 신설(F-004, 사용자 지시).
  **U-020 교체를 내가 직접 편집했고 사용자가 잡았다.** 코드는 존치 판정됐다.
  **접근이 생긴 것을 권한으로 착각한 것이다.** `[8]`이 예외 사유 5종을 미리 막아 뒀다 — 특히
  *"사용자가 「알아서 진행해」라고 함"*은 **집행 재량이지 역할 변경 승인이 아니다.**
- **★ 마운트가 호스트 쓰기 직후 파일을 절단한다**(`outbox/RULES/001`). **5/5 재현.**
  커지면 잘리고 줄어들면 NUL 패딩. `touch`·`sync`로 안 풀린다. **`grep`은 새 내용을 찾는다 —
  데이터는 신선하고 크기만 낡았다.** 호스트 Read가 정본이다.
  **★ `typecheck`·`build` 빨간불을 코드 결함으로 믿지 마라** — 오늘 내가 그래서 멈췄다.
  검증하려면 **마운트 밖(`/tmp`)에 호스트 Read 내용으로 트리를 세워라.** 그리고 `node_modules`에
  **win32 rollup만 있어** 샌드박스에선 `@rollup/rollup-linux-x64-gnu`를 따로 받아야 `vite build`가 돈다.
- **U-020 교체 완료 실측**: `tsc -b --force`·`eslint --max-warnings 0`·`vite build` 전건 exit 0,
  103 modules. 산출 CSS에 A안 값 14색 **전건 0**, U-020 값 전건 실재. **브라우저 실동은 미검증.**
- **로그인 stub 세션**(`LoginPage` "임시 세션(사용자/관리자)" 버튼, `:54-64` 부근)은 auth feature 착수 시
  **반드시 제거**한다. 실 API 없이 세션이 서는 구멍이다.
- **호스트 Glob이 실재 폴더에 거짓 0건을 반환한다.** 존재·부재·수량은
  **`Grep(".", path=<폴더>, files_with_matches)`로만** 판단하라. 격상은 닫혔다(`_broadcast/002` [6]).
- **`outbox/007`은 VOID** — 전체 scaffold 지시라 실행하면 완성본을 재생성한다. CC에게 경로를 주지 마라.
  **`outbox/008`은 철회됨**(정정본 009).
- **persist 방침** — 기획 ⓑ-4 지적으로 교환비율과 분리됐고 **보안 게이트 2 항목으로 이관**됐다
  (`_broadcast/003` [4] 기획). **내 레인 아니다. 추적만.**
- 커밋 규칙: 사용자가 "커밋해야 하는 순간엔 무조건 커밋 내용 같이 달라"고 지시했다.

## 막힌 것

- **없다.** 총괄 ② 배치 대기. `_broadcast/004`상 프론트 다음 항목은 member 1-1(디자인 목업) 뒤다.
