# 프론트(F) handover

성격: 휘발성 맥락 — 파일에 있는 내용은 경로 참조만. 이력 없음(덮어씀).
갱신: 2026-07-16

## 진행 중

- **⓪-b 자기 파일 정리 + ① 재정리 보고** — 착수 2026-07-16 · 출처 `management/outbox/_broadcast/002` + `consulting/outbox/_broadcast/006` [3]
  - 지금 어디까지: **⓪-b 3건 전건 집행 완료**(`_archive/frontend/screen-route-map.md`(F-003) ·
    `frontend/rules.md` · `references/claude-code-kickoff.md`). 유형 2 2건은 `[3.12]` 사용자 확인 받았다.
  - 다음 한 수: `outbox/REFORM/001`로 ① 4항 보고 발신 → 총괄 배치(②) 대기.

## 대기 중

- **★ 사용자 지시로 정지(2026-07-16)**: *"디자인 회신이다 잠시 대기해라. **프론트 레포 옮길 예정이다.**
  총괄과 대화하겠다."* → **CC 토큰 교체 지시를 발행하지 마라.** repo 경로가 바뀌면 킥오프의 작업
  디렉터리(`D:\Java\finalcall-frontend`)와 CC 지시의 경로가 전부 어긋난다.
  **사용자·총괄 결정이 먼저다.** 이건 내 `REFORM/001` ⓒ1(repo 생성 시점)·ⓒ3(경로 확정)이 실제로 물린 것이다.
- **`ux/outbox/019-프론트-009회신-focusring-primaryfg-확정.md` — 도착했으나 미처리(정지 지시).**
  파일 실재 확인(`Grep(".", path="docs/ux/outbox")`). **파일 원문 미독 — 사용자 구두 중계만 받았다.**
  재개 시 **019 원문을 직접 읽고** inbox-log에 기재한다. 중계 요지(원문 대조 전이므로 근거 아님):
  - 안건1 `focus-ring` = **(a) `#FAF7D5` CSS 변수 유지 — 현행이 맞다, 바꾸지 마라.**
    정본 `[2.2]`에 있던 것을 드롭인 블록이 누락했고 디자인이 `[2.6]`에 보정.
  - 안건2 `primary.fg` = **(a) 정적값 `'#FAF7D5'`, `--color-primary-fg` 변수는 삭제.**
  - **디자인 요청(미이행)**: `text-primary-500` **3곳의 실물 문자열 1줄씩**을 디자인에 알려라 —
    새 팔레트에 그 자리가 없어 **기계적 치환이 안 된다.**
    → **이건 코드 실측이 필요하고 코드는 `D:\Java\finalcall-frontend`(별도 저장소·미마운트)에 있다.**
      repo 이동이 끝나야 실측 가능하다. `REFORM/001` [탐색 방법]에 적은 구조적 구멍이 여기서 물렸다.
- **`outbox/009` 대기 큐 상태**: 019가 `회신대상:`으로 지목하면 **닫힌다**(`[5.26]`). 재개 시 재산출.

## 다음 세션이 알아야 할 판단

- **CC 작업 프롬프트: 현재 유효한 것이 없다.** 스켈레톤(006)은 완주했다
  (`notes/cc-reports/006-skeleton-scaffold.md`). 다음 지시(A안 → U-020 토큰 교체)는 디자인 회신
  (`outbox/009`) 후 발행한다. **킥오프의 「이번 작업」 placeholder를 그때 채운다.**
- **`outbox/007`은 VOID다. CC에게 경로를 주지 마라** — 전문이 전체 scaffold 지시라 실행하면 완성된
  스켈레톤을 처음부터 재생성한다. **`outbox/008`은 철회됨** — 핵심 주장이 거짓이다. 정정본이 009.
- **호스트 Glob이 실재 폴더에 거짓 0건을 반환한다.** 에러가 없어 의심 트리거가 안 걸린다.
  존재·부재·수량 판단은 **`Grep(".", path=<폴더>, files_with_matches)`로만** 하라. Read·Grep은 정상.
  **총괄 격상은 발신 불요로 닫혔다**(`_broadcast/002` [6] — 이미 5개 파트가 독립 실측, 막히는 파트 0).
- 토큰 교체 여부는 **내 자율**(D-084). 디자인 017은 지시가 아니라 권고였고 그 권고(완주 후 교체)가 옳았다.
- **로그인 stub 세션**(스켈레톤 검증 전용 임시 세션 버튼)은 auth feature 착수 시 **반드시 제거**한다.
  잊으면 실 API 없이 세션이 서는 구멍이 남는다.
- **`frontend/rules.md`(구 `CLAUDE.md`)와 D-039가 충돌 예정이다** — D-039는 이 파일을 프론트 repo
  루트로 **이관**한다고 서 있다. 이관하면 `docs/frontend/rules.md`가 사라져 C-073 구조가 깨진다.
  **지금은 안 막힌다**(repo 미생성). repo 생성 시점에 총괄에 올린다.
- **기획이 `frontend-planning/`을 `_archive/`로 보내고 `skeleton-plan.md`·`screen-spec.md`를 회수한다**
  (C-073 [5]). **아직 실행 전이다.** 실행되면 내 킥오프 필독 3·5번 경로가 죽는다 — 기획 완료 보고를
  받으면 그때 갱신한다. 미리 고치면 지금 거짓이 된다.
- 커밋 규칙: 사용자가 "커밋해야 하는 순간엔 무조건 커밋 내용 같이 달라"고 지시했다.
  매 작업 블록 말미에 `git add docs/frontend/` + 메시지 블록을 낸다.

## 막힌 것

- **U-020 토큰 교체** — 디자인 `outbox/009` 회신 대기. 추측으로 값을 넣으면 포커스 링이 파손된다.
