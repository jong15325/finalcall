# 백엔드 handover

성격: 휘발성 맥락 — 파일에 있는 내용은 경로 참조만. 이력 없음(덮어씀).
갱신: 2026-07-16

## 진행 중
**기능 레인 전체 보류 — 사용자 지시(2026-07-16, "체계 점검 중이니 기능은 나중으로 미룬다").**
아래 2건은 착수분이며 완주가 기본값([5.45])이나 사용자 결정이 우선한다([1.2]). 코드 변경 없이 정지.

- **024 잔액 조회 · 025 게이트웨이 엣지 핸들러 — CC 완료 보고 흡수** — 착수 2026-07-16 · **보류** ·
  지금 어디까지: `notes/cc-reports/024`·`025` 도착·정독 완료, 이슈 3건 식별(024 이슈1 회신 필요).
  코드는 **워킹트리에 이미 있고 빌드 그린** — 미커밋 상태로 정지 ·
  다음 한 수(해제 시): 이슈1(인증 주체 식별 위치 (a)서비스 vs (b)컨트롤러) 표준 확정 → 총괄 완료 보고 발신
- **029 프로필 조회 — 미실행 확정** — 착수 2026-07-15 · **보류** ·
  지금 어디까지: 호스트 Glob 실측 결과 `cc-reports/`에 024·025뿐, 029 부재 = CC 세션 미투입 ·
  다음 한 수(해제 시): 프롬프트 stale 검증 후 재투입(024 이슈1 확정 선행)

## 보류의 비용 (사용자 판정 완료 — 비용 없음)
- **QA RETEST-1·2 → 비용 아님.** 025 완료 보고가 트리거인 건 맞으나(067·083) **QA가 아직 기동 전이라
  트리거할 대상이 없다**(사용자 판정 2026-07-16). QA 진행 시점에 함께 낸다.
- **미커밋 체류 → 사실이 아니었다. 내 오류.** git 오브젝트 조회로 실측: 024 = `4c94471`,
  025 = `2ad3e1b`·`4c94471`, 킥오프 경로 정정 = `5635ca0`. **대상 파일 9/9 전부 HEAD 트리에 실재.**
  미커밋 코드 없음. 아래 "내 오류" 참조.

## 내 오류 (2026-07-16) — 재발 방지
- **"024·025 코드가 미커밋으로 체류한다"고 브리핑에 썼다. 실측 없이.** 근거는 전 세션 handover의
  050·056 맥락(B-015~027 미커밋)이었는데, **그건 07-14 이야기고 그 사이에 커밋됐다.**
  **낡은 맥락을 현재 상태로 읽었다.**
- **정확히 D-086이 겨눈 유형**(부재·수량을 근거로 삼으며 탐색 방법을 검증하지 않음). 실측 3건째다
  (B-028 참조자 5→7, UserRepository 열거 누락, 이번 건). **앞의 둘은 코드 열거였고 이번은
  git 상태다 — 대상만 바뀌었지 형태가 같다.**
- **처방**: handover의 과거 맥락을 현재 주장으로 옮길 때는 **재실측 후 옮긴다.** 특히 "미커밋"·"부재"·
  "N건" 형태. git 상태 주장은 **`git cat-file -e HEAD:<경로>`(오브젝트 조회)로 실측** —
  `git status`·`git diff`는 마운트 절단 때문에 근거로 못 쓴다(`[9.2]`:304 — 구 `[9.6]`은 C-066 감축으로
  삭제됐고 내용은 `[9.2]` 본문에 흡수됐다. 규칙 무변경).

## 새로 안 것 — C-058 (docs 작업 로그 git 추적 제외)
- 커밋 `deb2e2a`가 `docs/*/inbox-log.md`·`docs/*/notes/handover.md`를 **.gitignore에 넣었다.**
  사유(gitignore 주석): 상시 변경이 IDE 커밋 목록을 오염 · handover는 휘발성이라 이력 가치 없음
  (`rules [8.6]`) · inbox-log 유실 시 비용은 중복 처리뿐. **`decision-log.md`·`outbox/`는 제외 안 됨**
  — `근거:` 링크의 정본이자 합의 이력이라서.
- **→ 내 브리핑에 영향**: `inbox-log`·`handover` 수정은 **커밋 대상이 아니다.** 이제 커밋 블록을
  낼 조건은 산출물(`outbox/`·`decision-log.md`·코드)이 바뀐 때뿐이다.

## 대기 중
- **없음.** RULES/001은 `consulting/outbox/RULES/001`로 회신 완료(C-063, (a) 채택).
- `notes/cc-reports/024` 이슈1 — 인증 주체 식별 위치 표준. **내 소관(파트 내 컨벤션, D-084 자율)**.
  외부 회신 대기 아님. 기능 레인 보류에 함께 묶임.

## ★★ Grep이 `handover.md`·`inbox-log.md`를 조용히 건너뛴다 — 스코프 주의
**좁은 스코프로 Grep하면 이 두 파일이 결과에서 사라진다. 에러 없이.** 오늘 실피해 1건.
- **재현**(호스트 Grep, 2026-07-16): `Grep("접두어 스캔", path=docs)` → 6건(`backend/inbox-log.md`·
  `consulting/notes/handover.md` **포함**). 같은 패턴 `path=docs/backend` → **0건**.
  `path=docs/consulting/notes` → **0건**.
- **결정적 대조**: `Grep("\[[1-9]\.\d{1,2}\]", path=docs/backend/notes)` **한 번의 호출**에서
  `claude-code-kickoff.md`(추적됨)는 잡히고 `handover.md`(gitignore됨)만 빠졌다.
  **같은 디렉터리·같은 호출 → 영향 집합이 정확히 `.gitignore`(C-058) 2패턴과 일치한다.**
- **메커니즘은 특정 못 했다.** `path=docs`에서는 잡히는 게 gitignore 가설과 모순된다. 단정하지 않는다.
- **실피해(오늘)**: C-066 감축 인용 점검을 `notes/` 스코프로 돌려 **"0건"이 나왔다.** 파일을 직접
  안 열었으면 "삭제 번호 인용 없음"으로 오보할 뻔했다. **실제로는 2건이었다**(`[9.6]`·`[5.14]`).
- **회피**: 이 두 파일은 **`Read`로 직접 열거나 파일 경로를 직접 지정**한다. 디렉터리 스코프 Grep의
  "없음"을 근거로 쓰지 마라 — **`[9.1]`이 호스트 도구를 정본으로 못 박은 바로 그 지점에서 침묵 실패한다.**
- 컨설턴트 회부: `outbox/RULES/003`. 미회신 상태면 이 항목 유지.

## ★ 대기 큐에 뜨는 오탐 1건 — 조사하지 마라 (C-063 확정)
`backend/outbox/014-erd섹션6-Flyway매핑정정요청.md`가 **출근 때마다 영구히 대기 큐에 뜬다. 무시한다.**
- 실질은 이미 해소됐다(mgmt/044로 방식 확정 → erd v0.7 [6] 동기화 완료, design/030·mgmt/093).
- 뜨는 이유: 044가 정보 공유라 `회신대상:`을 안 달았고, `[5.18]`(발신 후 수정 금지) 때문에 **지금 소급해
  달 수 없다.** C-063이 `[5.20]`을 "닫는 행위"로 고쳤으나 **앞으로만 적용**이다.
- **이건 결함이 아니라 확정된 잔여다.** 컨설턴트가 RULES/001 회신에서 명시적으로 "무시하고 handover에
  한 줄 남겨라"라고 했다. **다음 세션이 같은 걸 또 조사하면 그 시간이 통째로 낭비다.**

## 다음 세션이 알아야 할 판단
- **[신규] spec 3종 경로 이동**: `docs/spec/domain-spec.md`·`erd.md`·`api-contract.md`(내용·버전 무변경
  v0.5 / v0.7 / v1.4). `notes/claude-code-kickoff.md` 필독 경로 정정 완료.
- **[신규·중대] bash 마운트 절단은 D-090보다 나쁘다.** "낡음"이 아니라 **행 중간 절단**이고, **이 3파일만**
  물려 있다(정상 파일이 섞여 있어 bash 안에서는 판별 불가). `erd.md` = bash 341줄 / 호스트 409줄,
  **[5] 인덱스 표 + [6] Flyway 매핑 전체가 없다.** → **V5 이후 Flyway 채번을 bash로 확인하면
  erd v0.6 구조 오류가 3회차로 재현된다.** 호스트 Read/Grep만. CC 킥오프에도 이 경고를 박아 뒀다.
- **규약 전면 개정(C-036~054)**: `common/rules.md`·`templates.md`가 2026-07-15자로 갈렸다. 내 사용분 변경:
  outbox 경로가 `<스레드>/NNN-주제.md`([5.13], **앞으로만** — 기존 flat 001~033은 소급 이관 금지),
  스레드명은 대문자 한 단어(**구 `[5.14]` — C-066 감축으로 삭제. 규칙 아님, 관행으로만 유지.
  내 다음 발신 = `MEMBER`**), 대기 큐는 손 관리 금지·grep 파생([5.26]),
  브리핑 형식 개정(templates [7]). 이 handover도 새 templates [8] 형식으로 재작성했다.
- **025 완료 시 "QA RETEST-1·2 트리거" 통지 의무**(067·083) — **cc-reports/025가 이미 명시했다.**
  내 총괄 완료 보고에 그대로 실어야 CLOSE 경로가 열린다. 잊으면 QA 엣지 포맷이 안 닫힌다.
- **025 검증 한계(투명성)**: `clean build` 미완주(QueryDSL clean-vs-compile 레이스 + `build/` 점유
  잔여 java 프로세스) → 증분 `build`로 그린. D-086 적용해 완료 보고에 이 한계를 명시할 것.
- **024 이슈1이 두 방식의 갈림길**: 로그아웃 선례는 (b) 컨트롤러 추출인데 내 024 지시서가 (a) 서비스
  직접 읽기를 문언으로 못 박아 CC가 (a)로 구현했다. **지시서가 선례를 안 보고 쓰였다** — 프로필·수정·탈퇴가
  같은 컨트롤러를 확장하므로 여기서 갈리면 member 전체가 오염된다. two-way door·소폭이니 지금 정한다.
- **내 반복 결함 패턴**: 열거·수량을 근거로 삼으며 탐색 방법을 검증하지 않는다(실측 2건: B-028 참조자
  5→7, UserRepository 열거 누락). **D-086이 이 유형을 정확히 겨눈다.** import 기준 grep은 동일 패키지
  사용처를 구조적으로 못 본다 — 심볼 기준·컴파일 검증 병행.
- **프롬프트 stale 검증은 착수 전 필수.** 매번 뭔가 나왔다(024·029·030 전부). 029 재투입 전 필수.
- **D-088 실효 조건**: 파일을 고쳐도 **실행 중 CC 세션의 컨텍스트는 갱신되지 않는다.** 새 세션엔 킥오프
  전문, 기존 세션 재사용 시엔 갱신분을 델타로 전달. (kickoff 경로 정정분도 동일 — 재사용 세션에 델타 필요.)
- **탈퇴 유닛 설계 시**: D-080(잔액 잔존과 무관하게 탈퇴 허용, `balanceForfeitAcknowledged` 필수,
  잔액 0이어도). MEMBER_002 차단 조건(진행 중 경매·홀드·주문)은 bid/auction/order 미구현이라 검사 대상이
  비어 있다 — 구조만 두고 도메인 완성 시 채우는 방식 허용(총괄 072, 자율. **완료 보고에 방식 명시 의무**).
- **MemberErrorCode는 아직 없다**(B-029로 024에서 제외). 프로필 수정·탈퇴 유닛에서 계약 [5] 확정 번호로
  신설: MEMBER_001=닉네임 중복(409), MEMBER_002=진행 중 거래 보유(409).
- **D-089 집행 필드**: 다음 B 발번부터 필수, 공란 금지. B-030이 첫 적용례.
- **D-084**: 파트 내 유닛 순서는 내 자율. 총괄이 유닛 순서를 지정하면 이의 대상.
- **D-083**: 진행 중 작업이 총괄 지시와 충돌하면 **이의 제기는 의무**. 침묵은 규약 위반.

## 보안 게이트2 이월 (변동 없음)
B-016·B-017·B-018·B-025 + m3. 비밀번호 변경은 범위 밖(068 — B-016과 묶어 게이트2).

## 재개 필독 (경로·순서)
1. 이 파일
2. `common/rules.md` · `common/templates.md`(2026-07-15 개정판) · `spec/` 3종(**`docs/spec/` — 호스트 Read만**)
3. `notes/cc-reports/` 스캔 → `inbox-log.md` → 수신함 스캔(호스트 Glob/Grep, `[X → 백엔드]`·`[→ 전 역할]`·`B-` 언급)
4. `decision-log.md` B-028·B-029·B-030
5. 확정 스펙 기준: api-contract **v1.4**([2.5] 회원 리소스·[4.4] 잔액·[1.6] 엣지 오류) · erd **v0.7** ·
   domain-spec **v0.5**([6.1]) + `CLAUDE.md`([5] 컨벤션·[7] 스타일)
6. 진행: 024 이슈1 확정 → 024·025 총괄 완료 보고 → 029 재투입 → 프로필 수정·탈퇴 분해
