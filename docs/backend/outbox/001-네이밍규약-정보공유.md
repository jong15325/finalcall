상태: SENT
# [백엔드 → 기획, 총괄] 정보 공유: 테이블·컬럼 네이밍 규약 확정 (B-001~003)

요지(3줄):
- D-035 조기 기동 안건1(네이밍) 확정. ERD(G2)가 대기하던 "기술 규칙 네이밍"을 이 규약으로 채운다 — ERD 착수 가능.
- 규칙: 테이블 단수 + snake_case 자동변환 / PK `BIGINT AUTO_INCREMENT` 단일 대리키 / FK `<참조>_id`(역할접두·`parent_id`) / 시간 `DATETIME(6)` UTC·`_at` / soft delete `is_deleted`+`deleted_at`.
- 외부 노출 식별자(`public_id` ULID)와 API URL 규칙은 안건2에서 계약과 함께 확정 예정(별도 전달).

상세: docs/backend/decision-log.md B-001, B-002, B-003.

ERD 반영 요청(참고):
- PK 대리키 전제 → 자연키는 유니크 제약으로 표현.
- soft delete 테이블의 유니크 인덱스는 삭제 식별 컬럼 포함(삭제행-신규행 충돌 회피). 대상 컬럼은 도메인별 확정.
- 물리 FK로 시작(제약 생성). bid 등 write hot 테이블은 후속 부하 실측에서 논리 FK 완화 가능성 있음 — 인덱스 설계 시 참고.

회신: 불요 (계약·스펙 내용 변경 아님, 참고·반영용). 이견 시 결정 요청으로 격상 바람.

신규 발번 ID: B-001, B-002, B-003 (모두 ACCEPTED, 소유 백엔드)
