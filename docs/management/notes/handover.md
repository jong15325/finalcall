# 총괄 handover (2026-07-12)

진행 중: design/outbox/012(아이템 소절 목차 안건 3건, D-048 예외 첫 실행) 검수 완료,
사용자 결정 직전에 세션 교체됨. 총괄 검수 의견 — 안건 1은 (a) 채택 권고(spec은 개념·규칙만,
구체 taxonomy 값은 ERD 정본), 안건 2는 (a) 채택 권고(기획이 가상 시드 초안 → 사용자·총괄 확정),
안건 3 목차는 승인하되 "8) 소유·이전(인벤토리 여부, 낙찰 후 인스턴스 귀속, 재판매 흐름)" 추가
조건 — 이게 없으면 ERD에서 item_instance 소유자 FK 설계 근거가 없다.
새 세션의 첫 작업: 이 안건을 사용자에게 재상정 → 확정 → D 발번 → 회신(다음 번호 outbox/022).

대기 중: 총괄 SENT 중 미회신 없음(017~021 발신 완료, 011까지 전부 ANSWERED 확인).
기획은 domain-spec 초안을 병렬 작성 중(아이템 외 섹션). P-008(홀드 해제 규칙) 자율 발번
통지받아 인덱스 등재 완료. 컨설턴트 대기 모드.

휘발성 맥락:
- 세션 bash(마운트) 뷰는 호스트보다 구버전일 수 있음 — 절단 의심 시 호스트 Read로 교차
  검증(D-043), 커밋 대행은 샌드박스가 최신일 때만. 파일 쓰기는 bash 쪽이 host로 확실히 전파됨.
- Glob이 한글 파일명 경로에서 빈 결과를 반환 — outbox 스캔은 Grep "→ 총괄\]"
  files_with_matches로 할 것.
- 사용자 성향: 추천안을 대체로 수용하나 관심 영역(결제·화폐 D-051, 아이템 D-048)은 직접
  확장·결정함. 결정 전 "대기" 요청 시 재촉하지 말 것. 브리핑 템플릿(D-055) 매 응답 필수.
- 커밋 메시지는 docs(총괄) 형식(D-029), D 발번 시점마다 커밋, 푸시는 사용자(자격증명 없음).

재개 필독(순서대로):
1. docs/management/collaboration-guide.md (규약 전체)
2. docs/management/decision-index.md (ID·게이트·추적 표 현황)
3. docs/management/decision-log.md 중 D-044~059 (도메인 확정 결정)
4. docs/management/notes/handover.md (이 파일)
5. docs/design/outbox/012-아이템소절-목차안건.md (첫 처리 대상)
