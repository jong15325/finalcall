# 총괄 handover (2026-07-13)

## 진행 중
- G1·G2·G3 전부 통과. 구현 단계 G4-n. 크리티컬 패스 = **백엔드 auth 구현**(사용자 "다른 작업 홀드,
  백엔드 집중" 지시).
- 백엔드: G4-1 auth 유닛 구현 중(Claude Code, backend/outbox/006~012). 006 완료. 순서
  006→007→{008·009·011}·012(SCG 게이트웨이). 010(refresh)은 계약 v1.1 확정으로 최종화 가능.
  auth 묶음 완료 시 총괄 완료 보고 예정.
- 기획: 046 처리 예정 — domain-spec v0.4·erd v0.5·api-contract **v1.2**(등급 제거 D-073 +
  목록/상세 응답 스키마) 일괄 반영 → 완료 보고 대기. v1.2 = 프론트 복사본 갱신·보안 델타 통지 대상.
- 디자인: 047 — 등급 색을 속성(4속성)/레벨 기준 재조정. 비주얼 방향·기본 테마는 사용자가 디자인
  대화와 직접 논의·재디자인(목업 갱신 → 총괄+사용자 최종, D-072).

## 대기 중 (총괄 SENT 미회신 / 후속)
- 046(기획 v1.2)·047(디자인)·048(전역할 D-074 전파) — 회신/적용 대기.
- 백엔드 auth 완료 보고 대기.
- 사용자 결정 큐(홀드): 프론트 repo 셋업 시점 · 실시간 푸시 채널(F-001 폴링 MVP) · 비주얼 방향/테마.
- QA 미기동 — 킥오프 프롬프트 필요 시 총괄 작성(보안·프론트·디자인 킥오프는 management/prompts/에 있음).
- 컨설턴트 대기 모드(D-042).

## 휘발성 맥락
- D-074(이번 세션 신설): 총괄이 파트 간 순서 능동 지정, 선행 미완료 시 후행 착수 금지. 앞으로
  총괄이 시퀀싱 주도. 파트별 진행 순서 로드맵은 사용자에게 채팅으로 제시함(파일 미저장 — 요청 시 저장).
- D-072 디자인 프로세스: 디자인은 기획 경유 + 시각 예시(목업) 의무 + 최종은 총괄+사용자. 디자인이
  docs/ux/mockups/ 에 목업 올리면 **총괄이 show_widget(visualize 도구)으로 렌더해 사용자에게
  보여줌** — 텍스트 아닌 "보고 고르게".
- 아키텍처 확정 경위: D-064(MSA) 발번 → 당일 D-065 롤백(단일 서비스) → D-068(SCG 엣지 게이트웨이,
  인증은 서비스 유지). 최종 = 모놀리식 + SCG 엣지(rate limit·직접접근 차단·라우팅), 인증 SecurityContext.
- 커밋: D 발번마다 사용자 실행(D-061), path-scope `git add docs/management/`(+ CLAUDE.md/ux 변경 시 병기).
  샌드박스 git 인덱스 오류로 `git status` 불가(git log는 됨) — 커밋은 사용자가 인텔리제이로.
- 수신함 스캔: Grep 패턴은 `총괄\]`(다중 수신 `[X → 기획, 총괄]` 포착 위해 `→ 총괄\]` 아님), glob `**/outbox/**`.
  비-outbox 문서가 걸리면 제외. inbox-log가 처리 경계.
- 사용자 성향: 결제·아이템·아키텍처·디자인 방향은 직접 결정(대기 요청 시 재촉 금지). 기술·보안 정합은
  총괄 위임(예 D-070 refresh, D-073 등급). 매 응답 끝 브리핑 4줄(+초안 요지, D-063) 필수.

## 재개 필독 (순서대로)
1. docs/management/collaboration-guide.md (규약 — 특히 §7 D-074 시퀀싱·선행 게이팅, §8 D-069 핸드오프)
2. docs/management/decision-index.md (ID·게이트 현황·추적표)
3. docs/management/decision-log.md D-060~074 (이번 세션 결정)
4. docs/management/inbox-log.md (처리 경계 — 어디까지 처리했는지)
5. docs/management/notes/handover.md (이 파일)
6. 확정 스펙 domain-spec·erd·api-contract (기획 v1.2 반영 후 최신 확인)

재개 첫 작업: "수신함 확인" → 기획 046 완료 보고(v1.2)·백엔드 auth 완료 보고 처리. v1.2 확정 시
프론트 복사본 갱신·보안 델타 전파. 이후 백엔드 다음 도메인 순서 지정(D-074).
