# 총괄 handover (2026-07-14)

## 진행 중 (2026-07-14 갱신 2)
- G1·G2·G3 통과, 구현 G4-n. auth(G4-1)·SCG 게이트웨이(D-068) 구현 완결·검수 통과(019·021). G4-1 게이트는 QA 검증으로 닫는 중.
- 사용자 레인 재편(058): QA 기동(auth+게이트웨이 검증)·프론트 스켈레톤·디자인 초안 병렬. 백엔드 구현(B) 대기.
- 조직(D-079, D-077 되돌림): 7역할(총괄D/기획P/백엔드B/프론트F/디자인U/QA Q/보안S). 기획(P)=서버도메인+프론트/화면 기획+디자인 조율 통합. 계약 정본=P. PF 동결(frontend-planning/는 P 소유 존치, 스켈레톤 기획 063·screen-spec은 P 인계).

## 대기 중 — 총괄 SENT 미회신 / 기대 보고
발신 대기(완료 보고):
- QA[신규 기동]: auth+게이트웨이 검증·defects → G4-1 게이트 판정(058, 킥오프 qa-kickoff-prompt.md).
- 프론트(F): 저장소 스켈레톤(Vite+React+TS, D-032) 착수(058).
- 디자인(U): 초안 진행 + grade 정합(054, U-004 SUPERSEDE).
- 백엔드 기획(P): api-contract v1.3 엣지 오류 명세(057) → 확정 시 프론트/PF/QA 전파.
- PF: 프론트·디자인 병렬 조율·screen-spec 정합.
백엔드(B): 구현 대기(다음 도메인 홀드, 사용자 지시). QA defects 대응만 예외. B-015~027 자기 로그 기재 미완(재요청).
사용자 결정 큐(홀드): 인덱스 B-001~014 백필 · 실시간 채널(F-001 폴링) · 비주얼 방향/테마 · v1.2 스펙 3종 지식 동기화(각 대화 업로드).
에스컬레이션 예상: 계약 v1.3(엣지 429), QA defects, PF 화면 정합, 보안 게이트2 이월분(B-016·017·018·025·m3·SEC-008·011).

## 휘발성 맥락
- D-074(이번 세션 신설): 총괄이 파트 간 순서 능동 지정, 선행 미완료 시 후행 착수 금지. 앞으로
  총괄이 시퀀싱 주도. 파트별 진행 순서 로드맵은 사용자에게 채팅으로 제시함(파일 미저장 — 요청 시 저장).
- D-072 디자인 프로세스: 디자인은 기획 경유 + 시각 예시(목업) 의무 + 최종은 총괄+사용자. 디자인이
  docs/ux/mockups/ 에 목업 올리면 **총괄이 show_widget(visualize 도구)으로 렌더해 사용자에게
  보여줌** — 텍스트 아닌 "보고 고르게".
- 아키텍처 확정 경위: D-064(MSA) 발번 → 당일 D-065 롤백(단일 서비스) → D-068(SCG 엣지 게이트웨이,
  인증은 서비스 유지). 최종 = 모놀리식 + SCG 엣지(rate limit·직접접근 차단·라우팅), 인증 SecurityContext.
- 커밋: D 발번마다 사용자 실행(D-061), path-scope `git add docs/management/`(+ CLAUDE.md/ux 변경 시 병기).
  샌드박스 git 인덱스 오류로 `git status` 불가(git log는 됨) — 커밋은 사용자가 인텔리제이로.
- 수신함 스캔: Grep 패턴은 `총괄\]`(다중 수신 `[X → 기획, 총괄]` 포착 위해 `→ 총괄\]` 아님), glob `**/outbox/**`.
  비-outbox 문서가 걸리면 제외. inbox-log가 처리 경계.
- 사용자 성향: 결제·아이템·아키텍처·디자인 방향은 직접 결정(대기 요청 시 재촉 금지). 기술·보안 정합은
  총괄 위임(예 D-070 refresh, D-073 등급). 매 응답 끝 브리핑 4줄(+초안 요지, D-063) 필수.

## 재개 필독 (순서대로)
1. docs/management/collaboration-guide.md (규약 — 특히 §7 D-074 시퀀싱·선행 게이팅, §8 D-069 핸드오프)
2. docs/management/decision-index.md (ID·게이트 현황·추적표)
3. docs/management/decision-log.md D-060~074 (이번 세션 결정)
4. docs/management/inbox-log.md (처리 경계 — 어디까지 처리했는지)
5. docs/management/notes/handover.md (이 파일)
6. 확정 스펙 domain-spec·erd·api-contract (기획 v1.2 반영 후 최신 확인)

재개 첫 작업: "수신함 확인" → 위 발신 대기 큐 순서로 도착분 처리(백엔드 012 → 디자인/프론트 grade
정합 → PF 후속). 백엔드 012 완료 시 QA 기동 여부 사용자 확인 후 다음 도메인 순서 지정(D-074).
보고는 도착 순서대로 처리하되 크리티컬 패스(백엔드) 우선. 에스컬레이션은 4기준으로 판정.
