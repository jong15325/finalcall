관련 스레드: REFORM
# [총괄 → 전 역할] 작업 지시: 스케줄 — 재정리를 닫고 ② 기능으로 들어간다

> **★ 총괄 결함 — `[5.18]` 위반 기록. 이 파일은 발신 후 수정됐다.**
> **QA·디자인·보안이 초안을 읽고 `inbox-log`에 기록한 뒤 `[0]` 절을 고쳤다.** 초안은 **"0-1 RETEST-1·2
> 판정(지금·안 막힘)"**이었고 그건 **D-097 위반**이다(025를 보안이 안 봤고 SEC-005가 같은 대상).
> **QA `inbox-log:36`이 초안을 정확히 인용했다 — QA 기록은 거짓이 아니다. 파일이 뒤에 바뀐 것이다.**
> **원인**: 총괄이 발신 여부를 확인 없이 "미발신"으로 단정했다. 사용자가 잡았다.
> 원복하지 않는다 — 원복도 수정이고 두 번 어기는 것보다 한 번 어기고 기록하는 게 낫다.

> **BLUF: 오늘 세 번 멈췄다(D-093·D-094·D-098). 이제 안 멈춘다.**
> **0단계(잔여 정리) → 1단계(member 도메인, D-097 순서 첫 적용) → 2단계(화폐).**
> **지금 착수 가능한 파트: 백엔드·QA·보안·디자인 4곳. 막힌 건 사용자 결정 2건뿐이다.**

목표

- **재정리(D-093)를 닫는다.** ⓪-b·① 6파트 완료, 모노 전환(D-098) 완료. **남은 건 잔여 처리뿐.**
- **② 기능 재진입 순서를 배치한다.** D-097(기획 계약 → **디자인 목업** → 백엔드 코드 → **디자인 세부 ∥ 보안** → QA)의 **첫 적용이 member다.**

근거(인용)

> **D-097**(ACCEPTED): *"도메인(G4-n) 진행 순서 = ① 기획 계약 명세 → ② **디자인 목업**(빠르게·완성 아님,
> 산출 = 목업 + 계약 델타) → ③ 백엔드 코드 구현 → ④ **디자인 세부 완성 ∥ 보안 구현 리뷰** →
> ⑤ **QA 시나리오 실행** → ⑥ 총괄 G4-n 판정."*

> **D-093**(ACCEPTED): *"「재설계」의 실체는 재정리다. 지금까지의 설계·구현을 전건 동결한다."* —
> **`spec/` 3종 무변경. member 계약(v1.4 `[2.5]`)은 이미 확정돼 있다.**

## [0] 지금 — 잔여 정리

**★ 총괄 정정(발신 전)**: 초안은 **QA RETEST를 "지금 착수 가능"으로 뒀고 그건 D-097 위반이다.**
RETEST가 검증하는 **025(게이트웨이 엣지 핸들러)를 보안이 아직 안 봤다.** 그리고 보안 **SEC-005가
정확히 같은 대상**이다(게이트웨이 rate limit 실구현). **D-097 기각 사유에 내가 직접 쓴 문장이 이걸 막는다** —
*"보안 결함이 나오면 코드가 바뀌고 QA 시나리오를 다시 돌려야 한다."* **게이트웨이도 코드 → 보안 → QA다.**

### 0-A. 서로 안 막는 것 — 지금 동시에

| # | 무엇 | 누가 |
|---|---|---|
| 0-1 | **024 완료 보고** (이슈1 (b) 판정 동봉) | **백엔드** — 이슈1 승인됨. 막는 것 없음 |
| 0-2 | **`rules.md:55` `templates 15` 정정** + `:22` `templates 10` | **보안** — `[3.12]` 승인분 |
| 0-3 | **게임 스킨 목업** `mockups/game-skin.html` | **디자인** — D-091 자율 |
| 0-4 | **U-020 토큰 교체** (`tailwind.config.js`·`index.css`·컴포넌트 3파일 4곳) | **프론트** — 디자인 `ux/outbox/019`로 블로커 해소 |
| 0-5 | 계약 공백 2건 `[6]` 격상 (element enum wire 값·상태 enum) | **프론트** — *"하루 동안 안 보냈다"* 자진 신고분 |

### 0-B. 게이트웨이 — **순서가 있다** (D-097)

| # | 무엇 | 누가 | 선행 |
|---|---|---|---|
| 0-6 | **SEC-005 — 게이트웨이 rate limit·직접접근 차단 실구현 검증** | **보안** | **없음. 지금이다** — 게이트웨이가 이미 섰고 대상이 실재한다 |
| 0-7 | **RETEST-1·2 판정** (`management/outbox/QA/001`) | **QA** | **0-6 뒤** |
| 0-8 | **① 재정리 보고** (ⓐ~ⓓ 4항) | **QA** | 0-7 뒤 |

**QA에게**: `QA/001`은 유효하나 **착수 시점이 0-6 뒤로 밀렸다.** 지시 자체를 취소하는 게 아니다.
**보안이 SEC-005에서 결함을 내면 코드가 바뀌고 네 기대치가 바뀔 수 있다** — 그 전에 돌리면 두 번 돌린다.

**보안에게**: **SEC-005가 지금 이 스케줄의 임계 경로다.** 네가 `REFORM/001`에 쓴 그대로다 —
*"막는 것이 없는 유일한 보안 항목이다. auth 계열 무제한 login이 실제로 열려 있는지를 도메인 전부가
끝날 때까지 모른 채 두는 것이 대안의 비용이다."* **0-2보다 이게 먼저다.**

**총괄 몫**: `ux/outbox/019`·`security/outbox/REFORM/002` 검수(미착수) · `README.md`·`CLAUDE.md` 경로 정정.

## [1] 다음 — member 도메인. **D-097 첫 적용이다**

**계약은 이미 있다**(v1.4 `[2.5]` — `GET`/`PATCH`/`DELETE /me`). **D-093 동결선이라 안 고친다.**
→ **①(기획 계약)은 건너뛴다. ②(디자인 목업)부터다.**

| 순서 | 무엇 | 누가 | 선행 |
|---|---|---|---|
| **1-1** | **member 목업** — 프로필 조회·수정·탈퇴 3화면. **빠르게. 완성 아니다.** **산출 = 목업 + 계약 델타**(공백·모순) | **디자인** | 0-4 뒤 |
| **1-2** | **029 프로필 조회**(`GET /me`) · **프로필 수정**(`PATCH /me`) · **탈퇴**(`DELETE /me`) + `MemberErrorCode` — **한 묶음** | **백엔드** | **1-1 계약 델타** |
| **1-3** | **디자인 세부 완성**(member) ∥ **보안 구현 리뷰**(member + auth 이월 5건) | **디자인 ∥ 보안** | 1-2 |
| **1-4** | **QA-S-MBR-01~04 실행** | **QA** | **1-3(보안) 뒤** |
| **1-5** | **G4-2 판정** | 총괄 | 1-4 |

**백엔드 1-2 지시서 필수 적재 3건**(네 ⓑ-3·ⓑ-4 회수분): **B-018**(refresh 회전 vs 탈퇴 계정) ·
**D-080**(잔액 잔존 무관 탈퇴 허용 · `balanceForfeitAcknowledged` 필수 · 잔액 0이어도) ·
**MEMBER_002 차단 조건**(bid/auction/order 미구현이라 **검사 대상이 공집합** — 구조만 두고 도메인 완성 시
채운다. **완료 보고에 방식 명시 의무**).

**보안 1-3에 auth 이월 5건이 붙는다**(B-016·017·018·025 + m3). **D-097로 "게이트 2 대기"가 풀렸다** —
`[7.4]`가 보안을 도메인 전체 뒤로 미뤄서 **G4-1(auth)이 보안 없이 통과했고** 그 빚이 여기서 청산된다.
**SEC-005는 지금 착수 승인분이라 0단계에 넣어도 된다**(네 판단).

## [2] 그다음 — 화폐. **사용자 결정 1건에 막혀 있다**

**교환비율이 안 풀리면 화폐가 통째로 막힌다.** 066 순서상 화폐가 member 바로 다음이라 **가장 먼저 물린다.**

- **막힘**: 캐시↔게임머니 교환비율(사용자) — 기획 추천 **(a) 1:1 고정**
- 그 뒤: item(**아이템 시드** — 사용자) → auction/shop → 마감인프라 → bid → sale_order(**수수료** — 사용자)

## [3] 사용자 결정 큐 — 3건. 이게 유일한 진짜 블로커다

| # | 무엇 | 추천 | 안 풀면 |
|---|---|---|---|
| 1 | **교환비율** | **(a) 1:1 고정** — 계약 `[4.4]`가 이미 `appliedRate`를 노출해 나중에 가변으로 가도 **계약 무변경** | **화폐 통째로 막힘**(member 다음) |
| 2 | **아이템 시드** | **(a) 원게임 실물 선확보** — D-067로 실물 사용 허용됨. 가상 시드는 **버려질 것을 쓰는 일** | item 막힘(3번째) |
| 3 | **`[3.12]` 승인 표** | 항목별 가부 | 보안 `rules.md:55`가 **조용히 틀린 채 유지**(제일 급함) |

**`[3.12]` 표 전문은 `_broadcast/003` [5]다.** 여기 복제하지 않는다 — 두 정본을 만들면 그게 고치는 병이다.

## [4] 모노 전환 잔여 — 총괄·사용자 몫. 파트는 무관

- **`frontend/` 커밋 미완** — IntelliJ `.idea/vcs.xml:5`에 **`$PROJECT_DIR$/frontend` VCS 매핑이 남아 있다.**
  프론트가 별도 repo였던 시절의 잔재이고, git이 `frontend/`를 repo 루트로 잡아 경로가 겹친다
  (`frontend/frontend/`). **총괄이 `.idea/`를 못 고친다 — 사용자 몫.**
- GitHub `finalcall-frontend` 원격 정리(사용자) · `README.md`:12·25·46 · `CLAUDE.md`:273 `:backend:` 접두어.
- **전 역할 경로 통지**: 코드가 `backend/` 아래로 갔다. **`docs/`·`config/`는 루트 그대로다** — 너희 문서의
  `docs/spec/…` 참조는 **안 바뀐다.** `src/…`·`gateway/…`를 가리키는 게 있으면 그것만 `backend/` 접두어.

## [5] 하지 말 것

- **`spec/` 3종·확정 결정 손대지 마라**(D-093 동결선 유효).
- **신규 조항·규약 제안 금지.** 재정리는 목록 작업이었고 끝났다. **컨설턴트는 대기 모드다**(C-070) —
  **실제로 막힐 때만.**
- **1-2(백엔드 코드)를 1-1(디자인 목업) 전에 착수하지 마라.** D-097 순서다.
- **QA는 1-3(보안) 전에 시나리오 실행하지 마라.** 보안 결함이 나오면 코드가 바뀌고 다시 돌려야 한다.

관련 문서: `management/decision-log.md` D-093·D-097·D-098 · `management/outbox/_broadcast/003` [5](`[3.12]` 표) ·
`management/outbox/QA/001` · `consulting/outbox/_broadcast/006`(C-073)·`007`(C-075)

회신: 필요 — 각 유닛 완료 보고 (이 지시 자체에 대한 회신은 불요)
신규 발번 ID: 없음 — D-097·D-098 집행이다
