상태: SENT
# [총괄 → QA] 작업 지시: V4 완료 — QA-001 재검증 착수 (082 경로 발동)

## 트리거
백엔드 V4 단위 완료(backend/outbox/033, 커밋 `df9836c`). **082에서 약속한 "총괄 검수 후 QA 재검증 통지" 시점이다.**

근거(인용): backend/033 — *"MBR-06 실행 증거: 단언 레벨 **DB 레벨** — `saveAndFlush`로 선검사(`existsBy`)를 우회해 제약을 직접 때리고 `DataIntegrityViolationException` 단언 / DB **실 MySQL(Testcontainers)** + `@AutoConfigureTestDatabase(replace = NONE)`로 H2 대체 차단 / 통과 여부 **통과**(그린)"*

## 총괄 검수 결과 (083 조건 1 개별 확인 — 약속분)
- 테스트 실재 경로: `src/test/java/com/finalcall/domain/member/UserRepositorySliceTest.java` (MBR-06 2종, 74~91행)
- **dev 그린 대체 아님** — DB 레벨 단언·실 MySQL 확인. 조건 충족.
- **증거 출처 한계**(백엔드 자진 명시): 테스트 실재·구현 방식은 백엔드가 직접 정독 확인, **통과 여부는 Claude Code 실행 보고 + 커밋 검증란이 근거이며 백엔드 재실행분 아님.** 이 한계를 알고 재검증하라 — **네 독립 재검증이 이 공백을 메운다.**

## 작업
1. **QA-001(Major, OPEN) 재검증** — V4가 FIX인지 판정. 불일치 잔존 시 결함 유지, 해소면 FIXED 전환.
2. **QA-S-MBR-05·06 재검증**(082 대기분). 특히 **MBR-06(활성 유일성)** — D-081이 기각한 (a)안이 실제로 배제됐는지가 핵심이다.
3. **재가입 생애주기**: `ReSignupIntegrationTest`(재가입 성공 / 재가입 후 로그인 — 다건 파손 부재) 신규 존재. 네 MBR-05 기대치와 대조하라.

## 참고 — 083 조건이 무력한 테스트를 막았다 (네 지적이 원인)
백엔드 보고: *"내 030 프롬프트 초안의 (c) 케이스는 signup API 경유라 **B-024 선검사가 먼저 409를 던져 DB UK를 타지 않았다.** 그대로 갔으면 (a)안 오적용을 못 잡는 무력한 테스트가 됐다."*

네 지적("(a)안이면 MBR-01~05는 전부 초록이고 MBR-06만 뚫림을 드러낸다") → 총괄 083 조건(DB 레벨 증거) → 백엔드가 API 경유의 무력함 발견·교체. **세 파트가 이어 붙어 유일한 검출 지점을 살렸다.**

## 범위 제한
- **member 시나리오(MBR-01~04) 실행은 아직이다** — 백엔드 프로필·수정·탈퇴 3유닛 미착수. 이번은 **재가입·활성 유일성 델타만**.
- RETEST-1·2(게이트웨이 엣지)는 별건 유지 — 백엔드 025 완료 통지 시 별도 지시.

## 신규 규약 통지
**D-090** — 저장소 파일의 목록·내용·편집 위치 판단은 **호스트 도구(Read·Grep·Glob·Edit)가 정본**. bash 마운트 뷰 조회 결과를 판단 근거로 쓰지 마라. 한 세션에 stale 5건·실피해 2건. **네 검증 작업에 직접 영향**이 크다 — 부재·수량 주장이 검증의 본체이기 때문이다(D-086 + D-090 결합). 정본: decision-log D-090, 전파 092.

회신: 필요 — QA-001 재검증 결과(FIXED 전환 여부) + 탐색 방법 명시(D-086).
신규 발번 ID: 없음 (082 경로 발동)
