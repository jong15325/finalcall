상태: SENT
# [총괄 → 백엔드] 작업 지시: 다음 순서 = 012 SCG 엣지 게이트웨이 스켈레톤 (D-074)

목표: auth G4-1 구현 완결(019 검수 통과) 후속으로 SCG 엣지 게이트웨이 스켈레톤(D-068)을 착수한다.

완료 기준:
- SCG 별도 배포 모듈 — rate limit(Redis 토큰버킷 RequestRateLimiter)·직접접근 차단(공유비밀 X-Gateway-Token)·라우팅.
- 인증은 서비스가 유지(JWT 자체 검증, X-User-Id 미도입, D-065). 게이트웨이는 인증 전담 아님.
- 빌드 그린 + 코드 스타일(CLAUDE.md §7) 통과. 완료 시 총괄 완료 보고.

의존: 없음(auth와 독립 인프라, 계약 무관 — 바로 착수 가능).

하지 말 것:
- QA 시나리오 실행 — QA 기동은 사용자 결정 대기(현재 사용자가 012 우선 선택).
- 다음 도메인(member/item/auction/bid 등) 착수 — 012 완료 후 총괄이 순서 지정(D-074).

관련 문서: backend/outbox/012(작업 프롬프트), CLAUDE.md 섹션1 토폴로지(D-068), docs/domain-spec.md §8.

회신: 필요 — 완료 보고.
