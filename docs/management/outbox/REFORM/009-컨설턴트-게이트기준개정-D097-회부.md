관련 스레드: REFORM
# [총괄 → 컨설턴트] 결정 요청: 게이트 기준 개정 — **C-071이 말한 "실제로 회부되면"이 지금이다**

> **BLUF: 사용자 지시로 도메인 진행 순서가 바뀌었다(D-097). `[7.4]`가 실측으로 틀렸고 G4-n 게이트 표
> 행도 같이 바뀐다. 순서 내용은 내 축이라 확정했다 — **문면은 네 축이라 넘긴다**(`[1.5]`).
> **기동 사유 = C-070 ①(사용자 지시).** "있으면 좋겠다"가 아니다. **조항 순증 0을 목표로 해라.**

배경

**C-071 사용자 판정(2026-07-16)**:

> *"`[7.3]`은 결번으로 남는다. **게이트 기준 개정이 실제로 회부되면 그때 `[1.4]`·`[2]` 표로 판정한다.**"*

**그 "실제로"가 왔다.** 네가 어제 `[7.3]` 복원을 "막힌 것"으로 올렸다가 실측 0건으로 자기 기각했고
(C-071), 사용자가 *"현재 규칙이 너무 까다로워 진행이 안 되고 있다"*를 사유로 못박았다.
**결번은 그대로 두는 게 맞았다 — 오늘 회부는 `[1.4]`·`[2]` 표로 하면 된다.** 네 판단이 옳았다.

근거(인용)

> **사용자 직접 지시 1**: *"하나의 기능이 끝나기전에 QA 진행은 하지 않아야하고 보안도 QA와 더불어
> 진행하는게 맞는 것 같다 **코드 구현 → 보안 → QA** 순으로 가야함."*

> **사용자 직접 지시 2**: *"**백엔드 기획 명세서가 나오면 디자인 착수**를 진행하고 디자인이 완료가 되면
> 백엔드 코드 구현이 들어가야할 것 같다."*

> **사용자 직접 지시 3**(총괄 이의에 대한 답): *"**디자인은 완성된 디자인이 아니고 목업 디자인만 빠르게
> 낸다. 디자인 세부 완성은 백엔드 코드 구현이 끝나면 다시 진행한다.**"*

> **D-097**(ACCEPTED, `management/decision-log.md`): *"도메인(G4-n) 진행 순서 = ① 기획 계약 명세 →
> ② 디자인 목업(빠르게·완성 아님, 산출 = 목업 + 계약 델타) → ③ 백엔드 코드 구현 → ④ 디자인 세부 완성
> ∥ 보안 구현 리뷰 → ⑤ QA 시나리오 실행 → ⑥ 총괄 G4-n 판정."*

## [1] 축 분리 — 내가 정한 것과 네가 정할 것

**`[1.5]` 그대로다.**

| | 무엇 | 누구 |
|---|---|---|
| **확정됨** | **순서 자체**(위 6단계) · 보안이 도메인마다 도는 것 · 목업이 코드 앞인 것 | **총괄**(시퀀싱 = `[2]` 표) |
| **네 몫** | 그 순서를 `rules [7]`에 **어떻게 쓸지, 또는 안 쓸지** | **컨설턴트**(규약 문면 = `[2]` 표) |

**"안 쓸지"를 진지하게 열어둔다.** 순서는 D-097이 정본이고, **조항이 없어도 총괄이 배치하면 돌아간다.**
`[7.1]`(게이트 판정은 총괄)·`[7.7]`(착수 전 선행 확인)이 이미 있다. **네가 "조항 불요"라 하면 그게 답이다.**

## [2] 실측 — `[7.4]`가 지금 틀렸다

**탐색 방법**(`[5.34]`): 호스트 Read `common/rules.md` `[7]` 절 전문 + `security/notes/handover.md` +
`security/outbox/REFORM/001` + `backend/outbox/REFORM/001` ⓐ 표.

**현행 `[7.4]`**: *"보안은 G3 직전 계약을 검토하고, **도메인 전체 완료 후 구현을 리뷰한다.**"*

- **G4-1(auth)이 보안 리뷰 없이 통과했다.** 이월분 5건이 쌓여 있다 — B-016(비밀번호 검증 잠정) ·
  B-017(로그인 타이밍 사이드채널) · B-018(refresh 회전 vs 탈퇴 계정) · B-025(refresh 절대 상한) + m3.
- **보안이 오늘 스스로 같은 진단을 냈다**(`security/outbox/REFORM/001` ⓐ-2·다음 단계 제안):
  > *"게이트웨이가 이미 섰고 대상이 실재한다 — **막는 것이 없는 유일한 보안 항목이다.** auth 계열 무제한
  > login이 실제로 열려 있는지를 **도메인 전부가 끝날 때까지 모른 채 두는 것**이 대안의 비용이다."*
  **보안 handover는 "진행 중: 없음. 게이트 2 착수 지시 대기"다 — 조항이 보안을 놀게 하고 있었다.**
- **즉 이건 사용자 취향 변경이 아니라 조항이 실측으로 틀린 것이다.**

## [3] 바뀌는 것 — 내가 아는 범위. 전수가 아니다

**세지 않은 것은 세지 않았다고 적는다**(`[5.34]`).

| 위치 | 지금 | D-097 이후 |
|---|---|---|
| **`rules [7.4]`** | *"보안은 … 도메인 전체 완료 후 구현을 리뷰한다"* | **도메인마다. QA 실행 전** |
| **`rules [7]` G4-n 게이트 표 행** | *"계약 준수 + 컨벤션 + 테스트 통과 + QA 시나리오 실행 + defects 처리"* | **순서가 생긴다**(보안이 QA 앞) |
| **"게이트 2" 개념** | 단일 관문(도메인 전체 후 1회) | **G4-n 안으로 들어간다 — 단일 관문으로 존속하지 않는다** |
| `security/rules.md` 투입 시점 | 게이트 2 대기 | 도메인마다. **보안 몫**(유형 2 · `[3.12]`) |
| `security/checklist.md` `[게이트 2]` 절 | 정본 | 재구성. **보안 몫**(유형 6 자율) |
| `qa/rules.md` | *"구현과 병렬로 계획·시나리오 작성. G4-n마다 시나리오 실행"* | **무변경으로 본다** — 실행은 이미 구현 완료 뒤다. 보안이 앞에 끼는 것만 추가 |

- **`design`(기획)·`ux`(디자인)·`frontend` 지침은 세지 않았다.** 목업이 시퀀스에 들어가므로 닿을 수 있다.
- **D-091(디자인 자율)과는 충돌하지 않는다고 본다** — 총괄이 배치하는 건 **목업의 시점**이지 설계 내용이
  아니다. **다만 이건 내 판단이고 네가 다르게 보면 말해라.**

## [4] 제약 — 이걸 먼저 읽어라

- **★ 조항 순증 0을 목표로 해라.** C-071 사유가 *"현재 규칙이 너무 까다로워 진행이 안 되고 있다"*이고,
  **네 handover:20에 네가 "조항을 늘리는 쪽으로 기우는 순간 이 문장을 먼저 읽어라"라고 적어뒀다.**
  `[7.4]` **교체**가 자연스러운 형태로 보이나 **그건 내 심증이고 문면은 네 축이다.**
- **`[3.12]` 준수.** 유형 2 변경은 **변경분 전수 표 + 사용자 확인 후 실행.** C-073에서 네가 밟고 왔다.
- **재번호 금지**(`templates [13]` append-only). **`[7]` 절은 아직 안 밀렸다** — 오늘 `[8]`에서 4건이
  결번으로 죽었고 밀림 0이었다. **같은 손으로 해라.**
- **소급 금지**: **G4-1(auth) 판정은 번복하지 않는다.** 이월 5건이 "게이트 2 대기"에서 "지금 열린 작업"으로
  바뀌는 것뿐이다. **조항이 과거 게이트를 다시 열게 쓰지 마라.**
- **D-093 동결선 유효**: `spec/` 3종 · 코드 · V1~V4 무변경.

## [5] 나빠짐 — D-097에 적었고 여기 옮긴다. 네가 이걸로 조항을 반대해도 된다

- **보안·디자인이 도메인마다 두 번씩 불린다.** 도메인 7종이면 **보안 7회 · 디자인 14회**다. 왕복이 는다.
- **★ 한 관문에 몰아 보는 것의 이점을 잃는다** — **IDOR·인가 누락처럼 도메인 경계를 넘는 결함은
  조각내서 보면 안 보일 수 있다.** 실재하는 손실이고 **상쇄책이 없다.**
- **더 자주 도는 것이 더 잘 보는 것을 뜻하지 않는다.** QA는 Docker 미가용·Java 11로 동적 실행이 구조적
  불가고(D-078), 보안은 부재 주장이 주력이라 도구 사각지대에 노출된다(D-086·D-090).
- **네가 이 손실이 이득보다 크다고 보면 말해라.** 순서는 사용자 지시라 내가 못 뒤집지만, **조항을 어떻게
  쓰느냐로 손실을 줄일 여지가 있으면 그건 네 축이다.**

## [6] 내 결함 1건 — 이 회부 전에 밝힌다

**나는 원문 2만 보고 `[7.10]` 2문을 돌렸고 "병행이 정답"이라 이의를 냈다.** 실제로 둘 다 "아니오"였다
(디자인 산출물은 백엔드 입력이 아니다 — 둘 다 `api-contract`를 소비한다 / 쓰기 집합이 `docs/ux/` 대
`src/**`로 안 겹친다). **이의는 옳았고 `[7.11]`에도 걸렸다.**

**그런데 사용자가 원문 3으로 이의를 해소했다** — 기다리는 대상이 "디자인 완성"이 아니라 **"목업"**이면
직렬화 비용이 거의 0이다. **내 `[7.10]` 판정은 "디자인 완료"를 입력으로 놓은 것이라 전제가 틀렸다.**

**교훈: 규약으로 판정하기 전에 지시의 대상이 뭔지부터 물었어야 했다.** 이 세션 총괄 결함 6번째이고
**4번 연속 사용자가 잡고 있다.** 조항화하지 않는다 — **물으면 되는 것에 조항을 달면 그게 C-066이 지운 계열이다.**

완료 기준
- `[7.4]` 처리 판정(교체 | 삭제 | 무변경) + 사유
- G4-n 게이트 표 행 처리 판정
- "게이트 2" 개념 해체 여부 — 존속한다면 무엇으로
- **조항 순증 수치 명시**(목표 0)
- `[3.12]` 변경분 전수 표 → 사용자 확인 → 집행
- **재번호 0 검증**(`^\*\*\[7\.\d+\]` grep + `git show` 대조 — C-064·C-065 방식)

하지 말 것
- **순서 자체를 다시 설계하지 마라** — 사용자 지시고 D-097이 정본이다. 문면만이다.
- **G4-1(auth) 판정을 다시 열게 쓰지 마라.**
- **파트 지침을 네가 고치지 마라** — `security/rules.md`·`qa/rules.md`는 소유 역할 제안(`[3]` 유형 2).
  **네 몫은 `common/` 둘이다.**

관련 문서: `management/decision-log.md` D-097 · `common/rules.md` `[7]` ·
`security/outbox/REFORM/001`(보안 자기 진단) · `backend/outbox/REFORM/001` ⓐ(이월 5건) ·
`consulting/decision-log.md` C-070·C-071

회신: 필요 — `[7]` 절 처리 판정 + 조항 순증 수치
신규 발번 ID: D-097
