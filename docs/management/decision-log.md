# FinalCall 총괄 결정 로그

총괄(PM/아키텍트) 대화에서 내린 프로젝트 수준 결정을 기록한다.
도메인 세부 결정은 docs/design/decision-log.md 에, 확정 스펙은 docs/domain-spec.md · docs/erd.md · docs/api-contract.md 에 남긴다.
전체 ID 현황은 docs/management/decision-index.md 참조.
모든 대화(기획/백엔드/프론트)는 이 문서를 공유 기준으로 삼는다.

---

## D-001. 설계 문서 확정 순서 (2026-07-12, 확정)

### 결정
domain-spec.md → erd.md → api-contract.md 순서로만 확정한다.
앞 문서가 확정되기 전에 뒤 문서를 작성하지 않는다.

### 이유
ERD는 도메인 개념·상태 전이의 산출물이고, API 계약은 그 둘의 산출물이다.
순서를 어기면 계약서가 흔들려 병렬 진행 중인 백엔드/프론트가 재작업하게 된다.

---

## D-002. 도메인 설계를 인증(로그인) 설계보다 우선 (2026-07-12, 확정)

### 결정
경매 도메인 설계를 먼저 진행한다. 인증은 다음 두 지점에서 처리한다.
- 사용자 모델: domain-spec 논의 주제 6(사용자와 거래 범위)에서 도메인이 요구하는 만큼 정의
- auth API(회원가입/로그인/토큰 재발급): api-contract.md 작성 시 맨 앞 섹션으로 우선 확정하여 프론트에 먼저 전달

### 이유
- 의존 방향: 사용자 속성, 역할 구분, 인증 필수 API 목록은 전부 도메인 설계의 출력물이다. 역방향 의존은 "사용자가 식별 가능하다" 정도이며 JWT 스켈레톤이 이미 보장한다.
- 리스크: 로그인은 정형화된 문제라 언제 설계해도 결과가 비슷하지만, 판매 방식·상태 전이·동시성은 결정에 따라 테이블과 API가 통째로 달라지는 고위험 구간이다. 불확실성이 큰 쪽을 먼저 푼다.

### 기각된 대안
- 로그인 우선 설계: 도메인 요구를 모른 채 추측으로 결정하게 되어 재작업 위험. 소셜 로그인 등 과잉 설계 유인.

---

## D-003. 문서 폴더 규약 (2026-07-12, 확정)

### 결정
- docs/management/ — 총괄 결정 로그, 각 대화용 프롬프트 등 조율 문서
- docs/design/ — 기획/설계 대화의 작업 문서(주제별 결정 누적 등)
- docs/ 루트 — 확정 스펙(domain-spec.md, erd.md, api-contract.md)과 스켈레톤 문서

### 이유
작업 중 문서와 확정 스펙을 분리해야 백엔드/프론트 대화가 "무엇을 기준으로 삼을지" 혼동하지 않는다.
확정 스펙은 docs 루트만 참조하면 된다.

---

## D-004. 입찰 규칙 4건 (2026-07-12, 확정)

기획/설계 대화의 결정 요청(주제 2)에 대한 총괄 결정. 4건 모두 기획 추천안 채택.

### 결정
1. 최소 입찰 단위: 계단식 테이블(가격 구간별 증분). 검증식 `신규입찰 >= 최고가 + 구간증분`.
   시작은 설정(코드/구성) 기반으로 하고, 운영 중 조정 요구가 실제로 생기면 DB 정책 테이블로 승격.
2. 판매자 자기 경매 입찰 금지, 최고가 보유자의 연속 입찰 금지. proxy(자동)입찰은 이번 범위 밖.
3. 소프트 클로즈 도입: 트리거윈도우 내 입찰 시 마감 연장(기본 T-30초 / +30초), 경매별 설정형.
   총연장상한은 필수 — 상한 없는 연장은 금지.
4. buyNow 상호작용: (b) 최고가 < buyNow인 동안 buyNow 유지. 입찰 상한을 buyNow 미만으로
   제약하여 즉시구매가 항상 유효하도록 한다.

### 이유·조건
- 1: 게임 아이템 가격대가 넓어 단일 증분 부적합. 단, 처음부터 DB 테이블화는 과잉 — 설정으로 시작.
- 3: 마감 시각이 동적이 되어 마감 처리(주제 7)가 "재예약 가능한 지연 처리" 구조여야 한다는
  제약이 생김. 프로젝트 핵심 도전(마감 직전 폭주)과 부합하므로 수용.
- 4: (a)는 buyNowPrice를 경매 속성으로 흡수한 D(주제 1) 결정의 의미를 약화시킴.
  buyNow 근접 구간에서의 입찰 vs 즉시구매 경합은 주제 4(동시성)에서 필수로 다룰 것.

---

## D-005. 경매 생명주기·상태 전이 5건 (2026-07-12, 확정)

기획/설계 대화의 결정 요청(주제 3)에 대한 총괄 결정. 5건 모두 기획 추천안 채택 + 조건 3개.

### 결정
1. (3-1) 예약 시작 지원: Auction에 SCHEDULED 상태 도입. 소프트 클로즈용 지연 인프라에
   얹으므로 한계비용 작음. FixedSale은 SCHEDULED 없음(비대칭 허용, 필요 시 확장).
2. (3-2) 종료 상태: status(SOLD/UNSOLD/CANCELLED) + resultType(BID/BUYNOW) 분리. enum 폭발 방지.
3. (3-3) 취소: 판매자 취소는 "입찰 0건 & 진행 중"일 때만. 입찰 존재 시 불가.
   관리자 강제 취소는 상태 무관 별도 권한.
4. (3-4) 경매 FSM은 SOLD(판매 성립)까지. 결제/정산/배송은 별도 Order 애그리거트로
   핸드오프(SOLD 시 Order 생성). 상세 범위는 주제 6에서 확정.
5. (3-5) FixedSale 기한: 선택적 endAt(있으면 만료 시 EXPIRED, 없으면 무기한).

### 조건 (기획 대화에 부과)
- 취소 vs 첫 입찰 경합(취소 순간 입찰 유입)을 주제 4 동시성 필수 케이스에 추가.
- FixedSale의 SOLD도 동일한 Order 핸드오프로 통일(구매 경로 단일화).
- SOLD 확정과 Order 생성의 원자성(단일 트랜잭션 vs 이벤트 분리)은 주제 4 또는 6에서 명시 결정.

### 범위 절단
- 유찰(UNSOLD) 후 재등록은 별도 플로우 없음 — 신규 등록으로 처리.

---

## D-006. 협업 체계: 5역할 구성과 결정 위임 (2026-07-12, 확정)

### 결정
- 역할 5개: 총괄 / 기획·설계 / 백엔드 / 프론트엔드 / QA. 상세는 collaboration-guide.md.
- 결정 위임: 에스컬레이션 4기준(범위 변경 / 경계 침범 / 아키텍처·기술 선택 / one-way door)에
  해당하는 것만 총괄로. 나머지는 각 역할 자율 결정 + 자기 결정 로그 기록.
- 계약 변경: api-contract 확정 후 변경은 총괄 경유 필수 (요청 → 총괄 승인 → 기획 수정 → 전파).
- 문서 소유권: 역할별 폴더 분리(design/backend/frontend/qa), 확정 스펙은 docs 루트,
  다른 역할의 작업 폴더 참조 금지. (읽기 규칙은 D-016으로 대체됨)

### 이유
- 전건 승인 방식은 총괄이 병목이 되고 각 역할이 결정권 없는 작성자로 전락.
  위임 기준을 "되돌리기 비용 + 경계 침범"으로 정의하면 통제와 속도를 양립.
- 계약 변경만은 중앙 통제: 병렬 진행 중 한쪽만 아는 계약 변경이 가장 비싼 사고이기 때문.

### 기각된 대안
- 전건 총괄 승인(현행): 병목·피로 누적.
- 계약 변경의 기획 직접 처리: 빠르지만 백/프론트 간 비대칭 정보 위험.

---

## D-007. 도구 구성과 프론트엔드 저장소 (2026-07-12, 확정)

### 결정
- 도구 구성: 클로드 프로젝트 대화 5개(총괄/기획/백엔드/프론트/QA) + Claude Code 2개(백엔드/프론트 코딩용).
- 대화 vs Claude Code 분업: 대화는 구현 계획·작업 단위 분해·Claude Code 작업 프롬프트 생성·
  설계 수준 리뷰. Claude Code는 그 프롬프트를 받아 실제 코딩만 수행.
- 프론트엔드는 별도 저장소로 분리, 추후 프론트 스켈레톤을 독립 구성.
- api-contract.md 원본은 백엔드 저장소(finalcall)의 docs/. 프론트 저장소에는 복사본을 두고
  문서 헤더에 원본 경로와 버전을 표기. 계약 변경 전파 시(가이드 6절) 총괄이
  "프론트 저장소 복사본 갱신"을 전파 체크리스트에 포함한다.
- 문서 단일 진실은 저장소. 클로드 프로젝트 지식은 스냅샷 업로드(어긋나면 저장소가 우선).

### 이유
- 대화/Claude Code 분업: Claude Code 세션은 컨텍스트가 소진되지만 계획·이력은 대화에 남는다.
  경계 없이 두면 같은 일을 두 군데서 중복 수행.
- 별도 저장소: 이미 구축된 백엔드 스켈레톤(디렉토리 구조·CLAUDE.md·CI 경로)을 건드리지 않고
  프론트 스켈레톤을 독립 구성 가능. 계약서 동기화 비용은 복사본+버전 표기 규칙으로 수용.

### 기각된 대안
- 모노레포: 계약서 동기화 문제는 없으나 백엔드 스켈레톤 구조 재조정 필요, 스켈레톤 재사용성 저하.

---

## D-008. 동시성 정책 4건 (2026-07-12, 확정)

기획/설계 대화의 결정 요청(주제 4)에 대한 총괄 결정. 4건 모두 기획 추천안 채택 + 조건 2개.
정책 수준 결정이며, 구현 기법(락 종류·Redis 완충 등)은 백엔드 자율.

### 결정
1. (4-A) 정합성 최종 보증은 DB(조건부 원자 갱신 + 유니크 제약). 분산락(Redisson)은
   처리량 최적화 수단이지 정확성 보장 수단이 아니다. 원칙: "정합성은 DB, 처리량은 락".
2. (4-B) 종료성 전이(SOLD/CANCELLED)는 조건부 CAS UPDATE(WHERE status='ACTIVE',
   affected=0이면 이미 종료). 즉시구매·마감낙찰·취소가 단일 승자 패턴 공유.
3. (4-C) SOLD 확정-Order 생성은 기본 단일 트랜잭션. 결제/외부연동이 범위에 들어오면
   outbox 이벤트 분리로 승격.
4. (4-D) 입찰 유효성 검증·최고가 갱신은 경매 단위 직렬화 + DB 최종 정합성.

### 조건
- 4-D: 소프트 클로즈 마감 연장 판단을 입찰 수용과 동일 직렬화 단위에 포함할 것.
  분리 시 "입찰 성공했으나 연장 누락" 틈 발생.
- 4-C: 최종 방식(단일 TX 유지 vs outbox 승격)은 주제 6 종료 직후 재확정 안건으로 자동 회부.

### 기각된 대안
- 4-A (a) 분산락이 정합성 보증: Redis 장애·락 리스 만료·클럭 스큐 시 중복 판매.
  one-way door 리스크라 기각.
- 4-C 즉시 outbox 확정: 결제 범위(주제 6) 미정 상태의 선행 투자.

---

## D-009. 결정 로그 세분화: 1 항목 = 1 원자적 결정 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: 기획 에스컬레이션(escalated-as 본 항목), relates-to D-006
- 결정: 결정 로그 1 항목 = 1 원자적 결정. D-009부터 적용, 기존 묶음 항목(D-001~008)은
  소급 재번호하지 않는다. ID는 접두어별 독립 증가·불변·재사용 금지.
- 이유: 묶음 번호는 "D-004 조건 2" 같은 애매한 참조를 유발. 참조 정밀도 실익이 관리 비용보다 큼.
- 기각: 주제/에스컬레이션 단위 번호 유지 — 크로스롤 참조가 늘어날수록 정밀도 손실 누적.

---

## D-010. 결정 로그 상태 라벨 도입 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: relates-to D-009
- 결정: 상태 라벨 4종 도입 — PROPOSED / ACCEPTED / ON-HOLD / SUPERSEDED(대체 ID 명시).
  참조 키워드 depends-on / relates-to / supersedes / escalated-as 함께 도입.
- 이유: 4-C(D-008)처럼 뒤집힐 수 있는 결정의 상태 추적 필요가 이미 실재.
- 기각: 미도입 — 보류·대체 이력이 본문 산문에 묻혀 추적 불가.

---

## D-011. 마스터 인덱스 유지 + ID 보고 규칙 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: relates-to D-009, D-010
- 결정: 총괄이 management/decision-index.md 에 전체 ID 표(ID·제목·상태·소유역할)를 유지한다.
  각 역할은 총괄로 보내는 모든 프롬프트 끝에 "신규 발번 ID" 1줄을 첨부한다.
- 이유: 자율 결정은 총괄을 거치지 않아 인덱스가 불완전해지는 구멍이 있음.
  모든 프롬프트가 총괄을 경유하는 구조를 이용해 보고 규칙으로 봉합.
  내용의 단일 진실은 각 역할 로그, 인덱스는 목차 — 이중 관리 아님.
- 기각: 인덱스 미유지 — 5개 로그 분산 시 크로스롤 현황 파악 불가.

---

## D-012. 컨설턴트 역할 신설 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: relates-to D-006 (역할 구성 변경)
- 결정: 협업 프로세스 설계 전담 컨설턴트 대화를 한시 역할로 신설. ID 접두어 C-xxx,
  작업 폴더 docs/consulting/. 산출물은 전부 PROPOSED 상태로 총괄에 회부하며
  확정 권한은 없다(확정 시 D 발번). 문서 체계·지침 계층 재설계 완료 후 역할 종료.
- 이유: 협업 규약 설계 자체가 규모 있는 작업이 됨. 진행 중인 기획과 분리해
  전담 대화에서 처리하는 것이 컨텍스트 오염 방지에 유리.

---

## D-013. 보안 파트 신설 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: relates-to D-006 (역할 구성 변경), D-012
- 결정: 보안 역할을 정식 파트로 신설. ID 접두어 S-xxx, 폴더 docs/security/.
  책임: 설계·계약·구현의 보안 검토(인증/인가, 입찰·구매 부정행위, 결제·정산,
  시크릿 관리, OWASP 기준), 위협 모델링, 취약점 리포트(findings.md).
  투입 시점: api-contract 확정 직전 보안 게이트 1회 + 도메인 구현 완료 시 리뷰.
  findings는 전 역할 열람 가능(D-016 이후 일반 규칙으로 흡수).
- 이유: 경매 플랫폼은 돈이 오가는 시스템 — 인증·부정 입찰·중복 결제가 도메인 리스크의
  핵심인데 QA(기능 검증)와 관점이 달라 별도 파트가 맞다. 구현 후 후행 검토만 하면
  재작업이 비싸므로 계약 확정 직전 게이트를 둔다.
- 기각: QA에 보안 검토 포함 — 기능 검증과 공격자 관점은 다른 역량, 혼합 시 둘 다 얕아짐.

---

## D-014. 문서 유형 6종 분류 체계 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-001, relates-to D-003
- 결정: docs 문서를 6유형(확정 스펙/지침/결정 로그/작업 노트/전달 프롬프트/보고·트래킹)으로
  분류, 유형별 위치·변경 규칙·기준(authority) 등급 규약화. 상세 표는 가이드 3절.
- 이유·기각 대안·레퍼런스: consulting/decision-log.md C-001 참조.

---

## D-015. 역할 폴더 표준 내부 구조 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-002, depends-on D-014
- 결정: 모든 역할 폴더는 decision-log.md + notes/ + outbox/ 동일 구조.
  역할 고유 산출물은 폴더 루트. management/만 decision-index.md와 prompts/(킥오프) 추가.
  프론트 문서는 백엔드 저장소 docs/frontend/로 일원화(D-007과 양립: 코드는 별도 저장소,
  문서 허브는 단일).
- 이유: consulting/decision-log.md C-002 참조.

---

## D-016. 읽기 규칙 재설계: 필요 기반 열람 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-003, supersedes D-006의 읽기 규칙 부분
- 결정: "타 역할 작업 폴더 참조 금지" 폐지. 대체: 상시 열람은 필독 문서만,
  필요 기반(pull) 열람은 사유·출처 표기 조건으로 자유. 기준 위계는
  확정 스펙 > D 로그 > 자기 로그 > 타 로그 > 노트(인용 금지). defects/findings
  열람 특례는 일반 규칙에 흡수되어 삭제.
- 이유: 접근 차단이 아니라 상태 라벨로 미확정 의존을 방지(특례 누적은 기본값 오류 신호).
  상세: consulting/decision-log.md C-003 참조.

---

## D-017. 전달 프롬프트 outbox 보관 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-004, depends-on D-015
- 결정: 발신 프롬프트는 자기 outbox/에 주제당 1파일(NNN-주제.md), 머리에 상태 1줄
  (SENT | ANSWERED → 결정 ID), 삭제 금지. 기존 escalation-prompts.md는 동결 보관.
- 이유: 요청 원문은 감사 추적의 반쪽. 상세: consulting/decision-log.md C-004 참조.

---

## D-018. docs 루트 순수성: 스켈레톤 프롬프트 이동 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-005, relates-to D-003
- 결정: spring-skeleton-prompts.md → docs/backend/notes/ 이동. CLAUDE.md(2곳)·README·
  문서 자체의 참조 경로 정정. docs 루트는 확정 스펙 외 파일 금지.
- 집행: 2026-07-12 총괄이 이동·정정 완료.

---

## D-019. 단계 게이트: 진입·통과 기준 명문화 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-006, relates-to D-001, D-013
- 결정: 진행 순서를 게이트 표(G1 spec → G2 erd → G3 contract+보안게이트1 → G4-n 구현 DoD)로
  형식화, 각 게이트에 통과 기준 명시. 통과는 인덱스 게이트 표에 1줄 기록(D 발번 없음 —
  상태 전이는 결정이 아님). 상세: consulting/02-collaboration-process.md 2.1.

---

## D-020. 메시지 유형 4종 표준 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-007
- 결정: 역할 간 전달을 4유형(결정 요청/작업 지시/완료 보고/정보 공유)으로 표준화.
  공통 헤더 `[발신 → 수신] <유형>: 제목` + 말미 `회신: 필요/불요`. 계약·스펙 영향
  사항은 정보 공유 유형 금지(결정 요청으로 격상). 가이드 5절 반영.

---

## D-021. 복붙 부담 최소화 3규칙 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-008
- 결정: 1) 같은 수신자행 독립 안건 묶음 전달(부분 회신 허용) 2) 게이트 통과 =
  프로젝트 지식 일괄 동기화 앵커(동기화된 문서는 경로 참조, 게이트 후 변경분만 발췌
  첨부) 3) 왕복 1회 완결 설계(후속 질문 유발 문구 금지). 규칙 1·3은 D-023 파일 버스에서도 유지.

---

## D-022. 동기화: 이벤트 기반 리추얼 3종 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-009, depends-on D-019, D-020
- 결정: 시간 기반 리추얼 미도입. 이벤트 기반 3종 — 게이트 통과 시 총괄 전파 /
  역할 작업 블록 종료 시 완료 보고 / 사용자 세션 재개 시 총괄 현황 브리핑(기존
  트래킹 문서에서 즉석 생성, 신규 문서 없음). 상세: consulting/02-collaboration-process.md 2.4.
- 조건: 컨설턴트 로그의 C-009 항목이 절단됨 — 02 문서 기준으로 자기 로그 복구 후 종결.

---

## D-023. outbox 메시지 버스: 파일 기반 전달 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-010, depends-on D-017, supersedes D-021 규칙 2의
  프롬프트 중계 부분
- 결정: 역할 간 모든 전달(요청·회신·보고·공유)은 발신자 outbox/에 파일로 남긴다.
  회신도 발신(자기 outbox에 회신 파일, `회신대상: <역할>/outbox/NNN` 명기).
  사용자 중계는 "수신함 확인해" 한 줄. 미처리 판별은 각 역할의 inbox-log.md
  (처리한 메시지 경로 목록). 상태 줄 SENT→ANSWERED 갱신은 발신자가 회신 파일 발견 시.
- 조건(하이브리드): 저장소 폴더 접근이 불가한 대화가 있으면 그 대화에 한해 기존
  복붙 방식 병행. 사용자 확인 결과 백/프론트/QA/보안 대화는 폴더 연결 세션으로
  운영 예정이라 기본은 완전 적용.

---

## D-024. ID 소유자 통지 규칙 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-011, depends-on D-023
- 결정: 타 역할 소유 ID·산출물을 논의·변경·재해석한 대화는 소유 역할 앞 정보 공유
  파일을 outbox에 남긴다(회신 불요, 1~3줄). 각 역할은 세션 재개 시 자기 접두어를
  전 역할 outbox에서 grep하여 미확인 언급을 점검(push+pull 이중화).
- 근거 실물: P-003~007 유령 ID 논의가 발견자(컨설턴트)에게 미공유된 사고.

---

## D-025. 표준 템플릿 패키지 10종 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-012, depends-on D-020, D-023
- 결정: 메시지 공통 골격 + 4유형, 결정 로그 항목, 작업 노트 머리, inbox-log,
  세션 재개 브리핑, defects/findings 항목 등 템플릿 10종 채택.
  정본을 management/templates.md로 이관(총괄 소유, 지침 유형). 이후 킥오프 프롬프트에
  "메시지·로그는 templates.md를 따른다" 1줄 포함. 원칙: 빈 섹션 금지(채우지 못하면 삭제).
- 조건: defects(QA-NNN)/findings(SEC-NNN) 번호 체계는 결정 로그 접두어(Q, S)와 별개
  체계이므로 영역 5(파트 지침)에서 재확인.

---

## D-026. 통합 추적 표 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-013, relates-to D-011
- 결정: decision-index.md의 보류 안건 표를 "추적 안건" 표로 확장 — 유형(보류/블로커/리스크),
  열(안건·유형·소유·해소 조건·상태). 신규 ID 접두어 없음. 등재는 역할 메시지 → 총괄,
  해소 시 상태 갱신 후 게이트 시점에 "해소됨" 섹션으로 이동.
- 이유: 추적 표면을 인덱스 1개로 유지. 인덱스는 전 역할 필독이라 추가 비용 0.

---

## D-027. 회고 2종: 게이트 회고 + 사고 회고 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-014, relates-to D-019, D-022
- 결정: 1) 게이트 회고 — 완료 보고 말미 선택 2줄(keep/change), 총괄이
  notes/retro-G<N>.md 취합, 채택분만 D 발번. 2) 사고 회고(blameless) — 체계가 뚫린
  사건 시 management/notes/incident-NNN.md 5항목(사건/영향/원인은 시스템에서 멈춤/
  재발 여부/방지 액션과 소유).
- 집행: incident-001(문서 소실 사고 일지) 소급 기록 — 방지 액션 "D 발번 시 총괄 커밋"
  운영 규칙을 절차로 승격.

---

## D-028. 병렬 작업 비동기 완료 규칙 3종 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-015, relates-to D-016, D-019, D-020, D-026
- 결정: 1) 착수 조건 — 작업 지시에 "의존:" 필드 필수, 의존 대상 확정 전 착수 금지
  (추적 표 블로커 등재). 2) 선완료 행동 우선순위 — 완료 보고(대기 중 명시) → 의존 없는
  작업 pull → 가정 선언 진행(가정 공개 + 재작업 자기 부담, 상대 전가 금지).
  3) 어긋남 해소 — D-016 기준 위계로 판정, 스펙과 다른 쪽이 수정, 스펙 공백이면
  결정 요청 격상. 선착순 기준 금지.
- 이유·기각 대안·레퍼런스: consulting/decision-log.md C-015 참조.

---

## D-029. docs git 규약: main 전용·이벤트 커밋·주체 유동 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-016, relates-to D-023, incident-001
- 결정: docs는 main(master) 전용, 브랜치 금지(파일 버스의 단일 파일 상태 전제).
  커밋은 이벤트 기반 — D 발번 / 게이트 통과 / 사고 복구 / 작업 블록 완료.
  주체 유동: 역할 대화가 커밋 메시지 작성, 기본 실행은 사용자, 지시 시 대화가 대행.
  형식 `docs(<역할>): 한글 제목`, 코드와 혼합 커밋 금지.
- 조건: 코드 커밋 대행 확장은 보류 — 빌드·테스트 검증 선행이 필요한 영역.
  CLAUDE.md 2절(코드 커밋·푸시는 사용자) 현행 유지.

---

## D-030. 프론트 저장소 git 규약 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-017, relates-to D-007
- 결정: 백엔드 대칭 — Conventional Commits·한국어 제목·feature/<도메인> → PR Squash.
  계약 복사본 동기화 커밋 고정 형식:
  `docs(contract): api-contract v<N> 동기화 (원본 finalcall@<해시7>)`.
  커밋 이력만으로 D-007 전파 완결을 증명. 프론트 CLAUDE.md 수록은 영역 5에서.

---

## D-031. 템플릿 증보 5종 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-018, relates-to D-025, D-027, D-029
- 결정: git 커밋 docs 경량판 / 사고 회고 / 게이트 회고 취합 / 게이트 전파 /
  계약 변경 요청 5종을 templates.md 11~15절로 편입.
  템플릿 신설 경로: 컨설턴트 재직 중 설계 → 총괄 채택. 종료 후 역할 제안 → 총괄 승인.

---

## D-032. 프론트엔드 스택 확정 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: relates-to D-007, D-030, 블로커 "프론트 코드 컨벤션" 해소
- 결정: TypeScript + React SPA(Vite) + TanStack Query(서버 상태) + Zustand(전역 최소)
  + Tailwind CSS. SSR 미도입.
- 이유: 핵심 기술 도전이 백엔드(동시성)이고 프론트는 계약 소비자 — 단순함 최우선.
  JWT는 CSR 처리가 단순(서버 측 토큰 처리 불요), SEO는 현 범위 목표 아님.
  실시간 최고가 갱신은 폴링/SSE 어느 쪽이든 SPA로 충분.
- 기각: Next.js(App Router) — SSR/SEO 실익 대비 JWT+SSR 복잡도와 프론트 학습 비중 증가.
  Redux Toolkit — 1인 규모에 보일러플레이트 과잉. styled-components — 런타임 비용, 감소세.

---

## D-033. 문서 파일 네이밍 규약 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-019, relates-to D-015, D-017
- 결정: 유형별 네이밍 통합 — outbox `NNN-주제.md` / notes 자유 / `incident-NNN.md` /
  `retro-G<N>.md`. 한글 파일명 공식 허용, 두 저장소에 `git core.quotepath=false` 권장.

---

## D-034. 용어집 미도입: 정본 문서 내 용어 섹션 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-020, relates-to D-014
- 결정: 별도 glossary.md 금지. 도메인 용어 정본 = domain-spec.md 용어 정의 섹션,
  프로세스 용어 정본 = 협업 가이드. 이중 관리 차단.
- 파급: 기획에 domain-spec 용어 정의 섹션 필수화 전파(outbox/008).

---

## D-035. api-contract 문서 형식 골격 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-021, relates-to D-001, D-019, D-030
- 결정: 헤더(v<N>·상태·변경 이력 표), 버전 규칙(G3 확정=v1, 이후 변경은 6절 경유+v+1),
  엔드포인트 표기 단위 고정, "공통 규약" 섹션 예약. G3 전 확정 필수.
- 조건(원안 수정): URL 네이밍·상태 코드·페이징 등 기술 내용은 "백엔드 기동 첫 작업"이
  아니라 G1 직후 백엔드 대화를 기술 규약 협의 모드로 조기 기동해 기획과 협의 확정한다.
  구현 착수는 G3 이후 유지. 이유: 기술 규약 없이 계약 v1을 쓰면 기동 직후 계약 변경
  절차를 밟는 자기모순 발생.

---

## D-036. erd 문서 형식 골격 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-022, relates-to D-019, D-035
- 결정: 헤더·변경 이력, 섹션 골격 — 네이밍 선언부 / Mermaid erDiagram / 테이블 정의 표 /
  인덱스 표(이유 열 필수) / Flyway V<N>__ 매핑. G2 전 확정 필수.
- 조건: 테이블·컬럼 네이밍 등 기술 규칙은 D-035와 동일하게 백엔드 조기 기동 협의로 확정.

---

## D-037. 지침 3계층: 공통 가이드 / 파트 지침 / 킥오프 부트스트랩 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-023, relates-to D-014
- 결정: 지침은 3계층 — 공통(collaboration-guide.md) / 파트 지침 파일(각 역할 폴더 또는
  저장소 CLAUDE.md) / 킥오프 프롬프트는 부트스트랩만(필독 목록·역할 선언, 지침 내용
  복제 금지 — 지침·킥오프 간 드리프트 방지).

---

## D-038. 번호 체계 분리: 결정 로그 vs 발견 티켓 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-024, D-025 조건 회답
- 결정: Q/S-xxx = 결정 로그(불변, 상태 라벨) vs QA/SEC-NNN = 결함·발견 티켓
  (OPEN→FIXED/WONTFIX 상태 갱신). 수명이 다르므로 시퀀스 분리. D-025 조건 종결.

---

## D-039. 프론트엔드 CLAUDE.md 확정 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-025, depends-on D-030, D-032
- 결정: 초안 채택. docs/frontend/CLAUDE.md로 보관, 프론트 저장소 생성 시 루트로 이관.
  핵심: feature 기반 구조, 서버(Query)/클라이언트(Zustand) 상태 분리, 계약 1:1 API 함수,
  계약에 없는 API 추측 사용 금지(스펙 공백 시 중단·보고, D-028).

---

## D-040. QA 파트 지침 확정 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-026, relates-to D-008, D-019, D-038
- 결정: 초안 채택 → docs/qa/qa-guide.md. 리스크 기반 1페이지 플랜, D-008 동시성 4건
  필수 시나리오, 기대 결과는 계약 근거 인용 의무(구현 코드·타 역할 노트 근거 금지).

---

## D-041. 보안 파트 지침 확정 + 게이트 1 차단 권한 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-027, relates-to D-013 (해석 확장)
- 결정: 초안 채택 → docs/security/security-guide.md. STRIDE-lite, 도메인 리스크 축 6개.
  게이트 1 차단 권한 명시 승인: Critical 발견 미해결 시 G3 통과 불가.
  Critical 발견 시 즉시 총괄 통지 + 구현 중단 요청 권한(최종 결정은 총괄·사용자).
- 이유: 차단 권한 없는 게이트는 형식적 절차. 요청 권한 구조라 거버넌스 침해 없음.

---

## D-042. 컨설턴트 대기 모드 전환 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: relates-to D-012 (한시 역할 조항 수정)
- 결정: 컨설턴트 임무(5개 영역 설계) 완료를 확인하되, 즉시 종료하지 않고 대기 모드로
  전환한다. 대기 중 신규 설계 작업·C 발번은 하지 않으며, 총괄 소집 시에만 활동
  (킥오프 검수, 프로세스 이슈 진단 등). consulting/ 폴더는 감사 추적 원문으로 보존.
  종료 여부는 G3(백/프론트/QA/보안 기동) 이후 재검토.
- 이유: 새 체계가 실전(병렬 구간)을 아직 안 거침 — 첫 게이트 통과와 다역할 기동에서
  프로세스 결함이 드러날 가능성이 있어 설계자를 즉시 해산하지 않는 보수적 선택.
- 기각: 즉시 종료(컨설턴트 제안) — 개선 경로(D-027 회고, D-031 템플릿 경로)가 있으나
  대개편급 이슈 발생 시 재기동 비용보다 대기 비용이 낮다는 사용자 판단.

---

## D-043. incident-001 원인 정정: 관측 계층 오탐 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: relates-to D-027, incident-001
- 결정: 컨설턴트 통보 "기획 파일 절단 2건"(7번째 사고)은 오탐으로 판정 — 호스트 파일은
  온전하며 절단은 세션 마운트 뷰에서만 관측됨. incident-001에 방지 액션 4 추가:
  "절단 의심 시 호스트 직접 읽기로 교차 검증 후 판정. 세션 뷰 단독으로 복구 작업 금지"
  (불필요 복구가 오히려 동시 편집 충돌 유발 가능).

---

## D-044. 아이템 도메인 모델: 서프형 단순화 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: 기획 주제 5 에스컬레이션, relates-to D-036
- 결정: (b) 채택 — 중앙 시드 item_template 마스터 + 정형 컬럼 인스턴스 속성(JSON 제거)
  + 얕은 2단 카테고리(대분류 → 종류) + 축(속성/등급). 메이플식 하이브리드
  (JSON 옵션 + 인덱스 승격) 기각.
- 이유: 템플릿 공간이 유한(수백 미만)해 마스터 성립 자명, 인스턴스 변이가 정형
  (레벨 + 특수스킬 2슬롯)이라 JSON 계층 불필요. "동일 템플릿 다른 가치" 문제는
  남아 학습 가치 유지. 상세: management/notes/item-reference-survival-project.md.
- 조건: 특수스킬 조합 필터와 시세 집계 단위(템플릿·레벨·스킬 조합)를 ERD(G2)
  검색·인덱스 설계의 필수 케이스로 반영할 것(D-036 인덱스 표 이유 열의 근거).

---

## D-045. 아이템-판매 관계: 마스터 참조 + 표시 필드 스냅샷 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: depends-on D-044
- 결정: 매물은 item_template FK 참조 + 인스턴스 속성 정형 컬럼 보유 + 등록 시점
  표시 필드 스냅샷(이력 보존). 기존 P-007 초안(스냅샷 단독)은 void 확정, 마스터 참조로 대체.

---

## D-046. 강화·합성 범위 절단 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: depends-on D-044
- 결정: 강화·합성 "행위"는 거래 플랫폼 범위 밖. 레벨은 인스턴스의 현재 상태 값으로만 취급.

---

## D-047. item_template 시드 방식 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: depends-on D-044
- 결정: 고정 시드 세트(운영/Flyway 마이그레이션 관리). 판매자 제안+승인 플로우 불필요.
  실존 게임 명칭·데이터 미사용 — 가상 명칭·수치로 시드 구성(IP 주의).

---

## D-048. 아이템 도메인 설계: 사용자 참여 필수 예외 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: relates-to D-006 (위임 규약의 도메인 한정 예외), D-044~047
- 결정: 아이템 도메인(item_template, 인스턴스 속성, 카테고리, 특수스킬 체계, 시드 구성)의
  설계는 에스컬레이션 4기준과 무관하게 자율 결정에서 제외한다. 기획은 아이템 관련
  domain-spec 세부·ERD 테이블 설계를 착수 시점부터 안건(선택지+추천)으로 총괄에 올리고,
  총괄이 사용자와 논의해 확정한다. two-way door 세부(컬럼명 수준)도 포함.
- 이유: 사용자가 게임 구조 선정부터 직접 주도한 관심 영역 — 완성 문서의 게이트 승인만으로는
  참여 시점이 늦다는 사용자 판단. 도메인 한정 예외라 위임 원칙(D-006) 전반은 유지.

---

## D-049. 사용자 브리핑 줄: 매 응답 채팅 요약 의무 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: 사용자 직접 지시(컨설턴트 경유), relates-to D-022, D-023
- 결정: 모든 역할 대화는 매 응답 끝에 채팅으로 3~5줄 요약을 제시한다 —
  (a) 한 일 (b) 발신·수신 메시지 경로 + 요지 1줄 (c) 신규 발번·상태 변경 (d) 사용자 할 일.
  파일에 쓴 내용도 요약은 채팅에 노출 필수. 파일 버스는 역할 간 채널이지
  사용자 보고 채널이 아니다. 향후 킥오프 부트스트랩 골격에 이 규칙 1줄 포함.
- 이유: 파일 버스(D-023)로 중계 부담은 제거됐으나 사용자의 관찰 지점이 함께 사라짐 —
  통제권(결정)과 가시성(관찰)은 별개 문제인데 후자가 누락됐던 것.

---

## D-050. 사용자 모델: 단일 User (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: 기획 주제 6 에스컬레이션, relates-to D-002
- 결정: 단일 User 엔티티 — 판매·구매 모두 가능(역할은 행위 기반), 관리자는 권한 플래그.
  판매자/구매자 엔티티 분리 기각(C2C 거래소에 과설계).

---

## D-051. 화폐 모델: 토스 테스트 결제 + 캐시/게임머니 2단 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: 기획 주제 6 에스컬레이션(사용자 확장 채택), relates-to D-008, D-013
- 결정: 실 PG 대신 토스페이먼츠 테스트 결제 API로 "캐시 충전"을 구현한다.
  화폐는 2단 — 캐시(결제로 충전) / 게임머니(캐시에서 교환, 거래·입찰·정산의 화폐).
  교환 비율은 추후 결정(파라미터화 설계, 추적 표 등재).
- 조건: 충전(토스 연동)은 별도 도메인으로 분리 — 승인 콜백 검증·멱등키 처리 필수,
  경매 거래 트랜잭션과 무관하게 설계. 역방향(게임머니→캐시, 환불·출금)은 범위 절단을
  권고하되 기획이 domain-spec에서 명시(이견 시 에스컬레이션). 보안 검토 축 3(자금
  정합성)에 충전 멱등성 추가.
- 기각: (a) 결제 미포함 — 잔액·홀드 동시성 학습 기회 상실. (c) 실 PG — 범위 과다.
  순수 내부 지갑(원 추천) — 사용자가 외부 결제 연습 포함을 원함, 테스트 API라 부담 낮음.

---

## D-052. 입찰 자금: 입찰 시 게임머니 홀드 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: depends-on D-051, relates-to D-008
- 결정: 입찰 순간 입찰액만큼 게임머니 홀드(에스크로). 지불 능력 보장 + 낙찰 시 차감.
- 조건: 홀드 해제 규칙(상위 입찰 등장 시 즉시 해제 vs 마감 일괄)을 domain-spec에 명시.
  홀드·해제·차감 경합은 동시성 필수 케이스에 추가(D-008 계열).

---

## D-053. 4-C 재확정: 단일 트랜잭션 유지 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: D-008 조건의 자동 회부 종결, depends-on D-051
- 결정: SOLD 확정 → Order 생성 → 게임머니 정산은 전부 내부 DB이므로 단일 트랜잭션 유지.
  outbox 승격 불요. 외부 연동(토스)은 충전 도메인에 한정되며 경매 거래 트랜잭션 경로에
  존재하지 않는다(D-051 조건). 충전까지 거래 TX에 엮는 설계 금지.

---

## D-054. 지침 변경 전파 의무 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: relates-to D-014(유형 2), D-021, D-049. 근거 실물: D-049(브리핑 줄)가
  기획에 미전파되어 기획 응답에 브리핑 누락 — 사용자 발견.
- 결정: 지침(문서 유형 2 — 가이드, templates, 파트 지침)이 변경되면 총괄은 활성 역할
  전원에게 정보 공유 메시지를 즉시 발신한다(변경 요지 + 해당 절). 게이트 동기화(D-021)
  까지 기다리지 않는다. 역할은 수신 시 해당 절만 재독하면 된다.
- 이유: 지침은 행동 규약이라 모르면 즉시 위반이 된다 — 게이트 주기 전파로는 공백 발생.

---

## D-055. 브리핑 줄 표준 템플릿 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: 사용자 직접 지시, depends-on D-049, relates-to D-031(템플릿 신설 경로)
- 결정: templates.md 16절에 브리핑 고정 골격 추가 — 4항목(한 일 / 수신·발신 / 발번·상태 /
  할 일) 순서 고정, 내용 없어도 항목 줄 유지("없음"). 전 역할 동일 형식.
- 이유: D-049가 내용만 규정하고 형식을 안 정해 대화마다 브리핑 모양이 흩어짐 — 사용자가
  스캔 위치를 학습할 수 없음. 형식 고정이 요약의 가독 가치를 완성한다.

---

## D-056. 마감 트리거: 재예약 가능한 지연 인덱스 하이브리드 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: 기획 주제 7 에스컬레이션, relates-to D-004(소프트 클로즈), D-008
- 결정: (c) 하이브리드 — 지연 인덱스(score=트리거 시각) + 짧은 주기 워커 + DB 재구축
  안전망. 소프트 클로즈 재예약은 score 갱신만으로 처리. DB가 진실(4-A 정합),
  인덱스 유실 시 재구축. 구체 기술(Redis ZSET 등)은 백엔드 자율.
- 조건: 트리거 정확도 목표를 domain-spec 비기능 요구로 명시(수치는 백엔드 조기 협의
  확정). 소프트 클로즈 30초 단위와 정합해야 함.
- 기각: (a) 순수 폴링 — 정확도/부하 트레이드오프. (b) 순수 지연 큐 — 연장마다
  취소+재삽입 churn.

---

## D-057. 시각 트리거 단일 인덱스 통합 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: depends-on D-056
- 결정: SCHEDULED 시작·경매 마감·FixedSale 만료를 단일 지연 인덱스로 통합(트리거 종류
  구분자만). 스케줄러 다중 운용 지양.

---

## D-058. 마감 처리 멱등·유실 복구 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: depends-on D-056, relates-to D-008(4-B), D-052
- 결정: 마감 처리는 4-B CAS로 멱등(이미 종료면 no-op) — 워커 다중화·중복 실행 안전.
  유실 복구는 DB 스캔(status=ACTIVE AND 트리거시각<=now), 부팅 시 + 주기 안전망.
  마감 시점의 최고입찰 차감 + 홀드 해제는 종료 전이와 동일 직렬화 단위(4-D 연장) —
  동시성 필수 케이스 등재.

---

## D-059. 세션 핸드오버 규칙 (2026-07-12) [ACCEPTED]

- 소유: 총괄 / 관련: 사용자 직접 지시(컨설턴트 경유), relates-to D-021, D-037, D-049
- 결정: 전 역할 공통 — 세션 종료 시 자기 폴더 notes/handover.md 작성(덮어쓰기, 이력은 git).
  내용은 "파일에 없는 기억"만 4개 섹션(진행 중 / 대기 중 / 휘발성 맥락 / 재개 필독),
  1페이지 이내, 기존 파일 내용은 경로 참조. 트리거: 사용자 지시 또는 게이트 통과 시 권장.
  새 세션은 킥오프 부트스트랩 + "handover 읽고 재개" — 첫 응답은 이해 확인 브리핑(D-049).
  사용자 절차 두 마디: "핸드오버 작성해" → "handover 읽고 재개해".
- 이유: 긴 세션의 누적 토큰 비용·압축 손실을 구조적으로 제거. 상태가 파일에 외부화된
  현 체계의 자연 귀결.

---

## D-060. 프로젝트 지식 등록 규칙 (2026-07-13) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-028, relates-to D-007, D-016, D-059
- 결정: 각 역할 대화의 프로젝트 지식(스냅샷)에 공통 필수 등록 5종을 확정한다 —
  (1) management/collaboration-guide.md (2) management/templates.md
  (3) 확정 스펙 3종 domain-spec.md·erd.md·api-contract.md (4) management/decision-log.md(총괄 D)
  (5) management/decision-index.md. 확정 스펙 교체 트리거 = 게이트 통과(G1/G2/G3).
  방침 A 채택: 5종 전부 등록. 등록 파일 헤더 문구 = "스냅샷 사본 — 정본: 저장소 <경로> ·
  기준 시점 <게이트/해시7> · 어긋나면 저장소 우선(D-007)". 휘발 5종은 필수, 안정 파일은 선택.
  등록/비등록 경계: 파트 지침(CLAUDE.md·frontend/CLAUDE.md·qa/security 지침)은 해당 파트만
  필독(공통 아님), 타 파트 로그·노트·outbox·자기 inbox-log는 비등록(필요 시 pull).
  파일 버스는 스냅샷 비대상 — 폴더 연결 또는 하이브리드 복붙(D-023). 등록·갱신 실행은 사용자.
  킥오프 부트스트랩·게이트 전파 체크리스트에 "휘발 5종 재등록" 1줄 추가.
- 이유: 등록 규칙 부재로 등재 누락 시 근거 문서를 못 보는 사고 위험. 스냅샷 낡음(경미)보다
  등재 누락(치명)이 더 위험 → 방침 A. 게이트 트리거로 확정 스펙 낡음을 경계. D-016(열람 범위·
  기준 위계)과 관심사(스냅샷 프로비저닝)가 달라 편입 대신 신규 D(D-009 원자성).
- 기각된 대안: 방침 B(안정 파일만 등록, 휘발은 pull) — pull 누락 시 근거 공백. D-016 편입 —
  원자성 위반·D-016 비대화.

---

## D-061. docs 커밋 실행 사용자 일원화 (2026-07-13) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-029, supersedes D-029 규칙3(커밋 주체)·대행 조항,
  relates-to D-022, D-054
- 결정: docs 커밋의 git 실행은 사용자 단일 실행자로 일원화한다. 각 역할 대화는 git을
  직접 실행(대행)하지 않고, 작업 블록 종료 시 완료 보고 말미에 `docs(<역할>): 제목` 커밋
  메시지 블록만 제안한다(D-022 리추얼 2와 결합). 실행 규율: 스테이징은 path-scope
  `git add docs/<역할>/`로 한정, `git add -A` 금지. 유지분(D-029에서 존치): 커밋 메시지
  형식·경량 템플릿, 타입 혼합 금지, 코드 커밋은 CLAUDE.md 2절 현행(원래 사용자 실행).
- 이유: D-029가 허용한 커밋 대행이 1인 중계·다중 대화 구조에서 다중 실행자 꼬임을 유발
  (2026-07-13 미커밋 다수 실증). 실행자 단일화로 스테이징 충돌·이력 꼬임을 제거.
- 기각된 대안: 대행 존치(D-029 현행) — 꼬임 재발. 역할별 자동 커밋 — 실행자 다중화 심화.

---

## D-062. 아이템 소절(§7) 서술 방향 확정 (2026-07-13) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from design/outbox/012, relates-to D-044~047, D-048, D-036
- 결정: domain-spec §7 아이템 도메인 서술 방향을 확정한다(D-048 사용자 참여 예외).
  - 안건1 서술 깊이: (a) spec은 개념·규칙만 — 템플릿/인스턴스 구분, 2단 카테고리, 매물-템플릿
    관계, 시드 정책, 범위, 검색·시세 축. 구체 taxonomy 멤버·특수스킬 목록·가상 명칭은
    ERD(G2)에서 확정.
  - 안건2 시드 초안 주체: (a) 기획이 완전 가상 명칭·수치 시드 초안 제시 → 사용자·총괄 확정.
    실존 게임 명칭·데이터 미사용(IP 회피).
  - 안건3 소절 목차: 승인 + 8) 추가. 최종 목차 = 1 개념 / 2 카테고리 계층 / 3 인스턴스 속성 /
    4 매물-템플릿 관계·표시 스냅샷 / 5 시드 정책 / 6 범위 / 7 검색·시세 축 /
    8 소유·이전(인벤토리 여부·낙찰 후 인스턴스 귀속·재판매 흐름).
- 이유: 8) 소유·이전이 없으면 ERD에서 item_instance 소유자 FK 설계 근거가 없다. spec은 개념
  계층·구체 값은 ERD 정본(D-036)이라 계층 분리로 중복·이중 동기화를 방지. IP 회피 위해 가상 시드.
- 기각된 대안: 안건1 (b) spec에 구체 값 명시 — ERD와 중복·동기화 부담. 안건3 원안(8 미포함)
  — 소유자 모델 근거 공백.

---

## D-063. 브리핑 초안 요지 항목 추가 (2026-07-13) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from C-030, relates-to D-049, D-055
- 결정: 사용자 브리핑 줄(D-049)에 조건부 5번째 항목 "초안 요지"를 추가한다. 초안·제안을
  생성한 응답의 브리핑에만 핵심 결정·선택지·추천을 2~5줄로 넣고, 단순 응답에는 생략한다
  (D-049 간결성 유지). templates 16절에 형식 반영 완료.
- 이유: D-049 "파일 내용 요지 채팅 노출" 원칙의 구체화. 기존 "경로+1줄"만으로는 최종
  결정권자(사용자)가 초안 실질을 파일 없이 판단하기 어려운 공백을 보완.
- 기각된 대안: 무조건 요지 첨부 — 단순 응답까지 비대해져 D-049 간결성 훼손.

---

## D-064. 아키텍처 방향: MSA + 게이트웨이 인증 전담 (2026-07-13) [SUPERSEDED → D-065]

- 소유: 총괄 / 관련: escalated-from backend/outbox/002(B-008), supersedes CLAUDE.md 섹션1
  단일서비스 전제, relates-to D-002(인증), domain-spec §6, D-032(프론트)
- 결정: 사용자 지시로 완전 MSA 채택(안건1 C). 인증 서비스·대기열 서비스·핵심 도메인 서비스
  등으로 분리하며, 서비스 분해는 On-Race형(gateway/auth/main/queue/common) 기준·세부 경계는
  후속 확정. 인증은 게이트웨이 전담(안건2 B): 게이트웨이 JWT 검증 → X-User-Id/X-User-Role
  주입, 클라 위조 헤더 선제거, 뒤단 공유비밀(X-Gateway-Token) 직접접근 403. 컨트롤러는
  @RequestHeader("X-User-Id"). CLAUDE.md 단일서비스 전제를 MSA로 개정한다.
- 이유: 사용자가 MSA를 프로젝트 목표(실배포+확장)로 확정. 게이트웨이 전면·인증 전담·대기열
  분리가 대규모 트래픽·동시성 목표와 정합. 지금(G1/G2, 도메인 코드 전)이 전환 적기 —
  계약·ERD를 처음부터 서비스 경계로 설계 가능.
- 파급(후속 필수): CLAUDE.md 전면 개정(서비스 분해 확정 후), domain-spec §6 인증 문구 개정,
  ERD 서비스별 데이터 경계, api-contract 헤더·서비스 라우팅, 스켈레톤(현 단일 앱) 재구성,
  보안(스푸핑·inter-service 인증), 프론트 진입점.
- 기각된 대안: (A) 단일 서비스·(B') 게이트웨이 엣지+서비스 인증 — 사용자 MSA 확정 방침과 불일치.
- 롤백: 발번 당일 사용자가 작업량 과다로 철회. D-065로 대체(단일 서비스 유지).

---

## D-065. 아키텍처 롤백: 단일 서비스 유지 (2026-07-13) [ACCEPTED]

- 소유: 총괄 / 관련: supersedes D-064, escalated-from backend/outbox/002(B-008),
  relates-to D-002(인증), CLAUDE.md 섹션1
- 결정: D-064(MSA + 게이트웨이 인증 전담)를 철회하고 단일 서비스 아키텍처를 유지한다.
  B-008 최종 회답 — 안건1 (A) 단일 서비스(게이트웨이 없음), 안건2 (A) 서비스 자체 JWT 검증
  현행(F1, SecurityContext). X-User-Id 헤더 규약 미도입. CLAUDE.md 단일서비스 전제·domain-spec
  §6·스켈레톤 원상 유지. 게이트웨이/대기열/MSA는 향후 필요·여력 시 재검토(현재 미채택).
- 이유: 사용자 판단 — MSA 전환 작업량(스켈레톤 재구성, D-008 단일 TX의 분산 트랜잭션화
  재설계, 다중 배포·운영)이 현 단계·규모 대비 과다. 단일 서비스로 도메인 구현을 먼저 완성.
- 기각된 대안: MSA 유지(D-064) — 작업량 과다로 사용자 철회.

---

## D-066. 아이템 ERD 모델 확정 (2026-07-13) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from design/outbox/014, relates-to D-044~047, D-048, D-062,
  D-008, B-001~007
- 결정: 아이템 ERD 모델 5안건 확정(D-048 사용자 참여 — 사용자 최종 확정).
  1. 아이템 타입 → item_template 마스터 정규화(a): 타입코드를 대분류·중분류(슬롯군)·속성·종류
     정형 컬럼으로 분해, 인스턴스는 template_id FK. 타입코드는 파생값.
  2. 특수스킬 정규화(a): skill1_id·skill2_id·skill_percent 3컬럼. skill_percent=스킬 발동확률.
     스킬 ID는 스킬 정의 마스터 FK.
  3. 골드포스 → 인스턴스 시간 속성(a): gf_expire_at 컬럼 채택. spec §7.3에 골드포스 축 추가,
     §7.7은 검색 필터에만 반영·시세 집계 키에서 제외(시간제라 집계 불안정 방지).
  4. 소유·이전(a): owner_id FK + public_id(ULID)/id(B-004) + item_ownership_history 신설
     (instance_id·from/to_owner_id·transferred_at·유형)로 전 체인 추적, first_owner 캐시 없음.
     거래 이전은 Order 연계.
  5. 인벤토리+에스크로+임시보관: 인스턴스 owner_id+slot_no 인라인(96칸 비즈니스 제약),
     출품=에스크로(인벤토리 제외, Auction/FixedSale 보유, 중복 출품 방지=CAS 단일 승자 D-008),
     임시보관 오버플로우 테이블(상한 없음, expire_at 선택·파라미터화). 위치 불변식(정규/임시/
     에스크로 XOR)은 ERD 제약 설계에서 확정.
- 이유: D-044(마스터+정형 컬럼)·spec §7 정합, 참조 SQL 인코딩 실증. 5-2 CAS 에스크로가 G1
  검수 관찰(인스턴스 중복 출품 잠금) 해소. 골드포스 시세 키 제외는 시간제 속성의 집계 불안정 회피.
- 후속: 기획이 domain-spec §7.3(골드포스 축)·§7.7(검색 필터) 보강 + 버전 갱신(D-066 근거,
  변경 이력 기록). 스킬 정의 마스터·taxonomy 멤버·가상 시드·위치 단일진실 표현은 ERD/시드 이월(D-047).
- 기각된 대안: 안건별 (b)(타입코드 원형/단일 int/이력 Order 파생/골드포스 미반영) — spec §7·
  검색·시세 요구 미충족. 골드포스 시세 키 포함 — 만료로 키 변동·과분할.

---

## D-067. 원게임 IP 회피 원칙 삭제 — 원게임 데이터 전면 사용 (2026-07-13) [ACCEPTED]

- 소유: 총괄 / 관련: amends D-044·D-047·D-062(안건2)·domain-spec §7.5·§12(IP 회피 조항),
  relates-to D-066
- 결정: 프로젝트 성격을 "실서비스가 아닌, 실서비스에 버금가는 연습·포트폴리오"로 확정하고,
  기존 "실존(원)게임 명칭·수치·데이터 미사용·완전 가상 구성(IP 회피)" 원칙을 삭제한다.
  원게임(참조: SurvivalProject)의 명칭·수치·코드를 시드·모델에 전면 사용할 수 있다.
  domain-spec §7.5·§12의 IP 회피 조항 제거(기획 개정 v0.3), ERD V4 시드·skill_code는 실제값 허용.
- 이유: 현실성(실서비스 버금) 목표 우선. 가상 명칭·수치 창작·확정 부담 제거로 시드 확정 비용 감소.
- 유의(비차단): 공개 저장소일 경우 원게임 명칭·자산에 잔여 상표·저작권 여지가 있음 —
  사용자 인지·수용(폐서비스·비상업 연습 전제).
- 기각된 대안: 구조·수치만 실제 + 명칭 가상 — 현실성 일부 손실. 원칙 유지 — 창작·확정 비용·현실성 저하.

---

## D-068. SCG 엣지 게이트웨이 채택 — SEC-005 해소 (2026-07-13) [ACCEPTED]

- 소유: 총괄 / 관련: refines D-065(게이트웨이 없음 → 엣지 게이트웨이 추가), relates-to D-064,
  S-001(SEC-005), CLAUDE.md E2
- 결정: 모놀리식(단일 서비스, D-065 유지) 앞에 SCG(Spring Cloud Gateway) 엣지 게이트웨이를
  별도 배포 단위로 둔다. 역할은 엣지 관심사에 한정 — rate limit(Redis 토큰버킷
  RequestRateLimiter), 직접접근 차단(공유비밀 X-Gateway-Token), 라우팅. 인증은 서비스 유지
  (SecurityContext, D-065) — 게이트웨이 인증 전담 아님, X-User-Id 미도입, 계약 불변.
  rate limit을 게이트웨이가 담당하므로 CLAUDE.md E2(앱 레벨 rate limiter off, 게이트웨이 담당)
  전제가 실체화된다. 향후 MSA 확장 시 게이트웨이 재사용.
- 이유: 보안 게이트 1 SEC-005(D-065 게이트웨이 제거로 auth 무차별 대입 방어 공백) 해소.
  rate limit은 엣지 관심사라 앱 레벨보다 게이트웨이가 적합(중앙·다중 인스턴스 일관). 완전 MSA의
  무게(서비스 분해·분산 TX·인증 전담) 없이 게이트웨이 이점만 취함. 계약 불변이라 G3 비차단·
  되돌리기 저비용.
- 파급: 백엔드 스켈레톤에 SCG 게이트웨이 모듈/배포 추가(구현 항목), 로컬 docker-compose·관측에
  게이트웨이 반영. 보안 게이트 2에서 rate limit·직접접근 차단 구현 검토. 계약·domain-spec·erd 무영향.
- 기각된 대안: 앱 레벨 rate limit(Resilience4j/bucket4j) — 동작하나 엣지 관심사엔 부적합·다중
  인스턴스 일관성 약함. 게이트 2 보류 — 실배포 방어 공백 잔존.

---

## D-069. 백엔드/프론트 → Claude Code 핸드오프 규약 (2026-07-13) [ACCEPTED]

- 소유: 총괄 / 관련: relates-to D-007(대화 vs Claude Code 분업), D-061(커밋 실행), D-023(파일 버스)
- 결정: 역할 대화(백엔드/프론트)가 Claude Code에 코딩을 넘기는 핸드오프를 규약화한다.
  1. Claude Code는 파일 버스 노드가 아니라 부모 대화의 '손' — 별도 outbox·수신함 스캔 없음.
     산출물은 코드 + 커밋 메시지 제안(git이 이력).
  2. 작업 프롬프트: 부모 대화가 자기 outbox에 파일로 남기고(<role>/outbox/NNN-<도메인>-<작업>.md,
     헤더 `[<역할> → Claude Code] 작업 지시`), Claude Code는 경로 지정으로 읽는다("구현해: <경로>").
     필수 항목은 templates 18.
  3. Claude Code 필독(작업 시): CLAUDE.md(자동) + 지정 작업 프롬프트 + 프롬프트가 참조한
     api-contract 절·erd·domain-spec 절 + notice 참조 구현.
  4. DoD: 계약 준수 + CLAUDE.md 컨벤션 + 테스트 통과 + 빌드 성공.
  5. 리포트 루프: Claude Code 출력(완료·이슈·계약 의문)은 사용자가 부모 대화에 전달 →
     부모 대화가 완료 보고/에스컬레이션을 자기 outbox로 재진입. 부모 대화가 버스에서 Claude Code를 대표.
  6. 프론트도 동일(frontend 대화 → 프론트 Claude Code, 프론트 저장소).
- 이유: 구현 본격화 전 작업 프롬프트 형식·필독·DoD를 표준화해 프롬프트 품질 편차·감사 추적
  공백 제거. Claude Code를 버스 노드로 만들지 않아 파일 버스를 단순 유지.
- 기각된 대안: Claude Code 전용 outbox·수신함 스캔 — 코드가 곧 산출물이라 불필요, 버스 복잡화.
  프롬프트 복붙만(파일 미보관) — 감사 추적 공백.

---

## D-070. refresh 토큰 회전 계약 정합 — /refresh 응답에 refreshToken 추가 (2026-07-13) [ACCEPTED]

- 소유: 총괄 / 관련: escalated-from backend/outbox/013, relates-to SEC-006(S-002), D-002,
  api-contract §2. 계약 변경 절차(가이드 6절) 첫 적용.
- 결정: api-contract §2 POST /refresh 응답에 refreshToken을 추가한다(회전한 신규 refresh 전달,
  선택지 a). SEC-006의 refresh 회전(재발급마다 이전 폐기·재사용 탐지)을 구현하려면 신규 refresh를
  클라가 수신해야 하므로 응답 미포함은 모순. api-contract v1 → v1.1, 6절 절차(기획 수정 + 변경
  이력, 프론트 복사본 갱신).
- 이유: SEC-006(보안 게이트 1 통과 조건)의 정확한 구현. 계약이 login엔 refreshToken이 있으나
  refresh엔 누락된 미명세 갭. 기술·보안 정합 건이라 총괄 결정(사용자 위임 범위).
- 기각된 대안: (b) 임계시점만 회전 — 재사용 탐지 약화로 보안 저하.

---

## D-071. 프로덕트 디자인(UX/UI) 파트 신설 (2026-07-13) [ACCEPTED]

- 소유: 총괄 / 관련: relates-to D-006(역할 구성), D-032·D-039(프론트). 사용자 지시. 역할표 개정.
- 결정: 프론트 웹 품질을 위한 프로덕트 디자인(UX/UI) 파트를 신설한다.
  - 명칭: 디자인(UX/UI). 기획(설계, P-xxx, docs/design/)과 구분 — 기획=도메인/스펙,
    디자인=UI/UX 시각·상호작용.
  - 폴더: docs/ux/ (역할 표준 구조 D-015: decision-log·notes·outbox·inbox-log + 산출물).
    결정 접두어 U(U-xxx). 티켓 별도 시퀀스 불요(산출물 문서로 대체).
  - 책임: (1) 디자인 시스템·토큰(색·타이포·간격·반경, Tailwind config 1:1 매핑) (2) UI 컴포넌트
    스펙(전 상태 default/hover/focus/disabled/active/error/loading·변형·반응형) (3) UX·IA·화면
    맵·주요 플로우·와이어프레임 (4) 접근성 WCAG 2.1 AA (5) 비주얼 방향(색·타이포·톤)
    (6) 디자인→프론트 핸드오프.
  - 관계: 프론트의 상류. 디자인 스펙(참고 지침, 계약이 최상위)→프론트 대화 구현 계획→프론트
    Claude Code. 계약(api-contract) 확정(G3 통과) 후 화면 설계 가능.
  - 게이트: 별도 강제 게이트 없음(1인 규모). 도메인별 UI 구현 전 디자인 스펙 선행 권장.
  - 도구: 설치된 design 플러그인 스킬(design-system·ux-copy·accessibility-review·design-critique·
    user-research·design-handoff) 활용.
- 이유: 사용자 디자인 비전문 — 프론트가 좋은 웹을 만들려면 디자인 시스템·UX·접근성 산출물이
  필요. 업계 표준(NN/g UX 역할, 디자인 토큰↔Tailwind, 컴포넌트 상태 스펙, WCAG 2.1 AA)을
  1인 규모로 경량화.
- 덜어낸 것(1인 경량화): 전담 디자인 조직·풀 Figma 파이프라인·대규모 사용자 리서치 세션 미도입.
  문서 기반 스펙 + 필요 시 목업(이미지/SVG)으로 대체.
- 기각된 대안: 디자인을 프론트에 흡수 — 시각·상호작용·접근성 즉흥화, 일관성 저하. 기획에 흡수 —
  도메인 설계와 관심사 상이.

---

## D-072. 디자인 파트 프로세스 개정 — 기획 경유·시각 예시·총괄+사용자 최종 (2026-07-13) [ACCEPTED]

- 소유: 총괄 / 관련: amends D-071(디자인 파트). 사용자 지시.
- 결정: 디자인 파트 프로세스에 3규칙을 추가하고 design-guide.md §11에 명문화한다(추후 핸드오프
  시에도 참조).
  1. 기획 경유: 디자인의 화면·플로우·도메인 표현(등급·아이템 속성·상태 등) 설계는 기획(도메인/
     스펙 소유)과 정합·협의를 거친다. 도메인 연계 결정은 기획 검토 필수.
  2. 시각 예시 의무: 결정 요청·제안 시 텍스트 스펙만이 아니라 사용자가 눈으로 확인할 수 있는
     시각 예시(렌더된 목업 — HTML/SVG·이미지, docs/ux/mockups/)를 함께 낸다. 비주얼 방향·주요
     컴포넌트·테마는 특히 필수.
  3. 최종 결정 주체: 비주얼·UX 최종 결정은 총괄+사용자가 한다. 디자인 자율 = 시각 예시·스펙
     초안·정합. 유의미한 시각/UX 선택은 시각 예시+추천으로 상정, 확정은 총괄+사용자.
  - 흐름: 디자인 초안(시각 예시) → 기획 정합 검토 → 총괄+사용자 최종 확정.
- 이유: 사용자 디자인 비전문 — 텍스트 스펙만으론 판단 어려워 시각 예시 필요. 디자인은 도메인
  진실(기획 소유)에 근거해야 정합. 브랜드·UX 방향은 프로젝트 정체성이라 총괄+사용자 확정.
- 기각된 대안: 디자인 전면 자율 — 비전문 사용자가 판단 못 하고 도메인·정체성과 어긋날 위험.

---

## D-073. 아이템 등급(grade) 축 제거 (2026-07-13) [ACCEPTED]

- 소유: 총괄 / 관련: amends D-044(속성·등급 축 → 등급 제거), relates-to D-062·D-066·D-067.
  escalated-from design/021. D-048 아이템 결정(사용자 확정).
- 결정: item_template의 grade 축을 제거한다. 원게임에 등급/희귀도 개념이 없고(아이템 = 레벨 +
  타입(속성×종류)) user_item 실구조에도 등급 필드가 없다(D-067 원게임 데이터 정합). 반영:
  domain-spec §7.2·§7.5(정체성 축=종류×속성, v0.4), erd(item_template.grade 컬럼 제거, 유니크
  (main_category,sub_group,element,kind), §5 인덱스 (element,kind), Mermaid, v0.5), api-contract
  §3(공통 목록 필터 grade 제거, 6절 v1.2). 디자인 U-004 5색 등급 티어 무효 → 시각 축을 속성
  (4속성)/레벨 기준으로 재조정.
- 이유: 원게임 데이터 정합(D-067). 등급은 원게임에 없는 축이라 유지 시 가상 설계·실데이터 불일치.
- 기각된 대안: 등급 유지 — D-067(원게임 전면 사용)과 불일치, 가상 등급 창작 부담.

---

## D-074. 총괄 시퀀싱·선행 완료 게이팅 규약 (2026-07-13) [ACCEPTED]

- 소유: 총괄 / 관련: strengthens D-028(착수 조건), relates-to D-019(게이트)·D-026(추적표).
  사용자 지시. 전 역할 적용(collaboration-guide 7절 반영).
- 결정: 파트 간 작업 순서를 총괄이 지정하고, 선행 미완료 시 후행 착수를 금지한다.
  1. 순서 지정: 총괄이 파트 간 작업 순서·의존(선행→후행)을 지정한다. 각 파트는 총괄 지정
     순서를 따른다.
  2. 선행 게이팅: 어떤 작업이 다른 파트의 선행 산출물에 의존하면, 그 선행이 완료·확정
     (ACCEPTED/게이트 통과/총괄 확인) 전에는 착수하지 않는다(D-028 착수 조건을 파트 간 순서로
     일반화·강화).
  3. 자문: 각 파트는 착수 전 "내 선행이 총괄 확정/확인됐는가"를 확인. 애매하면 총괄 확인 요청,
     그 전 착수 금지. 임의 선행 추정 착수 금지(선착순 기준 금지).
  4. 선행 대기 시: 병렬 구간 규칙 선완료 행동(대기 명시 → 의존 없는 작업 pull → 총괄 승인 하
     가정 선언)을 따른다.
  5. 추적: 총괄이 선행-후행 의존·블로커를 추적표(D-026)에 등재해 순서를 가시화.
- 이유: 다중 파트 병렬에서 선행 미완료 상태로 후행이 앞서면 재작업·어긋남 비용이 크다. 순서를
  총괄이 능동 조율해 병목·재작업을 사전 차단.
- 전파: 전 역할 통보(모든 파트 필독).

## D-075. CLAUDE.md 코드 스타일 규약 소절 추가 (2026-07-14) [ACCEPTED]

- 소유: 총괄 / 관련: relates-to B-020(스타일 자동화 도입)·backend/outbox/016(요청)·015(도입 작업).
  CLAUDE.md는 지침(유형2)·총괄 소유 → 총괄 승인·집행 사안.
- 결정: CLAUDE.md에 코드 스타일 규약 소절을 신설한다.
  1. 위치: 섹션 7로 말미 신설(백엔드 제안 §4~5 삽입안을 수정 채택). §4~5 사이 삽입 시 섹션 5·6이
     밀려 "CLAUDE.md 5절/6절" 교차참조 15곳+(claude-code-kickoff·spring-skeleton-prompts·
     templates.md §11·frontend/CLAUDE.md·consulting 로그)가 깨지므로, 재번호 없는 append로 파급 0.
  2. 내용: 정본=config 파일(checkstyle·editorconfig·formatter, 경로는 B-020 산출물과 일치),
     기반=Naver 핵데이(스페이스4), 강제=build check(위반 빌드 실패), 적용 의무=커밋 전
     `spotlessApply`+checkstyle 통과. 스타일 층만 담당, 섹션 5(아키텍처)와 병존.
- 이유: 설정 파일 강제만으로는 새 세션이 "어긴 뒤 빌드 실패"로 뒤늦게 인지한다. 필독(CLAUDE.md)에
  명시해 선제 준수 + 정본 경로 단일화.
- 기각된 대안: §4~5 사이 삽입(백엔드 A안) — 논리적 인접성은 낫지만 재번호 파급이 커 기각.
- 집행: 총괄이 CLAUDE.md 섹션 7 반영(D-061 커밋은 사용자).

## D-076. CLAUDE.md §5 Controller 규약 — 204 No Content 예외 명문화 (2026-07-14) [ACCEPTED]

- 소유: 총괄 / 관련: relates-to B-019(백엔드 자율 결정)·backend/outbox/019(요청). CLAUDE.md 지침(유형2)·총괄 소유.
- 결정: §5 Controller "반환 타입 항상 ApiResponse<T>"에 예외를 명문화한다 — 상태 변경만 하고 반환
  본문이 없는 응답(예: logout)은 204 No Content + `void`/`@ResponseStatus` 를 허용(ApiResponse 래핑 제외).
- 이유: 204 No Content는 정의상 본문이 없어 ApiResponse<T> 래핑이 불가·부적절하다. "항상" 문구와
  실제 구현(B-019)이 충돌하므로 예외를 명시해 새 세션 혼선을 차단한다. REST 정합.
- 집행: 총괄이 CLAUDE.md §5 반영(D-061 커밋은 사용자).

## D-077. 기획 파트 분리 — 백엔드 기획(P) / 프론트 기획(PF) 신설 (2026-07-14) [ACCEPTED]

- 소유: 총괄 / 관련: refines D-071(디자인 파트)·D-072(디자인 기획 경유)·D-007(프론트 문서 일원화),
  relates-to D-035(계약 소유). 사용자 지시. 전 역할 적용(collaboration-guide §1·§4 개정).
- 결정: 단일 기획 파트를 도메인 축으로 둘로 나눈다.
  1. 백엔드 기획(기존 기획, 프리픽스 P 유지, 폴더 docs/design/): 서버 도메인 담당. domain-spec·
     erd·api-contract 소유 유지. api-contract 정본은 계속 P가 쥔다(사용자 확정).
  2. 프론트 기획(신설, 프리픽스 PF, 폴더 docs/frontend-planning/): 디자인·프론트 담당 기획.
     화면 명세·클라이언트 도메인 정합, 프론트 구현(F)·디자인(UX/U) 조율. api-contract는 소비·
     변경요청 주체(6절 절차 — 직접 수정 금지).
  3. 디자인(UX/U) 파트는 별도 유지(시각 산출물 실무). D-072 "기획 경유"의 기획 = 이제 프론트
     기획(PF)이 그 위에서 조율(스펙·화면명세·정합). 시각 최종은 총괄+사용자 불변.
  4. api-contract 소유 = 백엔드 기획(P) 유지(Q1 사용자 확정). 계약은 서버 도메인 파생, 6절 변경
     절차 그대로.
- 이유: 단일 기획이 서버 도메인 + 프론트/디자인을 겸하면 병목. 도메인 축(서버 vs 클라이언트)으로
  분리해 병렬화. 계약 소유는 파생 방향(서버→계약)을 따라 P 유지로 단일 진실·책임 소재 보존.
- 기각된 대안: 계약 공동 소유(단일 진실 흐림)·프론트 이관(계약 서버 파생과 역행), 디자인 흡수
  (범위 과부하) — Q1·Q2에서 기각.
- 집행: collaboration-guide §1 역할표·§4 프리픽스 개정, 프론트 기획 킥오프 프롬프트 신설, 전 역할
  전파(outbox/052). 폴더 docs/frontend-planning/ 은 프론트 기획이 첫 출근 시 세팅.

## D-078. QA 두뇌(대화)/손(Claude Code) 분리 규약 (2026-07-14) [ACCEPTED]

- 소유: 총괄 / 관련: relates-to D-069(핸드오프)·D-007(도구 구성)·D-040(QA 지침)·D-074(시퀀싱).
  사용자 지시. collaboration-guide §8 반영.
- 결정: QA 작업을 두뇌/손으로 분리한다.
  1. 두뇌 = QA 프로젝트 대화: 테스트 전략·시나리오 설계·계약 정합 검증·결함 분류(triage)·게이트
     판정 권고·파일 버스 조율. QA는 이 대화에 유지한다.
  2. 손 = Claude Code: 실제 테스트 코드 작성·실행(`./gradlew test`·Testcontainers 동적 검증)으로
     pass/fail 증거 생성.
  3. 실행 경로: 스켈레톤·단순 도메인은 백엔드 dev 테스트 그린으로 대체 가능. 동시성 고위험(마감
     입찰 폭주·buyNow vs 입찰·취소 vs 첫 입찰·SOLD-Order 원자성 등)은 실행 증거 필수 — QA 설계
     시나리오를 (a) 백엔드 Claude Code가 테스트로 구현(기본, 테스트=백엔드 repo, 마찰 최소) 또는
     (b) 전용 QA Claude Code(독립 E2E·부하 검증 필요 시)로 실행.
  4. 착수 시점·경로((a)/(b))는 총괄이 도메인별 지정(D-074).
- 이유: QA 일이 두뇌(설계·판정)와 손(실행)에 걸쳐 있다. 대화는 동적 실행 불가라 정적 정합 검증까지만
  가능 — 조율·판정은 대화, 실행 증거는 Claude Code로 나눠 각 층의 강점을 쓴다(D-069 대화=두뇌/
  Claude Code=손 원칙의 QA 확장).
- 기각된 대안: QA 전체를 Claude Code로 이전(조율·triage·게이트 판정·파일 버스 상실). QA 전체를
  대화로만(동시성 실행 증거 불가).
- 집행: collaboration-guide §8 QA 분업 명문화. QA는 qa-guide에 반영(QA 소유). 현 단계는 대화 유지
  (백엔드 dev 테스트로 실행 대체), bid 도메인부터 실행을 Claude Code로 위임.
