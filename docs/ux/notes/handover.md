# 디자인 handover (2026-07-15)

성격: 세션 상태 스냅샷 — 파일에 없는 기억만. 정본은 각 파일. 1페이지 이내(D-059).

## 진행 중

- **운영 모드 = D-091(ACCEPTED)이 정본 근거다**(U-017 escalated-as, 095). 사용자·총괄에 **사전 승인을 묻지 않는다.** 기획(P)에서 필요한 것을 받아 자율 확정하고, 사용자는 완성 후 변경을 통보한다(사후 변경 요청권 = 거부권). 규칙 1(기획 경유)·규칙 2(시각 예시 의무 — 기획·총괄행 결정 요청에 한정) 유지. `design-guide` [1]·[11] 반영 완료.
- **완료**: 구조 와이어프레임(auth·지갑·마이페이지/탈퇴 4요소, 009 검수 통과·판단 4건 전건 승인) · **색 토큰 실측 확정(U-020, design-system v0.2 — U-016 집행 종결)**.
- **다음 작업 = 게임 스킨 목업**(`mockups/game-skin.html`, 미착수). 확정 토큰을 경매 상세·상거래 카드에 입혀 렌더한다. **D-091의 사후 변경 요청권은 볼 것이 있어야 행사된다** — 목업이 그 자리다. 상거래 카드는 기획 031로 블로커 해소돼 착수 가능하다.
- **목업에서 함께 실측할 선결 판정 1건**: `s`(26×28)에 구워진 레벨 배지가 목록에서 판독되는가(P-013 조건). 불충분하면 계약 `level`로 표기한다.

## 대기 중 (회신·블로커)

- **`outbox/017` → 프론트 회신 조건부 대기.** U-020 토큰 명칭 변경이 **실행 중인 스켈레톤 CC와 충돌**한다. 완주 권고 + 드롭인 config 전문 제공(교체 표면 = `tailwind.config.js` + CSS 변수 2개 파일. 스켈레톤은 토큰을 정의만 하고 소비처가 없어 중단 이득 0). **되돌아올 조건 1개**: 프론트가 토큰을 컴포넌트에 이미 바르기 시작했다면 권고가 뒤집힌다 — 그 회신이 오면 재판정.
- **골드포스 아트 교체** → 총괄 회신 대기(기획이 격상, design/032). 골드포스 활성 카드의 아트 처리만 막히고 나머지는 미차단. 기획 해석 수령: **D-066은 검색·집계 축 배제이지 표시 금지가 아니다**(계약 [3.3] `goldforceExpireAt` 노출이 근거).
- `outbox/014`(마운트 뷰 절단) · `016`(U-020 완료 보고) → 총괄, 회신 불요. `010`·`011`·`012`·`013` → 전건 ANSWERED.
- 원본 UI 자산 hex → 사용자 결정 큐(095 [7절]). **실측으로 실질 해소** — 큐에 남아 있어도 블로커 아니다.

## 휘발성 맥락 (파일에 없는 판단)

- **자산 경로 확보·실측 완료. U-012 이미지 블로커는 실질 해소다**(총괄 추적표엔 "사용자 제공 대기"로 열려 있을 수 있다).
  - 아이템 아트: `D:\private_server\SP\tool\Survival-project-PGF-decompressor-main\src\images\global\card\card_image\gold_black` — 648 = 9레벨 × 2크기(l 50×93 / s 26×28) × 4속성 × 9종류. 속성·종류가 계약 `element`·`kind`와 일치.
  - UI 크롬: `...\src\images\interface\global\card_info\` — `wnd_cardinfo.png`(211×307) · 탭/버튼 normal·click 쌍 · **`card_info.xml`에 자식 좌표**. 공통 버튼·스크롤은 `interface\global\`.
  - **접근 방법**: 위 경로를 폴더 연결하면 호스트 Glob·bash 이미지 디코딩 둘 다 된다. PNG는 **청크 CRC 전건 검증**을 걸어라(마운트 절단 방어) — 이번 324파일 무결성 실패 0.
- **팔레트·element 값은 U-020으로 정본화됐다**(design-system [2]). 아래는 출처 기록·판단 근거로만 남긴다.
  - 크롬 실측: 창 배경 `#001C33`(52%) · 패널 `#0667BD`(19%) · 테두리 `#3394DE` · 크림 `#FAF7D5` · 슬롯 `#000000`. 상태 언어 = 기본 파랑 / 눌림 주황 `#E25706` / 활성 황금 `#E2B206`. **`#0000FF`(3~6%)는 구형 투명 키 컬러 — 디자인 색 아님.**
  - element 아트 실측: water `#19B2FF`(200°) · fire `#FF5500`(20°) · earth `#95B259`(80°, **올리브** — amber 아님) · wind `#66CCCC`(180°, **청록** — green 아님). **관례 추정이 2건 반증됐다.**
  - **water(200°) ↔ 크롬 파랑(208°) 8° 차 / fire(20°) ↔ 눌림 주황(22°) 2° 차** — 여기서 3계층 규칙과 "element 칩은 `bg` 위에만"이 나왔다. 색 재배치는 아트와 어긋나서 기각했다(아트가 정본, 양보는 웹의 배치 규칙이 한다).
- **파생값이 위험 지점이다**(U-020 뿌리 점검). 실측 5색은 AA 전건 통과인데 **미달 1·오기 2가 전부 내가 손으로 만든 파생값에서** 나왔다(hover #1E7FD4 = 3.83:1). **대비비를 눈대중으로 적지 마라** — 그 숫자도 D-086의 "주장"이다. 검증 스크립트 패턴은 `outbox/016` 뿌리 점검.
- **아트 단위 = typeCode × 레벨**(P-013 확정). large 324개 md5 전수 → 고유 324·중복 0. **레벨 배지가 PNG에 구워져 있다.** 단 아트 위임은 전면이 아니다 — 레벨은 시세 집계·필터 축이라 **목록 판독성이 도메인 요구**(위 선결 판정). 상세(`l` 3x 이상)는 위임으로 충분. **크기별로 갈리며 일괄 규칙이 성립하지 않는다.**
- **자산 경로 = `gold_black/level{level}/{l|s}/{element}/{kind}.png`, 소비 필드 3개**(`level`·`element`·`kind`). 기획 031이 내 `{mainCategory}` 전제를 정정했다 — 648 산술에 그 차원이 없어 `gold_black`은 **고정 세그먼트**다. 계약 변경 0(P-012), `domain-spec [7]` 개정 불요.
- **게임과 웹의 겹침은 생각보다 작다.** 게임은 *보여주는* UI, 웹은 *입력받는* UI다. 게임에 텍스트 입력 필드가 사실상 없고(`input_btn_c/n` 2개뿐) 인벤토리 창도 자산에 없다. 로그인·검색·가격·입찰·필터·카운트다운·페이지네이션·토스트는 **전부 디자인이 만든다.** U-016 "차용의 실체는 시각 언어"가 이 뜻.
- **U-016 실행 원칙**: 크롬은 CSS 재현(버튼 텍스트가 이미지에 구워져 웹 카피 생성 불가), 비트맵 차용은 아이템 아트 한정. `image-rendering: pixelated` + 원본 정수배 확대. 50×93은 상세 주인공으로 작아 3x(150×279) 전제.
- 폰트는 **Pretendard 유지 추천**(U-003) — 게임 비트맵 폰트는 라이선스·가독성·다국어 문제. 사용자 미확답.
- **D-090/U-018 — 이번 세션 최대 교훈이자 한 겹 더 깊어졌다.** 마운트 뷰는 낡은 내용이 아니라 **파일을 바이트 단위로 절단해서** 서빙한다(`decision-log` 21KB → 9.8KB). `git status`가 손대지 않은 파일 6개를 "수정됨"으로, 허구의 `218 deletions`를 보고했다. **`git`이 그 절단본을 워크트리로 읽으므로 sandbox에서 커밋하면 저장소가 파손된다** — 지금 막고 있는 건 D-090이 아니라 D-061이다(총괄 통지 = `outbox/014`). **저장소 파일 판단은 무조건 호스트 Read/Grep/Glob/Edit. bash는 계산·이미지 디코딩만.**
- 커밋은 사용자 단일 실행(D-061). 역할은 `docs(디자인):` 메시지 블록만 제안, git 대행 금지, 스테이징 `docs/ux/` 한정. **호스트 `git status` 목록이 정본이다.**

## 재개 필독 (순서대로, 호스트 도구로 읽을 것)

1. `docs/ux/notes/handover.md` (이 파일)
2. `docs/ux/decision-log.md` — U-001~020. 특히 **U-020**(색 토큰·3계층 규칙) · U-016(비주얼 방향) · U-017(운영 모드 → D-091) · U-018(D-090) · U-019(D-092 3단)
3. `docs/ux/inbox-log.md` — 처리 이력(095·031·032까지). 수신 스캔은 **호스트 Grep 전용**, 패턴 3종 병행
4. `docs/ux/outbox/017` — 프론트 충돌 통지(조건부 회신 대기). 배경은 `015`
5. 확정 스펙: `docs/api-contract.md` **v1.4** [1.6]·[2.5]·[3.3] → `docs/erd.md` **v0.7** → `docs/domain-spec.md` **v0.5** [7]
6. `docs/frontend-planning/screen-spec.md` v0.3 (기능 명세, 기획 P 소유)
7. `docs/ux/design-system.md` **v0.2**(색 확정) · `ux-flows.md` · `mockups/`(wireframe-auth · member · wallet)

재개 방법: 킥오프 부트스트랩 + "handover 읽고 재개" → 첫 응답은 이해 확인 브리핑. **사전 승인을 묻지 말 것**(D-091) — 기획에서 받아 자율 확정, 완성 후 사용자가 변경 통보.
