# 디자인 handover

성격: 휘발성 맥락 — 파일에 있는 내용은 경로 참조만. 이력 없음(덮어씀).
갱신: 2026-07-16

## 진행 중

- **게임 스킨 목업** (`mockups/game-skin.html`, 미착수) — 착수 2026-07-16 예정 · 지금 어디까지: 재정리 블록(⓪-b·①)을 먼저 처리하느라 미착수. 상거래 카드 대기 항목 0(기획 033) · 다음 한 수: 확정 토큰(design-system v0.2)을 경매 상세·상거래 카드에 입혀 렌더. **총괄 002 [5]가 "목업은 계속해라"로 미차단 명시.**
  - **함께 실측할 선결 판정 1건**: `s`(26×28)에 구워진 레벨 배지가 목록에서 판독되는가(P-013 조건). 불충분하면 계약 `level`로 표기한다.
- **프론트 009 회신 2건** — 착수 2026-07-16 · `focus-ring` 값 확정 + `primary.fg` 정적/변수 판정. 프론트가 회신 즉시 CC 토큰 교체 지시를 발행한다고 명시 → **내 회신이 프론트 레인의 유일한 블로커다.**

## 대기 중

- `frontend/outbox/009` — 프론트가 내게 건 결정 요청. **내가 회신할 차례다(대기 아님).** 017 조건부 대기는 이것으로 종결 — 조건 [3](토큰을 컴포넌트에 이미 바름)이 충족돼 판정이 뒤집혔고, 프론트는 그럼에도 교체 진행으로 자율 판정(D-084).
- 원본 UI 자산 hex → 사용자 결정 큐(095 [7절]). **실측으로 실질 해소** — 큐에 남아 있어도 블로커 아니다.
- `gold_force` 18개 자산 용도 → 사용자 결정 큐(낮은 우선순위). **내 자율(D-091)이고 상거래 카드를 막지 않는다**(기획 033).

## 다음 세션이 알아야 할 판단

- **운영 모드 = D-091(ACCEPTED)이 정본 근거다**(U-017 escalated-as, 095). 사용자·총괄에 **사전 승인을 묻지 않는다.** 기획(P)에서 필요한 것을 받아 자율 확정하고, 사용자는 완성 후 변경을 통보한다(사후 변경 요청권 = 거부권). 규칙 1(기획 경유)·규칙 2(시각 예시 의무 — 기획·총괄행 결정 요청에 한정) 유지. `ux/rules.md` [1]·[11] 반영 완료.
- **`design-guide.md` → `rules.md` 개명 완료**(2026-07-16, C-073 ⓪-b). 호스트 Read → Write → 삭제. 살아있는 참조 6곳 정정(`ux-flows` 3 · `accessibility` 1 · `design-system` 2). 불변 문서(`outbox/**`·`decision-log.md`)는 구 이름 그대로 — 정상이다.
- **자산 경로 확보·실측 완료. U-012 이미지 블로커는 실질 해소다**(총괄 추적표엔 "사용자 제공 대기"로 열려 있을 수 있다).
  - 아이템 아트: `D:\private_server\SP\tool\Survival-project-PGF-decompressor-main\src\images\global\card\card_image\gold_black` — 648 = 9레벨 × 2크기(l 50×93 / s 26×28) × 4속성 × 9종류. 속성·종류가 계약 `element`·`kind`와 일치.
  - UI 크롬: `...\src\images\interface\global\card_info\` — `wnd_cardinfo.png`(211×307) · 탭/버튼 normal·click 쌍 · **`card_info.xml`에 자식 좌표**. 공통 버튼·스크롤은 `interface\global\`.
  - **접근 방법**: 위 경로를 폴더 연결하면 호스트 Glob·bash 이미지 디코딩 둘 다 된다. PNG는 **청크 CRC 전건 검증**을 걸어라(마운트 절단 방어) — 이번 324파일 무결성 실패 0.
- **팔레트·element 값은 U-020으로 정본화됐다**(design-system [2]). 아래는 출처 기록·판단 근거로만 남긴다.
  - 크롬 실측: 창 배경 `#001C33`(52%) · 패널 `#0667BD`(19%) · 테두리 `#3394DE` · 크림 `#FAF7D5` · 슬롯 `#000000`. 상태 언어 = 기본 파랑 / 눌림 주황 `#E25706` / 활성 황금 `#E2B206`. **`#0000FF`(3~6%)는 구형 투명 키 컬러 — 디자인 색 아님.**
  - element 아트 실측: water `#19B2FF`(200°) · fire `#FF5500`(20°) · earth `#95B259`(80°, **올리브** — amber 아님) · wind `#66CCCC`(180°, **청록** — green 아님). **관례 추정이 2건 반증됐다.**
  - **water(200°) ↔ 크롬 파랑(208°) 8° 차 / fire(20°) ↔ 눌림 주황(22°) 2° 차** — 여기서 3계층 규칙과 "element 칩은 `bg` 위에만"이 나왔다. 색 재배치는 아트와 어긋나서 기각했다(아트가 정본, 양보는 웹의 배치 규칙이 한다).
- **파생값이 위험 지점이다**(U-020 뿌리 점검). 실측 5색은 AA 전건 통과인데 **미달 1·오기 2가 전부 내가 손으로 만든 파생값에서** 나왔다(hover #1E7FD4 = 3.83:1). **대비비를 눈대중으로 적지 마라** — 그 숫자도 D-086의 "주장"이다. 검증 스크립트 패턴은 `outbox/016` 뿌리 점검.
- **아트 단위 = typeCode × 레벨**(P-013 확정). large 324개 md5 전수 → 고유 324·중복 0. **레벨 배지가 PNG에 구워져 있다.** 단 아트 위임은 전면이 아니다 — 레벨은 시세 집계·필터 축이라 **목록 판독성이 도메인 요구**(위 선결 판정). 상세(`l` 3x 이상)는 위임으로 충분. **크기별로 갈리며 일괄 규칙이 성립하지 않는다.**
- **자산 경로 = `gold_black/level{level}/{l|s}/{element}/{kind}.png`, 소비 필드 3개**(`level`·`element`·`kind`). 골드포스 활성 매물도 `gold_black` 유지 + 별도 표시(배지·테두리 등, **표현은 내 자율**) — 기획 033 (a) 확정. **D-066은 검색·집계 축 배제이지 표시 금지가 아니다**(계약 [3.3] `goldforceExpireAt` 노출이 근거, 총괄 승인).
- **게임과 웹의 겹침은 생각보다 작다.** 게임은 *보여주는* UI, 웹은 *입력받는* UI다. 게임에 텍스트 입력 필드가 사실상 없고(`input_btn_c/n` 2개뿐) 인벤토리 창도 자산에 없다. 로그인·검색·가격·입찰·필터·카운트다운·페이지네이션·토스트는 **전부 디자인이 만든다.** U-016 "차용의 실체는 시각 언어"가 이 뜻. **총괄이 이 프레임을 "원게임은 게임이고 이건 거래소다"로 확장했다**(033).
- **U-016 실행 원칙**: 크롬은 CSS 재현(버튼 텍스트가 이미지에 구워져 웹 카피 생성 불가), 비트맵 차용은 아이템 아트 한정. `image-rendering: pixelated` + 원본 정수배 확대. 50×93은 상세 주인공으로 작아 3x(150×279) 전제.
- 폰트는 **Pretendard 유지 추천**(U-003) — 게임 비트맵 폰트는 라이선스·가독성·다국어 문제. 사용자 미확답.
- **D-090/U-018 — 마운트 뷰는 낡은 내용이 아니라 파일을 바이트 단위로 절단해서 서빙한다**(`decision-log` 21KB → 9.8KB). `git status`가 손대지 않은 파일 6개를 "수정됨"으로, 허구의 `218 deletions`를 보고했다. **`git`이 그 절단본을 워크트리로 읽으므로 sandbox에서 커밋하면 저장소가 파손된다** — 지금 막고 있는 건 D-090이 아니라 D-061이다(총괄 통지 = `outbox/014`). **저장소 파일 판단은 무조건 호스트 Read/Grep/Glob/Edit. bash는 계산·이미지 디코딩·`rm`만.**
- **★ 호스트 Glob이 거짓 0을 낸다**(프론트 009 실측 — 파일 14개 폴더에 0건 반환). 내 `outbox/017` [2] 관측("cc-reports 없음")도 **같은 이유로 거짓이었다.** 총괄이 `_broadcast/002` [6]에서 **격상 불요로 닫았다**(대체 수단 `Grep(".", path=<폴더>, files_with_matches)`가 전 파트에 퍼져 막히는 파트 0). **부재 판정에 Glob을 쓰지 마라. Grep을 써라.**
- 커밋은 사용자 단일 실행(D-061). 역할은 `docs(디자인):` 메시지 블록만 제안, git 대행 금지, 스테이징 `docs/ux/` 한정. **호스트 `git status` 목록이 정본이다.**

## 재개 필독 (순서대로, 호스트 도구로 읽을 것)

1. `docs/ux/notes/handover.md` (이 파일)
2. `docs/ux/decision-log.md` — U-001~020. 특히 **U-020**(색 토큰·3계층 규칙) · U-016(비주얼 방향) · U-017(운영 모드 → D-091) · U-018(D-090) · U-019(D-092 3단)
3. `docs/ux/inbox-log.md` — 처리 이력. 수신 스캔은 **호스트 Grep 전용**, 패턴 3종 병행
4. `docs/ux/outbox/018` — 재정리 보고(ⓐ~ⓓ). `frontend/outbox/009` — 회신 대기 중인 결정 요청
5. 확정 스펙: `docs/spec/api-contract.md` **v1.4** [1.6]·[2.5]·[3.3] → `docs/spec/erd.md` **v0.7** → `docs/spec/domain-spec.md` **v0.5** [7] — **경로가 `docs/spec/` 아래로 이동했다**(`management/outbox/_broadcast/001`). **bash로 읽지 마라 — 이 3파일이 절단본으로 서빙 중이다.**
6. `docs/frontend-planning/screen-spec.md` v0.4 · `skeleton-plan.md` v0.3 (기능 명세, 기획 P 소유. **`_archive/` 이관 대상 — 산출물만 기획 폴더로 회수 예정**, C-073 [5])
7. `docs/ux/rules.md`(구 `design-guide.md`) · `design-system.md` **v0.2**(색 확정) · `ux-flows.md` · `mockups/`(wireframe-auth · member · wallet)
