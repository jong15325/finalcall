# 디자인 handover

성격: 휘발성 맥락 — 파일에 있는 내용은 경로 참조만. 이력 없음(덮어씀).
갱신: 2026-07-16

## 진행 중

- **★ D-097로 디자인이 도메인 순서의 2번째가 됐다** — 기획 계약 → **디자인 목업**(빠르게·완성 아님, **산출 = 목업 + 계약 델타**) → 백엔드 코드 → **디자인 세부 ∥ 보안** → QA → G4-n 판정. **내 목업이 백엔드 착수를 막는다.** member가 첫 적용.
- **member 1-1 완료**(`mockups/member.html` + `outbox/MEMBER/001`, 2026-07-17). 델타 후보 3 → **실질 2**(2 = `MEMBER_002` `blockedBy`, 3 = nickname 검증 전사). **델타 1은 실측으로 스스로 죽었다** — `GET /me/balance`는 024로 이미 있다(B-029). **교훈: 막힘은 전염된다고 가정하기 쉽고 그게 틀린다**("화폐가 막혔으니 잔액도 막혔겠지"). **다음 도메인 목업에서도 "이건 없겠지"를 실물로 확인해라.**
- **다음 = 1-3 디자인 세부 완성(member)**, 백엔드 1-2 뒤. 그 전에 게임 스킨 목업 사용자 확인분 반영.
- **게임 스킨 목업 v1 완료** (`mockups/game-skin.html`, 2026-07-16) — **사용자 육안 확인 대기.** 확정 토큰 v0.2.1을 상거래 카드 6종·경매 상세·색 계층 검증대에 렌더. **아트는 원게임 실물**(자산 폴더 연결 → `file://` 절대경로, 목업 전용). 다음 한 수: **사용자가 3-a(`s` 1배 레벨 사다리)를 보고 판독 여부를 말하면** 카드의 `Lv.` 병기를 뺄지 확정한다.
  - **P-013 선결 판정 = 잠정 "불가"**(3-a 렌더). 계약 `level` 텍스트 병기를 카드에 이미 반영. **크기별로 갈린다** — 상세(`l` 3배)는 아트 위임.
  - 목업이 일부러 **규칙 위반을 렌더한다**(4-a 오른쪽 = `primary` 위 element 칩). 계산이 취향이 아님을 보이려는 것 — 지우지 마라.
- **프론트 `text-primary-500` 3곳 용도 회신 대기**(`outbox/019` 말미) — 폐기된 6단 스케일의 "링크·강조 텍스트" 자리가 새 팔레트에 없다. `primary`를 `bg` 위 텍스트로 쓰면 **3.03:1로 본문 AA 미달.** 실물 문자열 받으면 내가 정한다(`border` #3394DE 5.3:1 + underline이 유력).

## 대기 중

- `frontend/outbox/019` 회신 — 위 1건. **프론트 CC 토큰 교체는 이것을 안 기다린다**(019에 명시). 막는 것 없음.
- 원본 UI 자산 hex → 사용자 결정 큐(095 [7절]). **실측으로 실질 해소** — 큐에 남아 있어도 블로커 아니다.
- `gold_force` 18개 자산 용도 → 사용자 결정 큐(낮은 우선순위). **내 자율(D-091)이고 상거래 카드를 막지 않는다**(기획 033).

## 다음 세션이 알아야 할 판단

- **운영 모드 = D-091(ACCEPTED)이 정본 근거다**(U-017 escalated-as, 095). 사용자·총괄에 **사전 승인을 묻지 않는다.** 기획(P)에서 필요한 것을 받아 자율 확정하고, 사용자는 완성 후 변경을 통보한다(사후 변경 요청권 = 거부권). 규칙 1(기획 경유)·규칙 2(시각 예시 의무 — 기획·총괄행 결정 요청에 한정) 유지. `ux/rules.md` [1]·[11] 반영 완료.
- **★ 자산 폴더를 연결하면 실물 아트가 렌더된다** — `D:\private_server\SP\tool\Survival-project-PGF-decompressor-main\src\images`. 목업이 `file://` 절대경로로 참조하고 상단 입력칸에서 바꿀 수 있다. **648개 CRC 전건 검증 재현(실패 0 · 치수 324/324) — handover 실측과 일치.**
- **`design-system` v0.2.1 — 내 정본에 구멍이 있었다**(프론트 009가 잡음). [2.2]가 정의한 `focus-ring`이 **[2.6] 매핑 표에 없었고**, 내 드롭인 블록(017 [4])이 [2.6]을 베껴 승계했다. **[2.6]이 핸드오프의 유일한 소비 표면인데 정의 절과 이중 관리라 갈린다 — 다음 토큰 신설 때 또 벌어질 수 있다.** 조항 안 만들었다(D-093), [2.6]에 대조 주만 달았다.
- **`outline-offset: 2px`는 AA 조건이다**(019 계산). offset이 있으면 링 인접색은 부모 표면뿐이라 전건 통과(최악 `primary` 5.25). **0이면 `primary-selected`(황금) 1.82:1 · `success` 1.60:1로 무너진다.** 활성 탭·현재 페이지가 정확히 포커스 대상이라 급소다. `[2.2]`에 근거 명시했다.
- **같은 hex `#FAF7D5`가 3토큰이고 처리가 셋 다 다르다** — `text`(변수) · `focus-ring`(변수) · `primary.fg`(**정적**, 조작 계층은 테마 무관). 다크에서 우연히 일치할 뿐이다. **`text`로 통합하면 라이트에서 파랑 버튼 위 글자가 검정이 된다.**
- **`design-guide.md` → `rules.md` 개명 완료**(2026-07-16, C-073 ⓪-b). 호스트 Read → Write → 삭제. 살아있는 참조 6곳 정정(`ux-flows` 3 · `accessibility` 1 · `design-system` 2). 불변 문서(`outbox/**`·`decision-log.md`)는 구 이름 그대로 — 정상이다.
- **자산 경로 확보·실측 완료. U-012 이미지 블로커는 실질 해소다**(총괄 추적표엔 "사용자 제공 대기"로 열려 있을 수 있다).
  - 아이템 아트: `D:\private_server\SP\tool\Survival-project-PGF-decompressor-main\src\images\global\card\card_image\gold_black` — 648 = 9레벨 × 2크기(l 50×93 / s 26×28) × 4속성 × 9종류. 속성·종류가 계약 `element`·`kind`와 일치.
  - UI 크롬: `...\src\images\interface\global\card_info\` — `wnd_cardinfo.png`(211×307) · 탭/버튼 normal·click 쌍 · **`card_info.xml`에 자식 좌표**. 공통 버튼·스크롤은 `interface\global\`.
  - **접근 방법**: 위 경로를 폴더 연결하면 호스트 Glob·bash 이미지 디코딩 둘 다 된다. PNG는 **청크 CRC 전건 검증**을 걸어라(마운트 절단 방어) — 이번 324파일 무결성 실패 0.
- **팔레트·element 값은 U-020으로 정본화됐다**(design-system [2]). 아래는 출처 기록·판단 근거로만 남긴다.
  - 크롬 실측: 창 배경 `#001C33`(52%) · 패널 `#0667BD`(19%) · 테두리 `#3394DE` · 크림 `#FAF7D5` · 슬롯 `#000000`. 상태 언어 = 기본 파랑 / 눌림 주황 `#E25706` / 활성 황금 `#E2B206`. **`#0000FF`(3~6%)는 구형 투명 키 컬러 — 디자인 색 아님.**
  - element 아트 실측: water `#19B2FF`(200°) · fire `#FF5500`(20°) · earth `#95B259`(80°, **올리브** — amber 아님) · wind `#66CCCC`(180°, **청록** — green 아님). **관례 추정이 2건 반증됐다.**
  - **water(200°) ↔ 크롬 파랑(208°) 8° 차 / fire(20°) ↔ 눌림 주황(22°) 2° 차** — 여기서 3계층 규칙과 "element 칩은 `bg` 위에만"이 나왔다. 색 재배치는 아트와 어긋나서 기각했다(아트가 정본, 양보는 웹의 배치 규칙이 한다).
- **파생값이 위험 지점이다**(U-020 뿌리 점검). 실측 5색은 AA 전건 통과인데 **미달 1·오기 2가 전부 내가 손으로 만든 파생값에서** 나왔다(hover #1E7FD4 = 3.83:1). **대비비를 눈대중으로 적지 마라** — 그 숫자도 D-086의 "주장"이다. 검증 스크립트 패턴은 `outbox/016` 뿌리 점검.
- **아트 단위 = typeCode × 레벨**(P-013 확정). large 324개 md5 전수 → 고유 324·중복 0. **레벨 배지가 PNG에 구워져 있다.** 단 아트 위임은 전면이 아니다 — 레벨은 시세 집계·필터 축이라 **목록 판독성이 도메인 요구**(위 선결 판정). 상세(`l` 3x 이상)는 위임으로 충분. **크기별로 갈리며 일괄 규칙이 성립하지 않는다.**
- **자산 경로 = `gold_black/level{level}/{l|s}/{element}/{kind}.png`, 소비 필드 3개**(`level`·`element`·`kind`). 골드포스 활성 매물도 `gold_black` 유지 + 별도 표시(배지·테두리 등, **표현은 내 자율**) — 기획 033 (a) 확정. **D-066은 검색·집계 축 배제이지 표시 금지가 아니다**(계약 [3.3] `goldforceExpireAt` 노출이 근거, 총괄 승인).
- **게임과 웹의 겹침은 생각보다 작다.** 게임은 *보여주는* UI, 웹은 *입력받는* UI다. 게임에 텍스트 입력 필드가 사실상 없고(`input_btn_c/n` 2개뿐) 인벤토리 창도 자산에 없다. 로그인·검색·가격·입찰·필터·카운트다운·페이지네이션·토스트는 **전부 디자인이 만든다.** U-016 "차용의 실체는 시각 언어"가 이 뜻. **총괄이 이 프레임을 "원게임은 게임이고 이건 거래소다"로 확장했다**(033).
- **U-016 실행 원칙**: 크롬은 CSS 재현(버튼 텍스트가 이미지에 구워져 웹 카피 생성 불가), 비트맵 차용은 아이템 아트 한정. `image-rendering: pixelated` + 원본 정수배 확대. 50×93은 상세 주인공으로 작아 3x(150×279) 전제.
- 폰트는 **Pretendard 유지 추천**(U-003) — 게임 비트맵 폰트는 라이선스·가독성·다국어 문제. 사용자 미확답.
- **★★ 도구 규칙이 2026-07-16에 통째로 바뀌었다(C-075). 아래가 현행이고, 종전 기억을 버려라.**
  - **`[9.1]` 살아 있다 — 저장소 파일의 목록·내용·존재·수량·편집 위치 판단은 호스트 도구.** 단 **`Glob`이 목록에서 빠졌다**(현행 = **Read·Grep·Edit**). 내 Glob 거짓 0 관측이 규약에 반영된 것이다. **부재 판정은 `Grep(".", path=<폴더>, files_with_matches)`.**
  - **`[9.2]`·`[9.4]`·`[9.25]` 결번 · `[9.3]` 표 삭제.** **`mv`·`cp` 금지가 실측으로 반증됐다** — 같은 마운트 안 `mv` = `rename(2)`, inode 보존, **내용을 읽지 않으니 절단할 자리가 없다.** **파일 이동을 `mv` 한 줄로 해도 된다.** 호스트 Read→Write→삭제 3단은 이제 의무가 아니다(해도 되지만 느리다).
  - **`[9.3]`은 번호가 살아있고 내용이 통째로 다르다 — 조용히 틀리는 형태다.** 현행: *"되돌릴 수 없는 것을 남기는 실행은 사용자에게 묻고 지시를 받아서 한다 — DB 스키마·외부 시스템·원격 저장소. **파일 조작은 묻지 않는다.**"* **종전의 "bash 허용/금지 표"가 아니다.**
  - **판정 기준이 "bash냐 호스트냐" → "되돌릴 수 있냐"로 바뀌었다.** 커밋 전 워크트리는 전부 되돌릴 수 있고, 정본이 되는 관문은 커밋 하나이며 그건 D-061·`[9.8]`이 이미 묶고 있다.
  - **`[9.8]` 완화**: docs 커밋 git 실행은 사용자 전담이나 **사용자가 지시하면 대행한다.** 스테이징 `docs/ux/` 한정·`git add -A` 금지(`[9.14]`)는 유지.
  - **내 `outbox/014`가 인용됐다** — *"지금 막고 있는 건 D-090이 아니라 D-061이다"*가 C-075 [2]의 근거로 채택됐다. 그 관측은 맞았다.
  - **절단은 그 순간 그 3파일의 캐시 현상이었지 `mv`의 성질이 아니었다**(C-075 [1] 실측 — 지금은 bash `wc -l`과 호스트 Grep이 `spec/` 3파일에서 완전 일치). **그래도 판단은 호스트로 한다** — `[9.1]`이 그대로다.
- **교훈(조항 아님)**: 내 `018` ⓓ가 잡은 "`[N]`과 「N절」이 둘 다 숫자라 구분이 안 된다"의 상위 형태가 이것이다. **번호를 안 쓰고 산문으로 규칙을 재진술하면 grep에 아예 안 걸린다.** 이 handover가 `[9.4]`를 번호 없이 풀어 적고 있었고 `\[9\.\d+\]` grep이 0건을 냈다. **다음에 규약이 바뀌면 번호 grep으로 끝내지 마라 — 산문도 읽어라.**

## 재개 필독 (순서대로, 호스트 도구로 읽을 것)

1. `docs/ux/notes/handover.md` (이 파일)
2. `docs/ux/decision-log.md` — U-001~020. 특히 **U-020**(색 토큰·3계층 규칙) · U-016(비주얼 방향) · U-017(운영 모드 → D-091) · U-018(D-090) · U-019(D-092 3단)
3. `docs/ux/inbox-log.md` — 처리 이력. 수신 스캔은 **호스트 Grep 전용**, 패턴 3종 병행
4. `docs/ux/outbox/018` — 재정리 보고(ⓐ~ⓓ). `frontend/outbox/009` — 회신 대기 중인 결정 요청
5. 확정 스펙: `docs/spec/api-contract.md` **v1.4** [1.6]·[2.5]·[3.3] → `docs/spec/erd.md` **v0.7** → `docs/spec/domain-spec.md` **v0.5** [7]. **경로는 `docs/` 루트 그대로다** — 코드만 `backend/` 아래로 갔다(D-098, `_broadcast/004` [4]). 내 문서엔 `src/`·`gateway/` 참조가 없어 **무영향**. 호스트 Read로 읽어라(`[9.1]`).
6. `docs/frontend-planning/screen-spec.md` v0.4 · `skeleton-plan.md` v0.3 (기능 명세, 기획 P 소유. **`_archive/` 이관 대상 — 산출물만 기획 폴더로 회수 예정**, C-073 [5])
7. `docs/ux/rules.md`(구 `design-guide.md`) · `design-system.md` **v0.2**(색 확정) · `ux-flows.md` · `mockups/`(wireframe-auth · member · wallet)
