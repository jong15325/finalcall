# 기획(P) handover (2026-07-15)

성격: 세션 상태 스냅샷 — **파일에 없는 기억만**. 정본은 각 파일. 1페이지 이내(D-059).

## 진행 중

- **미처리 지시 0 · 발신 회신 대기 0. 손이 빈 상태** — 다음 배치는 총괄이 한다.
- 설계 3종 전부 확정·구현 단계(G4-n): **domain-spec v0.5 / erd v0.7 / api-contract v1.4**.
- 화면 층: **screen-spec v0.3 / skeleton-plan v0.2** (프론트 F 구성·CC 생성의 선행 기준선 — 인계 완료).

## 대기 중

- **미커밋 다수**(D-061 = 사용자 단일 실행). 스펙 3종 · screen-spec · skeleton-plan · decision-log(P-009~013) · outbox 023~033 · inbox-log. 커밋 블록은 각 응답 말미에 제시했다.
- **persist 보안 안건**(내가 026에서 제기) — 추적표 ON-HOLD. 교환비율 해소 시 동반 처리(090 확인). **wallet 도메인 착수 전 확정 필수**. 스켈레톤은 메모리 세션 기본이라 지금은 안 막힌다.
- `gold_force` 18개 자산 실제 용도 — 사용자 결정 큐(낮은 우선순위). 디자인 자율(D-091), 블로커 아님.

## 휘발성 맥락 (핵심)

**역할** — D-079로 PF 흡수 → **기획(P) 단일**. 서버 도메인(계약 정본) + 프론트/화면 기획 + 디자인 상류 조율을 겸한다. PF-xxx 동결, P-xxx 계속(최신 **P-013**). 내 소유 = `docs/domain-spec.md`·`erd.md`·`api-contract.md` + `docs/frontend-planning/` + `docs/design/`.

**규약이 12건+로 급증했다. 실제로 몸에 붙여야 할 것:**
- **D-090 (가장 중요)** — 저장소 파일의 목록·내용·**편집 위치 판단**은 호스트 도구(Read/Grep/Edit)만. **bash 마운트 뷰 판단 금지**(에러 없이 낡은 내용을 서빙해 의심 트리거가 없다). bash는 git·빌드·테스트만.
- **D-083** 이의 제기는 권한이 아니라 **의무**. 총괄 지시가 진행 중 작업과 부딪히면 따르기 전에 말한다.
- **D-086** 부재("없음")·수량("N건") 근거는 탐색 방법 1구 명시 + 중대하면 교차검증.
- **D-087** 절 참조 `[N.M]`, `§` 금지(앞으로만·소급 금지).
- **D-089** 결정 로그에 `집행:` 필드(실행 문서 경로+반영 여부, 공란 금지).
- **D-091** 디자인이 비주얼·UX 자율 확정(D-072 규칙3 대체). **규칙1 기획 경유는 유지** — 상류 도메인 입력은 여전히 내 몫.
- **D-092** 확정 스펙 상향 **전파** 시 각 역할 3단(①델타 검토 →②반영 →③근거 줄 갱신). 내 스펙 상향에 직결.

**내 문서 관리 방식(규약 아님·자율, D-084)** — 버전 상향 마무리 2단: ①참조 버전 grep ②상태줄 == 변경이력 최신 행 대조. **한계 2개**: 버전 문자열만 보므로 구조 오류를 못 잡고, 자기 문서 상향 때만 도니 남이 올린 내 전사본은 못 덮는다(후자는 D-092가 보완).

**이번 세션 내 오류 4건 — 공통 원인은 "확인 없이 단정"이다. 반복 주의:**
1. 066 처리 시 "회원 스펙 불요"로 단정 → 실제 공백이었다(백엔드 022가 발견).
2. v1.4 상향 시 근거 줄·본문 v1.3 미전파(프론트 005가 발견).
3. **bash 뷰로 편집 위치를 판단해 erd `temp_storage` 표를 쪼갬**(자진 발견·복구. 이 관측이 D-090 근거가 됐다).
4. 032에서 (b)를 살려둔 탓에 **없는 선결 의존**을 만들어 디자인을 대기시킬 뻔했다(총괄 097 지적).
→ 규약을 인용할 때 **그 규약이 실물에서 무엇이 되는지**까지 내려가 확인할 것. 결론뿐 아니라 **작업 대상의 형상**도 교차검증할 것.

**도메인 핵심**
- 아이템 = 원게임 SurvivalProject 실구조(D-067 전면 사용). 타입코드 4자리 `[대분류][중분류/슬롯군][속성][종류]`, **등급 없음**(D-073).
- **아트 자산**: `gold_black` 648 = 9레벨 × 2크기 × 4속성 × 9종류. **`mainCategory` 차원 없음** → 경로 `gold_black/level{level}/{l|s}/{element}/{kind}.png`(P-012). 레벨마다 아트가 다르고 레벨 배지가 아트에 구워져 있다(P-013 — 목록 판독성은 도메인 요구).
- **D-048 유지** — 아이템 세부는 P 발번 금지·총괄 안건화. 아이템 정본 = D-044~047/D-062/D-066/D-073.
- **D-081** soft delete 자연키 UK = 생성 컬럼 패턴(`<자연키>_active`). 함정 = `UNIQUE(자연키, deleted_at)`(문자 준수하며 활성 중복 뚫림). 스윕 = `user` 1건·그 외 0건, 조건부 리스크 3건(erd [4] 말미 주).
- **D-080** 탈퇴 = 잔액 소멸 동의. 진행 중 거래는 차단(피해 귀속의 비대칭 — 잔액은 본인 손실, 거래는 타인 피해).
- 네이밍 `auction`/`shop`/`sale_order`. Flyway **V4까지 소비**(V4 = 자연키 UK 재구성).
- ON-HOLD: 캐시↔게임머니 교환비율 · 플랫폼 수수료 · 인증 세션 persist.

## 재개 필독 (순서)

1. `design/notes/handover.md` (이 파일)
2. `design/inbox-log.md` (수신 이력 — **097까지**) · `design/decision-log.md` (P-001·002·008~013)
3. 확정 스펙: `docs/domain-spec.md` v0.5 → `docs/erd.md` v0.7 → `docs/api-contract.md` v1.4
4. 화면 층: `frontend-planning/screen-spec.md` v0.3 · `skeleton-plan.md` v0.2
5. `management/decision-log.md`(최신 **D-092**) · `decision-index.md`
6. `management/collaboration-guide.md` · `templates.md`
7. 아이템 도출 근거 필요 시: `design/notes/erd-item-modeling.md`
