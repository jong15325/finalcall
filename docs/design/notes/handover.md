# 기획(P) handover

성격: 휘발성 맥락 — 파일에 있는 내용은 경로 참조만. 이력 없음(덮어씀). 형식 `templates.md [8]`.
갱신: 2026-07-16 (퇴근)

## 진행 중

- **★ 내일 착수 — `api-contract v1.5` 계약 변경 4건 일괄.** 지시 = `management/outbox/CONTRACT/001`(**아직 안 열었다면 이것부터 읽어라 — 여기 요약은 재료지 정본이 아니다**).
  - 착수 <2026-07-17 예정> · 지금 어디까지: **수신·인지만. 파일 미개봉.** · 다음 한 수: **`[6.1]`-③ 집행 — `api-contract.md` v1.5 한 번에 4건.**
  - **왜 지금 열렸나 = D-099**: *"D-093 동결선은 **재정리 국면 한정**이었다. ② 재진입 후 도메인 구현이 요구하는 계약 변경은 `[6]` 정상 절차로 간다. D-093이 `[6]`을 폐지한 게 아니다."* → **어제 handover의 "spec/은 동결이라 다시 그릴 것이 없다"가 오늘부로 절반 죽었다.** 동결은 유효하되 `[6]`이 뚫린다.
  - **4건 전부 `api-contract.md` 한 파일이다. v1.5 한 번으로 간다** — 네 번 돌리면 네 번 열고 전파가 네 번 나간다.
  - **내 판단이 걸린 자리 3곳**(총괄이 명시적으로 넘겼다): ① `details`를 `[1.4]` envelope 공통 선택 필드로 열지 `[2.5]` 국소로 둘지 — *"`errors`처럼 `NON_NULL`이 자연스러워 보이나 그건 내 심증"*(총괄) ② `nickname` 규칙을 실제로 정할지 "현행이 전부"를 명시만 할지 — **"signup과 같다"로 닫지 마라, signup에도 규칙이 없다는 게 백엔드 실측이다** ③ `element` wire 표기 — erd가 INT 코드라 대문자 문자열은 `[1.3]` 정합일 뿐 확정은 나.
  - **하지 말 것**: 범위 확대(**D-099는 전면 개방이 아니다**) · `[6.1]`-④ 전파(총괄 몫) · erd 변경 · **`[6.3]`·D-092 3단 생략**(검토 없는 근거 줄 갱신 = "v1.5 근거"가 거짓말이 된다).
  - **★ 막지 않는 것 확인**: 백엔드 1-2(member 코드)는 **v1.5를 안 기다린다.** 내가 늦어도 남이 안 선다.

## 대기 중

- **`[3.12]` 승인 1건** — `references/spec-format-checklist.md:3` **"유형 4 작업 노트" → "유형 2 참조 자료"**. 총괄이 `_broadcast/003` [5] **표 #11**로 12건과 합쳐 사용자에게 올렸다. **사용자 확인 전까지 실행 안 함.** 기각되면 ⓐ-11로 남긴다.
- **사용자 결정 2건**(`_broadcast/004` [3]): **교환비율**(추천 a 1:1 고정 — **화폐를 통째로 막는 유일한 건**) · **아이템 시드**(추천 a 원게임 실물 선확보). **v1.5는 이 둘과 무관하다 — 안 막힌다.**

## 다음 세션이 알아야 할 판단

**★ "재설계"는 없었다. 이 줄이 이 handover에서 제일 중요하다.**
- 전 세션 handover의 *"재설계가 오면 내 것이 정면으로 대상이다"* 는 **틀렸다.** D-093 = 재정리이고 `spec/` 3종은 **동결·무변경**이다(`management/_broadcast/002` [1]·[2] — 총괄이 오독을 자인했고 그 오독이 나와 백엔드를 세웠다).
- **이 대기를 다시 만들지 마라.** 사용자 말을 문서 수준 추론으로 덮은 사례고, 총괄이 물어서 잡혔다.

**내 레인의 실체 — ⓐ 15건은 `outbox/REFORM/002`에 있다. 여기 복제하지 않는다.**
- 뿌리 1줄: **`spec/`이 동결이라 "다시 그릴 것"이 없다.** 남은 건 안 쓴 것 + 결정 대기뿐이다.
- **다음 한 수 = ⓒ-1 교환비율.** 화폐가 member 바로 다음이라 셋 중 유일하게 다음 도메인을 직접 막는다. 총괄이 `_broadcast/003`·`004`에서 내 배치 근거를 그대로 채택해 큐 1번에 놨다.
- **★ ⓒ-3 persist는 풀렸다** — 총괄이 `_broadcast/003` [5]에서 **내 ⓑ-4 지적을 자기 오류로 수용**했다. 090의 "교환비율과 동반 처리"가 **틀린 묶음이었고**(하나는 돈, 하나는 XSS 노출면) 풀었다. → **보안 게이트 2 항목으로 이관, wallet 착수 전 판정.** 내 레인에서 나갔다.
- **D-097이 도메인 순서를 바꿨다**(`_broadcast/004`): 기획 계약 → **디자인 목업** → 백엔드 코드 → 디자인 세부 ∥ 보안 → QA → G4-n 판정.
- **★ 그리고 D-097이 하루 만에 값을 증명했다 — 이게 내일 작업의 뿌리다.** member는 계약이 v1.4에 이미 있어 내 ①을 건너뛰고 **디자인 목업(1-1)부터** 갔다. **목업이 코드 앞에 서니 백엔드 착수 전에 계약 공백 3건이 드러났다.** 그게 지금 내 앞의 v1.5다. **순서를 안 바꿨으면 백엔드가 짜고 나서 나왔을 것들이다.**
- **★ 내가 못 본 것을 남이 봤다 — 기록해 둔다.** 델타 1(*"`GET /me/balance`가 이미 있다"*)은 **디자인이 실측하다 스스로 죽였고**, 델타 3(*"signup에도 nickname 규칙이 없다"*)은 **백엔드 실측이 디자인 전제를 뒤집었다.** 내 REFORM/002 ⓐ 말미에 쓴 *"내 문서의 구멍을 내가 아니라 소비자가 찾았다"*가 하루 만에 세 번째·네 번째로 재현됐다. **ⓐ가 15건이라고 15건인 것이 아니다.**
- **디자인 문장 하나 — 내 ⓐ·ⓑ에 그대로 걸린다**: *"**막힘은 전염된다고 가정하기 쉽고, 그게 틀린다.**"* 화폐가 막혔다고 잔액도 막힌 게 아니었다. **내 ⓐ의 "막는 것" 칸을 다음에 셀 땐 실물을 열어보고 써라.**

**폴더가 바뀌었다 (C-073, 2026-07-16 집행분).**
- `screen-spec.md`·`skeleton-plan.md`는 이제 **`docs/design/` 루트**다(구 `frontend-planning/` — 폴더 소멸, `_archive/frontend-planning/`에 로그만 남음). **PF-xxx 동결.**
- 참조 자료 = `references/`(**유형 2 — 인용 가능, `[3.12]` 걸림**) · `notes/` = 휘발성 흔적(**인용 금지**). 헷갈리면 `rules [3]`.
- **`design/rules.md`는 없다. 만들지 마라** — C-070 판정(지침 없이 기동해 산출물을 냄) · D-093 신설 금지.

**★ bash 규칙이 오늘 뒤집혔다 (C-075). 내 `REFORM/002`를 그대로 믿지 마라.**
- **`[9.2]`·`[9.4]`·`[9.25]`는 결번이다.** **`mv` 써도 된다** — 같은 마운트 안 `mv` = `rename(2)`, inode 보존·내용 미독이라 절단할 자리가 없다. **컨설턴트가 실측으로 자기 조항을 반증했다**(C-059 근거 문장이 추론이었고 그가 `mv`를 써본 적이 없었다).
- **내가 오늘 6파일을 호스트 Read→Write→삭제로 옮겼다. 그 근거가 몇 시간 뒤에 죽었다.** 재작업 없다(결과는 같다). **`REFORM/002`는 불변이라 `[9.2]`·`[9.4]` 인용이 그대로 남아 있다** — 발신 시점엔 참이었다.
- **살아 있는 것**: **`[9.1]`**(목록·내용·존재·수량·편집 위치 판단은 **호스트 도구만**. bash 뷰·`git status`·`git diff` 내용 판단 금지) · **`[9.3]`**(되돌릴 수 없는 것 — DB 스키마·외부 시스템·원격 — 은 사용자에게 묻고 지시받아 실행. **커밋 전 워크트리는 안 묻는다**).
- **절단은 실재했으나 `mv`의 성질이 아니었다** — 그 순간 그 3파일의 캐시 현상. 오늘은 bash `wc`와 호스트 Grep이 `spec/` 3종에서 일치한다. **그래도 확인은 호스트로 해라**(`[9.1]`) — 언제 다시 갈릴지 모른다. 실측표 = `outbox/REFORM/001` [관측].
- **★ 삭제 권한**(조항에 없다): 마운트가 기본 읽기 전용이라 첫 `rm`이 `Operation not permitted`로 실패한다. **멈추지 말고 권한을 받아 끝내라** — 멈춘 상태(사본 있고 원본 남음 = 정본 둘)가 가장 나쁘다. 기획·QA 독립 재현, 총괄 `_broadcast/003` [3] 반영.
- **Glob은 실재 디렉터리에 `No files found`를 반환한다.** 오늘 6번째 재현. 대체 = `Grep(".", path=<폴더>, files_with_matches)`. **발신하지 마라 — 이미 닫혔다**(`management/_broadcast/002` [6]).

**번호를 외우지 마라 — 인용 전에 `rules.md`를 열어라. 오늘만 세 번 움직였다.**
- **102 → 98(C-073) → 94(C-075).** 죽은 것: `[8.11]`·`[8.13]`·`[8.15]`·`[8.17]` · `[3]` 유형 「7 뷰」 · `templates [10]` · `common/status.md`(D-095) · **`[9.2]`·`[9.4]`·`[9.25]`·`[9.3]` 표**(C-075). **`templates [11]`~`[15]`는 안 밀렸다.**
- **`[3.11]` 개정**(C-082): 절 참조는 **`<문서> [N.M]`** — `templates [15]` · `rules [6]` · `CLAUDE.md [4]`. 자기 문서 안이면 `[3.4]`. **`§`·`N절` 금지. 앞으로만** — 참인 기존 표기는 안 고친다(D-087 혼재 허용).
- **★ 내 C-082 스윕은 절반이다. 내일 v1.5에서 마저 세라.** 참조 형태가 **넷**이고 쿼리마다 사각이 다르다(`_broadcast/009` [1]): `<문서> N`(`templates 15`) · `<문서> §N` · `N절` · `(N절)`. **내가 어제 돌린 `\d+절|§`는 `<문서> N` 형태를 못 잡는다** — `skeleton-plan:6`(`(6절)`, 가장 위험한 형태)은 걸렸지만 그건 운이다.
  **컨설턴트 `_broadcast/009` [6]이 "안 센 것 1"로 `spec/` 3종을 명시했다 — 내 소유고 `[6]`이 걸린다.** *"없다는 뜻이 아니라 확인 안 했다는 뜻이다."*
  → **v1.5로 `api-contract.md`를 어차피 연다. 그때 같이 세라 — 한 파일을 두 번 열 이유가 없다**(컨설턴트가 디자인에 낸 판정과 같은 형태). 쿼리: `(templates|rules|CLAUDE\.md|api-contract|erd|domain-spec) +\d+\b` **와** `\d+절|§` **둘 다.** `domain-spec`·`erd`는 별건이라 `[6]` 발동 여부부터 판단해라.
- **결번은 열면 "없음"이라 걸리지만, 밀림과 "실재하는 다른 조항"은 조용히 틀린다.** QA `:14`가 그 실물이다(`templates 15`가 어제까진 없어서 걸렸는데 오늘 「작업 리스트」로 채워졌다).
- **내 문서의 낡은 참조 0건은 조심의 결과가 아니라 인용 습관 때문이다** — templates를 골격 이름으로만 인용하고 번호로 형식을 지목한 적이 없다. **습관이 바뀌면 이 방어도 사라진다.**

**계속 돌리는 내 절차 (규칙 아님 — `[5.35]` 뿌리 점검은 C-066에서 삭제됐다. 의무가 아닐 뿐 효과는 있었다)**
- 2단 마무리: ①참조 버전 grep ②상태줄 == 이력 최신 행 대조. 범위 밖 1건을 실제로 잡았다(screen-spec D-072 → D-091).
- **상대가 지목한 것을 그대로 찾지 말고 그 지목이 왜 나왔는지까지 내려가 실측해라.** 실측이 가설을 세 번 기각했다(총괄 `mv` 유도 → 절단 / 디자인 015 "구 토큰명 잔존" 0건인데 `accent` 군 자체가 죽어 있었음 / 오늘 컨설턴트 "references는 정적" 심증이 이관 당일 반증).

**소유·경계 (헷갈리면 여기)**
- **D-048 유지** — 아이템 세부는 P 발번 금지·총괄 안건화. 아이템 정본 = D-044~047/D-062/D-066/D-073.
- **D-091** — 디자인이 비주얼·UX 자율 확정. **상류 도메인 입력의 기획 경유는 유지**(규칙1).
- 최신 발번 **P-013**. 이력 = `design/decision-log.md`.
- 도메인·아이템 모델 근거 = `notes/erd-item-modeling.md`, 주제별 결정 = `notes/topic-4~6.md`.
- **D-098 모노레포 전환** — 코드가 `backend/` 아래로 갔다. **`docs/`·`config/`는 루트 그대로다.** 내 문서의 `docs/spec/…` 참조는 **안 바뀐다**(실측: `src/`·`gateway/` 참조 내 산출물에 0건).

## 막힌 것

- 없음.
