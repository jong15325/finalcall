# 컨설턴트 결정 로그 (C-xxx)

협업 가이드 4절 티켓 규약(D-009~D-011)을 따른다. 컨설턴트는 한시 역할(D-012)이며
모든 산출물은 PROPOSED로 총괄 회부, 확정 권한 없음(확정 시 D 발번).
상세 분석·진단·마이그레이션은 영역별 설계안 문서(01-docs-structure.md 등) 참조.

---

## C-001. 문서 유형 분류 체계 6종 정의

상태: ACCEPTED · escalated-as D-014 · 소유: 컨설턴트 · 관련: relates-to D-003, D-009 · 2026-07-12

결정
- docs/ 내 모든 문서를 6개 유형으로 분류하고, 유형별 위치·변경 규칙·기준(authority) 등급을 규약화한다.
  1. 확정 스펙 — docs 루트. 변경은 계약 변경 절차(가이드 6절). 기준 최상위.
  2. 지침 — 역할의 행동 규약(collaboration-guide, CLAUDE.md, 파트 지침). 변경은 소유 역할 + 총괄 승인.
  3. 결정 로그 — 각 역할 폴더의 decision-log.md. 항목 불변, 상태 라벨로만 갱신(기존 4절 규약 유지).
  4. 작업 노트 — 각 역할 폴더 notes/. 가변, 비공식. 타 역할이 근거로 인용 금지.
  5. 전달 프롬프트 — 각 역할 폴더 outbox/. 발신 이력 보관(C-004).
  6. 보고·트래킹 — decision-index.md, defects.md, findings.md, 진행 보고. 전 역할 열람.

이유
- 유형 5종이 실재하는데 규약은 결정 로그만 정의되어 혼입이 이미 발생
  (기획 로그에 "주제 4 파생 메모", "주제 5 분석" 등 작업 노트 혼입 — 실물 확인).
- Diátaxis의 핵심 원리 "서로 다른 필요에는 서로 다른 문서 유형, 혼합이 혼란의 최대 원인"을
  프로젝트 문서에 맞게 번안. 4분류(튜토리얼/하우투/레퍼런스/설명)는 사용자 문서용이라 그대로 이식하지 않고
  '필요 기반 분류 + 유형별 규칙'이라는 원리만 차용했다(덜어낸 것).

기각된 대안
- Diátaxis 4분류 직접 적용: 사용자 대상 제품 문서 분류라 프로젝트 운영 문서에 부적합.
- 현행 유지(결정 로그만 규약화): 혼입 재발. 로그가 노트를 흡수하면 ADR 불변성 신뢰 훼손.

레퍼런스: Diátaxis(diataxis.fr), ADR 불변성 원칙(AWS Prescriptive Guidance, Fowler).

---

## C-002. 역할 폴더 표준 내부 구조

상태: ACCEPTED · escalated-as D-015 · 소유: 컨설턴트 · 관련: depends-on C-001 · 2026-07-12

결정
- 모든 역할 폴더(design/ backend/ frontend/ qa/ security/ consulting/)는 동일한 내부 구조를 갖는다.
  ```
  <role>/
  ├── decision-log.md   # 결정 로그 (유형 3)
  ├── notes/            # 작업 노트 (유형 4)
  └── outbox/           # 전달 프롬프트 (유형 5)
  ```
- 역할 고유 산출물(qa/test-plan.md, security/findings.md 등)은 폴더 루트에 둔다.
- management/는 예외적으로 decision-index.md(유형 6)와 prompts/(킥오프 프롬프트 보관)를 추가로 갖는다.
  킥오프 프롬프트는 "타 대화를 기동하는 지침"이므로 발신함(outbox)과 구분한다.

이유
- 위치 규약 부재로 기획(design/prompts/escalation-prompts.md)과 총괄(management/prompts/)이
  서로 다른 방식을 즉흥 사용 중 — 같은 이름 prompts/가 다른 유형(발신함 vs 킥오프 보관)을 담고 있다.
- 구조가 역할마다 같으면 새 역할(보안 등) 추가 시 규약 재설명 비용이 없다.

기각된 대안
- 유형별 최상위 폴더(docs/notes/, docs/outbox/ 아래 역할별 하위): 소유권 규칙("자기 폴더에만 쓴다")과
  충돌하고, 역할 단위 열람·정리가 불편.

레퍼런스: docs-as-code의 규칙적 폴더 토폴로지(passo.uno docs-as-code topologies).

---

## C-003. 읽기 규칙 재설계: 필요 기반 열람, 기준은 문서 유형

상태: ACCEPTED · escalated-as D-016 · 소유: 컨설턴트 · 관련: supersedes 가이드 3절 읽기 규칙(D-006 일부) · 2026-07-12

결정
- "다른 역할의 작업 폴더는 참조하지 않는다"를 폐지하고 다음으로 대체한다.
  1. 기본 열람 범위: 각 역할은 필독 문서(확정 스펙, 총괄 결정 로그, 협업 가이드, 자기 폴더)만
     상시 읽는다. 타 역할 폴더를 상시 탐색하지 않는다.
  2. 필요 기반 열람(pull): 현재 작업에 타 역할 문서의 맥락이 필요할 때는 금지 없이 열람할 수 있다.
     열람 사유가 설명 가능해야 하며(예: "입찰 API 구현에 기획의 소프트클로즈 분석 노트 참조"),
     산출물에 출처를 남긴다.
  3. 기준(authority): 무엇을 근거로 작업할지는 문서 유형이 결정한다.
     확정 스펙 > 총괄 결정 로그(ACCEPTED) > 자기 역할 결정 로그 > 타 역할 결정 로그(맥락 참고)
     > 작업 노트(근거 인용 금지).
  4. 타 역할 문서에 의존해 산출물을 만들 때는 반드시 확정 스펙 또는 ACCEPTED 결정만 근거로 삼고,
     노트에서 얻은 맥락은 "미확정 참고"로 표기한다.

이유 (supersedes 사유)
- 원 규칙의 목적은 "확정 전 내용 의존 방지"였으나, 수단(열람 금지)이 목적보다 과했다.
  백엔드가 기획의 설계 근거(노트)를 못 보면 같은 질문을 프롬프트 중계로 다시 해야 하고,
  1인 중계 구조에서 이는 사용자 복붙 부담으로 직결된다.
- 의존 방지는 접근 차단이 아니라 상태 라벨(D-010)로 달성한다. ADR 프로세스에서 신뢰의 근원은
  "누가 읽는가"가 아니라 "항목의 상태가 명시되는가"다.
- 상시 전면 열람이 아닌 필요 기반(pull)으로 제한하는 이유: AI 대화는 읽은 문서가 전부
  컨텍스트를 점유한다. 무관한 타 역할 문서를 상시 로드하면 컨텍스트 낭비·오염이 발생하므로,
  "필요할 때, 사유를 갖고" 읽는 것이 통제와 효율을 동시에 만족한다(사용자 피드백 반영).
- 예외 누적 징후: defects.md 열람 예외가 이미 존재하고 findings.md 예외(D-013)가 추가됨.
  예외가 늘어나는 규칙은 기본값이 틀렸다는 신호다.

기각된 대안
- 현행 유지: 위 복붙 왕복 비용 + 예외 누적. AI 대화는 사람과 달리 "어깨너머 정보"로
  정치적 문제를 일으키지 않으므로 차단의 조직적 명분도 약함.
- 전면 자유(기준 규칙 없이): 원래 문제(미확정 의존)가 재발.

레퍼런스: ADR 상태 기반 신뢰 모델(AWS, Fowler), 오픈 핸드북 문화(GitLab Handbook)의
"기본 공개 + 정본 명시" 원칙.

---

## C-004. 전달 프롬프트 이력: outbox 보관 (삭제하지 않음)

상태: ACCEPTED · escalated-as D-017 · 소유: 컨설턴트 · 관련: depends-on C-002 · 2026-07-12

결정
- 발신 프롬프트는 역할 폴더 outbox/에 보관한다. 회신 후에도 삭제하지 않는다.
- 파일 단위: 주제당 1파일(`outbox/NNN-주제.md`). 파일 머리에 상태 1줄:
  `상태: SENT | ANSWERED → <결정 ID>`.
- 결정 로그에는 결과(결정)만, outbox에는 요청 원문이 남아 상호 참조한다.

이유
- 결정 ID만으로는 "무엇을 물었고 어떤 선택지가 제시됐는지" 복원 불가.
  D-004~005처럼 조건부 채택된 결정은 요청 원문이 감사 추적(audit trail)의 반쪽이다.
- 저장 비용은 사실상 0이고 git이 어차피 이력을 남긴다. 삭제는 절약이 아니라 정보 파기.
- 기획이 이미 escalation-prompts.md에 누적+회신 상태 표기를 자생적으로 실천 중 —
  검증된 관행을 규약으로 승격하는 것이 학습 비용 최소.

기각된 대안
- 회신 후 삭제: 감사 추적 소실. git 이력에서 찾는 것은 가능하나 발견 비용이 큼.
- 단일 누적 파일(현행 escalation-prompts.md 방식): 파일이 무한히 자라고 주제 검색성 저하.
  단, 마이그레이션에서 기존 파일은 분할하지 않고 동결 보관(소급 재작성 금지 원칙과 일관).

레퍼런스: ADR의 "기각된 것도 기록" 원칙(Rejected 상태 유지, AWS Prescriptive Guidance).

---

## C-005. spring-skeleton-prompts.md 이동 + CLAUDE.md 경로 정정

상태: ACCEPTED · escalated-as D-018 · 소유: 컨설턴트 · 관련: relates-to D-003, C-001 · 2026-07-12

결정
- docs/spring-skeleton-prompts.md → docs/backend/notes/spring-skeleton-prompts.md 이동.
- CLAUDE.md의 참조 경로를 정정한다. 현재 `docs/skeleton-prompts.md`로 표기되어 있어
  실제 파일명(spring-skeleton-prompts.md)과도 불일치 — 어차피 수정이 필요한 파일이다.
- README.md 말미의 링크도 함께 정정한다.

이유
- D-003이 "docs 루트 = 확정 스펙"으로 확정했는데 백엔드 작업 문서가 루트에 남아 규약 위반 실물.
- 스켈레톤은 구축 완료 상태라 이 문서는 이제 이력성 작업 노트다. 유형 4의 위치(backend/notes/)가 맞다.
- CLAUDE.md 경로가 이미 틀려 있으므로(깨진 참조) 이동에 따른 추가 수정 비용은 없다.

기각된 대안
- 루트 유지: "루트 = 확정 스펙" 규칙에 영구 예외 1건을 두는 것. 예외는 규칙을 침식한다.
- docs/backend/ 루트에 배치: 표준 구조(C-002)상 작업 노트는 notes/가 맞다.

레퍼런스: ADR/스펙 디렉토리 순수성 관행(docs/adr 단일 목적 디렉토리, AWS·GitHub ADR 예시).

---

## C-006. 단계 게이트: 진입·통과 기준 명문화

상태: ACCEPTED · escalated-as D-019 · 소유: 컨설턴트 · 관련: relates-to D-001, D-013, 가이드 7절 · 2026-07-12

결정
- 진행 순서(G1 spec → G2 erd → G3 contract → G4-n 도메인 구현)를 게이트 표로 형식화하고
  각 게이트에 통과 기준(exit criteria)을 명시한다. 상세 표는 02-collaboration-process.md 2.1.
- 게이트 통과는 총괄이 decision-index.md 게이트 표에 1줄 기록. D 발번 없음(상태 전이이지 결정이 아님).
- G3에 보안 게이트 1(D-013), 도메인 전체 완료에 보안 게이트 2를 통합.

이유
- "확정"의 성립 요건(누가 승인, 어디에 기록)이 미정의 — 병렬 시작 트리거가 게이트인데
  기준이 없으면 역할마다 확정 여부 인식이 갈린다.
- Phase-gate 원리에서 게이트의 핵심은 exit criteria다. 기존 절차에 기준만 얹는 최소 변경.

기각된 대안
- 게이트 통과를 D 발번: 결정 로그가 상태 전이 기록으로 오염(1결정=1번호 원칙 훼손).
- 스테이지 게이트 원형(위원회·양식) 도입: 승인자 고정(총괄+사용자)인 1인 구조에 과잉.

레퍼런스: Phase-gate process(Wikipedia), DoR/DoD(Scrum Alliance).

---

## C-007. 메시지 유형 4종 표준 (가이드 5절 확장)

상태: ACCEPTED · escalated-as D-020 · 소유: 컨설턴트 · 관련: relates-to 가이드 5절, D-011 · 2026-07-12

결정
- 역할 간 전달을 4유형으로 표준화: 결정 요청(현행) / 작업 지시 / 완료 보고 / 정보 공유.
- 공통 헤더 `[발신 → 수신] <유형>: 제목` + 말미 `회신: 필요(무엇을)/불요` 명시.
- 정보 공유 유형으로 계약·스펙 영향 사항 전달 금지(결정 요청으로 격상).
- 유형별 필수 요소는 02-collaboration-process.md 2.2 표.

이유
- 실제로 4유형이 이미 오가는데 규약은 결정 요청 1종만 정의 — 나머지가 즉흥 형식.
- 회신 기대치 명시(GitLab 비동기 규약)로 불필요한 회신 왕복을 구조적으로 제거.

기각된 대안
- 자유 형식 유지: 수신 대화가 회신 필요 여부를 추측 — 왕복 증가.
- 유형 세분화(6종+): 유형 판별 자체가 비용. 4종이 관찰된 실제 트래픽의 전부.

레퍼런스: GitLab Handbook 비동기 커뮤니케이션, Amazon 서면 문화(추천안 선행 원리는 현행 유지).

---

## C-008. 복붙 부담 최소화 3규칙

상태: ACCEPTED · escalated-as D-021 · 소유: 컨설턴트 · 관련: relates-to D-007, C-004, C-007 · 2026-07-12

결정
1. 묶음 전달: 같은 수신자행 독립 안건은 1프롬프트에 번호 매겨 묶음(부분 회신 허용).
2. 게이트 = 지식 동기화 앵커: 게이트 통과 시 총괄 전파 프롬프트에 "동기화 대상 파일 목록"을
   포함하고, 사용자는 그 시점에 각 대화의 프로젝트 지식을 일괄 갱신한다.
   프롬프트 참조 규칙: 마지막 게이트에서 동기화된 문서는 경로 참조(본문 붙여넣기 금지),
   게이트 이후 변경분만 발췌 첨부. 발췌에는 원본 경로·기준 게이트를 병기해 사본임을 명시.
3. 왕복 1회 완결 설계: "이 회신만 오면 진행 가능한가" 자문 후 발신. 후속 질문 유발 문구 금지.

이유
- 병렬 구현 시작 후 대화 7개의 왕복이 사용자 1인에 집중 — 전달 횟수와 길이가 병목.
- 기획의 D-004 4건 묶음이 이미 유효성을 실증.
- 사용자 확인 결과 현재 지식 동기화는 거의 하지 않고 프롬프트 본문에 의존 중 —
  무조건적 경로 참조는 전제가 성립하지 않는다. 상시 동기화 의무는 복붙보다 무거우므로,
  동기화를 게이트 이벤트에 묶어 횟수를 최소화하면서 경로 참조가 성립하는 기준선을 만든다.
  게이트 사이 변경분은 소량이라 발췌 첨부 비용이 작다.

기각된 대안
- 전달당 1안건 강제: 왕복 수 증가. 원자성은 안건 번호로 확보 가능.
- 무조건 경로 참조(초안): 지식이 낡은 상태에서 수신 대화가 존재하지 않는 문서를 참조하게 됨.
- 전면 본문 첨부(현행 관행): 낡은 사본 증식, 프롬프트 비대화. 단일 진실(D-007) 훼손.
- 상시 동기화 의무: 파일 변경마다 사용자가 업로드 — 부담 역전.

레퍼런스: Kanban WIP 관리(Atlassian — 대기 왕복을 WIP로 보고 제한), GitLab 비동기 문화.

---

## C-009. 동기화: 이벤트 기반 리추얼 3종

상태: ACCEPTED · escalated-as D-022 · 소유: 컨설턴트 · 관련: relates-to C-006, C-007, D-011 · 2026-07-12
(절단 사고 복구: 02-collaboration-process.md 2.4 기준으로 2026-07-12 원문 복원 — D-022 조건 1 이행)

결정
- 시간 기반 리추얼(데일리 등) 미도입. 이벤트 기반 3종만 규약화:
  1. 게이트 통과 → 총괄이 전 역할 전파 1프롬프트(변경점 + 역할별 할 일).
  2. 역할 작업 블록 종료 → 완료 보고(C-007 유형).
  3. 사용자 세션 재개 → 총괄이 현황 브리핑(인덱스 + ON-HOLD + SENT 대기 목록에서 즉석 생성,
     신규 문서 없음).

이유
- 대화는 세션 간 시간이 흐르지 않고 사용자가 유일한 클록 — "매일"이 아니라
  "세션 재개 시"가 자연스러운 동기화 주기.
- 브리핑을 기존 트래킹 문서(유형 6)에서 생성하면 이중 관리가 없다.

기각된 대안
- 데일리/주간 고정 보고: 이벤트 없는 날의 공회전 보고 생산.
- 리추얼 없음(현행): 대화 수 증가 시 사용자만 전체 상태를 아는 단일 장애점화.

레퍼런스: GitLab async standup(문서 기반 상태 공유), 스크럼 리추얼의 목적(가시성)만 차용하고
시간 박스는 제거.

---

## C-010. outbox를 메시지 버스로: 파일 기반 전달 + 지시 기반 수신

상태: ACCEPTED · escalated-as D-023 · 소유: 컨설턴트 · 관련: supersedes C-008 규칙 2 일부, depends-on D-017 · 2026-07-12

결정
- 역할 간 모든 전달(결정 요청·회신·보고·공유)은 발신자 outbox/에 파일로 남긴다.
  회신도 발신이다 — 수신 역할이 자기 outbox에 회신 파일을 쓰고 원본을 참조한다
  (`회신대상: <역할>/outbox/NNN`).
- 사용자 중계는 프롬프트 복붙 대신 한 줄 지시로 축소: "수신함 확인해."
  지시받은 역할은 전 역할 outbox/에서 `[X → 자기역할]` 헤더 파일을 스캔한다.
- 미처리 탐지: 각 역할은 자기 폴더에 inbox-log.md(처리한 메시지 경로 목록)를 유지.
  스캔 결과 중 inbox-log에 없는 파일이 신규다. 처리 후 inbox-log에 1줄 추가.
  (쓰기 규칙 "자기 폴더에만" 유지 — 타인 파일의 상태 줄을 건드리지 않는 방법)
- 발신자 상태 줄(D-017)은 유지하되 갱신 시점 변경: 상대 outbox에서 회신 파일을 발견하면
  발신자 자신이 ANSWERED로 갱신.

이유
- 회신이 채팅에만 존재하면 감사 추적이 반쪽(D-017 이유와 동일 논리가 회신에도 적용됨).
- 사용자 복붙이 "긴 프롬프트 2회/왕복"에서 "한 줄 지시 2회/왕복"으로 줄어 C-008의
  목표를 더 근본적으로 달성.
- 전제: 각 역할 대화가 저장소 폴더에 접근 가능해야 한다(사용자 확인 필요).
  접근 불가 대화가 남으면 그 대화에 한해 기존 복붙 방식 병행(하이브리드).

기각된 대안
- 수신자별 inbox/ 폴더(발신자가 수신자 폴더에 쓰기): 쓰기 소유권 규칙 파괴.
- 상태 줄 수신자 갱신 허용(1줄 예외): 예외는 규칙을 침식(C-005 교훈). inbox-log가 동등 기능.
- 현행 유지(채팅 중계): 회신 유실, 정보 비대칭 재발.

레퍼런스: 메일박스 패턴(액터 모델의 메시지 큐), GitLab 비동기 문화의 "문서가 채널".

---

## C-011. 정보 비대칭 방지: ID 소유자 통지 규칙

상태: ACCEPTED · escalated-as D-024 · 소유: 컨설턴트 · 관련: depends-on C-010, relates-to C-009, D-011 · 2026-07-12
(절단 사고 복구: 총괄 첨부 원문(management/outbox/002)으로 2026-07-12 복원 — incident-001 관련)

결정
- 어떤 대화가 타 역할 소유의 ID(C/P/B/F/Q/S/D-xxx)나 산출물을 논의·변경·재해석하면,
  그 대화(또는 총괄)는 소유 역할 앞으로 정보 공유 메시지(C-007 유형)를 outbox에 남긴다.
  회신 불요, 1~3줄이면 충분.
- 세션 재개 브리핑(C-009)에 "당신 ID를 언급한 신규 메시지" 항목 추가:
  역할이 세션 재개 시 자기 접두어(C-, P- 등)를 전 역할 outbox에서 grep하여
  inbox-log에 없는 언급을 확인한다.

이유
- 실물 사고: P-003~007 유령 ID 후속 논의(총괄↔기획)가 발견자인 컨설턴트에 미공유 —
  파일 흔적이 없어 스스로 발견할 수도 없었다.
- 통지 의무(push)와 재개 시 grep(pull) 이중화: push가 누락돼도 pull이 잡는다.
  둘 다 파일 기반(C-010)이라 사용자 추가 부담 없음.

기각된 대안
- 전 논의 전 역할 배포(cc all): 수신자 컨텍스트 오염 + 사용자 중계 부담 폭증.
- 통지 없이 pull만: 세션 재개 전까지 공백. 긴급 건은 push가 필요.

레퍼런스: 코드 오너십의 CODEOWNERS 자동 통지 관행(GitHub), 옵저버 패턴.

---

## C-012. 표준 템플릿 패키지 (메시지·로그·노트·브리핑·결함)

상태: ACCEPTED · escalated-as D-025 · 소유: 컨설턴트 · 관련: depends-on D-020, D-023, relates-to D-014, 영역 4 선행분 · 2026-07-12

결정
- 템플릿 10종을 표준화: 메시지 공통 골격, 메시지 4유형(결정 요청/작업 지시/완료 보고/
  정보 공유), 결정 로그 항목, 작업 노트 머리, inbox-log, 세션 재개 브리핑,
  defects/findings 항목. 정본: consulting/templates.md → 채택 시 management/templates.md
  (지침 유형, 총괄 소유)로 이동.
- 운영 원칙: 템플릿은 최소 골격만. 채울 내용 없는 섹션은 삭제(빈 섹션 금지).
  킥오프 프롬프트에 "메시지·로그는 templates.md를 따른다" 1줄만 넣으면 학습 끝.

이유
- 사용자 요구: 대화 산출물이 지저분해지지 않도록 형식 고정 필요.
- 유형·규약(D-014, D-020)은 확정됐으나 실제 "빈칸 채우기" 양식이 없어 대화마다
  형식을 재발명 — 총괄 회신(management/outbox/001)과 기획 outbox 파일들의 형식이 이미 서로 다름.
- 템플릿은 형식 논쟁을 소거하고 컨텍스트를 내용에만 쓰게 한다.

기각된 대안
- 역할별 템플릿 파일 분산: 같은 메시지 양식이 여러 곳에 복제 — 개정 시 불일치.
- 상세 양식(필수 항목 다수): 짧은 전달에도 양식 부담 — 빈 섹션·형식적 기재 유발.
  MADR 등 풀 템플릿 대신 최소 골격 채택.

레퍼런스: MADR(Markdown ADR) 템플릿의 "선택 섹션은 생략" 원칙, GitLab Handbook의
이슈·MR 템플릿 운영(저장소 내 단일 템플릿 파일).

---

## C-013. 통합 추적 표: 인덱스 "보류 안건"을 보류·블로커·리스크로 확장

상태: ACCEPTED · escalated-as D-026 · 소유: 컨설턴트 · 관련: relates-to D-011, D-022, 영역 3 · 2026-07-12

결정
- decision-index.md 보류 안건 표를 "추적 안건" 표로 확장(유형: 보류/블로커/리스크.
  열: 안건·유형·소유·해소 조건·상태). 신규 ID 접두어 없음 — 관련 ID·게이트로 식별.
- 등재: 역할 메시지 보고 → 총괄 등재. 해소: 상태 갱신 후 게이트 시점에 "해소됨" 섹션 이동.

이유
- 결정도 결함도 아닌 안건의 추적 장소·절차 공백. 인덱스는 전 역할 필독이라 표면 추가 비용 0.

기각된 대안
- 별도 issues.md: 추적 표면 이원화 — 정본 규약 재필요. 현 규모 과잉.
- 이슈 ID 접두어 신설(I-xxx): ID 체계 증식. 안건은 수명이 짧아 참조 정밀도 실익 없음.

레퍼런스: GitLab Handbook(문서 기반 추적), Kanban 보드의 blocked 표시 관행.

---

## C-014. 회고 2종: 게이트 회고(Plus/Delta 2줄) + 사고 회고(blameless)

상태: ACCEPTED · escalated-as D-027 · 소유: 컨설턴트 · 관련: relates-to D-022, D-020 · 2026-07-12

결정
- 게이트 회고: 게이트 통과 시 각 역할 완료 보고 말미 선택 2줄(keep/change).
  총괄이 management/notes/retro-G<N>.md 취합, 채택분만 D 발번.
- 사고 회고: 체계가 뚫린 사건 시 management/notes/incident-NNN.md 5항목
  (사건/영향/원인-시스템에서 멈춤/재발 여부-이전 대책이 왜 못 막았나/방지 액션-소유 명시).
  규칙 승격은 D 발번.

이유
- 프로세스 개선이 사고 후 즉흥 대응으로만 발생(실물: 문서 소실 2건 → 커밋 규칙).
  재발 질문 절차가 없으면 같은 부류 사고에 일회성 대응 반복.
- 시간 박스 회고는 D-022 결정과 일관되게 배제 — 게이트가 자연스러운 회고 시점.

기각된 대안
- 스크럼식 회고(회의·3열 보드·투표): 1인 구조에 회의 없음. 형식 과잉.
- 회고 미도입: 규약이 실전에서 깨질 때(오늘 2건) 학습이 축적되지 않음.

레퍼런스: Google SRE Postmortem Culture(blameless, 재발 분석, 액션 소유),
Plus/Delta 경량 회고(EasyRetro).

---

## C-015. 병렬 작업 비동기 완료 규칙: 착수 조건·선완료 행동·어긋남 해소

상태: ACCEPTED · escalated-as D-028 · 소유: 컨설턴트 · 관련: relates-to D-019, D-020, D-026, D-016 · 2026-07-12

결정
1. 착수 조건(DoR): 작업 지시 메시지에 "의존:" 필드를 필수화한다.
   의존 대상(문서·타 역할 산출물)이 확정(ACCEPTED 또는 게이트 통과) 전이면 착수 금지 —
   총괄이 추적 표(D-026)에 블로커로 등재하고 대기시킨다.
2. 먼저 끝난 역할의 행동 우선순위:
   a. 완료 보고 발신, "대기 중: <무엇>" 명시 (블로커 가시화)
   b. 의존 없는 다음 작업을 백로그에서 당겨(pull) 진행
   c. 남은 것이 의존 작업뿐이면 "가정 선언 진행": 가정을 정보 공유로 발신하고
      산출물 머리에 가정을 표기한다. 가정이 깨지면 재작업은 진행한 쪽 부담
      (투기적 선행은 허용하되 비용은 자기 몫 — 상대에게 전가 금지).
3. 어긋남 발견 시 해소 기준: 기준 위계(D-016)로 판정한다.
   확정 스펙과 다른 쪽이 수정한다. 스펙 자체가 문제면 6절 계약 변경 절차.
   양쪽 다 스펙 밖(스펙 공백)이면 결정 요청으로 격상 — 먼저 구현한 쪽이
   기준이 되지 않는다(선착순 기준 금지).

이유
- 게이트 사이 병렬 구간의 미시 동기화 규약 공백. 완료 시점 비대칭은 병렬 구조의
  상수이므로 "기다려라"가 아니라 "기다리는 동안의 합법적 행동"을 정의해야 한다.
- 가정 선언 진행을 금지하면 선완료 역할이 공회전, 무제한 허용하면 추측 위에 쌓은
  작업이 상대를 역으로 압박("이미 만들었으니 맞춰달라")한다. 가정 공개 + 재작업
  자기 부담이 두 폐해를 모두 차단한다.
- 선착순 기준 금지: 먼저 만든 쪽이 사실상 표준이 되는 관성은 계약 우선 원칙(D-007,
  api-contract 단일 진실)의 우회 통로다.

기각된 대안
- 의존 그래프 문서 별도 관리(크리티컬 패스·간트): 작업 단위가 수십 개가 아닌 현 규모에
  과잉. 추적 표 블로커 행 + 작업 지시 의존 필드로 충분.
- 선완료 시 무조건 대기: 공회전. AI 대화는 대기 비용이 없지만 사용자 세션 시간은 유한.
- 어긋남 시 협의 해소(케이스바이케이스): 판정 기준이 없으면 매번 에스컬레이션 —
  D-016 위계가 이미 있으므로 재사용.

레퍼런스: DoR(Scrum Alliance — 준비 안 된 작업 착수 금지), Kanban pull(Atlassian),
contract-first 개발(계약이 기준, 구현 선후는 무관).

---

## C-016. docs git 규약: main 전용·이벤트 커밋·역할 scope

상태: ACCEPTED · escalated-as D-029 · 소유: 컨설턴트 · 관련: relates-to D-022, D-023, incident-001, CLAUDE.md 6절 · 2026-07-12

결정
1. docs/ 변경은 main 직접 커밋, 문서용 브랜치 금지(파일 버스는 단일 파일 상태 전제).
2. 이벤트 기반 커밋: D 발번 / 게이트 통과 / 사고 복구 / 역할 작업 블록 완료 시.
3. 주체(유동 운영, 사용자 확정): 작업 블록 종료 시 수정한 역할 대화가 커밋 메시지를
   컨벤션(CLAUDE.md 6절 형식·본문 템플릿)에 따라 작성해 제시한다.
   기본 실행자는 사용자(직접 커밋·푸시). 사용자가 대행을 지시하면 해당 역할 대화가
   자기 폴더 변경의 커밋·푸시를 실행한다.
   코드 커밋은 현행 유지(Claude Code 제안·사용자 실행 — 코드까지 대행을 확장하려면
   CLAUDE.md 2절 개정 필요, 총괄 판단 사항).
4. 형식: `docs(<역할>): 한글 제목` (Conventional Commits, CLAUDE.md 6절 유지).
5. docs와 코드 혼합 커밋 금지.

이유
- 사고 6건(incident-001)의 근본 원인이 docs 미커밋 — 복구 수단이 대화 컨텍스트뿐이었다.
- 역할별 폴더 소유(D-015) 덕에 역할 단위 커밋이 자연 원자적·무충돌.
- 문서 리뷰는 게이트가 담당하므로 PR 경유는 지연만 추가.

기각된 대안
- 사용자 단독 커밋: 커밋 시점이 사용자 세션에 종속 — 이벤트와 커밋이 어긋나 사고 창 재발.
- 총괄 단독 커밋: 총괄이 타 역할 폴더 변경까지 커밋 — 소유권 흐림 + 총괄 병목.
- docs PR 리뷰: 리뷰어 부재. 게이트·검수가 동일 기능 수행 중.

레퍼런스: Conventional Commits v1.0.0, atomic commits(1커밋=1논리 변경),
GitLab docs-as-code(문서도 저장소가 단일 진실).

---

## C-017. 프론트 저장소 git 규약: 백엔드 대칭 + 계약 동기화 커밋

상태: ACCEPTED · escalated-as D-030 · 소유: 컨설턴트 · 관련: relates-to D-007, CLAUDE.md 6절, C-016 · 2026-07-12

결정
1. 백엔드와 대칭: Conventional Commits·한국어 제목·본문 템플릿 재사용.
   스켈레톤기 main 직접 → 도메인 개발기 feature/<도메인> → PR → main(Squash). GitHub Flow 경량.
2. 스켈레톤 커밋: `chore(skeleton): stage N - 설명` 관행 재사용.
3. 계약 복사본 동기화 커밋 고정 형식:
   `docs(contract): api-contract v<N> 동기화 (원본 finalcall@<해시7>)` —
   전파 체크리스트(D-007)의 완결 증명.
4. 프론트 CLAUDE.md(영역 5)에 수록.

이유
- 두 저장소 규약이 다르면 사용자·총괄이 문맥 전환 비용 지불. 대칭이 학습 비용 최소.
- 계약 복사본 커밋에 원본 해시를 박으면 "복사본이 어느 시점 원
---

## C-018. 템플릿 증보 5종 (git 커밋·사고 회고·게이트 회고·게이트 전파·계약 변경)

상태: ACCEPTED · escalated-as D-031 · 소유: 컨설턴트 · 관련: depends-on D-025, relates-to C-016, D-021, D-027, 가이드 6절 · 2026-07-12

결정
- management/templates.md에 5종 증보: 11 git 커밋 docs 경량판 / 12 사고 회고(incident) /
  13 게이트 회고 취합(retro-G) / 14 게이트 전파 메시지(동기화 파일 목록 포함) /
  15 계약 변경 요청(6절 전용 특수형). 초안: consulting/notes/templates-증보안.md.
- 템플릿 신설·개정 경로 명문화: 컨설턴트 재직 중엔 컨설턴트 설계 → 총괄 채택.
  역할 종료 후엔 필요 역할이 초안 제안 → 총괄 승인·편입(지침 유형 변경 규칙, D-014).

이유
- D-025 채택 후 실제 쓰임새에서 5개 공백 확인: docs 커밋(C-016 채택 시 즉시 필요),
  incident·retro(D-027 형식 확정됐으나 템플릿 미수록), 게이트 전파(D-021 동기화 목록
  의무가 형식 없이는 누락되기 쉬움), 계약 변경(6절 절차의 입력 형식 부재).
- 사용자 질의로 소유·생성 경로 모호함 확인 — 경로 규칙을 함께 명문화.

기각된 대안
- 필요할 때마다 즉흥 작성: D-025 이전 상태로 회귀 — 형식 재발명 반복.
- 총괄이 직접 설계: 컨설턴트 재직 중 설계 업무 이중화, D-012 역할 정의와 상충.

레퍼런스: GitLab Handbook 템플릿 운영(제안 → 소유자 승인 편입), Google SRE 포스트모템
템플릿(사고 기록의 형식 고정이 blameless 문화의 실행 장치).

---

## C-019. 문서 파일 네이밍 규약

상태: ACCEPTED · escalated-as D-033 · 소유: 컨설턴트 · 관련: relates-to D-015, D-017, D-025 · 2026-07-12

결정
- outbox: `NNN-주제.md` (역할별 독립 증가, 기확정 D-017). 주제는 한글 허용.
- notes: `주제.md` 자유 명명, 한글 허용. 접두 번호 없음(수명 짧고 참조는 경로로).
- 설계안·산출물: `NN-주제.md` (역할 내 순번) 또는 고정명(test-plan.md, findings.md 등
  가이드 3절 명시분).
- incident/retro: `incident-NNN.md`, `retro-G<N>.md` (D-027 확정 형식).
- 한글 파일명 공식 허용. 단 git 설정 `core.quotepath=false`를 두 저장소 공통 권장
  (로그에서 한글 파일명이 이스케이프되는 것 방지).

이유
- 실물 관행이 이미 한글 파일명(001-입찰규칙.md 등) — 금지하면 소급 개명 비용만 생긴다.
  UTF-8 환경의 git은 한글 파일명에 기술 제약이 없고, 가독성은 한글이 우월.
- 유형별 규칙이 이미 산재(D-017, D-027) — 한 곳에 모아 신규 역할 참조점 제공.

기각된 대안
- 영문 kebab-case 강제: 국제 협업·도구 호환 요구가 없는 한국어 단독 프로젝트에서
  가독성만 잃는다. 소급 개명 비용 발생.

레퍼런스: git core.quotepath 문서, 사내 위키 네이밍 관행(한글 제목 + 규칙적 접두).

---

## C-020. 용어집 미도입 — domain-spec 용어 정의 섹션으로 갈음

상태: ACCEPTED · escalated-as D-034 · 소유: 컨설턴트 · 관련: relates-to D-001, 기획 킥오프 프롬프트 · 2026-07-12

결정
- 별도 용어집(glossary.md)을 만들지 않는다. 도메인 용어의 정본은 domain-spec.md의
  "용어 정의" 섹션(기획 킥오프에 이미 계획됨)이다.
- 프로세스 용어(게이트, outbox, 추적 표 등)의 정본은 collaboration-guide.md 본문이다.
- 신규 용어가 두 정본 어디에도 없으면 해당 문서 소유 역할에 추가를 요청한다(별도 파일 신설 금지).

이유
- 용어집은 정의가 두 곳(스펙·용어집)에 존재하게 만드는 대표적 이중 관리 지점 —
  어긋나면 어느 쪽이 정본인지 논쟁이 된다(단일 진실 원칙 위반).
- 이 프로젝트 용어 수(도메인 ~20개 추정)는 스펙 섹션 하나로 충분한 규모.

기각된 대안
- 독립 glossary.md: 규모 대비 과잉 + 이중 관리. 용어가 100개 단위로 늘면 재검토.

레퍼런스: 단일 진실 원칙(docs-as-code), DDD의 유비쿼터스 언어(용어 정본은 도메인 모델
문서와 분리하지 않는다 — Evans).

---

## C-021. api-contract 문서 형식: 버저닝·변경 이력·엔드포인트 표기 골격

상태: ACCEPTED · escalated-as D-035 (조건: 기술 규약은 G1 직후 백엔드 조기 기동 협의) · 소유: 컨설턴트 · 관련: relates-to 가이드 6절, D-007, D-030, CLAUDE.md 5절 · 2026-07-12

결정
- 문서 헤더 고정: 버전 v<N>(정수 증가) · 상태(DRAFT|확정) · 기준 게이트 ·
  변경 이력 표(버전|날짜|변경 내용|사유|승인 D-ID).
- 버전 규칙: G3 확정 시 v1. 이후 모든 변경은 6절 절차 경유 + v+1. DRAFT 중엔 버전 없음.
  프론트 복사본 헤더에 원본 경로·버전·원본 커밋 해시(D-030 커밋 형식과 일치).
- 엔드포인트 표기 단위 고정: 메서드+경로 / 설명 / 인증 요구 / 요청(파라미터·바디 스키마) /
  응답(성공 스키마 + 에러 코드 목록, {DOMAIN}_{3자리} 참조) / 비고.
- 문서 앞에 "공통 규약" 섹션 예약: 인증 헤더, ApiResponse 포맷, 페이징·정렬 파라미터,
  URL 네이밍, 상태 코드 매핑 — 내용은 백엔드 기동 첫 작업으로 배정(기획과 협의 확정).
  컨설턴트는 자리만 만들고 기술 내용은 정하지 않는다(역할 경계).

이유
- 6절이 변경 이력 기록을 요구하나 형식 미정 — 계약 변경 절차의 실행 형식 공백.
- D-030 동기화 커밋이 참조하는 v<N>의 증가 규칙이 없으면 커밋 형식이 공회전.
- 엔드포인트 표기가 제각각이면 백/프론트가 같은 계약을 다르게 읽는다 — 계약서의 존재 이유 상실.

기각된 대안
- OpenAPI(yaml) 전면 도입: 정밀하나 기획 대화가 작성·유지하기 무겁고 사람이 읽는 협상
  문서로는 마크다운이 우월. 구현 후 코드 기반 OpenAPI 생성(springdoc)은 백엔드 자율로 열어둠.
- 기술 내용까지 컨설턴트가 확정: D-006 위임 원칙 위반 — 백엔드가 결정권 없는 작성자화.

레퍼런스: API 계약 우선 개발(contract-first), 문서 버저닝의 정수 증가 관행(RFC 스타일),
가이드 6절.

---

## C-022. erd 문서 형식: 버전 연동·테이블 정의 골격

상태: ACCEPTED · escalated-as D-036 (조건: 동일 조기 협의) · 소유: 컨설턴트 · 관련: relates-to D-001, CLAUDE.md Stage D, C-021 · 2026-07-12

결정
- 문서 헤더 고정: 버전 · 기준 domain-spec 버전 · 변경 이력 표(C-021과 동일 형식).
- 섹션 골격: 1) 네이밍 규약 선언부(테이블·컬럼·인덱스·FK 규칙 — 내용은 백엔드 협의 확정)
  2) Mermaid erDiagram 3) 테이블 정의 표(컬럼|타입|null|기본값|설명)
  4) 인덱스 표(이름|대상|이유) 5) Flyway 매핑(V<N>__설명 파일명, ERD 버전과 연동 표기).
- 기술 규칙(snake_case, FK 명명, 인덱스 명명)은 백엔드 기동 첫 작업으로 배정,
  선언부에 기입되면 이후 모든 도메인 테이블에 적용.

이유
- ERD가 G2 산출물인데 작성 형식 기준이 없다. JPA_DDL_AUTO=validate 정책상 스키마의
  단일 진실이 Flyway 마이그레이션이므로, ERD 문서와 마이그레이션 파일의 버전 연동이
  없으면 "문서 따로 스키마 따로"가 필연.
- 인덱스에 "이유" 열을 강제하는 것은 대규모 트래픽 프로젝트의 핵심 규율 —
  근거 없는 인덱스가 쓰기 성능을 갉아먹는다.

기각된 대안
- ERD 도구 산출물(이미지) 첨부: diff 불가, 리뷰 불가. Mermaid 텍스트가 git 친화적.
- 형식 자유: 도메인 추가마다 표기 재발명, 백/QA가 테이블 정의를 찾는 위치가 문서마다 다름.

레퍼런스: Flyway 버전드 마이그레이션 네이밍(V<N>__), Mermaid erDiagram(텍스트 기반
다이어그램의 diff 가능성), docs-as-code.

---

## C-023. 지침 3계층: 공통 가이드 / 파트 지침 파일 / 킥오프 부트스트랩

상태: ACCEPTED · escalated-as D-037 · 소유: 컨설턴트 · 관련: relates-to D-014, D-021, 킥오프 미결 포인트 · 2026-07-12

결정
- 지침을 3계층으로: 1) 공통(collaboration-guide·templates) 2) 파트 지침 파일
  (백엔드 CLAUDE.md 기존 / 프론트 repo CLAUDE.md / qa-guide.md / security-guide.md)
  3) 킥오프 프롬프트는 부트스트랩만(역할 정의·필독 목록·첫 작업, 지침 내용 복제 금지).
- 상세·부트스트랩 골격: 05-part-guides.md.

이유
- 킥오프에 지침 통합 시 개정마다 프롬프트 재발급 + 기동된 대화는 낡은 규칙으로 작동.
  파일이면 유형 2로 관리되고 게이트 동기화(D-021)로 갱신된다.
- 백엔드(짧은 지시 + CLAUDE.md)가 이미 이 모델을 검증.

기각된 대안
- 킥오프 통합(단일 프롬프트): 위 개정 문제. 프롬프트 비대화로 컨텍스트 낭비.
- 전 지침 단일 파일: 역할별 필독 범위가 흐려지고 무관 규칙까지 상시 로드.

레퍼런스: 백엔드 CLAUDE.md 운영 실적, GitLab Handbook(정본 문서 + 얇은 진입점).

---

## C-024. 번호 체계 분리: 결정 로그(Q/S-xxx) vs 결함·발견 티켓(QA/SEC-NNN)

상태: ACCEPTED · escalated-as D-038 · 소유: 컨설턴트 · 관련: relates-to D-025 조건, D-009, D-010 · 2026-07-12

결정
- Q-xxx/S-xxx = 결정 로그(4절 티켓 규약, 불변·상태 라벨). QA-NNN/SEC-NNN = 결함·발견
  티켓(defects.md/findings.md, OPEN→FIXED/WONTFIX 상태 갱신). 시퀀스 완전 분리.

이유
- 결정은 불변 기록, 결함은 상태가 변하는 작업 항목 — 혼합 시 로그 불변 원칙과 충돌.
- D-025 조건(번호 체계 재확인)의 회답.

기각된 대안
- 단일 시퀀스(Q-xxx에 결함 포함): 결함 상태 갱신이 로그 갱신 규칙을 침식, 인덱스 오염.

레퍼런스: 이슈 트래커와 ADR의 분리 관행(결함≠아키텍처 결정).

---

## C-025. 프론트엔드 CLAUDE.md 초안

상태: ACCEPTED · escalated-as D-039 · 소유: 컨설턴트 · 관련: depends-on D-032, D-030, relates-to C-023 · 2026-07-12

결정
- draft-frontend-claude.md를 프론트 저장소 루트 CLAUDE.md로 채택. 핵심: feature 기반 구조,
  서버 상태=TanStack Query·전역 클라이언트 상태=Zustand 최소(서버 데이터 복제 금지),
  계약 1:1 API 함수 + ApiResponse 언랩, 시간 UTC 수신·표시 변환, strict TS·any 금지,
  git은 D-030, 문서는 docs/frontend/ 허브.

이유
- D-032 스택의 공인 관행(피처 구조, 서버/클라이언트 상태 분리)을 백엔드 CLAUDE.md와
  같은 밀도로 성문화 — 프론트 Claude Code 기동 시 즉시 사용 가능.

기각된 대안
- Airbnb 스타일 가이드 등 전문 채택: 대형 규정집은 학습 비용 대비 실익 없음.
  도구 세부(ESLint 규칙)는 스켈레톤 구축 시 F-xxx 자율.

레퍼런스: Robin Wieruch React 구조, ReactBlueprint, TanStack Query 공식 구조 논의.

---

## C-026. QA 지침(qa-guide.md) 초안

상태: ACCEPTED · escalated-as D-040 · 소유: 컨설턴트 · 관련: depends-on C-023, C-024, relates-to D-008, D-016 · 2026-07-12

결정
- draft-qa-guide.md를 docs/qa/qa-guide.md로 채택. 핵심: 리스크 기반 1~2페이지 test-plan,
  동시성 4건(D-008) 필수 시나리오, 기대 결과에 계약 근거 인용 의무(인용 불가=스펙 공백
  =계약 질의), QA-NNN 결함 흐름, Critical 즉시 총괄 통지.

이유
- 기대 결과의 근거 인용 의무가 핵심 규율 — 근거 없는 기대는 QA의 추측이 계약을 대체하는
  통로(D-028 선착순 금지와 동일 논리).

기각된 대안
- IEEE 829식 문서 체계: 1인 구조 과잉. 리스크 기반 경량 플랜으로 대체.

레퍼런스: BrowserStack 리스크 기반 테스트, QA Touch/TestLodge 1페이지 플랜.

---

## C-027. 보안 지침(security-guide.md) 초안

상태: ACCEPTED · escalated-as D-041 (차단 권한 명시 승인) · 소유: 컨설턴트 · 관련: depends-on C-023, C-024, D-013 · 2026-07-12

결정
- draft-security-guide.md를 docs/security/security-guide.md로 채택. 핵심: 게이트 2회 절차
  (게이트 1은 G3 차단 권한, Critical 미해결 시 통과 불가), STRIDE-lite 위협 모델 표,
  도메인 리스크 축 6개(인가/입찰 부정/자금 정합성/시간 조작/주입·IDOR/시크릿),
  SEC-NNN 발견 흐름, 계약 변경 시 보안 의견 1줄 첨부.

이유
- 게이트에 차단 권한을 명시하지 않으면 보안 검토가 자문으로 격하 — 돈이 오가는
  시스템에서 Critical 미해결 통과는 one-way door.

기각된 대안
- ASVS 전면 준수: 인증 수준 요구가 없는 프로젝트에 과잉. 통제 선정 참조로만 사용.
- 정식 STRIDE 워크숍: 참가자가 없다. 표 1개/도메인의 반복 갱신으로 대체.

레퍼런스: OWASP Threat Modeling Cheat Sheet, OWASP Security Culture, ASVS.

---

## C-028. 프로젝트 지식 스냅샷 등록 규칙: 공통 필수 집합·경계·최신성

상태: ACCEPTED · escalated-as D-060 · 소유: 컨설턴트 · 관련: relates-to D-007, D-016, D-021, D-023 · 2026-07-13
(총괄 작업 지시 management/outbox/022 회답)

결정
1. 공통 필수 등록 5종 (전 역할 대화가 프로젝트 지식에 등록):
   1) management/collaboration-guide.md
   2) management/templates.md
   3) 확정 스펙 3종 docs/domain-spec.md · erd.md · api-contract.md — 확정본 기준.
      미확정 단계는 현재 초안(domain-spec DRAFT)을 등록하되 헤더에 DRAFT 표기.
   4) management/decision-log.md (총괄 D 로그, ACCEPTED만 근거)
   5) management/decision-index.md (마스터 인덱스·게이트·추적)
   → 총괄 권고 방침 A(5종 전부 등록) 채택.
   확정 스펙 교체 트리거: 게이트 통과 시 확정본으로 교체 등록
   (G1→domain-spec, G2→erd, G3→api-contract). 게이트가 곧 확정=등록 갱신 시점(D-019·D-021 정렬).

2. 등록/비등록 경계:
   - 등록(공통): 위 5종.
   - 등록(파트 한정): 각 파트 지침이 이미 지정한 필독(백엔드 CLAUDE.md, 프론트 CLAUDE.md,
     qa-guide.md, security-guide.md) — 해당 파트 대화에만. (파트별 필독 목록 재설계 아님 —
     D-016·파트 지침을 그대로 인용. D-016 본문 개정은 별건.)
   - 비등록: 타 파트 decision-log·notes·outbox(필요 시 pull), 자기 inbox-log(자기 폴더 운영 파일).
   - 파일 버스(outbox·inbox-log)는 스냅샷 등록 대상이 아니다 — 폴더 연결(우선) 또는
     하이브리드 복붙(D-023)으로 접근. outbox 미등록은 의도된 설계이며 정공법은 폴더 연결.

3. 최신성 방침 (방침 A 채택 + 부속 규칙):
   - 안정/휘발 구분: 안정(1·2·확정 스펙) vs 휘발(4·5·초안 스펙 — 결정마다 낡음).
   - 휘발 파일 갱신 주기: 매 결정이 아니라 게이트 동기화 앵커(D-021)에 맞춰 재등록.
     게이트 사이 최신 상태가 필요한 대화는 폴더 연결로 라이브 열람하거나 하이브리드로 발췌 수신.
   - 등록 파일 헤더 문구(휘발 파일 필수 / 안정 파일 선택):
     ```
     > 스냅샷 사본 — 정본: 저장소 <경로>. 기준 시점: <게이트 G<N> 또는 커밋 해시7>.
     > 어긋나면 저장소가 우선(D-007). 이 사본은 오프라인 대화의 출발점.
     ```
   - 폴더 연결 대화에서는 등록이 백업일 뿐, 라이브 저장소 열람이 정본(D-007).

4. 규약 형태: 신규 D 권장(D-016 확장 아님).
   - 근거: D-016은 "무엇을 읽고 무엇을 근거로 삼는가(열람 범위·기준 위계)"를 규정.
     본 규칙은 "무엇을 스냅샷에 올려 제공하는가(프로비저닝)"로 관심사가 다르다.
     D-016에 얹으면 1결정=1원자(D-009) 위반 + D-016 비대화.
   - 총괄 승인 시 신규 D 발번, C-028은 escalated-as로 갱신.

이유
- 022가 경고한 "등록 누락 → 근거 문서 부재 사고"의 비용이 방침 A의 경미한 낡음보다 크다.
  재개 시 어차피 필독으로 읽으니 스냅샷이 출발점이면 충분(총괄 권고와 일치).
- 방침 B(안정만 등록, 휘발은 재개 pull)의 실패 모드는 재개 시 pull 누락 → 낡은·부재 결정
  맥락으로 작업, 정확히 022가 막으려는 사고. B는 부담을 매 재개 규율에 전가한다.
- 방침 A의 낡음은 "저장소 우선" 헤더 + 게이트 재등록 + ACCEPTED-only 근거 규칙(D-016)으로
  경계지어진다 — 역할은 상태 확인 없이 낡은 결정에 근거하지 않는다.
- 게이트 앵커 재등록은 이미 D-021이 정한 리듬 — 휘발 파일을 여기 얹으면 신규 부담이 없다.
- 파일 버스를 스냅샷에서 뺀 것은 의도다: outbox는 매 메시지 증가·수명이 짧다. 이번
  "총괄 메시지 안 보임" 사고의 교훈은 등록 목록 확대가 아니라 폴더 연결 정착이다.

기각된 대안
- 방침 B(안정만 등록): pull 누락 사고. 등재 누락이 더 위험(022 총괄 판단과 동일).
- D-016 확장으로 편입: 관심사 혼합, 원자성(D-009) 위반.
- 파일 버스도 공통 등록: outbox 무한 증가로 스냅샷 비대화 + 매 메시지 재업로드 부담. 폴더 연결이 대체.
- 등록 자동화(자동 sync) 전제: 현재 프로젝트 지식은 수동 스냅샷 업로드(D-007). 자동 sync가
  가능해지면 재검토 트리거로 둔다.

레퍼런스: 12-factor의 config/build artifact 분리(안정 vs 휘발 자원 구분), docs-as-code
단일 진실(D-007), 캐시 무효화를 이벤트(게이트)에 묶는 패턴.

---

## C-029. docs 커밋 실행 일원화: 역할이 메시지 작성, 사용자가 단일 실행

상태: ACCEPTED · escalated-as D-061 · 소유: 컨설턴트 · 관련: supersedes C-016(D-029) 규칙 3·대행 조항, relates-to D-021, D-022, D-023, incident-001 · 2026-07-13
(사용자 지시 — 대기모드 전달, 커밋 꼬임 해소)

결정
1. 커밋 메시지 작성 = 각 역할. 형식 `docs(<역할>): 한글 제목`(C-016.4 유지).
   소유권·attribution은 실행자가 아니라 메시지 scope로 보존한다.
2. git 실행(add·commit·push) = 사용자 단일 실행자. 역할 폴더 단위 커밋(한 폴더=한 커밋),
   여러 폴더가 밀려 있으면 폴더별로 나눠 커밋한다.
3. 실행 시점: 작업 블록 종료(D-022 리추얼 2, 완료 보고 시) + 게이트(D-021 앵커)에 몰아 실행.
   완료 보고에 "커밋 메시지 제안" 1블록 포함을 의무화 — 사용자가 그대로 실행한다.
4. 스테이징 규율: 항상 `git add docs/<역할>/`로 path-scope. `git add -A`/`git add .` 금지
   (공유 작업트리의 타 역할 미커밋 변경 혼입 방지). docs·코드 혼합 커밋 금지(C-016.5 유지).
5. 역할 대화의 git 대행(C-016.3 후단) 폐지. 다중 실행자가 반쪽 커밋·혼입을 유발함이 실증됨.
6. 코드 커밋은 현행 유지(Claude Code 제안·사용자 실행).

이유
- C-016이 "사용자 단독 실행"을 기각한 근거는 "커밋 시점이 사용자 세션에 종속 → 이벤트와
  커밋이 어긋나 손실 창"이었다. 그러나 1인 중계 구조에서 모든 이벤트가 사용자 중계를 거치므로
  이벤트≈사용자 세션 — 기각의 전제가 성립하지 않는다.
- 다중 실행자의 실비용이 실증됨(2026-07-13): 공유 작업트리에 여러 역할 미커밋 변경이 겹치면
  매 커밋이 "타 역할 혼입" 검사를 요구하고, 마운트 지연·index.lock 같은 환경 변수까지 겹치면
  반쪽 커밋이 난다.
- attribution은 메시지 scope(`docs(<역할>):`)와 폴더 소유(D-015)가 보증한다 — 실행 일원화가
  소유권을 흐리지 않는다. 실행자와 저작자를 분리하는 것은 CODEOWNERS의 원리와 같다.
- 손실 창(incident-001)의 근본 원인은 미커밋 적체이지 실행자 수가 아니다. 커밋을 완료 보고
  리추얼에 묶어 블록 종료 즉시 실행하면 해소된다.

기각된 대안
- 현행 유지(다중 실행자 + 대행): 오늘 꼬임 재발. path-scope만으로 무해해지나 실행자가 여럿이면
  규율 준수 검증 비용이 실행마다 발생.
- 총괄 단독 실행: 총괄이 타 역할 폴더까지 커밋 → C-016이 이미 기각(소유권 흐림·병목).
  사용자 실행은 attribution을 메시지로 유지하므로 이와 다르다.
- 역할별 브랜치·워크트리 분리: 파일 버스(outbox)의 전 역할 즉시 가시성 전제를 파괴(C-010).

레퍼런스: 모노레포 디렉터리 오너십 + CODEOWNERS(저작·리뷰와 커밋 실행자 분리),
trunk-based development(단일 브랜치 빈번 커밋), incident-001 교훈.

---

## C-030. 사용자 브리핑에 '초안 요지' 항목 추가 (D-049 확장)

상태: ACCEPTED · escalated-as D-063 · 소유: 컨설턴트 · 관련: relates-to D-049, D-023, D-025(templates 16) · 2026-07-13
(사용자 지시 — 대기모드 전달)

결정
1. 초안·제안(C/P/B/F/Q/S-xxx, 스펙 초안, 결정 요청 등 사용자 검토·결정이 필요한 산출물)을
   생성한 응답은 브리핑에 "초안 요지" 항목을 추가한다.
2. 내용: 생성한 초안명 + 핵심 결정/선택지/추천을 2~5줄. 사용자가 파일을 열지 않고도
   검토·결정할 수 있는 최소 실질.
3. 위치·조건: 브리핑 구조 안에 넣되(정확한 자리·표기는 templates 16에서 확정),
   초안을 생성하지 않은 응답에는 항목을 생략한다(조건부 — 빈 항목 강제 아님).
4. 원칙 정합: D-049의 "파일에 쓴 내용도 요지는 채팅 노출 필수"를 구체화한다.
   기존 '발신: 경로 — 요지 1줄'로는 초안 실질이 안 보이던 공백을 메우는 것이며,
   상세 본문은 여전히 브리핑 위에 둔다.

이유
- 최종 결정권자는 항상 사용자(가이드 1절)인데, 초안은 파일(outbox·decision-log)로 외부화되어
  사용자가 열지 않으면 실질을 못 본다. '경로 + 1줄'로는 채택/기각 판단 근거가 부족하다.
- 브리핑을 짧게 유지(D-049)하는 취지와 충돌하지 않도록 조건부(초안 생성 시에만) + 소량(2~5줄)으로 제한.
- 파일 외부화의 목적(토큰·감사 추적)은 유지하되, 사용자 검토 채널만 브리핑으로 복원한다.

기각된 대안
- 초안 전문을 브리핑에 노출: 브리핑 비대화, D-049의 스캔 가능성 훼손.
- 모든 응답에 요지 확대: 초안 없는 단순 응답까지 장황해진다. 조건부가 맞다.
- 별도 요약 파일 생성: 파일 증식 + 또 하나의 "안 열어보는 파일". 채팅 노출이 목적에 직결된다.

레퍼런스: D-049 원칙(채팅 요지 노출), GitLab async(결정에 필요한 맥락은 요약과 함께 제시).

---

## C-031. 착수 지시의 근거 인용 의무 (작업 지시 템플릿 강화)

상태: ACCEPTED · escalated-as D-082 (조건: 적용 대상 = 산출물 착수 지시 한정 — 규약·프로세스 지시는 결정 로그·사례 경로를 근거로) · 소유: 컨설턴트 · 관련: relates-to D-020, D-025(templates 3), D-040, D-074 · 2026-07-15
(총괄 회부 management/outbox/077 안건 1. 사례 1 — 066 지시 → 022 격상)

결정
- templates 3(작업 지시)의 `관련 문서: <경로만>` 필드를 `근거: <문서 §절> — <핵심 1줄 인용>`으로 강화한다.
- 착수 범위의 근거를 문서·절 단위로 특정하고 핵심 문장을 1줄 인용한다. 경로 나열만으로는 지시가 성립하지 않는다.
- 인용 불가 = 근거 부재 = 스펙 공백. 이 경우 작업 지시를 낼 수 없고 계약 질의·결정 요청으로 격상한다(가이드 6절 / D-020).
- 적용: 총괄이 내는 모든 작업 지시(파트 간 순서 지정 포함).

이유
- 사례 1 실물: 066이 착수 범위를 "계약 §2 이후 회원 조항"으로 지정했으나 그 조항이 실재하지 않았다
  (api-contract:96 전방 참조 미이행). 인용을 요구했다면 인용할 문장이 없어 지시 자체가 성립하지 않았고,
  파생 4단(068·069·027·072)이 발생하지 않았다.
- 검증을 별도 절차·체크리스트로 추가하면 마찰이 늘고 형식적 통과로 부패한다. 대신 형식에 내장한다 —
  인용하려면 실재를 확인할 수밖에 없으므로 검증이 부산물로 강제된다. 절차 추가 0, 필드 1개 강화.
- 검증된 패턴의 재사용: QA는 D-040(C-026)에서 "기대 결과에 계약 근거 인용 의무, 인용 불가=스펙 공백=계약 질의"를
  이미 운영 중이다. 동일 메커니즘의 작업 지시 확장이라 학습 비용이 없다.
- 총괄 결함을 "총괄이 더 주의한다"가 아니라 형식으로 막는다(회부 조건: 총괄 방어 금지).

기각된 대안
- 총괄 착수 전 체크리스트 신설: 신규 문서 + 매 지시 점검 = 절차 추가. 과설계 금지 위반.
- 현행 유지(경로만 기재): 사례 1 재발. 경로는 "그 안에 실재한다"를 증명하지 못한다.
- 총괄 지시에 사용자 승인 이중 검토: 중계 턴 추가 = 병목.

레퍼런스: D-040(C-026) QA 인용 의무 운영 실적, contract-first(계약이 근거), 현지현물(문서 아닌 실물 확인).

---

## C-032. 진행 중 작업 보호: 완주 기본값 + 충돌 시 이의 제기 의무

상태: ACCEPTED · escalated-as D-083 · 소유: 컨설턴트 · 관련: strengthens D-028(선완료 행동)·D-074(시퀀싱) · 2026-07-15
(총괄 회부 077 안건 2. 사례 2 — 073 확대 → 076 정정)

결정
1. 완주 기본값: 이미 착수된 작업은 기본적으로 완주한다. 총괄이 중단·선점시키려면 "무엇이 왜 막히는가"를
   근거로 제시해야 하며, 근거 없는 순서 재편은 진행 중 작업을 중단시키지 못한다.
2. 이의 제기는 권한이 아니라 의무: 총괄 지시가 자기 진행 중 작업과 충돌하면 해당 파트는 즉시 이의를 제기한다.
   침묵은 규약 위반이다.
3. 중단이 정당한 경우: 진행 중 작업의 산출물이 파생으로 무효화됨이 근거로 입증될 때만. 그 외(순서 선호·정리 편의)는 완주.

이유
- 사례 2 실물: 073이 V4를 전체 선행 1번으로 올려 진행 중 023을 무효화할 뻔했다. 실제로 023(순수 파일 이동)은
  V4에 막히지 않았고 076이 원 판단으로 환원했다. 기본값이 완주였다면 총괄의 확대가 자동 무해화된다 —
  근거를 대야만 중단되므로.
- 비용 비대칭: 중단은 투입분 폐기 + 재개 시 컨텍스트 재구축을 부른다. 반면 완주 비용은 대개 작다
  (076이 실측으로 보여줌 — 대부분의 "선행"은 실제로 충돌하지 않았다).
- 이의를 권한으로 두면 쓰이지 않는다. 사례 2에서 백엔드는 권한이 있었으나 침묵했고 사용자가 잡았다.
  의무로 두어야 침묵이 위반이 되고, 현장 지식이 총괄 오류를 자동 교정한다(안돈: 현장의 정지 의무).
- 076이 이미 "진행 중 작업이 총괄 지시와 충돌하면 즉시 알려라 — 그럴 권한이 있다"고 지시했다.
  권한에서 의무로 승격해 규약화하는 것이다.

기각된 대안
- 중단/선점/완주 판정 매트릭스 신설: 케이스 열거라 유지·학습 비용이 크다. 실제 판단은 "근거 있나" 한 질문으로 끝난다. 과설계.
- 총괄 판단 우선(현행 관행): 사례 2 재발.
- 중단 전 사용자 승인: 중계 턴 추가. 근거 제시 의무로 충분하다.

레퍼런스: 도요타 안돈 코드(현장의 라인 정지 의무), 중단 비용 비대칭, D-028 선착순 기준 금지의 연장.

---

## C-033. 파생 범위 판정: 발견자 기본값 + 총괄 시퀀싱 경계

상태: ACCEPTED · escalated-as D-084 · 소유: 컨설턴트 · 관련: refines D-074(1항), relates-to D-006(위임)·D-016(기준 위계) · 2026-07-15
(총괄 회부 077 안건 3. 사례 2 — 028 판정 vs 073 확대)

결정
1. 파생의 선행 범위는 발견자가 판정한다(기본값). 발견자는 판정 근거(무엇이 왜 막히는지)를 보고에 명시한다.
2. 총괄이 그 범위를 확대하려면 근거를 제시해야 한다. 근거 없는 확대는 무효이며 발견자 판정이 유지된다.
   축소·유지는 총괄 자유.
3. 총괄 시퀀싱 경계: 총괄은 파트 간(inter-part) 순서를 지정한다. 파트 내 유닛 순서는 해당 파트 자율이다.
   D-074 1항의 "작업 순서 지정"이 유닛 단위까지 내려가지 않음을 명문화한다.

이유
- 사례 2 실물: 백엔드가 코드 실측으로 "탈퇴·프로필 수정 두 유닛의 공통 선행"으로 좁힌 것이 정확했고,
  총괄이 문서 수준에서 전체 선행으로 확대한 것이 틀렸다.
- 정보 비대칭이 구조적이다: 발견자는 코드·스키마 실측으로, 총괄은 문서 수준 추론으로 판정한다.
  D-016 기준 위계가 근거의 질로 판정을 가르듯, 파생 범위도 실측이 추론에 우선한다.
- 076이 이미 "나머지 순서는 네 자율이다. 총괄이 유닛 단위 순서까지 지정하지 않는다 — 그게 이번 꼬임의
  원인이었다"고 실천했다. D-074에 경계선 1줄을 추가해 성문화하는 것이다.
- D-006 위임 원칙과 정합: 구현 기법 선택이 백엔드 자율이면, 유닛 순서도 그 일부다.

기각된 대안
- 총괄 판정(현행 관행): 사례 2 재발. 총괄에겐 실측 접근권이 없다.
- 협의로 합의: 왕복 증가 + 교착 시 결론 없음. 기본값 + 확대 시 입증 책임이면 왕복 1회로 끝난다.
- 발견자 판정 절대(총괄 확대 전면 금지): 발견자가 자기 작업만 보고 전역 의존을 놓칠 수 있다.
  근거 있는 확대는 열어둔다.

레퍼런스: 현지현물(실측 우선), D-006 위임 원칙, subsidiarity(결정은 정보가 있는 최말단에서).

---

## C-034. 파생 뿌리 점검 1문 — 계보 표 미도입

상태: ACCEPTED · escalated-as D-085 · 소유: 컨설턴트 · 관련: relates-to D-026(추적표)·D-011(관련 필드)·D-027(사고 회고) · 2026-07-15
(총괄 회부 077 안건 4. 사례 3 — 022 계약층 / 028 구현층 동일 뿌리)

결정
1. 별도 파생 계보 표·다이어그램을 도입하지 않는다. 계보는 기존 결정 로그의 관련 필드
   (depends-on/relates-to/escalated-as)와 outbox `회신대상` 줄이 이미 방향 그래프로 형성한다.
2. 스펙 공백·결함을 처리하는 회신·완료 보고에 뿌리 점검 1줄을 넣는다:
   "뿌리 점검: 같은 원인이 다른 층(계약·ERD·구현·테스트)에도 공백을 만들었는가 — <있음: ID / 없음>".
3. 파생 메시지는 기존 "관련 ID"에 뿌리 ID를 포함한다(신규 필드 없음).

이유
- 사례 3 실물: 022(계약층)와 028(구현층)은 "회원·탈퇴가 스펙에서 통째 누락"이라는 같은 뿌리의 층별 파생이다.
  022 처리 시 다른 층을 점검하지 않아 028을 예고 없이 다시 맞았다.
- 레버리지는 표가 아니라 질문이다. 계보 표를 만들어도 "다른 층에도 있나"를 묻지 않으면 028은 똑같이 놓친다.
  반대로 질문 1줄이면 표 없이도 잡힌다.
- 계보 표는 매 파생마다 갱신 비용이 발생하고(1인 시뮬 규모에서 순수 부채), 기존 관련 필드가 이미 그래프를
  이룬다 — 이중 관리(D-034 용어집 기각과 같은 논리).
- 층(계약·ERD·구현·테스트)이 이 프로젝트의 실제 문서 위계라 점검 축이 자명하고 열거가 짧다. 질문이 가볍다.

기각된 대안
- 파생 계보 표 신설(D-026 확장): 유지 비용 + 스냅샷 표에 시계열을 얹는 목적 혼입. 과설계.
- 계보 다이어그램 자동 생성: 도구·유지 비용. 규모 대비 과잉.
- 현행 유지(관련 필드만): 사례 3 재발 — 필드는 기록이지 점검이 아니다.

레퍼런스: D-027 사고 회고의 "재발 여부 — 이전 대책이 왜 못 막았나"(같은 원리를 파생에 적용),
D-034 용어집 기각(이중 관리 회피), 5 Whys의 뿌리 추적.

---

## C-035. 부재 증명의 근거는 탐색 방법 명시 (D-043 일반화) — '도구 사각지대' 일반 규약은 기각

상태: ACCEPTED · escalated-as D-086 · 소유: 컨설턴트 · 관련: generalizes D-043(교차검증), relates-to D-082(근거 인용)·C-003 · 2026-07-15
(총괄 회부 management/outbox/078 참고 항목 — 검증 도구 사각지대 3건 판단)

결정
1. **3건을 묶는 "도구 사각지대" 일반 규약은 만들지 않는다.** 총괄 심증대로 공허해진다.
   세 건은 실패 계열이 서로 다르기 때문이다 — (1) QA 기준에 erd 누락 = **기준 커버리지** 결함,
   (2) bash 마운트 stale = **환경 결함**, (3) import 기준 grep = **질의 방법** 오류.
   공통점은 "도구를 믿었는데 못 봤다"는 서술 수준일 뿐 메커니즘이 아니다. 메커니즘이 다른 것을
   한 규칙으로 묶으면 검증 불가능한 훈계가 된다. 각 건은 국소 조치(075 / D-043 / 백엔드 자체)로 종결.
2. **다만 (2)·(3)이 공유하는 좁은 메커니즘 하나만 D-043의 일반화로 성문화한다.**
   탐색 결과의 **부재("없음")나 수량("N건")을 결정·보고의 근거로 삼을 때는 탐색 방법을 1구 명시한다**
   (예: "import 기준 grep", "호스트 Read 교차확인"). 방법을 명시할 수 없으면 근거로 쓸 수 없다.
3. D-082(근거 인용)의 대칭 조항으로 편입한다 — **근거는 있으면 인용, 없으면 방법**. 신규 필드·문서 없음.
4. 적용 범위를 과장하지 않는다: 본 조항은 (1) 기준 커버리지 계열을 잡지 못한다. 그건 075로 종결된 별건이다.

이유
- **부재 증명은 비대칭이다.** 긍정 결과는 자기 증명적이지만(찾았으면 실재한다), 부재·수량은 전적으로
  도구의 커버리지에 의존한다. 사각지대는 언제나 거짓 음성으로만 나타나므로, 부정 결과가 정확히
  사각지대가 무는 지점이다. 그래서 "도구 일반"이 아니라 "부정 결과"로 좁히면 규칙이 공허해지지 않는다.
- 사례 (2)·(3)은 같은 메커니즘의 두 사례다: bash가 "파일 없음"(실제로는 stale 뷰), grep이 "참조 N건"
  (실제로는 동일 패키지 사용처 누락). 둘 다 부재·수량을 단일 도구 결과로 단정했다.
- 방법 명시는 절차를 추가하지 않고 형식에 내장한다 — "import 기준 grep"이라고 쓰는 순간 동일 패키지
  누락이 작성자·독자 모두에게 드러난다. D-082가 인용으로 실재를 강제한 것과 정확히 같은 장치의 대칭이며,
  추가 학습 비용이 없다.
- **D-043이 이미 이 규칙의 1호 사례다.** 마운트 stale 한 도구에만 교차검증을 걸어둔 상태다. 일반화하지
  않으면 도구가 바뀔 때마다 D를 새로 발번하게 된다(grep용, 다음 도구용). 예외·개별 규칙이 누적되는 것은
  기본값이 틀렸다는 신호다(C-003 논리). 일반화가 규약 증식을 막는 쪽이다.
- 비용: 부정 결과를 근거로 쓸 때 1구 덧붙이는 것뿐. 모든 탐색에 부과되지 않고 "근거로 삼을 때"로 한정된다.

기각된 대안
- "도구 사각지대를 인지하라" 일반 규약 신설: 검증 불가능·공허. 총괄 심증이 옳다.
- 규약화 전면 불요(국소 조치만): D-043이 이미 1호로 존재하므로 일반화 없이는 도구별 D 증식으로 귀결.
  국소 조치의 합이 규약보다 무거워지는 지점이다.
- 도구별 사각지대 목록 문서화: 도구가 바뀌면 즉시 낡는 부채. 유지 주체도 없다.

레퍼런스: absence of evidence ≠ evidence of absence(부재 증명의 비대칭), D-043(교차검증 1호 사례),
C-003(예외 누적은 기본값 오류 신호), D-082 근거 인용의 대칭.
