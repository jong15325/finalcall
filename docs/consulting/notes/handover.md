# 컨설턴트 handover

성격: 휘발성 맥락 — 파일에 있는 내용은 경로 참조만. 이력 없음(덮어씀).
갱신: 2026-07-16

## 진행 중

- **규약 감축 — 착수 2026-07-16 · 사용자 지시. C-066 완료.**
  156 → 100. 편입 기준 3문 → 1문("지우면 뭐가 깨지나"). 결번 유지로 번호 이동 0.
  다음 한 수: **파트 이의를 기다린다**(`_broadcast/002` 3-(4) 기준). **백엔드 이의 없음(RULES/003).**
  **`templates.md`는 아직 안 봤다** — 작성 규칙 34개도 같은 기준으로 재판정 대상이다.
  **백엔드 관측이 그 근거다**: "`rules`의 브리핑 11개는 한 번도 안 봤다, 없어진 걸 오늘 알았다."
  **남은 89개에도 같은 질문을 해야 한다 — 누가 실제로 읽나.**

- **호스트 도구 침묵 — 미확인 2건.** C-067로 gitignore 건은 닫혔으나 계열일 수 있는 것이 남았다.
  ① **Glob이 실재 디렉터리에 두 번 0건**(`management/outbox/REFORM/*`, `design/outbox/**/*.md`)
  ② **Grep `glob` 필터가 조용히 안 먹음**(`!common/rules.md` 제외 · `{a,b}/**` 한정).
  **셋 다 "호스트 도구가 조용히 0건"이다. 안 세었다 — 세면 계열이 된다.**
  또 `path=docs` 스코프에서는 철회 전에도 잡혔던 것이 gitignore 가설로 설명 안 된다.

- **B-1 `_archive/` 이관** — **미착수.** 사용자 착수 지시를 받았으나 실행 안 됨. 차단 요인 없다.
  - 대상: `management/collaboration-guide.md` · `management/templates.md` · `consulting/01~05-*.md` ·
    `consulting/templates.md` · `consulting/draft-*.md` 3종 · `management/prompts/` 7종 ·
    `frontend-planning/`(D-079 동결) · 구 flat outbox
  - 다음 한 수: 이관 대상을 **호스트 Glob이 아니라 Grep으로** 확정(아래 [도구] 참조) → `docs/_archive/` 생성 → **`[9.4]` 호스트 Read→Write→삭제**로 이동. **`mv` 금지** — 기획이 spec 이동에서 절단 3파일(41/68/34줄)을 실측했다.
  - 병행 가능: `[7.11]` 쓰기 집합 무충돌. 총괄이 REFORM/001에서 확인함.

- **C 파트 지침 4종 정합** — B-1 후. **내가 쓰는 게 아니다.**
  `qa/qa-guide.md` · `security/security-guide.md` · `ux/design-guide.md` · `frontend/CLAUDE.md`
  — 유형 2 변경 규칙: **소유 역할이 제안하고 컨설턴트가 승인한다**(`[3.2]`).

## 대기 중

- 없음. 내 발신 전건 `회신: 불요`다(체계 축은 내 종결이라 통지 형태).
- 미확인: 총괄이 `REFORM/003`(006 회신)을, 백엔드가 `RULES/001`(C-063 회신)을 아직 안 읽었다.

## 판단 대기 — 다음 세션이 결정할 것

1. **`[5.21]` 구멍** — "사용자 지시 시" 수신함 스캔에 **접두어 스캔이 안 걸려 있다.** `[5.25]`가
   "둘 다 한다"고 하고 `[8.7]`은 출근에 둘 다 넣었는데, `[5.21]`의 사용자 지시 경로엔 `[5.22]`만 있다.
   **실제로 기획 REFORM/001을 놓칠 뻔했고 사용자 지적으로 잡았다.**
2. **`design/notes/handover-kickoff-prompt.md` 삭제 여부** — 기획이 넘긴 안건. 판정은 체계 축(내 것),
   실행은 기획(`[3.2]`). 기획이 폐기 주만 달아둔 판단은 옳다.
3. **뿌리: 규약 선언 ↔ 실물 이행 시차** — **총괄(REFORM/003)과 기획(REFORM/001)이 독립으로 같은 곳에
   도달했다**: "체계=컨설턴트 선언 / 프로젝트=총괄 시퀀싱 / 실행=소유 역할로 갈려 있어 그 사이에
   '정본이 거짓인 구간'이 생긴다." **두 번 나왔으면 관측이 아니라 패턴이다.** 다만 처방이 안 보인다 —
   "선언과 실물 사이를 표기하라"는 C-035·C-062가 두 번 기각한 형태에 가깝다.
   **07-16 갱신: 번호 밀림이 이 패턴의 세 번째다** — 인용은 선언 시점에 얼었고 정본은 움직였다.

## 다음 세션이 알아야 할 판단

- **`[5]` 절 번호를 내가 세 번 밀었다** — C-058(`[5.7]~[5.10]` 지시 충돌 신설, +4) · C-058 부수
  (`[5.41]` 추천-정본 정합, +1) · C-057(`[5.21]~[5.25]` 수신함 신설, +5). **세 번 다 `집행:`에
  "재번호"라고 내가 적어놨다**(decision-log :1933 · :1991 · :2158). `templates.md [13]`이
  "중간에 삽입해 뒤를 밀지 않는다 — 밀면 불변 문서의 인용이 영구히 어긋난다"고 금지한 것을 저자가 했다.
- **깨진 인용은 소급 정정하지 않는다** — `[5.18]`·[4.2] 불변. 대조표는 `consulting/outbox/_broadcast/001`.
- **총괄은 C-064까지 모른다.** REFORM/003으로 통지했으나 미확인. 인덱스 백필 요청 = C-064.
- **016부터 스레드 폴더.** 015는 부트스트랩 예외였고 끝났다. 현재 스레드: `REFORM`(총괄) · `RULES`(백엔드) · `_broadcast`(전 역할).
- **`spec/` 이동은 기획이 완료했다.** `docs/spec/` 3종 실재(227/409/374줄).
- **날짜 오기**: C-036~064가 전부 `2026-07-15`인데 실제는 **07-16**이다. **`[4.2]` 불변이라 안 고친다.**
- **`[5]` 절이 57개다** — 다른 절의 3배. 비대한 절일수록 삽입이 잦고 밀림 피해가 크다. 이번이 실증이다.

## 도구 (실측)

- **Glob이 두 번 0건을 반환했다** — `docs/management/outbox/REFORM/*`, `docs/design/outbox/**/*.md`.
  **둘 다 실재했고 Grep이 찾았다.** **파일 목록은 Grep으로 확인할 것.**
- **Grep의 `glob` 필터가 조용히 안 먹는다** — `!common/rules.md` 제외도, `{consulting,backend}/**` 한정도
  무시되거나 0건을 냈다. **경로 한정이 필요하면 `path` 인자로 준다.**
- 번호 **중복·결번**은 `^\*\*\[N\.\d+\]` Grep으로 잡힌다(C-064). **번호 밀림은 안 잡힌다** — 밀린 번호는
  전부 연속이고 실재하는 다른 조항을 가리킨다. **밀림은 `git show <커밋>:<파일>` 대조로만 보인다**(C-065).

## 자기 관측 (반복되는 것)

- **버스에 안 써서 남이 헛수고한 게 2번이다**(015 미기록 · C-064 미통지). "회신 불요"는 "발번을 알리지 마라"가 아니다.
- **자기 재발 방지 노트를 읽고도 재발한다.**
- **자기가 쓴 규칙을 자기가 어긴 게 이제 3번이다** — `[5.36]`·`[8.1]` 미기재(015) · `[5.54]` 폴링(C-060) ·
  `templates [13]` 재번호 금지(C-065). **셋 다 "내가 저자라서 안 읽는다"가 공통이다.**
- **문제를 볼 때마다 조항을 하나 더 썼다** — 66개 결정 중 **조항을 지운 건 C-066이 처음이다.**
  C-035·C-062·C-064에서 "신규 조항 신설"을 세 번 기각했으면서도 **기각의 대안이 늘 "다른 조항"이나
  "도구"였지 "삭제"가 아니었다. 삭제가 선택지에 없었다.**
- **사용자가 세 번 같은 곳을 짚었다** — 커밋 폴링(C-060) · 번호 밀림(오늘) · 비대함(C-066).
  **셋 다 내가 만들었고 셋 다 남이 먼저 봤다.** 다음 비대화 때 이 줄을 근거로 나를 쳐라.
- **사용자 증상을 내 언어로 번역하지 마라.** 커밋 문제를 3번 오독했다(C-058).
- **부분 검증이 전체 검증으로 읽힌다** — C-064가 "중복·결번 0건"을 확인하고 번호 정합을 끝냈다고 믿었다.
  **밀림은 세지 않았다.** 이 관측의 4번째 사례이고, **직전 사례가 바로 그 C-064다.**

## 막힌 것

- 없음.
