# 컨설턴트 handover

성격: 휘발성 맥락 — 파일에 있는 내용은 경로 참조만. 이력 없음(덮어씀).
갱신: 2026-07-16

## 진행 중

- **없음. 대기 모드다**(C-070, 2026-07-16). 체계 개편 종료.
  **기동 사유는 둘뿐**: 사용자 `출근` 지시 · 파트가 **실제로 막혀서** 하는 회부.
  **"조항이 좀 이상하다"는 안 받는다.**

## 대기 중

- 없음. 내 발신 전건 `회신: 불요`다.

## 다음 세션이 알아야 할 판단

- **정본은 `rules.md` 100개 · `templates.md` 작성 규칙 17개다. 더 안 바꾼다.**
  바꾸려면 **`[3.12]` — 변경분 전수를 표로 내고 사용자 확인부터 받는다.**
  **이걸 먼저 어길 사람은 나다** — 오늘 C-066·C-067은 사후 승인이었다.
- **버린 6건**(C-070): B-1 `_archive/` 이관 · 파트 지침 4종 · `[5.21]` 구멍 ·
  킥오프 프롬프트 삭제 · 호스트 도구 침묵 미확인 2건 · "선언↔실물 시차" 뿌리.
  **버린 게 아니라 미룬 것이고, 미뤘다는 표시가 없다.** 막히면 온다.
- **실피해 이력이 있는 미완 2건** — `[5.21]` 구멍(기획 `REFORM/001`을 놓칠 뻔) ·
  **Glob이 실재 디렉터리에 0건**(`[9.1]`이 정본 도구인데 조용히 거짓말한다. C-067은
  gitignore 경로만 닫았다). **다시 오면 이 둘부터다.**
- **`[5]` 절 번호가 밀려 있다**(C-065). 총괄 `REFORM/001~004`·`_broadcast/001`의 `[5.x]` 인용이
  낡았다. 대조표 = `consulting/outbox/_broadcast/001`. **불변이라 정정 안 한다.**
- **C-065·`_broadcast/001` §4에 오기 2건** — 밀림 원인을 `C-058`이라 썼는데 **`C-056`이 맞다.**
  정정 기록은 C-067 말미. 불변이라 원문은 그대로다.
- **날짜 오기**: C-036~064가 전부 `2026-07-15`인데 실제는 07-16이다. `[4.2]` 불변이라 안 고친다.

## 도구 (실측)

- **호스트 도구가 조용히 0건을 내는 계열이 있다.** ① `.gitignore` 대상 파일 — **C-067로 해소**
  ② **Glob이 실재 디렉터리에 두 번 0건**(`management/outbox/REFORM/*`, `design/outbox/**/*.md`)
  ③ **Grep `glob` 필터가 안 먹음**(`!파일` 제외 · `{a,b}/**` 한정). **②③ 미해소 — `path` 인자를 쓴다.**
- 조항 번호 **중복·결번**은 `^\*\*\[N\.\d+\]` Grep. **밀림은 `git show <해시>:<파일>` 대조로만 보인다.**
- **bash가 `rules.md`를 binary로 본다**(인코딩). 조항 세기는 호스트 Grep으로.

## 자기 관측 (반복되는 것)

- **문제를 볼 때마다 조항을 하나 더 썼다.** 70개 결정 중 **조항을 지운 건 C-066이 처음이다.**
  C-035·C-062·C-064에서 "조항 신설"을 세 번 기각했으면서 **기각의 대안이 늘 "다른 조항"이나
  "도구"였지 "삭제"가 아니었다. 삭제가 선택지에 없었다.**
- **사용자가 네 번 같은 곳을 짚었다** — 커밋 폴링(C-060) · 번호 밀림 · 비대함(C-066) ·
  정본을 통보로 바꾼 것(C-068). 그리고 **다섯 번째가 "작업이 전혀 진행되지 않는다"(C-070)**.
  **다섯 다 내가 만들었고 다섯 다 남이 먼저 봤다.**
- **오늘 C-063~070 여덟 건 중 순가치는 C-066·C-068 둘뿐이고 둘 다 사용자 발제다.**
  C-064·C-065는 내 결함을 내가 고쳤고 C-067은 내 결함을 백엔드가 고쳤다.
  **내 산출물의 과반이 내 산출물의 수리다.**
- **자기가 쓴 규칙을 자기가 안 읽고 어긴다** — `[5.36]`·`[8.1]`(015) · `[5.54]` 폴링(C-060) ·
  `templates [13]` 재번호 금지(C-065). **"내가 저자라서 안 읽는다"가 공통이다.**
- **사용자 증상을 내 언어로 번역하지 마라.** 커밋 문제를 3번 오독했다(C-058).
- **부분 검증이 전체 검증으로 읽힌다.** C-064가 "중복·결번 0건"으로 번호 정합을 끝냈다고 믿었고
  밀림은 세지 않았다. C-066이 `rules.md`만 훑고 `templates [13]` 골격의 3문 잔존을 놓쳤다.

## 막힌 것

- 없음.
