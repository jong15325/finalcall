# 컨설턴트 handover

성격: 휘발성 맥락 — 파일에 있는 내용은 경로 참조만. 이력 없음(덮어씀).
갱신: 2026-07-16

## 진행 중

- **없음. 대기 모드로 복귀했다**(C-070).
  **기동 사유는 둘뿐**: 사용자 `출근` 지시 · 파트가 **실제로 막혀서** 하는 회부.
- **폴더 구조 정형화(C-073)는 내 몫이 끝났다.** 구조 정본 = `_broadcast/006`. **실물 이동은 각 파트,
  착수 배치는 총괄이다**(`[3.2]`·`[1.4]`). **파트가 옮기는 걸 내가 확인하러 가지 마라** — 이의가 오면
  그때 받는다.

## 대기 중 — 사용자 앞 1건

1. **QA 구동 시점** — 사용자가 "추후 다시 말하겠다"(2026-07-16). **내용을 모르고, 찾지 마라.**
   실측 완료: 전 outbox `[X → 컨설턴트]`에 없다. 사용자가 직접 말할 때까지 대기다.
   **`[7.3]`이 없어서 총괄이 못 보낸 것이 아니다** — 그 가설은 C-071에서 실측 없이 세운 것으로 판명됐다.

## 다음 세션이 알아야 할 판단

- **사용자 판정(07-16): "현재 규칙이 너무 까다로워 진행이 안 되고 있다."** C-071의 사유이고,
  **이 대화의 기본값이다.** 조항을 늘리는 쪽으로 기우는 순간 이 문장을 먼저 읽어라.
  **`[7.3]` 복원은 기각됐다. 다시 꺼내지 마라** — 결번이 정상이다.
- **레퍼런스 대조 결론(07-16, 수정 0건).** 원문 대조는 Anthropic·Cognition 2편만이고 나머지는
  검색 수준이다(신뢰도 한 칸 낮음).
  - **템플릿 계열은 관행과 수렴한다** — `[4.2]` 불변·`[4.4]` 상태·`templates [6]` = Nygard ADR /
    MADR(`Good/Bad because`) / Diátaxis. **outbox 파일 버스는 Anthropic이 명시 권장하는 패턴이다**
    (*"minimize the game of telephone"*). **여기는 건드릴 게 없다.**
  - **관행과 반대인 것 3건**: ① `[5.1]` 직통 + `[1.6]` 총괄 비-라우터 = Cognition이 *"unstructured
    swarm… mostly a distraction"*이라 한 형태(권장은 map-reduce-and-manage) ② 7역할 멀티 라이터 =
    *"writes stay single-threaded"*와 반대 ③ 조항 102 = *"heuristics rather than rigid rules"*와 반대.
    **`effort budget`이 우리에게 0개다** — Anthropic은 그걸 프롬프트에 박는다.
  - **실측**: `[5]` 28 + `[9]` 22 = **49%가 조정 비용**. 경매를 만드는 조항은 `[6]`3+`[7]`10 = 13개.
  - **내가 두 번 과장했고 두 번 다 사용자가 잡았다.** "서로 못 본다" → **틀림**(`[3.8]` 읽기 전면 허용,
    총괄이 실제로 읽어서 C-038 오기를 잡았다). "동시 쓰기 위험" → **약함**(대화를 하나씩 켠다).
    **남는 진짜 비용은 "뭘 열지 찾는 색인 비용"이고 그게 `[5]` 28개다.**
  - **다음 질문은 하나다**: `[9.18]`의 "안 읽음이 기본값"을 바꿀 건가, 색인 비용을 계속 낼 건가.
    **내가 먼저 꺼내지 마라. 사용자가 열면 그때.**
- **전 파트 작업 중단 중**(사용자 지시, 총괄 handover:13~18). 범위 = ① 코드 유지 ② 앞으로 할 작업
  삭제·재설계 ③ 규칙·규약 중 불필요한 **파일** 삭제. **③은 B-1로 집행 완료**(consulting/outbox/REFORM/004).
  **기능 레인 해제는 사용자가 직접 중단시켰다 — 총괄 조치 불요. 총괄을 기동시키지 마라. 이미 출근해 있다.**
- **정본은 `rules.md` 100개 · `templates.md` 작성 규칙 17개.** 바꾸려면 **`[3.12]` — 변경분 전수를
  표로 내고 사용자 확인부터.** **이걸 먼저 어길 사람은 나다** — C-066·C-067이 사후 승인이었다.
- **`[5]` 절 번호가 밀려 있다**(C-065). 대조표 = `consulting/outbox/_broadcast/001`. 불변이라 정정 안 한다.
- **C-065·`_broadcast/001` §4 오기**: 밀림 원인을 `C-058`이라 썼는데 **`C-056`이 맞다**. 정정은 C-067 말미.
- **C-066·`_broadcast/002` 오기**: supersede 대상을 `C-049`라 썼는데 **`C-038`이 맞다**(총괄 `REFORM/007`).
  본문 불변이라 못 고친다 — **C-038 상태 라벨 + 대체 사유 주**로 닫았다. 전파는 불요로 판정.
- **버린 4건**(C-070): 파트 지침 4종 정합 · `[5.21]` 구멍 · 호스트 도구 침묵 2건 ·
  "선언↔실물 시차" 뿌리. **버린 게 아니라 미룬 것이고 표시가 없다.**
  (B-1·킥오프 삭제는 닫혔다 — B-1 = REFORM/004, 킥오프 = 기획 폐기 주)
- **★ `[15]` 함정 — 내가 만들었고 총괄이 잡았다.** `qa-guide.md:14`가 `templates 15`를 계약 변경 형식으로
  가리키는데, **어제는 결번이라 "없음"이 나와 안전했고 오늘 C-072가 그 자리를 채워 조용히 틀리게 됐다.**
  `_broadcast/001`에서 **내가 경고한 형태를 내가 하루 뒤 재현했다.** QA에 `rules [6]`으로 정정 지시함
  (`_broadcast/006` [4]). **나머지 3종(`security`·`design`·`frontend`)의 `templates N` 참조는 아무도 안 셌다.**
- **C-073의 값 3개 — 내가 알면서 열었다.** ① `references/`가 유형 2라 참조 자료 한 줄에도 내 승인이
  걸린다(상쇄 논거는 **심증이고 실측 아님**) ② 8역할이 파일을 옮긴다 — 절단이 8곳에서 가능 ③ **`<역할>/rules.md`
  8개가 다시 부풀 자리다.** C-066이 공통에서 한 일을 파트가 각자 반복할 수 있고 **막지 않기로 했다.**
  **다음 세션이 "파트 규칙이 늘었다"를 보면 그건 예측된 것이지 새 발견이 아니다.**

## 도구 (실측)

- **호스트 도구가 조용히 0건을 내는 계열.** ① `.gitignore` 대상 — **C-067로 해소**
  ② **Glob이 실재 디렉터리에 0건** — 4회 재현. **파일 목록은 `Grep(".", path=<폴더>, files_with_matches)`로.**
  ③ **Grep brace-glob이 매치 있는 파일을 건너뛴다** — 총괄 독립 실측(handover:21).
  **부재 주장에 glob 쓰지 마라. `path` 인자로 폴더 전체를 돌려라.**
- **bash `rm`이 기본적으로 막혀 있다**(Operation not permitted). `mcp__cowork__allow_cowork_file_delete`로
  1회 승인받으면 폴더 전체에 열린다. `[9.4]` 이동은 여전히 **호스트 Read → Write → `rm`**.
- 조항 **중복·결번**은 `^\*\*\[N\.\d+\]` Grep. **밀림은 `git show <해시>:<파일>` 대조로만.**
- **bash가 `rules.md`를 binary로 본다.** 조항 세기는 호스트 Grep으로.

## 자기 관측 (반복되는 것)

- **실측 없이 부재를 단정한다.** 어제 3건 연속에 이어 **`[7.3]` 막힘 가설이 4번째**였다 —
  총괄이 못 보낸 증거는 0건인데 "막힌 것"으로 올렸다. `[5.34]`를 어겼고, 그 조항을 남에게
  들이댄 게 나다. **C-071이 그 실증으로 남아 있다.**
- **빈 수신함을 못 견딘다.** **수신함이 비면 대기가 정답이다.**
- **문제를 볼 때마다 조항을 하나 더 썼다.** 그리고 **지운 조항을 하루 만에 되살리려 했다** —
  삭제 판정도 신설만큼 틀리지만, **되살리려는 충동이 삭제보다 자주 틀린다.**
- **내 산출물의 과반이 내 산출물의 수리다.** C-064·C-065는 내 결함을 내가, C-067은 백엔드가,
  **C-038 오기는 총괄이** 잡았다.
- **자기가 쓴 규칙을 자기가 안 읽고 어긴다** — **"내가 저자라서 안 읽는다"가 공통이다.**
- **내가 방금 쓴 ID는 내가 확인하지 않는다** — 방금 썼으니 안다고 느끼기 때문이다(C-038 3홉 전파).
- **총괄·백엔드가 내 기준을 나보다 정확히 적용한다.**
