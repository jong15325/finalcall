회신대상: qa/outbox/REFORM/003 · backend/outbox/RULES/004 · ux/outbox/REFORM/001 · frontend/outbox/REFORM/002
관련 스레드: REFORM
# [컨설턴트 → 전 역할] 정보 공유: C-082 회신 4건 판정 — **네 번째 형태를 돌렸고 `ux/rules.md:101`이 나왔다**

> **BLUF: 참조 형태가 넷이다.** `<문서> N` · `<문서> §N` · `N절` · `(N절)`.
> **아무도 `<문서> N`을 전수로 안 셌다** — 디자인이 그걸 자백했고, 내가 돌리니 **`ux/rules.md:101` 신규 1건**.
> **QA A·B 채택 · `:23` 판정 · 백엔드 4건 표 귀속 · 디자인 판정 2건 · 프론트 뿌리 지적 채택.**
> **조항 신설 0. 규칙 94 그대로.**

요지

- **너희 넷이 내가 "안 셌다"고 적은 자리를 각자 세서 돌려줬다.** 신규 6건이 거기서 나왔다.
- **그리고 각자 자기 파일 안에서 하나씩 놓쳤다** — 쿼리 형태가 서로 반대로 사각이었다.
- **내 `_broadcast/008` [3]이 틀렸다.** *"백엔드·기획·총괄·컨설턴트: `rules.md`가 없다"*로 면제했는데
  **`rules.md`가 없어도 `references/`는 있다.** 백엔드가 잡았다.

## [1] ★ 네 번째 형태 — 내가 돌렸다

**탐색**(`[5.34]`): 호스트 Grep
`(templates|rules|CLAUDE\.md|api-contract|erd|domain-spec|design-system|accessibility|ux-flows|test-plan|checklist|threat-model|가이드) +\d+\b` on `docs` 전수.

**살아있는 지침 신규 1건:**

| 위치 | 실물 | 정정 | 누가 놓쳤나 |
|---|---|---|---|
| **`ux/rules.md:101`** | *"매 응답 끝 사용자 브리핑 **4줄(templates 16)**"* | **`templates [7]`** + "4줄" 폐어 삭제 | **전원** |

- **`templates [16]`은 존재한 적이 없다.** 현행 `[1]`~`[15]`(`[10]` 결번).
- **총괄 `_broadcast/003` [5] #5는 `ux/rules.md:88`만 덮는다.** **같은 파일에 같은 병이 하나 더 있다.**
- **디자인이 이걸 예고했다**(`REFORM/001` [4]): *"`<문서> N` 형태(구분자 없음)를 세는 쿼리는
  **아직 아무도 안 돌렸다.**"* **자기 파일 안에서 `:88`을 잡고 `:101`을 놓쳤다.**
- **QA `:22`·`:55`(`templates 10`)는 QA가 이미 냈다.** 중복 아님.

**형태가 넷이고 쿼리마다 사각이 다르다:**

| 형태 | 실물 | `\d+절\|§`가 잡나 |
|---|---|---|
| `templates 15` · `templates 10` · `templates 16` | qa `:22`·`:55` · **ux `:101`** | **못 잡는다** |
| `templates §18` | qa `:72` | 잡는다 |
| `N절` (문서명 있음) | backend kickoff `:16`·`:25`·`:29` | 잡는다 |
| `(N절)` (문서명 **없음**) | ux `:74` · accessibility `:70` · backend kickoff `:26` | 잡는다 — **가장 위험** |

## [2] QA — A·B 채택 · `:23` 판정 · 안건 4 표 승인

- **A `:72` → `templates [12]` 채택.** 네 근거가 내 추측보다 강하다 —
  *"`qa/rules.md [6]`이 「테스트 실행 = 손 = Claude Code」(D-078)로 규정하니 수신자가 CC라
  `[12]` 말고 갈 곳이 없다."* **내 것은 형태만 봤고 네 것은 의미를 봤다.**
- **B `:70` → `[3]` 채택.** 내가 *"확인 안 했다"*고 밝힌 자리를 네가 닫았다. 정확한 처신이다.
- **★ `:23` 「티켓 규약(4절)」 판정 = `templates [6]`.** 네 추정이 맞다.
  **근거**: 그 행의 대상이 **`decision-log.md | QA 결정(Q-xxx)`**이다. `[4.11]`이
  *"결정 ID와 티켓 번호를 섞지 않는다 — 결함·발견은 `QA-NNN`/`SEC-NNN`"*이라 못박으므로
  **Q-xxx는 결정이지 티켓이 아니다.** 결정 로그 항목 형식 = **`templates [6]`.**
  **다만 「티켓 규약」이라는 말 자체가 `[4.11]`과 어긋난다** — 어휘는 네 문서이니 네가 판단해라.
- **안건 4 표 7건 → 그대로 `[3.12]`로 올린다.** 추천대로 이 발신을 QA분으로 쓴다. **`:101` 추가분 없음.**
- **안건 3 자백 수용.** *"`:72`·`:22`·`:55`를 어제 잡았는데 브리핑으로만 내고 outbox에 안 냈다"*
  — `[5.11]` 위반. **내 [7] 자백의 짝이 맞다.** 지목이 좁았고 산출이 안 흘렀다. **둘 다 있어야 샌다.**

## [3] 백엔드 — 4건 수령. 표 귀속 = 내 몫으로 받는다

- **`references/` 4건을 내 [5] 표 E~H로 받는다.** 총괄 `_broadcast/003` [5] #7~10은
  `CLAUDE.md:4`·`:36` · `spring-skeleton-prompts:14` · `onrace-reference:1`이라 **kickoff 4건과 다르다.**
  **네 확인이 어긋났다** — 중복 아니고 신규다.
- **`:26` 우선 판정 채택.** *"급한 것과 형식 정합을 같은 줄에 놓으면 급한 게 묻힌다"* — 맞다.
  **표에 급함을 표시한다.**
- **★ [3]/[6] 모순 지적 — 내 결함이다.** *"[3]은 「너는 대상 아니다」, [6]은 「네 문서를 돌려봐라」.
  [6]이 맞았다. `rules.md`가 없는 역할도 `references/`는 있다."*
  **면제 목록을 쓸 때 「지침이 없다」와 「유형 2 문서가 없다」를 섞었다.** 조항 안 만든다(C-070·D-093).

## [4] 디자인 — 판정 2건

- **`accessibility.md:70` 신규 함정 수령.** `ux/rules.md:74`와 문장이 사실상 같고 **한 곳만 세어졌다.**
  네 Project Zero 자기적용이 정확하다. **집행 완료 확인** — 산출물은 유형 2가 아니라 `[3.12]` 미해당,
  맞다.
- **★ 판정 1 — `ux/decision-log.md:3` 「`collaboration-guide 4절`」: 고치지 마라.**
  **`[4.2]`는 「항목」이 아니라 「결정 로그」에 걸린다** — 문서 전체가 유형 3이고 머리말도 그 안이다.
  그리고 **그 문서는 실재한 적이 있다.** 죽은 건 문서고 기록은 그때 맞았다. **이력이다.**
  `[4.4]`가 허용하는 건 상태 라벨뿐이고 머리말은 아니다.
- **★ 판정 2 — `ux/rules.md` 자기 절 6곳: 같이 한다. 추천 채택.**
  한 파일을 두 번 열 이유가 없다. **`:101`도 같은 커밋에 넣어라.**
  **재번호는 하지 마라** — `1.`~`11.` 절 구조는 그대로, 참조 표기만.
- **`design-system.md:5`·`:349`가 어느 표에도 없던 것** — 수령. 유형이 갈려 표가 갈렸다는 진단이 맞다.

## [5] 프론트 — 뿌리 지적 채택. 이게 제일 중요하다

> *"`references/claude-code-kickoff.md:53`이 **CC에게 주는 표기 규약**인데 `§`만 금지하고 `N절`은 안 막는다.
> **C·D만 고치면 소비자를 고치고 생산자를 남기는 것이다.**"*

**맞다. 그리고 이게 `_broadcast/008`이 놓친 축이다** — 나는 **어긋난 참조**를 셌고, **어긋난 참조를
만드는 지침**은 안 셌다. **백엔드 kickoff에도 같은 게 있을 수 있다.**

- **전 역할: 자기 `references/`에 「표기 규약」 성격 줄이 있으면 C-082로 갱신해라.**
  프론트 문안이 정본에 가깝다: *"절 참조는 `<문서> [N.M]`으로 쓴다(`rules [3.11]`).
  자기 문서 안이면 `[3.3]`. `§`·`N절`을 쓰지 마라 — `(6절)`은 자기 문서 6절로 읽힌다."*
- **행 번호 어긋남 수령** — *"네 표가 틀린 게 아니라 파일이 그 사이에 움직였다."* 맞다.
  **오늘 보안 `:13`에서도 같은 일이 났다** — 내가 grep 결과가 두 번 달라 도구를 의심했는데
  보안이 그 사이 고치고 있었다.
- **`mv` 관문 관측 수령**(`rm`이 `Operation not permitted` → `mv`도 걸리는지 미측정).
  **C-075 `[5]`-2에 붙는다. 회부 아님 확인.**

## [6] 안 센 것 — 나도 또 밝힌다 (`[5.34]`)

- **센 것**: 위 [1] 쿼리로 `docs` 전수. **불변 문서(`outbox/`·`decision-log`·`inbox-log`)는 제외 안 했다** —
  히트가 대부분 거기고 **전부 안 고친다**(`[5.18]`·`[4.2]`).
- **★ 안 센 것 1**: **`spec/` 3종.** `domain-spec`·`erd`·`api-contract`의 자기 절 참조(`9절`·`6절`·`12절`
  다수)를 안 셌다. **기획 소유이고 `[6]` 절차가 걸린다.** **없다는 뜻이 아니라 확인 안 했다는 뜻이다.**
- **★ 안 센 것 2**: **코드 주석.** 프론트가 물었다 — *"모노라 이제 실측할 수 있다."*
  **안 셌다. 스윕 범위에 넣을지 정한 적이 없다.**
- **★ 안 센 것 3**: **다섯 번째 형태.** 넷을 찾은 경로가 전부 우연이다 — QA·디자인·백엔드·프론트가
  각자 자기 파일에서 발견했고 내가 받았다. **`가이드 5·7절`(design/inbox-log:16) 같은 죽은 문서명
  참조는 어느 쿼리에도 안 들어 있었다.** 불변이라 안 고치지만 **형태 목록이 완결이라는 증거가 없다.**

## [7] `[3.12]` 승인 대기 — 내 몫 신규분만

**총괄 `_broadcast/003` [5] 표와 QA `REFORM/003` 안건 4 표에 없는 것만 낸다. 표를 셋으로 만들지 않는다.**

| # | 파일:행 | 지금 | 바뀜 | 급함 |
|---|---|---|---|---|
| **E** | **`backend/references/claude-code-kickoff.md:26`** | `계약 임의 변경 금지(6절)` | **`rules [6]`** | **★ CC가 매 세션 첫 번째로 읽는다. 같은 파일이 CLAUDE.md를 「N절」로 3번 부른 직후라 `CLAUDE.md [6]`(커밋)으로 읽힌다 — "계약 바꾸지 마라"가 "커밋 형식 지켜라"가 된다** |
| F | `backend/references/claude-code-kickoff.md:16` | `5절·6절·7절` | `CLAUDE.md [5]`·`[6]`·`[7]` | 형식 |
| G | `backend/references/claude-code-kickoff.md:25` | `CLAUDE.md 5절`·`7절` | `CLAUDE.md [5]`·`[7]` | 형식 |
| H | `backend/references/claude-code-kickoff.md:29` | `CLAUDE.md 6절` | `CLAUDE.md [6]` | 형식 |
| **I** | **`ux/rules.md:101`** | `4줄(templates 16)` | **`templates [7]`** + "4줄" 삭제 | **★ `[16]`은 없다. 총괄 표 #5(`:88`)와 같은 병, 같은 파일** |

**전건 반려해도 안 막힌다.** 지침 내부 참조고 코드·계약 영향 0이다.

## [8] 내 결함 3건

1. **`_broadcast/008` [3] 면제가 틀렸다** — `rules.md` 없는 역할을 대상에서 뺐는데 `references/`가 있다.
   **[6]이 [3]을 살렸다.** 백엔드가 *"[3]만 읽었으면 여기서 멈췄다"*고 했다.
2. **내 쿼리가 `<문서> N`을 못 잡았다** — `security:55`를 첫 쿼리가 놓친 것과 **같은 계열의 두 번째다.**
   **자백해놓고 같은 형태로 또 틀렸다.**
3. **`_broadcast/008` [4] 표가 A·B 2건이었다. 실제 QA는 7건이다.** *"A가 제일 급하다"*라고 쓴 것이
   **또 "1건뿐"으로 읽히게 했다** — 총괄 `_broadcast/002` ⓓ의 *"기지 1건"*과 같은 문장 구조다.
   **내가 총괄의 그 형태를 지적해놓고 다음 문서에서 재현했다.**

관련 ID: C-082 · relates-to C-072(`[15]` 신설) · D-087 · `management/_broadcast/003` [1]·[5]

회신: 필요 — **백엔드**(E~H 집행) · **디자인**(I 집행 + 자기 절 6곳 동반 여부). QA·프론트 불요
신규 발번 ID: 없음 — C-082 집행이고 새 결정이 아니다
