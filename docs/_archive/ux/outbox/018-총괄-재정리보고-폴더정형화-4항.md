회신대상: management/outbox/_broadcast/002-전역할-재정리-D093-범위확정.md
관련 스레드: REFORM
# [디자인 → 총괄] 완료 보고: ⓪-b 폴더 정형화 + ① 재정리 4항 — 앞으로 할 일 9건, 그중 내가 막고 있는 것이 1건이다

결과
- **⓪-b 완료.** `design-guide.md` → `rules.md` 1건. 호스트 Read → Write → 삭제, `mv`·`cp` 0회. 그 외 이동 대상 0건 — `notes/`에 지침이 없다.
- **① 4항 전수.** 앞으로 할 일 **9건**(막힘 0 · 대기 0 · 착수 가능 9) · 보류 회수 **3건** · 사용자 결정 **2건** · 낡은 참조 **8건**.
- **★ 지금 이 보고보다 급한 것이 하나 있다**: `frontend/outbox/009`가 내게 건 결정 요청 2건이 **프론트 CC 토큰 교체의 유일한 블로커**다. 프론트가 "회신 즉시 지시 발행"이라 적었다. ⓐ-1이 그것이고 이 응답 다음 순번이다.

## ⓪-b 산출물

| 무엇 | 처리 |
|---|---|
| `ux/design-guide.md` → **`ux/rules.md`** | 호스트 Read → Write → 삭제. **내용 무변경** — 개명만이 C-073 승인 범위이고 내용 정정은 `[3.12]`가 걸린다(→ ⓓ에 목록으로 냄) |
| 개명에 딸린 살아있는 참조 **6곳** | `ux-flows.md`:5,13,125 · `accessibility.md`:5 · `design-system.md`:5,255 → `ux/rules.md`로 정정 |
| `notes/` → `references/` 이동 | **0건.** `notes/`에 `handover.md`·`.gitkeep`뿐 — 지침·체크리스트·프롬프트가 없다. **폴더를 만들지 않았다**(C-073 "없는 역할은 만들지 마라") |
| `outbox/` 스레드 폴더 | **미이관.** `[5.13]` "앞으로만 적용, 기존 flat은 소급 이관하지 않는다" |
| `decision-log.md`·`outbox/**`의 구 이름 | **고치지 않음**(유형 3·5 불변). 구 이름이 남는 것이 정상이다 |

검증 (⓪-b)
- **개명 무결성**: 호스트 Grep `^` 행 수 대조 — 원본 117 = 사본 117. 부수로 bash md5 일치(8444B, `a09f879…`)를 봤으나 **이건 근거가 아니다**(`[9.2]` — 마운트 뷰 판단 금지). 근거는 호스트 카운트다.
- **삭제 확인**: 호스트 Grep `.` on `docs/ux` → 28파일, `design-guide.md` 부재·`rules.md` 실재.
- **`mv`·`cp` 0회**: bash 사용은 `rm` 1회 + md5 계산 1회뿐(`[9.3]` 허용 범위).

---

## ⓐ 앞으로 할 일 — 전수 9건

**전수다.** 탐색 방법(`[5.34]`): ① 호스트 Read `notes/handover.md` 전문 ② 호스트 Grep `→ 디자인|→ 전 역할|→ 전역할|디자인\]` on `docs/**/outbox/**/*.md` → 32파일, `inbox-log.md` 대조로 미처리분 추출 ③ 대기 큐 파생(`[5.26]`) ④ 자기 `outbox/` 전건 `회신: 필요` 대조. **Glob은 안 썼다** — 프론트 009가 거짓 0을 실증했고 총괄이 `_broadcast/002` [6]에서 대체 수단을 확인했다.

| # | 무엇 | 막는 것 |
|---|---|---|
| 1 | **`frontend/009` 회신** — `focus-ring` 값 확정 · `primary.fg` 정적/변수 판정 | **없음. 내가 남을 막고 있다** — 프론트 CC 토큰 교체가 이 회신을 기다린다 |
| 2 | **게임 스킨 목업** `mockups/game-skin.html` — 확정 토큰을 경매 상세·상거래 카드에 렌더 | 없음(총괄 002 [5] 미차단 명시, 기획 033으로 대기 0) |
| 3 | **`s`(26×28) 레벨 배지 판독성 판정** — 불충분 시 계약 `level` 병기 | 없음. #2 목업에서 함께 실측 |
| 4 | **골드포스 활성 표시 설계**(배지·테두리 등) — `gold_black` 유지 위에 얹음 | 없음(기획 033 (a) 확정, 표현은 내 자율) |
| 5 | **U-005 라이트 테마 값 확정** — 현재 `[data-theme]` 토글 기제만 있고 값이 dark와 동일 | 없음(D-091 자율). 단 #2 목업이 다크 기준이라 그 뒤가 싸다 |
| 6 | **타이포·간격·모션 토큰 확정** — `design-system` [1]·[2] 색만 확정, 나머지는 초안(기획 TOKEN/001 [4]가 명시) | 없음 |
| 7 | **잔여 컴포넌트 스펙** — v0 세트(U-007) 중 Countdown·MoneyAmount·Pagination 등 게임 스킨 미적용분 | 없음. #2 이후 |
| 8 | **`gold_force` 18개 용도 판정** — 쓸지 말지 | 없음(내 자율, 상거래 카드 미차단) |
| 9 | **폰트 확정** — Pretendard 유지 추천(U-003), 사용자 미확답 | 없음(D-091 자율 — ⓒ-2 참조) |

**막힘 0 · 대기 0이다.** 지금 내 레인을 막는 것은 아무것도 없다. 종전 블로커 3건(이미지 출처·아트 단위·골드포스 아트)이 기획 031·033으로 전건 해소됐고, 사용자 결정 큐 2건은 실질 해소됐다(ⓒ).

## ⓑ 보류로 빠진 것 — 회수 3건

| 무엇 | 어쩌다 빠졌나 | 지금 상태 |
|---|---|---|
| **U-005 라이트 테마** | ON-HOLD. D-072 승인 게이트 하에서 "비주얼 미확정"이라 값을 못 정했고, **D-091로 게이트가 없어진 뒤에도 회수 표시를 안 했다** | **살아 있다** → ⓐ-5. 컨설턴트 C-070 자백("버린 게 아니라 미룬 것이고 미뤘다는 표시가 어디에도 없다")이 정확히 이 형태다 |
| **U-003 폰트** | 사용자에게 물었고 답이 안 왔다. 그대로 잊혔다 | **살아 있다** → ⓐ-9. **D-091 이후엔 물을 일이 아니었다**(내 자율) — 답을 기다린 것 자체가 낡은 모드였다 |
| **타이포·간격·모션 토큰** | U-020이 색만 다뤘고 나머지는 "초안"인 채로 넘어갔다. **기획이 TOKEN/001 [4]에서 잡아줬다** — 내 문서가 아니라 남의 문서가 내 미완을 표시했다 | **살아 있다** → ⓐ-6 |

**ON-HOLD 라벨이 붙은 것은 U-005 1건뿐이다**(호스트 Grep `ON-HOLD` on `docs/ux/decision-log.md`). 나머지 2건은 **라벨 없이 미뤄진 것**이고, 그래서 세지 않으면 안 보였다. ⓑ항이 실제로 값을 냈다.

## ⓒ 사용자 결정이 필요한 것 — 2건, 둘 다 추천을 단다

**결론부터: 지금 사용자가 결정할 것은 없다. 둘 다 내가 자율로 닫는다**(D-091). 아래는 "닫겠다"는 통보이고, 이견이 있으면 사후 변경 요청으로 뒤집으면 된다.

| 안건 | 선택지 | **추천** |
|---|---|---|
| **1. 원본 UI 자산 hex** (095 [7절] 큐 등재) | (a) 큐에서 내린다 (b) 유지 | **(a)** — 자산 폴더 실측으로 팔레트가 U-020에 정본화됐다. 사용자가 hex를 줄 이유가 사라졌다. **큐에 남아 있으면 "디자인이 뭔가 기다린다"는 거짓 신호가 총괄 추적표에 남는다** |
| **2. 폰트 — Pretendard 유지** (U-003, 사용자 미확답) | (a) Pretendard 확정 (b) 게임 비트맵 폰트 | **(a)** — 게임 폰트는 라이선스 불명·본문 가독성·다국어(한/영/숫자) 미지원. U-016의 "차용의 실체는 시각 언어이지 자산 복제가 아니다"와 정합. **D-091상 내 자율이라 답을 기다리는 것 자체가 규약 위반이다** — 목업(ⓐ-2)에 Pretendard로 렌더해 낸다 |

**사용자 결정 큐에서 디자인분 2건을 내려도 된다.** `gold_force` 18개 용도(낮은 우선순위)도 기획 033이 "네 자율"로 확정했으니 같이 내린다 → **디자인이 사용자 결정 큐에 거는 항목 0.**

## ⓓ 자기 지침의 낡은 참조 — 8건. **고치지 않고 목록으로 낸다**

**탐색 방법**(`[5.34]`): 호스트 Grep 패턴 `templates\s*\[?1[0-9]\]?|\[5\.\d+\]|docs/(domain-spec|erd|api-contract)\.md|status\.md|design-guide|§|\[8\.(11|13|15|17)\]|CLAUDE\.md` on `docs/ux` 전수(28파일). **⓪-b 이동 후에 셌다.**

**총괄 002 [4] "재정리하다 보니 이것도 고쳐야겠다가 나오면 고치지 말고 목록에 적어라" + `[3.12]`(유형 2 변경은 사용자 확인)에 따라 아래는 미집행이다.** `rules.md`는 유형 2다.

### 유형 2 — `ux/rules.md` (사용자 확인 필요, 미집행)

| 행 | 지금 | 정본 | 형태 |
|---|---|---|---|
| :74 | "계약 영향 발견 시 **계약 변경 요청(6절)**으로 총괄 격상" | **`common/rules.md [6]`** | **★ QA `:14`와 같은 병이다.** 내 6절 = 접근성(WCAG). **열면 뭔가 있고 조용히 틀린다.** 계약 변경 요청 형식은 애초에 없다 |
| :88 | "매 응답 끝 사용자 브리핑 4줄(**templates 16**)" | **`templates [7]`** · "4줄"도 폐어 | 결번(`[16]` 없음) → 열면 "없음". **다만 오늘 `[15]`가 생겼다 — 다음에 하나 더 생기면 이것도 조용히 틀린다** |
| :86 | "결정은 U-xxx(1결정=1번호, 상태 라벨, **가이드 4절**)" | **`templates [6]`** | **조용히 틀린다.** 내 4절 = UI 컴포넌트 스펙 |
| :32 | "프론트 **CLAUDE.md** 5절" | **`frontend/rules.md`** (C-073 프론트 몫) | 프론트 개명 후 무효. **아직 안 옮겨졌으면 지금은 유효** |
| 절 번호 체계 | `1.`~`11.` 산문 | `templates [13]` 골격 = `[N.M]` + 명령형 | **손대지 않기를 권고한다.** 재번호하면 `decision-log`·`outbox` 인용이 영구히 어긋난다(C-064·C-065가 두 번 기각한 그 형태). **거짓이 아니라 모양만 다르다** |

**★ 위 3건은 전부 "자기 문서의 절 번호를 정본인 양 가리키는" 한 가지 뿌리다.** `rules.md`·`templates.md`가 대괄호 `[N]`을 쓰고 내 지침이 "N절"을 쓰는데, **둘 다 숫자라 구분이 안 된다.** 컨설턴트가 `_broadcast/001`에서 경고한 "실패 신호 없는 오조준"이 내 문서 안에 3개 있었다.

### 유형 4·산출물 — 집행 완료

| 무엇 | 처리 |
|---|---|
| `notes/handover.md`:44 — `docs/api-contract.md`·`erd.md`·`domain-spec.md` | **정정 완료** → `docs/spec/` (`_broadcast/001` 지시분. 유형 4라 `[3.12]` 미해당) |
| `design-guide` 참조 6곳(`ux-flows`·`accessibility`·`design-system`) | **정정 완료** — ⓪-b 개명의 직접 귀결 |
| `mockups/*.html`·`decision-log`·`outbox/**`의 `§` 표기 | **미정정 — 정상이다.** D-087 "앞으로만, `§` 교체만을 위한 개정 금지" |

### 안 걸린 것 — 부재 근거를 밝힌다

- **`[5.x]` 밀림 0건** — 호스트 Grep `\[5\.\d+\]` on `docs/ux` → **살아있는 문서에 히트 0.** 컨설턴트 `_broadcast/001` [2]가 디자인분으로 `ux/outbox/TOKEN/001:13`을 적었는데 **그 파일은 `design/outbox/TOKEN/001`(기획 것)이다.** 경로 오기이고, 설령 내 것이어도 outbox는 불변이라 미대상이다.
- **`status.md` 인용 0건 · `[8.11]`·`[8.13]`·`[8.15]`·`[8.17]` 인용 0건 · `templates [10]` 인용 0건** — 오늘 죽은 조항을 내 문서가 가리키지 않는다. 같은 Grep 실행.
- **`_archive/` 인용 0건.**

## 완료 기준 대비

- **ⓐ~ⓓ 4항 완료 보고 발신**: 충족 — 이 문서
- **전수 여부·탐색 방법 명시**: 충족 — ⓐ·ⓓ 각 항에 Grep 패턴·대상·건수 명기. **전수다**
- **신규 조항·지침·규약 0건**: 충족 — `rules.md` 내용 무변경(개명만), 신규 발번 0
- **`spec/` 3종·코드·Flyway 무변경**: 충족 — 쓰기 집합이 `docs/ux/` 전부다(`[3.2]`·`[7.10]` 병행 조건)
- **`mv`·`cp` 금지**: 충족 — bash는 `rm` 1회·md5 계산 1회

## 다음 단계 제안

- **② 배치를 기다리지 않는다.** ⓐ-1(프론트 회신) → ⓐ-2(게임 스킨 목업)로 이 응답 직후 진행한다. 총괄 002 [5]가 "목업은 계속해라"로 미차단을 명시했고, 프론트 009는 남의 레인을 내가 막고 있는 건이라 순서 배치를 기다릴 성질이 아니다.
- **총괄 추적표 정정 요청 1줄**: 디자인이 사용자 결정 큐에 건 항목 **3건 전건 해소**(원본 hex·`gold_force` 용도·U-012 이미지). **추적표에 남아 있으면 "디자인이 대기 중"이라는 거짓 신호다.**

회신: 불요 (이견 시에만)
신규 발번 ID: 없음 — 재정리는 결정이 아니라 실측이다. ⓒ 2건은 D-091 자율 범위라 U 발번 없이 닫고, 뒤집히면 그때 발번한다
