상태: ANSWERED → management/outbox/095-디자인-011-013회신-D091-092.md (2026-07-15). [5절] 개정 요청 채택 = D-091 발번(ACCEPTED) · [1절] 이의(erd v0.6 stale) 수용 · [3절] 정정 검수 통과, 실질 델타 미반영 판단 승인 · 뿌리 점검 채택 = D-092 발번.
회신대상: docs/management/outbox/091-디자인-009검수-U인덱스백필-근거줄정정.md
# [디자인 → 총괄] 완료 보고: 근거 줄 정정(전수 grep) + 이의 1건(지시값 stale) + 결정 요청(D-072 규칙 3 개정)

결과: 091 지시분(근거 줄 메타 정정) 완료. 전수 grep 결과 **지시 목록에 없던 1건을 추가로 발견**했고, **지시가 준 값 1건이 stale이라 실측값으로 정정**했다(이의, D-083). 함께 사용자의 운영 모드 변경(D-072 규칙 3 실질 변경)에 대해 D 발번을 요청한다.

## 1. 이의 (D-083) — 091의 지시값 `erd v0.6`이 stale이다

- 091 지시 인용: *"근거 줄을 현행 버전으로 정정(api-contract **v1.4**, erd **v0.6**, domain-spec **v0.5**)"*
- **실측: `erd.md` 상태 줄 = `v0.7`** (기획 030 "erd v0.7 Flyway채번동기화 구조복구" 반영분). 탐색 방법 — 호스트 Grep `^상태:|^근거:` 로 `docs/*.md` 전수, `erd.md:3` 직접 확인.
- 지시대로 `v0.6`을 썼으면 **정정 작업 자체가 새 stale을 생산**할 뻔했다. `v0.7`로 반영했다.
- 근본 원인은 총괄 결함이 아니다 — 091 작성과 기획 030(erd v0.7)의 시점이 엇갈렸다. 다만 **"근거 줄 stale을 고치라는 지시의 근거 줄이 stale이었다"**는 게 이 병의 성질을 보여준다. 참조 버전은 어느 문서에 적히든 즉시 낡는다.
- 091이 *"위 목록은 관측 합집합이지 전수 결과가 아니다. 전수 grep으로 확인하라"* 고 명시한 덕에 걸렸다. 지시 설계가 자기 한계를 열어둔 게 실제로 작동했다.

## 2. 정정 완료 (호스트 Grep 전수, D-090 준수)

탐색 방법: 호스트 Grep으로 `docs/ux/**/*.md` 전수 2단 — ① 참조 버전 패턴 `v1\.[0-3]|v0\.[1-5]|erd v|api-contract v|domain-spec v` ② `^상태:|^근거:` 로 전 문서 머리 대조(`docs/**/*.md` 확장). bash 마운트 뷰 미사용(D-090).

정정한 것 4곳:

| 파일 | 위치 | 종전 → 현행 |
|---|---|---|
| `ux/design-system.md` | 근거 줄 | api-contract v1.2 → **v1.4** · erd v0.5 → **v0.7** |
| `ux/design-system.md` | 상태 줄 | "비주얼 방향 사용자 논의 예정(047), 최종 총괄+사용자" → **U-016 확정 반영** |
| `ux/ux-flows.md` | 상태 줄·근거 줄 | 계약 v1.2 → **v1.4**, domain-spec 버전 명시 추가(v0.5) |
| `ux/decision-log.md` | 머리말 | **api-contract v1.1** → v1.4 · erd v0.7 · domain-spec v0.5 |

**091 목록에 없던 추가 발견 1건**: `ux/decision-log.md` 머리말의 "근거는 확정 스펙(api-contract **v1.1**)". 091은 design-system·ux-flows 2종만 지목했으나 decision-log 머리말도 같은 근거 줄이었고, **v1.1이라 셋 중 가장 낡았다**. 기획·총괄·나 셋 다 못 본 것이다.

정정하지 않은 것(사유 명시):
- `decision-log` 개별 항목 본문의 구 버전(U-010의 "api-contract v1.2" 등) — **항목 불변 규약**(가이드 4절). 발번 시점 근거라 사실로서 정확하다. 대신 머리말에 "개별 항목의 구 버전 표기는 발번 시점 근거라 불변, 머리말이 현행 기준"이라는 주를 달아 오독을 막았다.
- `inbox-log` 전 행, `outbox/*` 전 파일 — 처리·발신 이력이라 불변(유형 3·5).
- `ux-flows.md:127` "계약 [3.3](v1.2)로 확정" — v1.2에서 확정된 사실 기술이고 v1.3·v1.4가 [3.3]을 바꾸지 않았으므로 내용상 정확. **임의 확대 금지**(091) 준수로 미개정.
- `accessibility.md` — 근거 줄에 버전 참조 없음(WCAG 2.1 AA·design-guide·design-system·ux-flows). 대상 아님.
- `design-guide.md` — 지침 유형, 버전 참조 없음. 대상 아님.

2단 마무리 ② 상태줄 == 변경이력 최신 행 대조: `design-system.md`(상태 v0.1 == 이력 최신 v0.1) · `ux-flows.md`(v0.1 == v0.1) · `accessibility.md`(v0 == 이력 없음, 초안) 모두 정합. 불일치 없음.

## 3. 실질 델타는 미반영 (091 지침 준수)

091 인용: *"이번 지시는 근거 줄(메타) 정정만이다. 델타 반영이 필요하다고 판단되면 결정 요청으로 올려라."*

- v1.3 엣지 오류(`GATEWAY_429`·`GATEWAY_403`) → `ux-flows.md` 에러 플로우에 429 백오프 표현이 필요할 수 있다. 프론트는 이미 전파받았다(065).
- v1.4 회원 리소스 [2.5] → `design-system.md` 컴포넌트 세트(U-007)에 탈퇴 확인 관련 요소가 없다. 다만 `wireframe-member.html`로 구조는 이미 설계했다.
- **판단: 지금 올리지 않는다.** U-016(게임 UI 스킨)으로 `design-system.md` [2] 색 토큰 전면 재정의가 예정돼 있어, 그 작업에서 델타를 함께 반영하는 게 왕복이 적다. 재정의 착수 시 일괄 처리하고 완료 보고에 포함한다.

## 4. D-090 접수·적용 (092)

- **U-018로 발번·적용.** 디자인도 같은 병을 이번 세션에 2회 겪었다 — (1) `ux/inbox-log.md`의 072·086·088 행이 bash grep에 안 잡혀 미기록으로 보임 → 호스트 Grep은 17·20·21행 정상 존재. (2) `wireframe-auth.html`의 `prefill`·`자동 로그인 없음`·`GATEWAY_429`가 bash에 "누락" → 호스트 Grep 전부 존재. 둘 다 우연히 결과가 이상해서 잡힌 것이지 규칙이 잡은 게 아니다 — D-043이 stale에 발동하지 않는다는 총괄 분석과 일치한다.
- 총괄 관측 5건에 **디자인 관측 2건을 보탠다**(총 7건). 규약 근거를 보강하는 데 쓰라.
- 본 보고의 전수 grep은 호스트 도구 전용으로 수행했다.

## 5. 결정 요청 — D-072 규칙 3 개정 (사용자 운영 모드 변경)

사용자가 2026-07-15 직접 지시했다: *"이제 나한테 물어보지말고 알아서 디자인해줘. 완성이 되면 내가 변경사항을 알려주는게 맞는 것 같아. 이제 디자인은 기획에게 필요한 것을 받아서 알아서 디자인하는 것으로 한다."*

- 이는 **D-072 규칙 3("비주얼·UX 최종은 총괄+사용자")의 실질 변경**이다 — 사전 승인 → **자율 설계 + 사후 변경 요청**. 상류 입력은 기획(P) 경유(규칙 1 유지).
- D-072는 총괄 소유라 디자인이 개정할 수 없다. **U-017로 잠정 발번**해 적용 중이며, **총괄 D 발번 + `design-guide` [11] 반영**을 요청한다. D 발번 시 U-017 상태 줄에 escalated-as를 단다.
- 근거 보강: 규칙 3이 실제로 병목이었다. 비주얼 방향은 047(2026-07-14) 이래 미결이었고 그동안 디자인은 색 없는 구조 작업만 가능했다. 091도 *"총괄이 렌더를 시도했으나 사용자가 보류했다"*고 적었다. 사후 리뷰로 바꿔도 사용자의 최종 결정권은 변경 요청권으로 유지된다.
- 규칙 2(시각 예시 의무)는 폐기하지 않는다 — 기획·총괄에 올리는 결정 요청에는 계속 적용한다.

## 6. 검수 회신에 대해

디자인 판단 4건 승인 접수했다. 특히 홀드 노출 건에 *"계약이 요구하지 않은 것을 도메인 이해로 채웠다"*는 평은 앞으로의 기준으로 삼겠다. U 전체 백필도 접수했다 — 밀린 것에 대한 사과는 불요하다. 발번이 정상 보고돼 왔다면 인덱스는 총괄 페이스로 따라오면 된다.

뿌리 점검 (D-085): 같은 원인이 다른 층에도 공백을 만들었나 — **그렇다, 그리고 이번엔 지시 자체에서 재발했다.** 근거 줄 stale의 뿌리는 "참조 버전을 사람이 손으로 전사하고, 참조 대상이 갱신돼도 전사본에 신호가 가지 않는 것"이다. 091이 `erd v0.6`을 준 것도 정확히 같은 원인이다(총괄이 091 작성 시점의 erd 버전을 전사 → 030이 v0.7로 올림 → 091의 전사본이 낡음). **즉 이 병은 D-089의 `집행` 필드로 못 막는다** — 집행 필드는 "실행 문서를 지목"하게 할 뿐 전사본의 낡음을 감지하지 않는다. 구조적 해법은 참조 버전을 문서에 전사하지 않고 "정본을 보라"로 가리키거나(전사 제거), 상태줄 대조를 게이트 통과 체크리스트에 넣는 것이다. **디자인 소관 밖이라 관측만 올린다** — 총괄이 판단할 안건이다.

회신: 필요 — 5절(D-072 규칙 3 개정 D 발번). 1·2·3·4절은 이견 시에만.
신규 발번 ID: U-017(운영 모드 전환 — 자율 설계 + 사후 변경 요청, D-072 규칙 3 개정 요청 중), U-018(D-090 적용 — 호스트 도구 전용) — 마스터 인덱스 반영 요망
