# 보안 handover

성격: 휘발성 맥락 — 파일에 있는 내용은 경로 참조만. 이력 없음(덮어씀).
갱신: 2026-07-16

## 진행 중

- 없다. **내일 첫 수: 아래 「대기 중」 C-082 잔여 5건.**

## 대기 중

- **member 보안 구현 리뷰(D-097 ④ = `_broadcast/004` 1-3)** — 선행 = 1-2(백엔드 코드) ←
  1-1(디자인 목업 계약 델타). **잔여 auth 이월 4건**(B-016·017·018 + m3)이 여기 붙는다.
  **025는 QA 선행이라 먼저 뺐다 — 완료**(`outbox/GATEWAY/001`).
- `outbox/REFORM/003` — 총괄 회신(불요). **`rules.md:12` `[3.12]` 1건만 사용자 확인 대기.**
- **SEC-015 배정** — 백엔드. 비차단이라 급하지 않다(실배포 전이면 충분).

### ★ C-082 잔여 5건 — 내일 첫 수 (2026-07-16 퇴근 시점 스캔분, 미착수)

**컨설턴트 `_broadcast/008` [3]이 *"보안은 이미 새 형식이다"*로 면제했는데 실측이 불완전했다.**
그가 `rules.md:13`·`:42` 2행만 봤다. **그의 [6](*"자기 문서에서 직접 돌려봐라"*)을 이행해 나온 것:**

| 위치 | 실물 | 정정안 | 유형 |
|---|---|---|---|
| `rules.md:19` | `위협 모델(3절 형식)` | `[3]`(자기 문서) | **유형 2 → `[3.12]`** |
| `rules.md:20` | `체크리스트(4절 기반)` | `[4]`(자기 문서) | **유형 2 → `[3.12]`** |
| `rules.md:48` | `위 3절 축 1~4` | `[3]`(자기 문서) | **유형 2 → `[3.12]`** |
| `threat-model.md:3` · `:71` | `security/rules.md 3절` · `CLAUDE.md 4절` | `rules [3]` · `CLAUDE.md [4]` | 산출물 — **자율** |
| `checklist.md:1` · `:140` · `:150` | `security/rules.md 4절` · `CLAUDE.md 4절` · `계약 §2` | `rules [4]` · `CLAUDE.md [4]` · `api-contract [2]` | 산출물 — **자율** |

- **★ `threat-model.md:3`·`checklist.md:1`은 내가 오늘 만들었다.** ⓪-b에서 `security-guide N절` →
  `security/rules.md N절`로 고치며 **파일명만 바꾸고 `N절` 형식을 그대로 남겼다.**
  C-082가 그 뒤에 왔으니 위반은 아니나 — **고치는 김에 형식을 안 본 것이다.**
- **탐색**: 호스트 Grep `\d+절|§` + `(templates|rules|CLAUDE\.md|…) +\d+\b` on `docs/security`,
  `outbox/**` 제외(`[5.18]` 불변). **009 [1]의 4형태 쿼리 둘 다 돌렸다.**
- **`findings.md`의 `§4.4`·`§2` 다수(20여 건)는 계약 조문 참조다.** 형식만 보면 대상이나
  **`api-contract`가 자기 문서 안에서 `§`를 쓰는지 확인 안 했다** — 확인 후 판단. `spec/`은 기획 소유고
  컨설턴트도 009 [6]에서 *"`spec/` 3종은 안 셌다"*로 남겼다. **여기 손대기 전에 그것부터 봐라.**
- **컨설턴트에 회부하지 않았다** — 자기 것은 자기가 고친다. **다만 그의 [3] 면제 실측이 2행 기준이라
  불완전했다는 사실은 남긴다** — 백엔드가 009에서 지적한 *"[3]만 읽었으면 멈췄다"*와 같은 형태다.

## 다음 세션이 알아야 할 판단

- **★ 「게이트 2 = 도메인 전체 완료 후」는 죽었다.** `rules [7.4]` 삭제(C-074) + D-097 →
  **보안은 도메인마다 ④**(백엔드 코드 후·QA 앞). **내 handover가 그 조항을 근거로 "게이트 2 대기"를
  세 세션 들고 있었다.** `checklist.md` [게이트 2] 절이 현행이다. `rules.md:12`는 아직 옛 문장이다.
- **★ SEC-013은 내 오탐이었고 발신까지 갔다**(철회 완료, `findings.md`에 사유·원문 보존).
  `RateLimit429IntegrationTest`가 커밋 `4c94471`에 **처음부터 있었다.** 원인:
  **Glob `**/*.java`가 `Showing 100 of 111`로 잘렸고 내가 그걸 전수로 읽었다.**
  **에러가 아니었다 — 잘렸다고 화면에 적혀 있었다.**
  - **`[9.1]`이 오늘 Glob을 뺐다**(C-075, 현행 = Read·Grep·Edit). **내가 그 개정의 실물 근거다.**
  - **재검증은 Grep 한 번이었고 즉시 나왔다. 처음부터 Grep이었으면 안 틀렸다.**
  - **교훈은 `checklist.md` [도구 규율] 부재 주장 규율 4항에 넣었다. 게이트 2 전에 읽어라.**
    핵심은 3번이다 — **목록에 잘림 표시가 있으면 전수가 아니다.**
- **긍정 근거 하나가 옆의 부재 추론을 정당화하지 못한다.** `GatewayApplicationTests`를 Read한 건
  긍정이었지만 거기서 "그러므로 다른 테스트는 없다"로 건너뛴 순간 부재 주장이었다.
  **내가 세운 방어("부정을 긍정으로 바꿔 세라")를 내가 이 형태로 우회했다.**
- **같은 계열 5번째다**(게이트 1 오탐 2건 · 초안 2건 · SEC-013). **넷은 발신 전에 잡혔고 하나는 갔다.**
  **문장을 쓰기 전에 탐색을 돌려라. 도구가 아니라 순서가 원인이다.**
- **`[9.3]`이 조용히 틀리는 자리다.** 번호가 살아있고 **내용이 반전됐다** — 현행 = *"되돌릴 수 없는
  것을 남기는 실행만 사용자 확인. **파일 조작은 묻지 않는다**"*. **`mv`·`cp` 금지는 없어졌다.**
  `[9.2]`·`[9.4]`·`[9.25]`는 결번이라 열면 걸린다.
- **인용보다 복사가 더 조용하다.** `rules.md:12`가 `[7.4]` 내용을 번호 없이 복사해서 `[N.M]` 대조에
  안 걸렸다. **출근 시 인용만 세지 말고 「내 지침이 남의 조항을 문장으로 갖고 있는 자리」도 봐라.**
- **★ 「너는 대상 아니다」를 받으면 그래도 자기 문서를 돌려라.** 컨설턴트가 `_broadcast/008` [3]에서
  보안을 면제했는데 **2행만 보고 내린 판정이었고 5건이 남아 있었다**(위 대기 큐). 그의 [6]이
  *"직접 돌려봐라"*로 자기 [3]을 살렸다. **면제는 남이 세어준 것이고, 남의 쿼리엔 사각이 있다.**
- **`decision-log.md:4`(`collaboration-guide.md 4절`)는 고치지 않는다 — 확정됐다.**
  컨설턴트 `_broadcast/009` [4] 판정: *"`[4.2]`는 「항목」이 아니라 「결정 로그」에 걸린다 —
  문서 전체가 유형 3이고 머리말도 그 안이다. 그 문서는 실재한 적이 있다. 이력이다."*
  **REFORM/001 ⓓ에서 "애매하다"로 남겼던 자리가 닫혔다.** 다시 열지 마라.
- **★ 범위가 다르면 같은 코드도 다시 봐라.** SEC-015(공유비밀 비상수시간 비교)는 **SEC-005 때
  같은 파일 같은 행을 읽고도 안 보였다.** 그때 질문은 *"차단이 존재하는가"*(O)였고 *"차단이 어떻게
  비교하는가"*는 안 물었다. **이월 리뷰를 "이미 본 코드"라고 건너뛰지 마라** — B-016·017·018·m3에
  같은 원칙을 적용한다.
- **★ 인접한 것을 같은 것으로 보면 사이가 빈다.** 총괄이 「SEC-005 = 025」로 배치했는데 절반만
  맞았다 — SEC-005는 `RateLimitResponseGlobalFilter`를 읽지도 않았다. **겹친다 ≠ 같다.**
  오늘 내가 같은 형태로 두 번 틀렸다(SEC-013·SEC-015).
- **백엔드 코드를 의심 기본값으로 읽지 마라.** 구현이 계약보다 촘촘한 자리가 반복해서 나온다
  (placeholder fail-fast 함정 방어 · `.set()` 위조 헤더 선제거 · 429 회귀 테스트 ·
  **429 직렬화 실패 폴백이 fail-safe** — 상태 유지·본문만 생략). **내 산출 4건 중 순수 발견은
  SEC-015 하나다** — SEC-013 오탐 · SEC-014는 B-027 후속에 이미 있었다.
- **SEC-005 FIXED는 조건부다** — SEC-014(LB 뒤 단일 버킷)가 실배포에서 발현하면 rate limit이
  "있는데 안 듣는" 상태가 되고 라벨이 거짓이 된다. **지금 못 고친다**(배포 토폴로지 미확정 ·
  XFF 무조건 신뢰는 버킷 무한 분할로 더 나쁘다). **SEC-007 FIXED도 여기 걸린다.**
- **080/085 왕복 결론**: 인용 검증(D-082)은 **무조건 계속**, 부정 결과 보고(D-086)는 **교차검증 후**.
  **오탐이 두려워 인용 검증을 멈추는 쪽으로 읽지 마라.**
- 착수 지시를 받으면 **`근거(인용)` 필드부터 확인한다**(D-082). 없으면 이의 대상이고 이의는 의무다(D-083).
- **코드 경로는 `backend/` 아래다**(D-098). `docs/`·`config/`는 루트 그대로 — 내 `docs/spec/…` 참조는 불변.

## 막힌 것

- 없다.
