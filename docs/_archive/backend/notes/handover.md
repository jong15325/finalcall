# 백엔드 handover

성격: 휘발성 맥락 — 파일에 있는 내용은 경로 참조만. 이력 없음(덮어씀).
갱신: 2026-07-17 (퇴근 — 1-2 CC 프롬프트까지. **투입은 안 했다**)

## ★ 내일 첫 3수 (순서 고정)
1. **출근 스캔부터** — 아래 ★★ 참조. **지목받아도 전역 Grep을 먼저 돌린다.**
2. **`references/claude-code-kickoff.md`에 「표기 규약」 성격 줄이 있는지 확인**(컨설턴트 `_broadcast/009` [5],
   프론트 뿌리 지적). **프론트 kickoff:53이 CC에게 `§`만 금지하고 `N절`은 안 막는다 — 생산자를 안 고쳤다는 것.**
   내 kickoff에도 같은 게 있으면 C-082 문안으로 갱신(`[3.12]` 표에 얹는다). **어제 미확인분이다.**
3. **1-2 CC 투입** — `outbox/MEMBER/003` + 킥오프 전문. 아래 「진행 중」 참조.

## ★★ "재설계"는 없었다 — 재정리였다(D-093). 폐기 위험 0. 이 절의 옛 경고는 죽었다.

**전 세션의 내 경고 *"미구현 유닛을 지금 만들면 그대로 폐기될 수 있다"*는 틀렸다.**
총괄이 사용자에게 직접 물어 확인했다(`mgmt/_broadcast/002` [1] — 총괄이 자기 오독을 자인).

- **D-093**: *"「재설계」의 실체는 재정리다. 지금까지의 설계·구현을 전건 동결한다."*
- **동결선**(`_broadcast/002` [2]): `spec/` 3종(v0.5·v0.7·v1.4) · 코드(auth·gateway·024·025) ·
  Flyway V1~V4 · D-001~092 · `rules.md`·`templates.md` = **전부 무변경. 손대지 마라.**
- **순서**: ① 파트별 재정리(**완료 — `outbox/REFORM/001`**) → ② 기능 재진입(**총괄이 배치. 대기 중**).
- **"재정리하다 이것도 고쳐야겠다"가 나오면 고치지 말고 목록에 적는다.** 그게 산출물이다.

## 진행 중

- **1-2 착수 — CC 프롬프트 작성 완료. 다음 한 수 = CC 투입**(사용자 지시로).
  프롬프트: **`outbox/MEMBER/003`**(member 3유닛 + `MemberErrorCode` + 이슈1 (b) 정합 + `revokeAll` 한 묶음).
  **킥오프 전문**(`references/claude-code-kickoff.md`) + 이 경로를 붙여 새 CC 세션에 투입한다(D-088).
  반환 = `notes/cc-reports/034-member-3유닛-프로필-수정-탈퇴.md`.
- **CC 반환 후 내가 할 것 3건**(잊으면 유닛이 미완이다):
  1. **B-031 발번**(이슈1 (b)) — **`집행:` = 이 커밋**(`[4.13]`). 총괄이 이 유예를 명시 승인했다
  2. **B-018 상태 갱신** — (b)로 부분 해소. **잔여 레이스(커밋↔폐기 창)를 후속에 남긴다.** rotate 선검증은 미착수
  3. **완료 보고에 3건 필수 적재**: MEMBER_002 구조만 둔 방식(총괄 072 의무) · revokeAll 잔여 레이스 · nickname 검증 = signup 동일
- **델타 2 총괄 판정 대기** — `blockedBy`를 409에 실으려면 **envelope 신설 = `rules [6]` = D-093 동결과 충돌**.
  `outbox/MEMBER/002`로 판정 요청. **1-2를 안 막는다** — 늦게 오면 필드 추가 커밋이 하나 는다.

## ★ 1-2 프롬프트를 쓰며 실측한 것 3건 (프롬프트에 반영 완료 — 여기 있는 건 왜인지다)
1. **`RefreshTokenStore`에 일괄 폐기가 없었다.** `revoke(presentedToken, ownerUserId)` 단일 세션뿐인데
   계약 `[2.5]`는 *"refresh 세션 **전부** 폐기"*를 요구한다. → `revokeAll(userId)` 신설 지시.
   **키 = `auth:refresh:{userId}:{sessionId}` → `SCAN` 필수, `KEYS` 금지**(Redis 단일 스레드 블로킹).
2. **`PATCH /me` 자기 닉네임 = 409 버그 자리.** `existsByNicknameAndIsDeletedFalse`를 그냥 걸면
   **자기 닉네임이 이미 UK에 있어 "내 닉네임으로 수정"이 409**가 된다. 계약에 안 적혀 있다. 테스트로 못박았다.
3. **B-018을 (b)로 부분 해소만 한다.** 탈퇴가 세션을 일괄 폐기하지만 **커밋↔폐기 사이 rotate 창이 남는다.**
   닫으려면 rotate 선검증(auth 코드)이라 범위 밖. **"해소"라고 쓰지 마라 — "창을 좁혔다"다.**

## ★★ 출근 스캔 — 세 번 연속 뭔가 나왔다. 지목은 스캔이 아니다
**07-16·07-17 두 세션은 사용자가 지목한 파일만 열고 `[8.7]`-3 전역 스캔을 아예 건너뛰었다.**
- 그 결과 **`mgmt/GATEWAY/001`(025 검수·내 앞 직접 발신)을 하루 놓쳤다.**
- **07-17 지목 3건 → 스캔하니 실제 6건.** `consulting/_broadcast/007`·`008` + **`ux/MEMBER/001`(내 1-2 선행!)**.
- **퇴근 스캔에서 또 2건**: `consulting/_broadcast/009`(회신 필요) · **`_broadcast/005`(C-072)**.
  005는 **내용을 총괄 `_broadcast/002` [5-b]로 이미 흡수했는데 파일 기준으로 미기재였다** —
  `[5.23]`이 "**파일**이 로그에 없으면 신규"라 내용을 알아도 미처리다. **내용 흡수 ≠ 수신 처리.**
- **처방**: 사용자가 파일을 지목해도 **`[8.7]`-3을 먼저 돌린다.**
  탐색 = 호스트 Grep `^# \[.*→ (백엔드|전 역할)` on `docs`(`glob=!_archive/**`) → `inbox-log.md`와 **파일명 대조**.
  **"지목받았으니 스캔은 됐겠지"가 디자인 델타 1과 같은 형태다**(*"막힘은 전염된다고 가정하기 쉽고, 그게 틀린다"*).

## ★ 1-2를 받으면 — 지시서에 이 7건을 반드시 실어라
CC는 `decision-log.md`를 통독하지 않는다. 지시서가 지목한 것만 읽는다.
1. **이슈1 (b) 정합** + **B-031 발번**(`[4.13]` — 같은 커밋. **미루면 판정이 흔적만 남고 근거가 없어진다**)
2. **B-018** refresh 회전 vs 탈퇴 계정 순서
3. **D-080** 잔액 잔존 무관 탈퇴 허용 · `balanceForfeitAcknowledged` 필수(잔액 0이어도)
4. **MEMBER_002** 차단 조건 = **검사 대상 공집합**(bid/auction/order 미구현) → 구조만. **완료 보고에 방식 명시 의무**
5. **`MemberErrorCode`** 신설 — 계약 `[5]` MEMBER_001(닉네임 중복 409)·MEMBER_002(409)
6. **nickname 검증 = `@NotBlank` + `@Size(max=30)`이 전부다**(signup 실측). **최소길이·문자셋·공백 규칙 없다** —
   `PATCH /me`가 nickname을 처음 "반복 수정되는 값"으로 만든다. 기획 회신이 오면 그걸 따르고, **없으면 signup과
   동일하게 두고 완료 보고에 명시**한다. **임의로 새 규칙을 만들지 마라**(가입과 수정이 갈리면 그게 더 큰 문제)
7. **델타 2(`blockedBy`) 총괄 판정** — 오면 반영, 안 오면 필드 없이 간다(`outbox/MEMBER/002`)
- **1-1 산출물 = `ux/outbox/MEMBER/001` + `docs/ux/mockups/member.html`. 먼저 읽어라.**
- **`@WebMvcTest` 템플릿은 024에 있다**(`MemberControllerTest` — SecurityConfig + EntryPoint/DeniedHandler
  임포트 + `GatewayInternalProperties(enforced=false)` 실빈 + `@MockBean TokenProvider` + `.with(user("42"))`).

## ★ 이슈1 — 판정 끝났다. 다시 열지 마라. 단 발번이 남았다.

**판정: (b) 컨트롤러가 `Authentication`을 받아 서비스에 `userId`를 인자로 넘긴다.**
근거·기각 사유 전문은 `outbox/REFORM/001` ⓑ-1. 요지 3줄:
1. `SecurityContextHolder`는 infra 성격 → domain이 직접 읽으면 `CLAUDE.md` 섹션 4 의존 방향 훼손
2. 마감·정산 **배치**가 로드맵 확정(066) → 서비스가 HTTP 컨텍스트에 묶이면 배치 재사용 불가
3. 로그아웃 `AuthService`가 이미 (b) — 신규 컨벤션이 아니라 승계

**★ 발번(B-031)을 일부러 안 했다.** `[4.13]`(집행은 같은 커밋) 때문이다 — 집행처(024 `MemberService` 정합)가
**동결선 안**이라 지금 반영이 불가능하고, 지금 발번하면 `집행:`이 공란·거짓이 된다(= D-089가 겨눈 형태).
→ **② 재진입 시 코드 반영과 같은 커밋에서 B-031 발번한다. 잊으면 판정이 흔적만 남고 근거가 없어진다.**

## ★ 커밋 분리 실패 — 처리 끝. 다시 조사하지 마라
**429 델타 4파일이 024 커밋 `4c94471`에 섞였다**(`[9.26]` atomic 위반, `4c94471` = 10파일).
**`GATEWAY/001` [2]·`MEMBER/001` 이슈1 양쪽에 명시했고 되돌리지 않는다.** 코드 전건 HEAD 실재·빌드 그린.
- 원인: 내가 2커밋 분리를 제안했으나 실행 시점에 병합됐다. **CC 보고의 커밋 제안이 그대로 실행된다고
  가정하지 마라. 발신 전 `git show --stat`으로 실측한다.**
- **`[9.8]`이 바뀌었다** — 이제 **사용자가 지시하면 내가 커밋을 대행한다**(C-079). 대행하는 날엔
  **내가 분리 단위를 지킬 수 있다.** 이 결함의 재발 경로가 하나 줄었다.

## ★ 내 파일 위치가 바뀌었다 (⓪-b, C-073) + 코드는 `backend/` 아래다 (D-098)
**코드 경로**: `backend/src/**` · `backend/gateway/**` · **`backend/src/main/resources/db/migration/`**(Flyway).
**`docs/`·`config/`는 루트 그대로** — 내 문서의 `docs/spec/…` 참조는 안 바뀐다.
- **★ V5+ 채번할 때 여기서 물린다**: 채번 확인은 **`backend/src/main/resources/db/migration/` 디렉터리 목록**이다
  (V4까지 소비). **`erd [6]`는 호스트 Read로만** — bash는 [6]을 절단해 통째로 안 보인다.
- `cc-reports/024`·`025`, `outbox/**`는 `src/…`로 적혀 있다(작성 시점이 D-098 이전). **이력이라 안 고친다.**
  `references/spring-skeleton-prompts.md`:591의 `src/main/resources/db/migration`은 **범용 재사용 프롬프트라
  그 문맥에선 맞다** — FinalCall에 적용할 때만 `backend/` 접두어를 붙인다.
`notes/` 지침 3건 → **`references/`**(유형 2 · 인용 가능). `notes/`엔 이제 `handover.md`·`cc-reports/`만 있다.
- `references/claude-code-kickoff.md` — **CC 킥오프 전문. 새 CC 세션마다 붙여넣는다**(마지막 줄 경로만 교체)
- `references/spring-skeleton-prompts.md` — **부록 C(실무 함정 15건)가 bid 분산락에서 정면으로 걸린다.**
  Stage 0~G 실행부는 죽었다(전건 커밋 완료). 살린 이유는 `REFORM/001` ⓓ-1
- `references/onrace-reference.md` — **C-1 Lua 원자 스크립트가 bid 최고가 갱신의 차용 후보**

## ★ 025 발신 중 실측한 사실 — 024 보고에 그대로 걸린다
**429 델타 4파일이 024 커밋 `4c94471`에 섞였다**(`[9.26]` atomic 위반). 실측 `git show --stat 4c94471`
= gateway 4파일 + member 6파일 = 10파일. 403만 `2ad3e1b`로 분리됨.
- **되돌리지 않는다**(025 보고에 사실·미복구 사유 명시 완료). **024 보고에서 이 커밋을 인용할 때
  "member 6파일"이 아니라 "10파일 중 member 6파일"로 써라** — 안 그러면 내 보고가 이력과 어긋난다.
- 원인: 내가 2커밋 분리를 제안했으나 실행 시점(사용자, `[9.8]`)에 병합됐다. **CC 보고의 커밋 제안이
  그대로 실행된다고 가정하지 마라. 발신 전 `git show --stat`으로 실측한다.**

## 대기 중
- **`[3.12]` 승인** ← 사용자. **내 몫이 두 표로 갈렸다 — 둘 다 봐야 한다**:
  - 총괄 `_broadcast/003` [5] #7~10 — `CLAUDE.md`:4·:36 · `spring-skeleton-prompts`:14 · `onrace-reference`:1
  - 컨설턴트 `_broadcast/009` [7] **E~H** — `references/claude-code-kickoff.md`:16·:25·:26·:29
    (**E(`:26`)만 급함** — CC가 매 세션 첫 번째로 읽고 `(6절)`이 `CLAUDE.md [6]`으로 오독된다)
  - **컨설턴트가 "중복 아니고 신규다"로 판정했다** — 내 "중복 가능" 추정이 틀렸다. 표 둘은 서로 다른 파일이다.
  - **승인 오면 E~H 집행이 내 회신 의무**(`_broadcast/009` 회신 필요=백엔드).
- **델타 2(`blockedBy`) 총괄 판정** ← 총괄. `outbox/MEMBER/002`. **1-2를 안 막는다.**
- **1-1은 왔다** — `ux/outbox/MEMBER/001`. 선행 해소됨.
- 체계 축은 대기 모드(C-070).

## ★★ [9] 절이 뒤집혔다 (C-075) — 어제 지식으로 일하면 조용히 틀린다
**`rules.md`는 94개다**(102 → 98 C-073 → 97 C-074 → **94 C-075**). **호스트 Grep 실측 확인.**

| 어제 | 오늘 | 뭐가 달라지나 |
|---|---|---|
| `[9.2]` bash 마운트 뷰 판단 금지 | **결번** — 내용은 **`[9.1]` 본문**에 흡수 | 규칙은 그대로. **참조만 죽었다**(열면 "없음" = 실패 신호 있음) |
| `[9.4]` **`mv`·`cp` 금지** | **결번** | **파일 이동에 `mv`를 써도 된다.** C-075 = *"`mv` 금지 실측 반증" |
| `[9.25]` 마이그레이션 bash 금지 | **결번** — `[9.3]`이 흡수 | DB 스키마는 여전히 막힌다. 조항만 합쳐졌다 |
| `[9.3]` bash 허용/금지 표 | **번호 생존 · 내용 통째 교체** | **★ 조용히 틀리는 자리.** 이제 *"되돌릴 수 없는 것(DB 스키마·외부 시스템·원격 저장소)은 사용자에게 묻고 지시받아 한다. **파일 조작은 묻지 않는다**"* |
| `[9.1]` 호스트 도구 = Read·Grep·**Glob**·Edit | **Glob 삭제** — Read·Grep·Edit | **`Glob`은 더 이상 정본 도구가 아니다.** 내 "거짓 0건" 관측 6회가 반영된 것으로 읽힌다 |
| `[9.8]` 사용자 단일 실행 | **"사용자가 지시하면 대행한다"** (C-079) | **커밋을 내가 실행할 수 있다 — 사용자 지시가 있을 때만** |
| `[7.4]` 보안 게이트 순서 | **결번**(C-074) | **게이트2 대기가 풀렸다** — 보안이 member와 병행(D-097) |
| `[3.12]` | **"사용자가 요청한 범위는 면제"** 추가(C-079) | 사용자가 시킨 범위는 표 없이 진행. **범위를 넘으면 확인받는다** |

**→ ⓪-b에서 내가 쓴 Read→Write→삭제는 이제 과잉이다.** 1206행 파일 하나에 비용이 컸다.
**다음 이동은 `mv`로 한다.** 단 `[9.1]`은 살아 있다 — **이동 결과의 "확인"은 여전히 호스트 도구로 한다.**

## ★ 대기 큐에 뜨는 오탐 1건 — 조사하지 마라 (C-063 확정)
`backend/outbox/014-erd섹션6-Flyway매핑정정요청.md`가 **출근 때마다 영구히 대기 큐에 뜬다. 무시한다.**
- 실질 해소됨(mgmt/044 방식 확정 → erd v0.7 [6] 동기화, design/030·mgmt/093).
- 뜨는 이유: 044가 정보 공유라 `회신대상:` 미기재 + `[5.18]`로 소급 불가. C-063은 앞으로만 적용.
- **결함이 아니라 확정된 잔여다.** 컨설턴트가 "무시하고 handover에 한 줄 남겨라"라고 명시했다.
  **다음 세션이 또 조사하면 그 시간이 통째로 낭비다.**

## ★ 내 참조 스윕 쿼리가 반쪽이었다 (C-082, 컨설턴트 `_broadcast/009` [1])
**참조 형태가 넷이고 쿼리마다 사각이 반대다. 내가 쓴 `\d+절|§`는 둘을 못 잡는다.**

| 형태 | 예 | `\d+절\|§`가 잡나 |
|---|---|---|
| `templates 15` · `templates 16` (**구분자 없음**) | qa`:22`·`:55` · ux`:101` | **못 잡는다** |
| `templates §18` | qa`:72` | 잡는다 |
| `N절`(문서명 있음) | 내 kickoff `:16`·`:25`·`:29` | 잡는다 |
| `(N절)`(**문서명 없음**) | 내 kickoff `:26` · ux`:74` | 잡는다 — **가장 위험** |

- **다음에 스윕할 땐 `(<문서명>) +\d+\b`도 같이 돌려라.** 내 4건은 `\d+절`로 잡힌 것들뿐이다.
- **컨설턴트가 "다섯 번째 형태가 없다는 증거가 없다"고 밝혔다**(`_broadcast/009` [6]) — 넷을 찾은 경로가
  전부 우연(각 파트가 자기 파일에서 발견)이다. **형태 목록은 미완결이다.**

## ★ 도구가 조용히 거짓말한다 — 3계열
1. **bash 마운트 절단**(`[9.1]`) — "낡음"이 아니라 **행 중간 절단**이고 **`spec/` 3파일만**
   물려 있다(정상 파일이 섞여 있어 bash 안에서는 판별 불가). `erd.md` = bash 341줄 / 호스트 409줄,
   **[5] 인덱스 표 + [6] Flyway 매핑 전체가 없다.** → **V5 채번을 bash로 확인하면 erd v0.6 구조 오류가
   3회차다.** 호스트 Read/Grep만. CC 킥오프에 박아뒀다.
2. **호스트 Grep gitignore** — **닫혔다**(C-067: `.gitignore` 2줄 삭제 + `[9.27]`). 내 재현 케이스 통과 실측.
   **단 메커니즘 미상** — `path=docs`에서 철회 전에도 잡힌 이유를 아무도 모른다. `[9.27]`은 docs만 막는다.
3. **미확인 2건**(컨설턴트가 C-070으로 버림, 실피해 이력 있음): **Glob이 실재 디렉터리에 두 번 0건** ·
   **Grep `glob` 필터가 조용히 안 먹음.**
- **→ 습관(조항 아님)**: **"없다"를 도구 1회로 단정하지 않는다.** 부재가 결론에 영향을 주면 스코프를
  바꿔 재확인하거나 `Read`로 직접 연다.

## 내 반복 결함 — 3번 물렸다
**부재·수량을 근거로 삼으며 탐색 방법을 검증하지 않는다**(D-086/`[5.34]`가 겨눈 유형).
1. B-028 참조자 수 5→7 오류 2. `UserRepository` 열거 누락 3. **"024·025 미커밋" 오보**(2026-07-16 —
전 세션 handover의 07-14 맥락을 재실측 없이 현재 주장으로 옮겼다. 사용자가 "실제 git 확인" 지시로 잡음).
- **처방**: 과거 맥락을 현재 주장으로 옮길 때 **재실측 후 옮긴다.** git 상태는
  **`git cat-file -e HEAD:<경로>`(오브젝트 조회)** — `git status`·`git diff`는 마운트 절단 때문에
  근거로 못 쓴다(`[9.1]` 본문 2번째 줄. **행번을 적지 마라** — `[9.2]`:304→:308로 두 번 밀렸다).
- import 기준 grep은 **동일 패키지 사용처를 구조적으로 못 본다** — 심볼 기준·컴파일 검증 병행.

## 다음 세션이 알아야 할 판단
- **`[3.12]`**(C-068 + **C-079 개정**): 유형 1·2(`spec/` 3종·`rules.md`·`templates.md`·`CLAUDE.md`·파트 지침)
  변경은 **변경분 전수 제시 + 사용자 확인 후 실행.** **코드(`backend/src/**`)·발신·발번·notes는 면제.**
  **★ C-079로 "사용자가 요청한 범위는 면제" 추가** — 시킨 범위면 표 없이 진행하고 **범위를 넘으면 확인받는다.**
- **체계 축 대기 모드**(C-070). **체계 안건 발신 금지 — 실물이 막힐 때만.** 오늘 내 발신 3건이 전부
  체계였고 컨설턴트가 직접 지적했다: *"셋 다 좋은 산출물이었다는 게 문제를 가린다."* 재설계 국면엔
  이 유혹이 다시 온다.
- **`[5]` 절 번호 밀림**(컨설턴트 `_broadcast/001`): 내 `notes/`는 **전부 정본 일치 — 조치 없음**
  (컨설턴트 실측 + 내 자체 대조 독립 일치). 낡은 인용 1건은 `outbox/RULES/002`:7 `[5.29]`이나
  **불변이라 안 고친다**(`[5.18]`).
- **D-088 실효 조건**: 파일을 고쳐도 **실행 중 CC 세션의 컨텍스트는 갱신되지 않는다.** 새 세션엔 킥오프
  전문, 기존 세션 재사용 시엔 갱신분을 델타로 전달.
- **탈퇴 유닛 설계 시**(폐기 위험 없음 — D-093으로 확정): D-080(잔액 잔존 무관하게 탈퇴 허용,
  `balanceForfeitAcknowledged` 필수, 잔액 0이어도) + **B-018**(refresh 회전 vs 탈퇴 계정 순서)을
  **지시서에 반드시 실어라** — CC는 `decision-log.md`를 통독하지 않는다(`REFORM/001` ⓑ-4).
- **★ MEMBER_002 트리거는 탈퇴가 아니라 auction·bid·settlement 쪽에 걸었다**(`REFORM/001` ⓑ-3).
  차단 조건(진행 중 경매·홀드·주문)은 지금 **검사 대상이 공집합**이라 구조만 만든다(총괄 072 승인).
  **"도메인 완성 시 채운다"의 트리거가 없었다** → 나중에 오는 유닛의 DoD에 넣는 방식으로 회수했다.
- **MemberErrorCode는 아직 없다**(B-029로 024에서 제외). 계약 `[5]` 확정 번호:
  MEMBER_001=닉네임 중복(409), MEMBER_002=진행 중 거래 보유(409).
- **D-089 집행 필드**: 다음 B 발번부터 필수, 공란 금지. B-030이 첫 적용례.
- **D-084**: 파트 내 유닛 순서는 내 자율. 총괄이 유닛 순서를 지정하면 이의 대상.
- **D-083**: 진행 중 작업이 총괄 지시와 충돌하면 **이의 제기는 의무.** 침묵은 규약 위반.

## 보안 게이트2 이월
**`outbox/REFORM/001` ⓐ-10~14 참조**(B-016+m3 · B-017 · B-025+B-013 · B-027 · 비밀번호 변경).
트리거 = 게이트2 개시(총괄). 여기 복제하지 않는다(`[8.4]`).

## 재개 필독 (경로·순서)
1. 이 파일 — **특히 맨 위 ★★ 2개**
2. `common/rules.md`(**94개** — 위 [9] 절 표를 먼저 봐라) · `common/templates.md`(`[10]` 결번, `[15]` 작업 리스트) ·
   `spec/` 3종(**`docs/spec/` — 호스트 Read만**)
   - **★ 출근 절차에 대조가 추가됐다**(사용자 지시, 2026-07-17): 내 `handover`·`references/`의 `[N.M]`
     인용을 현행과 대조한다. **번호 생존만 보지 마라 — `[9.3]`은 번호가 살아 있고 내용이 통째로 바뀌었다.**
     **탐색**: 호스트 Grep `^\*\*\[N\.M\]`으로 생존 확인 → **살아 있으면 본문까지 읽고 뜻이 같은지 본다.**
3. `notes/cc-reports/` 스캔 → `inbox-log.md` → 수신함 스캔(호스트 Grep, `[X → 백엔드]`·`[→ 전 역할]`·`B-` 언급)
4. `decision-log.md` B-028·B-029·B-030
5. **`outbox/REFORM/001`** — 앞으로 할 일 전수 19건·회수 4건. **②를 받으면 여기서 시작한다**
5. 확정 스펙 기준: api-contract **v1.4**(`[2.5]` 회원 리소스·`[4.4]` 잔액·`[1.6]` 엣지 오류) ·
   erd **v0.7** · domain-spec **v0.5**(`[6.1]`) + `CLAUDE.md`(`[5]` 컨벤션·`[7]` 스타일)
   — **재설계로 갈릴 수 있다. 버전부터 확인해라.**
6. **재설계 범위 확인 → 그 다음에 순서를 정한다.** 재설계 무관분(025 완료 보고)은 먼저 낼 수 있다.
