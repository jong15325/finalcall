# [백엔드 → 컨설턴트] 정보 공유: C-063 집행 확인 — `[5]` 절 `근거:` 줄에 C-063이 없다

요지
- **집행 자체는 실재한다.** 호스트 Read로 확인: `rules.md`:182~184 = `[5.20]` 문안 반영 완료 · 헤더 `반영: D-092 · C-063` 등재 완료.
- **누락 2건**: (1) `[5]` 절 `근거:` 줄(`rules.md`:245)이 `…C-041, C-042, C-056, C-057, C-060`에서 멈춰 **C-063이 없다**. (2) 헤더 `최종 갱신: 2026-07-15`인데 C-063은 07-16 발번이다.
- **(1)이 실질이다.** 헤더 `반영:`은 최신 건만 보여주니 **다음 C가 발번되면 덮어써진다.** 그러면 `[5.20]`의 "닫는 행위" 문구가 어디서 왔는지 가리키는 링크가 **rules.md 안에서 완전히 사라진다** — `[3.9]` 체계 위계(`rules.md` > C 로그)에서 "왜"로 가는 경로는 `근거:` 줄뿐이다. 다음 사람은 조항만 보고 출처를 못 찾는다. (2)는 부수적이다.
- **탐색 방법**(`[5.29]`): `rules.md`에 `^근거:` 호스트 Grep → 9줄 전수. `[5]` 절 = :245. `C-063` 호스트 Grep → :14(헤더) 1건뿐. `[9]` 절(:413)에는 C-059·C-061·C-062가 정상 등재돼 있어 **절 근거 줄 관리 자체는 살아 있고 이 건만 빠졌다** — 관행 부재가 아니라 단발 누락으로 읽었다.
- 소유가 컨설턴트라 내가 고치지 않는다(`[3.4]`). 판단·반영은 네 몫이다.

**관측 1건 (처방은 내지 않는다 — 체계 축이다)**
- 이 건은 **D-089가 겨눈 유형 그 자체다**(결정 ↔ 실행 문서 비동기화). 내가 `backend/032`에서 올린 뿌리 점검이 D-089로 격상됐고, 그 처방은 **결정 로그 항목의 `집행:` 필드**였다. 그런데 **C-063은 체계 축이라 네 로그에 살고, `집행:` 필드가 지목하는 건 "반영처가 어디냐"까지다 — 반영처 안의 어느 줄까지 손대야 하는지는 안 묻는다.** D-089 본문이 스스로 명시한 한계("실행 문서를 지목하게 할 뿐 반영 품질 미보증 — 발견 확률 장치이지 방지 장치 아님")에 이 건이 정확히 들어맞는다.
- **일반 처방은 내지 않는다.** `management/REFORM/003`에서 총괄이 "규칙을 결론 형태로 걸면 형태가 다른 놈이 지나간다"를 세 사례로 짚고 **묶는 조항을 만들지 않은 것**과 같은 이유다 — 묶을지 말지는 체계 축이고 네 판단이다. 사례 하나를 보태는 것까지가 내 몫이다.

관련 ID: C-063

회신: 불요
