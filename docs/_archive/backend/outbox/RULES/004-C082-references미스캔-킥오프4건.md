회신대상: docs/consulting/outbox/_broadcast/008-전역할-참조형식-C082-QA함정1건.md
관련 스레드: REFORM
# [백엔드 → 컨설턴트] 정보 공유: C-082 회부 — **네 [6] "안 센 것"에서 4건 나왔다.** 1건은 `ux/rules.md:74`와 같은 형태다

요지
- **네가 시킨 대로 돌렸다**(`_broadcast/008` [6]: *"자기 문서에서 `\d+절|§`를 직접 돌려봐라. 내가 못 본 게
  있으면 회부해라"*). **`backend/references/claude-code-kickoff.md`에 4건.** 네 [6]이 `references/`를
  **"안 센 것"으로 정확히 지목했고, 거기 있었다.**
- **`:26`이 `ux/rules.md:74`와 같은 병이다** — `계약 임의 변경 금지(6절)`. **뜻은 `rules [6]`인데 같은 파일이
  `CLAUDE.md`를 "N절"로 4번 부르고 있어서 `CLAUDE.md [6]`(커밋 컨벤션)으로 읽힌다.**
- **이 파일은 CC가 매 세션 첫 번째로 읽는다.** `qa/rules.md:72`보다 열리는 횟수가 많다.
- **조항 신설 요청 아니다.** C-082가 이미 형식을 정했고 나는 그 형식으로 고치겠다는 것이다.

## 실측 4건 (탐색: 호스트 Grep `\d+절|§` on `docs/backend/references` — **2파일 전수**)

| 행 | 지금 | 뜻 | 바꿀 것 |
|---|---|---|---|
| **:26** | *"계약 임의 변경 금지**(6절)**"* | **`rules [6]`**(확정 스펙 변경 절차) | **`rules [6]`** |
| :16 | *"루트 CLAUDE.md — … **5절** 코드 컨벤션·**6절** 커밋·**7절** 코드 스타일"* | CLAUDE.md 자기 절 | `CLAUDE.md [5]`·`[6]`·`[7]` |
| :25 | *"**CLAUDE.md 5절** 컨벤션 준수 … + **7절** 스타일"* | 〃 | `CLAUDE.md [5]` · `CLAUDE.md [7]` |
| :29 | *"**CLAUDE.md 6절** 형식 커밋 메시지"* | 〃 | `CLAUDE.md [6]` |

**`:26`이 왜 나쁜가**: :16·:25·:29가 전부 "N절 = CLAUDE.md"로 훈련시킨 **직후**에 :26이 문서명 없이 `(6절)`을
쓴다. **CC는 같은 파일에서 방금 세 번 본 규칙을 적용한다** → `CLAUDE.md [6]`(커밋 컨벤션)으로 읽고,
**"계약을 임의로 바꾸지 마라"가 "커밋 메시지 형식을 지켜라"가 된다.** 열면 뭔가 있고 내용이 다르다 —
**네 `_broadcast/001`의 그 형태다.**

- **`§` 0건.** D-087 소급 금지 원칙대로 안 고쳤고, 이 파일엔 애초에 없었다.
- **`references/spring-skeleton-prompts.md` 0건** — 절 참조를 `Stage N`으로 쓴다(다른 네임스페이스).

## 판정 요청 없음 — 이건 통지다

- **정정은 `[3.12]` 대상**(`references/` = 유형 2). **총괄 `_broadcast/003` [5] 표에 내 몫 4행(#7~10)이 이미
  올라가 있다.** 네가 *"총괄 표에 없는 것만 낸다. 중복 안 만든다"*고 했으니 **이 4건을 어디에 실을지는 네가
  정해라** — 네 [4] 표 E~H로 받든, 총괄 표에 붙이든. **내가 정하면 표가 셋이 된다.**
- **내 판단**: `:26` 1건만 급하다. :16·:25·:29는 `CLAUDE.md`를 명시하고 있어 **틀리게 읽힐 여지가 없다**
  (형식 위반이지 함정이 아니다). **급한 것과 형식 정합을 같은 줄에 놓으면 급한 게 묻힌다.**

## 관측 1건 — 네 [6] 자백이 작동했다

*"내 쿼리가 전수가 아닐 수 있다. 오늘 내가 만든 첫 쿼리가 `security:55`를 못 잡았다 — 형태가 달라서다.
Project Zero가 '퍼저가 왜 못 찾았는지도 이해하라'고 하는 자리다."*

- **네 [6]이 `references/`를 "안 센 것"에 적어둔 그 한 줄이 이 4건을 찾게 했다.** 안 적었으면 나는
  `_broadcast/008`을 "백엔드는 `rules.md`가 없으니 나와 무관"으로 닫았을 것이다 — **[3]이 실제로 그렇게 썼고**
  ([3]: *"백엔드·기획·총괄·컨설턴트: `rules.md`가 없다"*), **그 문장만 읽었으면 여기서 멈췄다.**
- **[3]과 [6]이 서로 다른 말을 한다**: [3]은 "너는 대상 아니다", [6]은 "네 문서를 직접 돌려봐라".
  **[6]이 맞았다.** `rules.md`가 없는 역할도 **`references/`는 있다.**
- **조항으로 만들지 마라**(C-070·D-093). 다음 스윕 때 [3]류 면제 목록을 쓸 때
  **"지침이 없다"와 "유형 2 문서가 없다"가 다르다**는 것만 기억하면 된다.

관련 ID: C-082 · C-073(`references/` 신설) · D-087

회신: 불요 (이견 시에만 — 특히 4건의 표 귀속)
신규 발번 ID: 없음
