회신대상: docs/management/outbox/MEMBER/001-백엔드-기능레인해제-시퀀싱.md
관련 스레드: REFORM
# [백엔드 → 총괄] 완료 보고: 024 잔액 조회 — `GET /api/v1/me/balance` + 이슈1 (b) 판정 흔적

결과
- 계약 v1.4 `[4.4]` `GET /api/v1/me/balance` 구현·커밋·빌드 그린. **0-2 닫는다**(`_broadcast/004`).
- **이슈1 = (b) 컨트롤러가 `Authentication`을 받아 서비스에 `userId`를 넘긴다.** 총괄 승인 완료
  (`_broadcast/003` [4]). **현 024 코드는 (a)다 — 정합은 1-2에서 한다**(아래 「범위 밖」).
- **B-031 미발번.** `[4.13]` 때문이다. 1-2 코드 커밋에서 발번한다(총괄 승인).

산출물 (**D-098 모노 전환 후 경로 — 재실측했다**)
- `backend/src/main/java/com/finalcall/api/member/MemberController.java` (A) — 클래스 레벨 `/api/v1/me`(B-015), `@GetMapping("/balance")`, 반환 `ApiResponse<MemberBalanceResponse>`
- `backend/src/main/java/com/finalcall/api/member/MemberBalanceResponse.java` (A) — record + `@Builder` + `static from(UserBalance)`
- `backend/src/main/java/com/finalcall/domain/member/MemberService.java` (A) — `@Transactional(readOnly = true)`·`@ServiceLog`
- `backend/src/main/java/com/finalcall/domain/member/UserBalanceRepository.java` (M) — `findByUserId` 추가
- `backend/src/test/java/com/finalcall/domain/member/MemberServiceTest.java` (A) — 단위 2건
- `backend/src/test/java/com/finalcall/api/member/MemberControllerTest.java` (A) — 슬라이스 2건
- CC 보고 원본: `docs/backend/notes/cc-reports/024-member-잔액조회.md` (**경로가 `src/…`로 적혀 있다 — 작성 시점이 D-098 이전이다. 불변 아님이나 이력이라 안 고친다**)

완료 기준 대비 (024 지시서 + 계약 `[4.4]`)
- **응답 4필드 일치**: `cashBalance`·`gameMoneyBalance`·`gameMoneyHeld`·`gameMoneyAvailable`. **충족** —
  `gameMoneyAvailable`은 `balance − held` **파생값이고 컬럼을 안 만들었다**(erd 무변경).
- **인증 필수**: **충족** — 미인증 401(`COMMON_005`).
- **잔액 행 부재 처리**: **충족** — `COMMON_999`(500, 불변식 위반). **`MEMBER_` 코드 일절 미사용**(B-029) →
  기획 채번(MEMBER_001=닉네임 중복)과 **충돌 표면 0**.
- **`CLAUDE.md` [5] 컨벤션**: **충족** — Controller `ApiResponse`·try-catch 없음 / Service readOnly·`@ServiceLog` /
  DTO record + `from` / 엔티티→DTO 변환은 api 계층.
- **MEMBER_002 차단 조건**: **해당 없음** — 잔액 조회 유닛이다. 탈퇴(1-2) 소관.

검증
- **커밋 실재**: `git cat-file -e HEAD:<경로>` **6파일 전건 OK**(위 `backend/` 경로로 재실측). 커밋 `4c94471`.
- **테스트**: `MemberServiceTest` 2/2 · `MemberControllerTest` 2/2. `./gradlew clean build` 두 모듈 BUILD SUCCESSFUL,
  checkstyle·spotlessCheck·ArchUnit 통과.
- **`[9.1]` 준수**: 경로 판단은 호스트 도구, git 실재 확인은 오브젝트 조회(`cat-file`). `git status`·`git diff` 미사용.

## 이슈1 — (b) 판정. 024 코드는 아직 (a)다

**판정 근거 전문은 `outbox/REFORM/001` ⓑ-1.** 여기 복제하지 않는다. 요지:
`SecurityContextHolder`는 infra 성격이라 domain이 직접 읽으면 `CLAUDE.md` 섹션 4 의존 방향이 훼손된다 ·
**마감·정산 배치가 066에 확정**돼 있어 서비스가 HTTP 컨텍스트에 묶이면 그때 못 쓴다 ·
로그아웃 `AuthService`가 이미 (b)라 신규 컨벤션이 아니라 승계다.

**현 024 = (a)** — 내 지시서가 *"MemberService … SecurityContext에서 사용자 식별"*이라 썼고 CC가 그대로 했다.
**CC 잘못이 아니라 내 지시서 문언이 원인이다.** CC는 이걸 이슈로 올렸고(cc-report 이슈1) 그 판단이 옳았다.

**정합은 1-2에서 한다** — `MemberController`가 프로필 조회·수정·탈퇴로 확장되는 그 커밋이다.
**지금 고치지 않는 이유**: D-093 동결선(코드 무변경) + 1-2가 같은 컨트롤러·같은 커밋을 건드린다.
**따로 고치면 diff가 2회로 갈리고 B-031 `집행:`이 어느 커밋을 가리켜야 하는지 모호해진다.**

## 이슈 (총괄 흡수 — 판정 요청 아님)

1. **커밋 분리 실패 — `[9.26]` atomic 위반**(`GATEWAY/001` [2]에 기보고. **여기서 반복하는 이유는 이 보고가
   `4c94471`을 인용하기 때문이다**). **`4c94471` = member 6파일 + gateway 429 델타 4파일 = 10파일**이다.
   실측 `git show --stat 4c94471`. **되돌리지 않는다**(코드 전건 HEAD 실재·빌드 그린,
   이후 커밋 다수). → **이 보고의 산출물 6건은 "10파일 중 member 6파일"이다.**
2. **[정보] `@WebMvcTest` 슬라이스 첫 도입** — 리포지토리 최초다. `SecurityConfig` +
   `JwtAuthenticationEntryPoint`·`JwtAccessDeniedHandler` 임포트, `GatewayInternalProperties(enforced=false)`
   실빈 + `@MockBean TokenProvider`, 인증 주입은 `.with(user("42"))`. **1-2 컨트롤러 슬라이스가 이걸 재사용한다.**
3. **[정보] 빌드 플레이크 1회** — 첫 `clean build`에서 전 `@SpringBootTest` 33건이 동시 `initializationError`
   (공유 Testcontainers 초기화 레이스 시그니처), **코드 변경 없이 재실행 그린**. 코드 결함 아님.
   **동일 증상은 재실행으로 판별**한다. 025의 `clean build` 미완주와 **다른 건이다**(그건 `build/` 점유).

범위 밖 (1-2로 넘긴다)
- **이슈1 (b) 코드 정합** + **B-031 발번**(`[4.13]` — 같은 커밋)
- `MemberErrorCode` 신설(B-029 후속 — 계약 `[5]` MEMBER_001·002)
- 프로필 조회(029)·수정·탈퇴 — **1-1 디자인 계약 델타 선행**(D-097). **먼저 착수하지 않는다**
- 잔액 원자적 갱신·홀드 실입금 — 화폐/bid 도메인

회신: 불요 (이견 시에만)
신규 발번 ID: 없음 (B-031은 1-2 코드 커밋에서 — `[4.13]`)
