상태: ANSWERED → management/outbox/056-백엔드-게이트웨이검수-020회신.md (검수 통과)
회신대상: management/outbox/051-백엔드-012SCG착수-순서지정.md (012 SCG 착수 지시)
# [백엔드 → 총괄] 완료 보고: SCG 엣지 게이트웨이 스켈레톤 (D-068)

## 산출물
- `:gateway` 서브프로젝트(WebFlux): 라우팅(단일 서비스 경로 기반 프록시), Redis RequestRateLimiter(인증 계열 우선),
  하류 X-Gateway-Token 부착 default filter(.set() 덮어쓰기).
- 서비스 측 GatewayAccessFilter: 직접접근 차단(헤더 불일치 403, actuator·error 제외, JWT 필터보다 앞).
- 별도 배포 산출물 2개: finalcall-0.0.1-SNAPSHOT.jar, gateway-0.0.1-SNAPSHOT.jar.

## 완료 기준 충족 여부
- 라우팅·rate limit·직접접근 403 동작 확인. [충족]
- `./gradlew clean build` BUILD SUCCESSFUL — 두 모듈 compileJava/Test·checkstyleMain/Test·spotlessCheck·test·bootJar 전건.
  테스트 61건 전건 통과(서비스 60·게이트웨이 1), 실패·에러 0. [충족]
- §7 스타일: checkstyle(maxWarnings 0)·spotlessCheck 양 모듈 통과. [충족]
- 인증 서비스 유지(D-065): 게이트웨이 JWT 검증·X-User-Id 미도입. [충족]
- 전제: Testcontainers(MySQL 8.0/Redis 7) 로컬 Docker 데몬 up.

## 설계 결정 (자율 — 마스터 인덱스 반영 요망)
- B-026 게이트웨이 멀티모듈 구조(루트=서비스 + :gateway 형제, build 격리)
- B-027 직접접근 차단 메커니즘(gateway.internal 프리픽스·필터 우선순위·.set() 헤더 덮어쓰기)

## 열린 사안 (별도 결정 요청 outbox/020)
- 안건1 엣지 오류 응답 포맷(429 envelope 통일 여부, 계약 영향 — 프론트 의존).
- 안건2 관측성(Stage G)·로컬 compose 게이트웨이 추가 후속 순서.

## 규약 시사점(향후 도메인)
- 모든 클라 트래픽 게이트웨이 경유 전제(enforced=true) → 신규 도메인 엔드포인트 자동 직접접근 차단(actuator·error만 예외).
- 통합테스트 base는 게이트웨이 미경유(enforced=false), 차단 검증만 enforced=true 오버라이드.
- Spring Cloud 2025.0.0: 신규 스타터 spring-cloud-starter-gateway-server-webflux + prefix spring.cloud.gateway.server.webflux.*.

## 회신
- 회신: 필요 — 게이트웨이 스켈레톤 검수, 020 안건(429·후속) 회신, 다음 도메인 순서 지정(D-074).
- 신규 발번 ID: B-026·B-027(020과 동일, 중복 반영 주의).
