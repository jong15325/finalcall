# 보안 handover

성격: 휘발성 맥락 — 파일에 있는 내용은 경로 참조만. 이력 없음(덮어씀).
갱신: 2026-07-16

## 진행 중

- **재정리(D-093/094)** — 착수 2026-07-16 · 지금 어디까지: ⓪-b 완료(`security-guide.md` →
  `rules.md`) + ① 4항 발신(`outbox/REFORM/001`) · 다음 한 수: **`rules.md:55`·`:22` 정정
  사용자 확인 대기.** 확인 오면 2줄 Edit로 끝난다.

## 대기 중

- `outbox/REFORM/001` — 총괄 회신(회신 불요로 냈다). **ⓒ 2건은 사용자 판정 대기.**
- **게이트 2 착수 지시** — 선행 = 도메인 전체 구현(G4-n). **SEC-005만 선행 배치 가능한지
  총괄 판정 요청함**(REFORM/001 다음 단계 제안). 내 자율 아님(`[7.1]` 착수 시점 = 총괄).

## 다음 세션이 알아야 할 판단

- **`rules.md:55`가 살아 있는 함정이다.** `templates 15` → 열면 「작업 리스트」가 나온다.
  **조용히 틀린다.** 정정안 = `common/rules.md [6]`. **QA는 같은 걸 오늘 닫았고 내 것만 남았다** —
  006/002가 이 함정을 QA 단독으로 프레이밍한 탓이다. **고치기 전엔 이 지침을 근거로 쓰지 마라.**
- **`checklist.md` 위치는 내 판정이다.** C-073 [1]이 `references/`를 *"참조 자료·체크리스트·프롬프트"*
  라 적었으나 **`[3]` 트리는 `test-plan.md`·`findings.md`를 `<산출물>`로 둔다.** checklist는
  게이트 판정 기록(O/X/근거)이 쌓이는 산출물이지 정적 참조가 아니다 → **root 유지.**
  006 [3] 표가 보안 행에 1건만 둔 것과도 일치. **이견 오면 근거는 이거다.**
- **오늘 부재 주장 2건이 발신 전에 틀렸다**(초안 "templates 15 = 2건" / "SEC-003 어디에도
  등재 없음"). **둘 다 호스트 Grep이 잡았다 — bash 탓이 아니라 세기 전에 결론을 쓴 탓이다.**
  게이트 1의 오탐 2건과 같은 계열이고 이번이 3·4번째다. **게이트 2는 부재 주장이 검사의
  주류다. 문장을 쓰기 전에 Grep을 돌려라 — 순서가 규칙이다.**
- 게이트 2 검사 순서(내 자율, D-084): 1순위 SEC-002(충전 confirm — 클라 amount 신뢰 시
  **무상환 캐시 발행 = 즉시 Critical**), 2순위 SEC-005. 정본은 `checklist.md [게이트 2]`.
- **080/085 왕복 결론**: 인용 검증(D-082)은 **무조건 계속**, 부정 결과 보고(D-086)는 **교차검증 후**.
  둘은 다른 규칙이다. **오탐이 두려워 인용 검증을 멈추는 쪽으로 읽지 마라.**
- 게이트 2 착수 지시를 받으면 **`근거(인용)` 필드부터 확인한다**(D-082). 없으면 이의 대상이고
  이의는 권한이 아니라 의무다(D-083).

## 막힌 것

- **미처리 수신 11건**(REFORM/001 ⓐ-7). `inbox-log`가 039 → 079로 건너뛰고 그 사이 5건이
  등재 없이 지나갔다. **총괄 002가 "그 이상 안 열어도 된다"고 해 이번 블록에선 안 열었다.**
  **② 기능 재진입 전 배치 필요 — 총괄에 넘겼다.**
