# 보안 handover

성격: 휘발성 맥락 — 파일에 있는 내용은 경로 참조만. 이력 없음(덮어씀).
갱신: 2026-07-16

## 진행 중

- 없다.

## 대기 중

- **member 보안 구현 리뷰(D-097 ④ = `_broadcast/004` 1-3)** — 선행 = 1-2(백엔드 코드) ←
  1-1(디자인 목업 계약 델타). **auth 이월 5건**(B-016·017·018·025 + m3)이 여기 붙는다.
  **auth 이월분은 1-2를 안 기다린다(코드가 이미 있다) — 선행 착수 가부는 총괄 판정 요청함**(REFORM/003).
- `outbox/REFORM/003` — 총괄 회신(불요). **`rules.md:12` `[3.12]` 1건만 사용자 확인 대기.**

## 다음 세션이 알아야 할 판단

- **★ 「게이트 2 = 도메인 전체 완료 후」는 죽었다.** `rules [7.4]` 삭제(C-074) + D-097 →
  **보안은 도메인마다 ④**(백엔드 코드 후·QA 앞). **내 handover가 그 조항을 근거로 "게이트 2 대기"를
  세 세션 들고 있었다.** `checklist.md` [게이트 2] 절이 현행이다. `rules.md:12`는 아직 옛 문장이다.
- **★ SEC-013은 내 오탐이었고 발신까지 갔다**(철회 완료, `findings.md`에 사유·원문 보존).
  `RateLimit429IntegrationTest`가 커밋 `4c94471`에 **처음부터 있었다.** 원인:
  **Glob `**/*.java`가 `Showing 100 of 111`로 잘렸고 내가 그걸 전수로 읽었다.**
  **에러가 아니었다 — 잘렸다고 화면에 적혀 있었다.**
  - **`[9.1]`이 오늘 Glob을 뺐다**(C-075, 현행 = Read·Grep·Edit). **내가 그 개정의 실물 근거다.**
  - **재검증은 Grep 한 번이었고 즉시 나왔다. 처음부터 Grep이었으면 안 틀렸다.**
  - **교훈은 `checklist.md` [도구 규율] 부재 주장 규율 4항에 넣었다. 게이트 2 전에 읽어라.**
    핵심은 3번이다 — **목록에 잘림 표시가 있으면 전수가 아니다.**
- **긍정 근거 하나가 옆의 부재 추론을 정당화하지 못한다.** `GatewayApplicationTests`를 Read한 건
  긍정이었지만 거기서 "그러므로 다른 테스트는 없다"로 건너뛴 순간 부재 주장이었다.
  **내가 세운 방어("부정을 긍정으로 바꿔 세라")를 내가 이 형태로 우회했다.**
- **같은 계열 5번째다**(게이트 1 오탐 2건 · 초안 2건 · SEC-013). **넷은 발신 전에 잡혔고 하나는 갔다.**
  **문장을 쓰기 전에 탐색을 돌려라. 도구가 아니라 순서가 원인이다.**
- **`[9.3]`이 조용히 틀리는 자리다.** 번호가 살아있고 **내용이 반전됐다** — 현행 = *"되돌릴 수 없는
  것을 남기는 실행만 사용자 확인. **파일 조작은 묻지 않는다**"*. **`mv`·`cp` 금지는 없어졌다.**
  `[9.2]`·`[9.4]`·`[9.25]`는 결번이라 열면 걸린다.
- **인용보다 복사가 더 조용하다.** `rules.md:12`가 `[7.4]` 내용을 번호 없이 복사해서 `[N.M]` 대조에
  안 걸렸다. **출근 시 인용만 세지 말고 「내 지침이 남의 조항을 문장으로 갖고 있는 자리」도 봐라.**
- **백엔드 코드를 의심 기본값으로 읽지 마라.** SEC-005 검증에서 구현이 계약보다 촘촘했고
  (placeholder fail-fast 함정 방어, `.set()` 위조 헤더 선제거, 429 회귀 테스트), **내 산출 2건 중
  1건은 오탐이고 1건(SEC-014)은 백엔드가 B-027 후속에 이미 열어둔 것이었다.** 순수 발견은 0이다.
- **SEC-005 FIXED는 조건부다** — SEC-014(LB 뒤 단일 버킷)가 실배포에서 발현하면 rate limit이
  "있는데 안 듣는" 상태가 되고 라벨이 거짓이 된다. **지금 못 고친다**(배포 토폴로지 미확정 ·
  XFF 무조건 신뢰는 버킷 무한 분할로 더 나쁘다). **SEC-007 FIXED도 여기 걸린다.**
- **080/085 왕복 결론**: 인용 검증(D-082)은 **무조건 계속**, 부정 결과 보고(D-086)는 **교차검증 후**.
  **오탐이 두려워 인용 검증을 멈추는 쪽으로 읽지 마라.**
- 착수 지시를 받으면 **`근거(인용)` 필드부터 확인한다**(D-082). 없으면 이의 대상이고 이의는 의무다(D-083).
- **코드 경로는 `backend/` 아래다**(D-098). `docs/`·`config/`는 루트 그대로 — 내 `docs/spec/…` 참조는 불변.

## 막힌 것

- 없다.
