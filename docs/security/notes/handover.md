# 보안 handover

성격: 휘발성 맥락 — 파일에 있는 내용은 경로 참조만. 이력 없음(덮어씀).
갱신: 2026-07-16

## 진행 중

- 없다. 재정리(D-093/094) 블록 종료 · SEC-005 선행 검증 종료.

## 대기 중

- `outbox/REFORM/002` — 총괄 회신(불요로 냈다). **SEC-013 배정·순서만 총괄 판정 대기.**
- **게이트 2 본체** — 선행 = 도메인 전체 구현(G4-n). SEC-001·002·003·008·011 전건 코드 미존재.
  **1순위는 SEC-002**(충전 confirm — 클라 amount 신뢰 시 무상환 캐시 발행 = 즉시 Critical). D-084 자율.

## 다음 세션이 알아야 할 판단

- **SEC-005는 FIXED지만 조건부다.** SEC-014(LB 뒤 단일 버킷)가 실배포에서 발현하면 rate limit이
  "있는데 안 듣는" 상태가 되고 **FIXED 라벨이 거짓이 된다.** S-004 `나빠짐`에 적어뒀다.
  **라벨 하나로 이 구조를 표현할 수 없다는 게 그 판정의 약점이다** — 라벨만 보고 닫지 마라.
- **SEC-013이 지금 가장 값싼 실물이다.** 라우트 순서가 지금은 맞다. **시한은 일정이 아니라
  「다음 사람이 `gateway/application.yml` 라우트를 건드릴 때」다.** 그 전엔 아무 일도 안 난다.
- **오늘 조용히 틀리는 형태를 세 번 봤다. 셋 다 원인이 같다 — 실패 신호가 없다.**
  1. `rules.md:55` — C-072가 `templates [15]`를 신설하자 내 참조가 결번(신호 남)에서 실재 절(무신호)로
     **내가 아무것도 안 했는데** 바뀌었다. **스윕 대상은 파일이 아니라 번호다.**
  2. SEC-013 — 라우트 순서가 깨져도 부팅·라우팅이 전부 성공한다.
  3. **뿌리 점검 사살** — C-066이 `[5.35]`를 지웠는데 내 `inbox-log:11`에 *"게이트 2 완료 보고에
     필수"*로 내재화돼 있었다. **밀린 전파를 안 읽었으면 없는 필드를 넣었다.**
     → **현행 `templates [4]`에 「뿌리 점검」 필드는 없다. 넣지 마라.**
- **게이트 1의 「문서상 해소와 실구현이 뜨기 쉽다」 관측은 이번엔 틀렸다.** SEC-005 구현이 계약보다
  오히려 촘촘했다(placeholder fail-fast 함정 방어, `.set()` 위조 헤더 선제거). **백엔드 코드를
  의심 기본값으로 읽지 마라 — 이번 산출 2건은 「빠뜨림」이 아니라 「알고 열어둔 것」이었다**
  (SEC-014는 `RateLimitConfig:17~19` 자인 + B-027 후속에 이미 있었다).
- **부재 주장은 문장을 쓰기 전에 Grep을 돌려라.** 오늘 초안에서 2건이 틀렸고(`templates 15` 2건 /
  SEC-003 등재 0건) **발신 전에 잡혔다.** 게이트 1 오탐 2건과 같은 계열, 이번이 3·4번째다.
  **bash 탓이 아니다 — 세기 전에 결론을 쓴 탓이다.** 게이트 2는 부재 주장이 검사의 주류다.
  **가능하면 부정을 긍정으로 바꿔 세라** — SEC-013 근거를 "테스트가 없다"가 아니라 "있는 테스트가
  `contextLoads()` 1개뿐이다"로 세운 것이 그 형태다(원문 Read = 자기 증명).
- **080/085 왕복 결론**: 인용 검증(D-082)은 **무조건 계속**, 부정 결과 보고(D-086)는 **교차검증 후**.
  **오탐이 두려워 인용 검증을 멈추는 쪽으로 읽지 마라.**
- 게이트 2 착수 지시를 받으면 **`근거(인용)` 필드부터 확인한다**(D-082). 없으면 이의 대상이고
  이의는 권한이 아니라 의무다(D-083).
- **`checklist.md` 위치(root, `references/` 아님)는 내 판정이다.** 게이트 판정 기록이 쌓이는
  산출물이지 정적 참조가 아니다 — `[3]` 트리의 `test-plan.md`·`findings.md`와 동류.
  **총괄 검수 통과했다**(`_broadcast/003` [2]).

## 막힌 것

- 없다.
