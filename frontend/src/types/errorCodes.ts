/**
 * 계약 §5 에러코드 표 + §1.6 게이트웨이 엣지 코드 (FC-056 복원).
 *
 * 백엔드 도메인 ErrorCode enum과 1:1 (`GATEWAY_*` 는 엣지 발생 — 1:1 규칙 예외, §5 주석).
 * 에러 분기는 이 상수로 한다(문자열 리터럴 산발 금지).
 *
 * ★ **이 표는 계약이 정본이고 여기는 사본이다.** 계약에 등재됐는데 여기 누락되는 사고가 실제로
 *   두 번 있었다 — `BID_007`(v1.8 신설, FC-049에서 보완)·`ITEM_003`(v1.6 신설, FC-054에서 보완).
 *   그래서 `errorCodes.test.ts` 가 계약 문서 §5 표를 직접 파싱해 누락을 빌드에서 잡는다.
 *   계약이 개정되면 표를 여기 추가하라 — 테스트가 먼저 붉어진다.
 */
export const ERROR_CODES = {
    // 공통
    COMMON_004: 'COMMON_004', // 분산락 획득 실패 (409)
    COMMON_005: 'COMMON_005', // 세션 무효(탈퇴 주체 등, §2.5) (401)

    // 인증·회원
    AUTH_001: 'AUTH_001', // 중복 loginId (409)
    AUTH_002: 'AUTH_002', // 중복 nickname (409)
    AUTH_003: 'AUTH_003', // 로그인 자격 불일치 (401)
    AUTH_004: 'AUTH_004', // refresh 만료·무효·재사용 (401)
    AUTH_005: 'AUTH_005', // 권한 없음(관리자 등) (403)
    MEMBER_001: 'MEMBER_001', // 닉네임 중복(프로필 수정, §2.5) (409)
    MEMBER_002: 'MEMBER_002', // 진행 중 거래 보유로 탈퇴 불가(§2.5) (409)

    /*
     * 이메일 인증(§2·§4.1~4.4). 7개는 재시도성·안내가 각각 달라 **한 문구로 뭉뚱그리지 않는다**(spec §8 F3).
     *   불일치=다시 입력 / 만료·시도초과=코드 재발급 / 쿨다운=잠시 후 / 이미 인증=재시도 불필요 /
     *   미설정=먼저 등록 / 이미 사용 중=다른 이메일. 사용자 문구는 feature `*Errors.ts`에서 이 상수로 분기한다.
     */
    EMAIL_001: 'EMAIL_001', // 인증 코드 불일치 — 코드 다시 입력 (422)
    EMAIL_002: 'EMAIL_002', // 코드 만료·미발송(존재 비노출 통일) — 코드 재발급 (422)
    EMAIL_003: 'EMAIL_003', // 시도 횟수 초과(코드 폐기) — 코드 재발급 (429)
    EMAIL_004: 'EMAIL_004', // 재전송 쿨다운 — 잠시 후 재시도 (429)
    EMAIL_005: 'EMAIL_005', // 이미 인증된 이메일 — 재시도 불필요 (409)
    EMAIL_006: 'EMAIL_006', // 이메일 미설정 — 먼저 이메일 등록 (409)
    EMAIL_007: 'EMAIL_007', // 이메일 이미 사용 중(유니크 위반, signup·set-email) — 다른 이메일 (409)

    // 경매
    AUCTION_001: 'AUCTION_001', // 아이템 미소유·미보유·미존재 (403)
    AUCTION_002: 'AUCTION_002', // 이미 출품중 (409)
    AUCTION_003: 'AUCTION_003', // buyNowPrice <= startPrice (422)
    AUCTION_004: 'AUCTION_004', // 경매 없음 (404)
    AUCTION_005: 'AUCTION_005', // 즉시구매 미설정 (422)
    AUCTION_006: 'AUCTION_006', // 이미 종료 (409)
    AUCTION_007: 'AUCTION_007', // 입찰 존재로 취소 불가 (409)
    AUCTION_008: 'AUCTION_008', // 경매 시간 파라미터 위반 (422)
    AUCTION_009: 'AUCTION_009', // 판매자 자기구매 (403)

    // 입찰
    BID_001: 'BID_001', // 최소 증분 미달 (422)
    BID_002: 'BID_002', // buyNowPrice 이상 (422)
    BID_003: 'BID_003', // 자기 경매 입찰 (403)
    BID_004: 'BID_004', // 연속(최고가 보유자) 입찰 (409)
    BID_005: 'BID_005', // 게임머니 잔액 부족 (422)
    BID_006: 'BID_006', // 마감/종료됨 (409)
    /*
     * 경매 미개시 — SCHEDULED 이고 startAt 미도래 (409).
     * ★ BID_006("마감/종료됨")과 **반드시 분리**한다. "아직 시작 안 함"과 "이미 끝남"은 안내 문구·
     *   재시도 가능성이 정반대다 — 전자는 기다리면 되고 후자는 끝이다. 한 문구로 뭉뚱그리면
     *   사용자가 끝난 경매를 기다리거나 곧 열릴 경매를 포기한다.
     */
    BID_007: 'BID_007', // 경매 미개시 (409)

    // 고정가
    SHOP_001: 'SHOP_001', // 아이템 미소유·미보유 (403/409)
    SHOP_002: 'SHOP_002', // 이미 출품중 (409)
    SHOP_003: 'SHOP_003', // 고정가 없음 (404)
    SHOP_004: 'SHOP_004', // 이미 판매/종료 (409)
    SHOP_005: 'SHOP_005', // 게임머니 잔액 부족 (422)
    SHOP_006: 'SHOP_006', // 판매자 자기구매 (403)

    // 아이템·인벤토리
    ITEM_001: 'ITEM_001', // 아이템 없음 (404)
    ITEM_002: 'ITEM_002', // 소유자 아님 (403)
    /*
     * relocate 대상이 임시보관(TEMP) 상태가 아님 (409).
     * ★ INV_001("만실")과 **반드시 분리**한다 — 만실은 칸을 비우면 풀리지만, 이 코드는 그 아이템이
     *   이미 옮겨졌거나(다른 탭에서 처리) 출품 중이라는 뜻이라 사용자가 할 일이 정반대다(목록 새로고침).
     */
    ITEM_003: 'ITEM_003', // relocate 대상이 임시보관 상태 아님 (409)
    INV_001: 'INV_001', // 인벤토리 만실 (409)
    INV_002: 'INV_002', // 슬롯 점유 (409)

    // 주문
    ORDER_001: 'ORDER_001', // 주문 없음 (404)
    ORDER_002: 'ORDER_002', // 당사자 아님 (403)

    // 화폐
    CHARGE_001: 'CHARGE_001', // 승인 검증 실패 (422)
    CHARGE_002: 'CHARGE_002', // 금액 불일치 (422)
    CHARGE_003: 'CHARGE_003', // 충전 소유자 불일치 (403)
    EXC_001: 'EXC_001', // 캐시 잔액 부족 (422)
    EXC_002: 'EXC_002', // 역방향 교환 미지원 (422)

    // 검색 (search-spec, §5·§4.5)
    SEARCH_001: 'SEARCH_001', // 검색엔진 일시 불가(ES 미가용·타임아웃) (503)
    SEARCH_002: 'SEARCH_002', // 재색인 이미 진행 중(single-flight, §4.5) (409) — ★ 재색인 API 미구현(FC-116 이월), 정합상 등재
    SEARCH_003: 'SEARCH_003', // 재색인 job 없음(§4.5) (404) — ★ 재색인 API 미구현(FC-116 이월), 정합상 등재

    // 게이트웨이 엣지 (§1.6 — 도메인 enum 미등재)
    GATEWAY_429: 'GATEWAY_429', // rate limit 초과 (429)
    GATEWAY_403: 'GATEWAY_403', // 게이트웨이 미경유 직접접근 차단 (403)
} as const

export type ErrorCode = (typeof ERROR_CODES)[keyof typeof ERROR_CODES]

/**
 * envelope 파싱 자체가 불가능한 경우(네트워크 단절·비정형 응답)의 클라 전용 코드.
 * ★ 계약 §5 표에 없다 — 서버가 보내는 코드가 아니라 **클라이언트가 만든 코드**다.
 *   계약 표와 대조하는 테스트가 이 값을 "계약 누락"으로 오탐하지 않도록 분리해 둔다.
 */
export const CLIENT_ERROR_CODES = {
    NETWORK_ERROR: 'NETWORK_ERROR',
} as const
