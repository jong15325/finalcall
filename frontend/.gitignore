# dependencies
node_modules

# build output
dist
dist-ssr

# 게임 카드 아트 복사본(FC-049) — 정본은 docs/game_ui/**. scripts/sync-game-art.mjs 가 생성한다.
# 바이너리를 레포에 이중 등재하지 않기 위해 추적하지 않는다.
public/art
# 브랜드 자산 복사본(FC-067) — 정본은 docs/game_ui/common/**. scripts/sync-brand-assets.mjs 가 생성한다.
public/brand

# local env — 실제 시크릿은 커밋 금지, .env.example만 추적
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ts build info
*.tsbuildinfo

# vite build output (Ecme 템플릿 관례 — vite.config.ts build.outDir)
build
